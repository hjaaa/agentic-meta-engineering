# R004 failure：conclusion=needs_attention → 默认 WARNING（非 strict 不阻断）
# 场景：切到 tech-research，review conclusion=needs_attention，触发 WARNING
description: "R004 failure - conclusion needs_attention triggers WARNING"
req_id: "REQ-2099-111"
target_phase: "tech-research"

meta:
  id: "REQ-2099-111"
  title: "R004 failure fixture"
  phase: "definition"
  created_at: "2026-01-01 00:00:00"
  branch: "feat/req-2099-111"
  base_branch: "develop"
  reviews:
    definition:
      latest: "REV-REQ-2099-111-definition-001"
      conclusion: "needs_attention"

reviews:
  - suffix: "definition-001"
    phase: "definition"
    conclusion: "needs_attention"
    with_signoff: true

# 注意：non-strict 模式下 needs_attention → WARNING（不是 ERROR），旧/新链路均为 warn
expected_conclusion: "warn"
expected_codes: ["R004"]
