# fixture: 含 requirements/REQ- 字面量的 YAML 配置（gate 层 changed_files 格式）
schema_version: "1.0"

gates:
  - id: GATE-EXAMPLE
    plugin: example
    severity: error
    applies_when:
      changed_files:
        - "requirements/REQ-*/meta.yaml"
        - "requirements/REQ-2026-003/artifacts/*.md"
    failure_message: |
      修复路径 requirements/REQ-2026-003/notes.md 中的问题。
