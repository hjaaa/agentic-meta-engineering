# 示例文档 fixture

## 普通字面量引用（自动改）

详见 `requirements/REQ-2026-002/artifacts/detailed-design.md` 中的说明。

## 引用块内的字面量（叙述性引用，放行）

> 历史记录：需求目录从 `requirements/REQ-2026-002/plan.md` 迁移到 `runs/` 体系。

## 代码块内字面量（放行，叙述性引用）

```bash
ls requirements/REQ-2026-002/artifacts/
```

## ADR 历史引用（白名单段落，跳过）

此处内容见 requirements/REQ-2026-001/plan.md 的 D-002 决策。
