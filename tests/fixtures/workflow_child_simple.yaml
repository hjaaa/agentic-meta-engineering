# F-011 e2e fixture：子 workflow，2 个 bash 节点
# 验证：2 节点全跑通 → workflow_completed（e2e AC-6 用）
name: workflow-child-simple
version: 1
category: assist
provider: claude
model: sonnet

nodes:
  - id: child-step-1
    bash: 'echo "child step 1 done"'
    next: child-step-2

  - id: child-step-2
    bash: 'echo "child step 2 done"'
