---
schema_version: "1.0"
feature_id: __FEATURE_ID__
title: __TITLE__
status: pending
complexity: __COMPLEXITY__
depends_on: __DEPENDS_ON__
touches: __TOUCHES__
created_at: __ISO8601__
updated_at: __ISO8601__
review_report: null
---

# __FEATURE_ID__ · __TITLE__

## 需求描述

（从 features.json 的 description 复制）

## 接口/数据结构

（从 features.json 的 interfaces 复制；若 `interfaces_frozen: false` 在此备注"契约可能随实现调整"）

## 依赖

### 依赖的 feature

（从 features.json 的 `depends_on_features` 复制；若为空写"无"。实际开始前会校验这些前置 feature 状态为 `done`）

### 依赖的模块

（从 features.json 的 `depends_on_modules` 复制）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

（从 features.json 的 `touches` 复制，作为 subagent 开发边界约束）

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 单元测试覆盖主流程
- [ ] 边界条件处理
- [ ] 无明显安全/并发问题
- [ ] `/code-review` 通过
