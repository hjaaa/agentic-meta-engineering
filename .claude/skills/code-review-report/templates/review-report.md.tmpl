# 代码审查报告 · __REQ_ID__ · __TIMESTAMP__

## 结论

**__CONCLUSION__** · __N_CRIT__ critical / __N_MAJ__ major / __N_MIN__ minor

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项
- `blocked` — AI 机器评估发现关键问题

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：__ROUTING_DECISION__
- **确认人**：__ROUTING_CONFIRMED_BY__
- **确认时间**：__ROUTING_CONFIRMED_AT__
- **跑了哪些 checker**：__CHECKER_ROUTE__
- **跳过的 checker（含原因）**：
  - __SKIPPED_CHECKER_NAME__：__SKIPPED_CHECKER_REASON__

## 人工确认提示

本次评审输出 `conclusion = __CONCLUSION__`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人在主对话进行软确认（不再写入 verdict 字段；软确认仅作用于当前流程动作）：

- `looks_clean` 且无 `required_fixes` → 回复确认（如"OK 转 done"），feature-lifecycle-manager 才会把对应 feature 转 done
- `needs_attention` → 默认建议先修复；如判断风险可接受，需在主对话**显式接受风险**（如"接受当前 needs_attention 风险，转 done"）后才允许转 done
- `blocked` → fail-closed，必须先把 critical 问题修掉再重审，无法人工确认绕过

未获用户软确认时 feature-lifecycle-manager 不会把对应 feature 转为 done；GATE-REVIEW-VERDICT 在 phase-transition / submit 时仍会按 conclusion 阻断 blocked / needs_attention 的越级。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-1** [__source_checker__ · __original_severity__] → critic: __critic_verdict__ → **最终处置：__final_disposition__**
  - 裁决依据：__rationale__
- **F-2** [...] → ...
- ...

## Critical（必须修复）

### [__tag__] __file__:__line__

__description__

**建议**：__suggestion__

## Major（强烈建议修复）

### [__tag__] __file__:__line__

...

## Minor（改进建议）

### [__tag__] __file__:__line__

...

## Follow-up Notes（不阻塞，但值得提醒）

- __note__（来自 F-__id__）

## 未运行的 checker（如有）

- ⚠️ __checker_name__（原因：__reason__）

## 审查元信息

- **审查范围**：__scope_summary__（__files_changed__ 文件，+__ins__ -__del__ 行）
- **并行 checker**：__n_checkers__，总耗时 __elapsed__s
- **Critic 统计**：rejected __n_rejected__ / not_proven __n_not_proven__ / not_rebutted __n_not_rebutted__
- **综合模型**：__quality_reviewer_model__
- **Base SHA**：__base_sha__
- **Head SHA**：__head_sha__
