# 代码审查报告 · REQ-2026-011 · F-009 · 2026-05-15 15:42:07

## 结论

**looks_clean** · 0 critical / 0 major / 11 minor (全部 downgrade-to-suggestion)

- `looks_clean` — AI 机器评估无阻塞问题（本次结论）
- `needs_attention` — AI 机器评估发现待改进项
- `blocked` — AI 机器评估发现关键问题

## 路由说明

本次审查路由由 `code-review-routing.py` 自动决策（F-014 起取消人类卡点 A）：

- **决策**：`accept`
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-15 15:29:39
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker
- **跳过的 checker（含原因）**：
  - security-checker：路径未命中 must/suggest 任何规则
  - concurrency-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则
  - auxiliary-spec-checker：路径未命中 must/suggest 任何规则

## 待 sign-off 提示

本次评审输出 `conclusion = looks_clean`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人在 tty 终端执行：

```bash
python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-011-code-F-009-001 --decision approved
# 或快捷命令糖：
/code-review:signoff REV-REQ-2026-011-code-F-009-001 --decision=approved
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-009 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-CR-001** [complexity-checker · major] → critic: not_rebutted → **downgrade-to-suggestion**
  - 依据：`_classify_nodes` CCN≈18 数字事实；但项目惯例（`workflow_dispatcher` 多函数 CCN>10）允许线性聚合分类；建议 IB sweep 拆 helper。
- **F-CR-002** [complexity-checker · major] → critic: not_proven → **downgrade-to-suggestion**
  - 依据：实测嵌套深度 2（非 4）；CCN=13 含内嵌函数；可读性可接受。
- **F-CR-003** [complexity-checker · minor] → critic: not_rebutted → **downgrade-to-suggestion**
  - 依据：CCN=12 来自 CLI 入口线性流水；阈值 10 对入口函数偏严。
- **F-CR-004** [error-handling-checker · major] → critic: not_rebutted → **merge_with F-CR-005/006 → downgrade-to-suggestion**
  - 依据：`_classify_nodes` outer try 静默吞异常；与 F-CR-005/006 同根因（verbose 路径多处 fail-soft 无日志）；非功能 bug，加 `logger.debug` 即可。
- **F-CR-005** [error-handling-checker · major] → critic: not_rebutted → **merge_with F-CR-004 → downgrade-to-suggestion**
  - 依据：main workflow load `except: pass` 缺日志；设计就是 fail-soft，但缺可观察性。
- **F-CR-006** [error-handling-checker · major] → critic: not_proven → **merge_with F-CR-004 → downgrade-to-suggestion**
  - 依据：`_ready_nodes` 异常面有限（同模块 `workflow_scheduler.py`），ready 置空后 blocked 兜底仍有 `awaiting_deps` 信号；归并到统一日志改进。
- **F-CR-007** [error-handling-checker · minor] → critic: not_proven → **downgrade-to-suggestion**
  - 依据：第二次 `read_events` 已经过 main() AC-08 检查；finding 自身承认无 risk，属注释级建议。
- **F-CR-008** [error-handling-checker · minor] → critic: not_proven → **downgrade-to-suggestion**
  - 依据："jsonl 读取失败" 字串与 `run_state.py:282` 紧耦合；属 lint 层重构，不影响功能。
- **F-CR-009** [error-handling-checker · minor] → critic: not_proven → **downgrade-to-suggestion**
  - 依据：e2e 子串断言已通过 AC；补 `ERROR:` 前缀是断言收紧偏好。
- **F-CR-010** [design-consistency-checker · major] → critic: not_proven → **downgrade-to-suggestion**
  - 依据：detailed-design pseudo 中 `indent` 残留参数；main() 顶层只调一次 verbose，子 run 不输出 verbose 段，indent 实际无功能缺失。
- **F-CR-011** [design-consistency-checker · major] → critic: rejected → **drop**
  - 依据：features.json:278 AC 契约明文 5 分类 **不含 paused**；detailed-design line 1170 "paused" 是 description 残留（workflow_paused 是顶层 state 名，非节点状态）；实现按 AC 正确。
- **F-CR-012** [design-consistency-checker · major] → critic: not_proven → **drop**
  - 依据：features.json:286 是 "AC-06 e2e (b)"，**不是**顶级 AC-9；该场景已由 `test_e2e_incomplete_dispatch_in_verbose_output` (test_status_verbose_stale.py:130-188) 完整覆盖；`via_main` 是 CLI 主路径互补测试。
- **F-CR-013** [design-consistency-checker · minor] → critic: not_proven → **drop**
  - 依据：项目惯例多文件手动解析（`workflow_continue.py` 同模式）；argparse 是偏好而非缺陷。
- **F-CR-014** [design-consistency-checker · minor] → critic: rejected → **drop**
  - 依据：`_render_status` 在 baseline workflow_status.py:33 即返 `str`（pre_existing，非 F-009 改动）；reviewer 误把 pseudo 变量名当类型契约。

**裁决统计**：14 candidate → 4 drop（rejected/not_proven 验证成立）+ 3 merge（F-CR-004/005/006 同根因合并）+ 11 minor downgrade-to-suggestion + 0 keep major + 0 keep blocker。

## Critical（必须修复）

（无）

## Major（强烈建议修复）

（无 — 3 条 error_handling major 经裁决合并后 downgrade 为 suggestion，详见 Follow-up Notes）

## Minor（改进建议）

（无 keep minor — 全部 downgrade 为 suggestion；详见 Follow-up Notes 与 verdict.suggestions）

## Follow-up Notes（不阻塞，但值得提醒）

> 后续 IB sweep 收敛建议，不阻断 sign-off：

- **IB-31**（complexity sweep · F-CR-001/002/003）：`_classify_nodes` CCN≈18 / `_render_status_verbose` CCN≈13 / `main` CCN≈12 — 抽 `_compute_terminal_ids` / `_compute_awaiting_nodes` / `_compute_blocked_nodes` / `_render_stale_warn` / `_check_jsonl_readable` / `_load_workflow_soft` 6 helper；目标各函数 CCN ≤ 8
- **IB-32**（error_handling sweep · F-CR-004/005/006 同根因合并）：verbose 路径三处宽 except 统一加 `logger.debug("fail-soft", exc_info=True)` 或 stderr WARN 区分场景（ImportError / yaml 错误 / 数据结构异常）
- **IB-33**（error_handling 小项 · F-CR-007/008/009）：(1) `_infer_awaiting_kind` 第二次 read_events 加注释 `# warnings already surfaced in main()`；(2) "jsonl 读取失败" 字串抽常量 `WARN_JSONL_UNREADABLE_PREFIX` 与 `run_state.py:282` 同源；(3) e2e AC-08 断言收紧为 `assert "ERROR: jsonl unreadable" in captured.err`
- **IB-34**（design_consistency 文档对齐 · F-CR-010/011）：detailed-design.md §3.7.1 pseudocode 与 features.json AC 对齐（删 "paused (k)"、把 "indent: int = 0" 注解为"未来扩展，本期 main 单次顶层调用不需要"、明确 `_render_status` 返回 `str`）；纳入 development→testing phase-transition 前 doc-refresh 批次
- **趋势观察 trend-D-design-doc-drift 轻度触发**：design-consistency-checker 4 条 major 中 critic 全数 rejected/not_proven，Judge 全 drop；根因 detailed-design pseudocode 多处与 features.json AC 不一致；建议 IB-34 一次性消除

## 未运行的 checker（路由跳过）

- ⚠️ security-checker（路径未命中 must/suggest 任何规则）
- ⚠️ concurrency-checker（同上）
- ⚠️ performance-checker（同上）
- ⚠️ history-context-checker（同上）
- ⚠️ auxiliary-spec-checker（同上）

## 审查元信息

- **审查范围**：F-009 scoped（嵌入模式）；3 文件 +878 -10 行
  - `scripts/lib/workflow_status.py` (+283 -10)
  - `tests/e2e/test_status_verbose_stale.py` (+224 -0)
  - `tests/lib/test_workflow_status_verbose.py` (+371 -0)
- **并行 checker**：3 个（complexity / error-handling / design-consistency）
- **Critic 统计**：rejected 2 / not_proven 8 / not_rebutted 4
- **综合 Judge**：code-quality-reviewer（claude opus）
- **Base SHA**：76d58ca
- **Head SHA**：c8c6d09
- **Verdict 文件**：`requirements/REQ-2026-011/reviews/code-F-009-001.json`
- **Review ID**：`REV-REQ-2026-011-code-F-009-001`

---

**5/8 维度评分**（来自 verdict.dimensions）

| 维度 | 评分 | 主要 issues |
|---|---|---|
| design_consistency | 88 | 2 minor（设计文档对齐） |
| security | 95 | — |
| concurrency | 95 | — |
| complexity | 80 | 3 minor（CCN 偏高，IB-31 sweep） |
| error_handling | 82 | 2 major + 4 minor 全 downgrade-suggestion（IB-32/33 sweep） |
| auxiliary_spec | 92 | — |
| performance | 95 | — |
| history_context | 90 | — |

**综合分**：86

**trend monitor 信号**：
- trend-D-design-doc-drift（轻度触发，详见 IB-34）
- trend-G-meta：不适用（rev1 首派）
- trend-Q-orphan-scheduler / trend-E-fd-lifecycle：不适用
