# 代码审查报告 · REQ-2026-011 · 2026-05-15 12:51:00

## 结论

**blocked(62)** · 1 critical / 5 major / 4 minor (keep) + 7 suggestion

- 维度评分：complexity 95 / security 82 / concurrency 88 / performance 88 / **error_handling 70** / **design_consistency 55** / auxiliary_spec 75

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认（routing.py 自动决策）：

- **决策**：`all`（8 全集兜底，2 个 .py 文件未命中 must/suggest 任何规则）
- **确认人**：`hj19961223@gmail.com`
- **确认时间**：`2026-05-15 12:43:00`
- **跑了哪些 checker**：complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec（8 全集）
- **跳过的 checker**：无（all 决策）

## 待 sign-off 提示

本次评审输出 `conclusion = blocked`，**这只是 AI 的机器评估**，不代表合并通过。本 rev 已 blocked 需先派 rev2 修复 critical + 5 major，待 rev2 looks_clean / needs_attention 后再 sign-off：

```bash
python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-011-code-F-007-002 --decision approved
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-007 转 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细（26 候选 finding：10 keep / 10 downgrade / 6 reject）

### Keep (10 进入正文)

- **F-CR-001** [design-consistency · critical → blocker] → critic: `not_rebutted` → **保留 critical**
  - 裁决依据：detailed-design.md:170 §2.2.3 明文「必填：data.attempt」；save_node_result.py:298 实测只写 `{"output": output}`，attempt 局部变量被丢弃。设计漂移确证。
- **F-CR-005** [error-handling · major] → critic: `not_rebutted` → **保留 major**
- **F-CR-006** [error-handling · major] → critic: `not_rebutted` → **保留 major**
- **F-CR-007** [error-handling · major] → critic: `not_rebutted` → **保留 major**
- **F-CR-009** [design-consistency + auxiliary-spec · major] → critic: `not_rebutted` → **保留 major**（grep 实证 rebuild_run_state 仅 import 行出现）
- **F-CR-010** [auxiliary-spec · major] → critic: `not_rebutted` → **保留 major**（grep 实证 read_events 测试中仅 import 行出现）
- **F-CR-018** [error-handling · minor] → critic: `not_rebutted` → **保留 minor**
- **F-CR-023** [design-consistency · minor] → critic: `not_rebutted` → **保留 minor**（与 F-CR-001 配对）
- **F-CR-025** [auxiliary-spec · minor] → critic: `not_rebutted` → **保留 minor**
- **F-CR-026** [auxiliary-spec · minor] → critic: `not_rebutted` → **保留 minor**

### Downgrade (10 → suggestion / 维持)

- **F-CR-002** [security · major] → critic: `not_proven` → **降 minor / suggestion**（CLI 一次性进程威胁模型薄弱）
- **F-CR-008** [design-consistency · major] → critic: `not_proven` → **降 minor**（F-006 上游保证 started.attempt 必填）
- **F-CR-011** [concurrency · minor] → critic: `not_proven` → **维持 minor**（doc 与实现 gate 顺序差异）
- **F-CR-012** [concurrency · minor] → critic: `not_proven` → **降 suggestion**（AC 未列幂等性）
- **F-CR-013** [performance · minor] → critic: `not_proven` → **降 suggestion**
- **F-CR-014** [performance · minor] → critic: `not_proven` → **降 suggestion**
- **F-CR-016** [performance · minor] → critic: `not_proven` → **降 suggestion**
- **F-CR-017** [error-handling · minor] → critic: `not_proven` → **降 suggestion**
- **F-CR-019** [security · minor] → critic: `not_proven` → **降 suggestion**
- **F-CR-024** [auxiliary-spec · minor] → critic: `not_proven` → **降 suggestion**

### Reject (6 不进入正文)

- **F-CR-003** [security · major] → critic: `rejected` → **drop**（run_state.py 越 F-007 touches scope）
- **F-CR-004** [concurrency · major] → critic: `rejected` → **drop**（设计声明 CLI 一次性进程 + LOCK_EX 封死，TOCTOU 不可触发）
- **F-CR-015** [performance · minor] → critic: `rejected` → **drop**（越 F-007 scope）
- **F-CR-020** [design-consistency · minor] → critic: `rejected` → **drop**（设计 §3.1 用 `...` 占位伪代码，签名简化合理）
- **F-CR-021** [design-consistency · minor] → critic: `rejected` → **drop**（同 020）
- **F-CR-022** [design-consistency · minor] → critic: `rejected` → **drop**（同 020）

## Critical（必须修复）

### [F-CR-001] scripts/lib/save_node_result.py:294-299

`_handle_approval_repair` 写 `approval_repair_completed` 事件时漏 detailed-design §2.2.3 明文要求的必填字段 `data.attempt`：

```python
# 现状（漏 attempt）
event = {"type": "approval_repair_completed", "node_id": node_id, "data": {"output": output}}
# 应该
event = {"type": "approval_repair_completed", "node_id": node_id, "data": {"attempt": attempt, "output": output}}
```

`attempt` 在 `:290 _check_attempt_or_fail` 已校验非 None 且与 started 对齐。下游消费方按设计 §2.2.4 用单一 key 反扫 attempt 会失败。

**建议**：1 行修复 + 配对在 test_ac06 加 `assert completed["data"]["attempt"] == 1` 断言（F-CR-023）。

## Major（强烈建议修复）

### [F-CR-005] scripts/lib/save_node_result.py:82,87

`_parse_output` 两条 exit 1 路径用裸 `ERROR:` 前缀，缺 E-NODE-RESULT-NNN 错误码。

**建议**：分配 E-NODE-RESULT-005（output 解析失败）/ E-NODE-RESULT-006（output 必须是 JSON object），与 001-004 风格统一。

### [F-CR-006] scripts/lib/save_node_result.py:206,220

`_check_attempt_or_fail` 两条 exit 1 路径（attempt required / mismatch）缺错误码。

**建议**：分配 E-NODE-RESULT-007（attempt required）/ E-NODE-RESULT-008（attempt mismatch），并在模块顶部 docstring 错误码表中列出。

### [F-CR-007] scripts/lib/save_node_result.py:356,375

`main` 两处 `except WorkflowError` 输出 `f"ERROR: {exc}"`，无错误码兜底。

**建议**：分配 E-NODE-RESULT-099 通用兜底码。

### [F-CR-009] scripts/lib/save_node_result.py:38

`rebuild_run_state` 死 import（实际用 `RunState.rebuild()` 类方法）。

**建议**：直接删 `rebuild_run_state`。

### [F-CR-010] tests/lib/test_save_node_result.py:35

`read_events` 死 import（测试用 `_read_jsonl` helper）。

**建议**：直接删 `read_events`。

## Minor（改进建议）

### [F-CR-018] scripts/lib/save_node_result.py:360-361

`read_events` / `RunState.rebuild` 在 try 块外，非 WorkflowError 异常会暴露 traceback。

### [F-CR-023] tests/lib/test_save_node_result.py:248-252

`test_ac06_approval_repair_happy_path` 未断言 `data.attempt == 1`，导致 F-CR-001 critical 漏检（与 F-CR-001 配对修复）。

### [F-CR-025] tests/lib/test_save_node_result.py:134,153,172

3 处 `run_dir, jsonl_path = _make_run_dir(...)` 解包后 `run_dir` 未使用。改为 `_, jsonl_path = ...`。

### [F-CR-026] tests/lib/test_save_node_result.py:361

`test_ac11` 中 `simulated_cmd` 死变量。直接删。

## Follow-up Notes（不阻塞）

- F-CR-011（gate 顺序与文档不一致）— attempt 闸目前在 node_match 前，文档表述是 state→node_match→attempt。功能等价仅影响双错误场景 exit code 优先级。rev2 可顺手对齐。
- F-CR-008（expected_attempt is None 静默跳过）— critic 降 minor，F-006 上游保证 started.attempt 必填；建议 rev2 改 fail-closed 增强健壮性。
- F-CR-002（@file 路径沙箱）— 威胁模型薄弱，suggestion；future hardening。
- F-CR-012~F-CR-017/F-CR-019/F-CR-024 — 性能 / 可读性 suggestion，单独 PR 或后续需求。

## 审查元信息

- **审查范围**：嵌入模式 / F-007 / 2 文件新建（save_node_result.py 380 行 + test 376 行，+756 -0）
- **并行 checker**：8 全集，总耗时 ~205s
- **Critic 统计**：rejected 6 / not_proven 10 / not_rebutted 10
- **综合模型**：code-quality-reviewer（sonnet）
- **Base SHA**：04b76945
- **Head SHA**：911c0fd
- **测试结果**：本地 pytest 11 passed in 0.50s（F-007 专项）+ 全套 410 passed 0 回归
