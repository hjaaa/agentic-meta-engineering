# 代码审查报告 · REQ-2026-011 · F-006 rev2 · 2026-05-15 11:32:38 (+08:00)

## 结论

**looks_clean** · 0 critical / 0 major / 2 minor

- `looks_clean` — AI 机器评估无阻塞问题 ✅
- `needs_attention` — AI 机器评估发现待改进项
- `blocked` — AI 机器评估发现关键问题

rev1 6 keep finding 全部闭合（CC 16→10 / main 102→82 / max_attempts invalid 走 warning / e2e 11 assert 拆 3 helper / warning 补 run_id+node_id / prompt_ref+node_id 锚定）。rev2 引入 2 minor follow-up（_load_workflow_node 与 workflow_continue 重复 + bool 测试盲区），均不阻断 sign-off。

## 路由说明

- **决策**：accept（rev2 修复 3 文件，未触 must）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-15 11:11+
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker（3）
- **跳过的 checker**（5 个，路径未命中 must/suggest）：security / concurrency / performance / history-context / auxiliary-spec

## 待 sign-off 提示

本次评审输出 `conclusion = looks_clean`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人执行：

```bash
# 对话式 sign-off
python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-011-code-F-006-002 --decision approved
# 或 slash command 糖：
/code-review:signoff REV-REQ-2026-011-code-F-006-002
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-006 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细（9 条候选，rev2 全集）

> rev1 keep finding 6 条 100% 闭合（已在结论摘要确认），本段仅展示 rev2 新候选 finding 的处置。

- **F2-CR-001** [complexity · major] → critic: rejected → **最终处置：downgrade → minor follow-up（合并入 IB-24）**
  - 裁决依据：rev1 dropped F-CR-006 复发；git blame 证函数体在 febba745 引入但 rev2 仅加 run_id 不改结构；workflow_continue._load_workflow_for_run 同款。Judge 自研 CC 复算 ≈ 11-13 仍偏高，与 workflow_continue 95% 重复属实——降为 minor 入 IB-24（抽 workflow_loader_helpers.load_workflow_for_run 共用）
- **DC-R2-001** [design-consistency · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：run_id 入参是 rev1 keep F-CR-011 闭合落地的必需依赖（warning 上下文），与 F2-CR-001 同根因不重复保留
- **DC-R2-006** [design-consistency · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：tests/lib/test_workflow_reject_repair.py:186-213 已覆盖 5KB big_reason + manifest fallback；e2e 重复属过度覆盖
- **F2-CR-002** [complexity · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：Judge 复算 _extract_max_attempts CC≈5（reviewer 估 10 不准）；rev1 review L91 显式建议抽出此 helper 处理 isinstance 三元，本就是设计预期高密度
- **F2-CR-003** [complexity · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：main 82 行（L250-331）相对 rev1 102 行 -19.6%，4 helper 已抽离结构清晰；82 vs 80 阈值差 2 行属合理浮动；reviewer ≤60 是个人估算非门禁
- **EH-NEW-002** [error-handling · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：yaml `max_attempts: null` 与缺字段都进同条 L137 默认分支（None 不进 warning），单独加测试是过度防御
- **DC-R2-005** [design-consistency · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：_assert_after_* 三 helper 都有完整 docstring + 命名语义清晰；项目无既定 helper 命名规范文档可参照
- **F2-CR-004** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep (minor)**
  - 裁决依据：grep tests/lib/*.py sys.path 注入 14 处全部 lib→lib，**仓库内首例 lib→e2e 跨层 import**（test_workflow_reject_repair.py:35-38）；rev1 review L111 「拆 helper 复用」未指定通过 sys.path hack 实现
- **EH-NEW-001** [error-handling · minor] → critic: not_rebutted → **最终处置：keep (minor)**
  - 裁决依据：parametrize L313 仅 [0, -2, 'foo'] 缺 True/False；_extract_max_attempts L135 `isinstance(raw, int) and not isinstance(raw, bool)` 的 bool 屏蔽是 D-009 兜底关键语义，未锁定属真盲区

## Critical / Major（必须修复）

无。

## Minor（改进建议，不阻断 sign-off）

### [design_consistency / test-architecture] tests/lib/test_workflow_reject_repair.py:35-38

仓库内首例 lib→e2e sys.path hack：`sys.path.insert(0, tests/e2e)` + `from test_approval_reject_repair import _assert_after_reject`。lib 测试对 e2e 测试文件产生硬依赖，违反测试层级独立性。

**建议**：将共享断言 helper 提取到 `tests/conftest.py` 或 `tests/_shared/approval_assertions.py`，两侧均从公共模块导入。

### [error_handling / test_coverage] tests/lib/test_workflow_reject_repair.py:313

`@pytest.mark.parametrize("bad_value", [0, -2, "foo"])` 缺 `True` / `False`。`_extract_max_attempts` L135 `isinstance(raw, int) and not isinstance(raw, bool)` 的 bool 屏蔽是关键 D-009 兜底语义（Python 中 `True/False` 是 `int` 子类，若不屏蔽 `True` 会被当 `max_attempts=1` 接受），当前测试未锁定。

**建议**：parametrize 加 `True` / `False` 两组，期望走默认 3 不打 warning。

## Follow-up Notes（IB backlog）

- **IB-24**（合并 F2-CR-001 + rev1 trend-G-meta 边缘观察）：抽 `workflow_loader_helpers.load_workflow_for_run(run_state, run_dir, root, run_id)` 共用，消除 workflow_continue.py:176 与 workflow_reject.py:46 的 95% 重复
- **IB-25**（新增）：tests 层级独立性整理——把 `_assert_after_*` 提到 `tests/conftest.py` 或 `tests/_shared/approval_assertions.py`，消除 lib→e2e sys.path hack（F2-CR-004）
- **IB-26**（新增）：`_extract_max_attempts` parametrize 补 True/False 锁定 bool 屏蔽语义（EH-NEW-001）

## 未运行的 checker

- ⚠️ security-checker / concurrency-checker / performance-checker / history-context-checker / auxiliary-spec-checker（routing 跳过，路径未命中 must/suggest）

## Trend Monitor

- **trend-G-meta**（修复反引入新问题）：**未触发**。rev2 修 6 keep finding 全闭合 + 0 行为回归（18 tests passed），同步引入 2 minor（F2-CR-004 + EH-NEW-001）。比例 6:2 + 2 项均 minor + 2 项与 rev1 6 keep **根因不同**（前者属测试架构 + 测试盲区，后者属 main CC + 校验缺位），不构成同模式恶化循环。F-009/F-010/F-011 trend-G-meta 触发标志是「修一引一同型 bug」，rev2 不符。
- **trend-Q-orphan-scheduler**：不适用。
- **trend-test-coverage-drift**：rev1 已注 F-CR-016/F-CR-018 双锚定缺失首次出现，rev2 已闭合并新增 prompt_ref / node_id / data.output 三层断言，**反向消除**该 trend 风险。

## 审查元信息

- **审查范围**：embedded · F-006 rev2 · feat/req-2026-011 vs develop（3 修复文件）
  - 309 修改行（workflow_reject.py +144 / test_workflow_reject_repair.py +59 / test_approval_reject_repair.py +106）
- **并行 checker**：3（complexity / error-handling / design-consistency）；5 个 routing 跳过
- **Critic 统计**：rejected 3 / not_proven 4 / not_rebutted 2
- **综合裁决**：code-quality-reviewer，最终 keep 2 minor / drop 6 / downgrade 1（F2-CR-001 → minor IB-24）
- **conclusion / score**：looks_clean / 88 (估算 = (88+90+88+92+92+92+92+92+92)/8 ≈ 91，verdict 实记 score 见 reviews/code-F-006-002.json)
- **维度评分**：design_consistency=88 / security=92 / concurrency=92 / complexity=68→approx80（critic drop 后 F2-CR-001 已 downgrade）/ error_handling=90 / 其他 92
- **rev2 vs rev1**：error_handling +12 / design_consistency -3 / complexity -6
- **Base SHA**：f0eba38
- **Head SHA**：55dda1c
- **修复 commit**：fe2fc03
- **Verdict**：requirements/REQ-2026-011/reviews/code-F-006-002.json
- **supersedes**：REV-REQ-2026-011-code-F-006-001
