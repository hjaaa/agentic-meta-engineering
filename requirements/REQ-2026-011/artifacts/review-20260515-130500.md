# 代码审查报告 · REQ-2026-011 · 2026-05-15 13:05:00 · rev2

## 结论

**looks_clean(88)** · 0 critical / 0 major / 1 minor (keep) + 3 suggestion

- supersedes: `REV-REQ-2026-011-code-F-007-001`
- 维度评分（rev1→rev2 趋势）：

| 维度 | rev1 | rev2 | Δ |
|---|---|---|---|
| complexity | 95 | 95 | 0 |
| security | 82 | 82 | 0 |
| concurrency | 88 | 88 | 0 |
| performance | 88 | 88 | 0 |
| error_handling | 70 | **90** | +20 |
| design_consistency | 55 | **90** | +35 |
| auxiliary_spec | 75 | **90** | +15 |
| **综合** | **62** | **88** | **+26** |

## 路由说明

- **决策**：`all`（8 全集兜底）
- **确认人**：`hj19961223@gmail.com` / 2026-05-15 13:00:00
- **跑了哪些 checker**：8 全集（complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec）

## 待 sign-off 提示

本次评审输出 `conclusion = looks_clean(88)`。rev1 全部 required_fixes（1 critical + 5 major + 4 minor）已闭合，11 AC + 410 全套 0 回归。可 sign-off：

```bash
python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-011-code-F-007-002 --decision approved
```

## rev1 keep finding 闭合验证（8 checker 一致 CLOSED）

| Finding | Severity | 闭合证据 |
|---|---|---|
| F-CR-001 | critical | `save_node_result.py:309` `data={"attempt": attempt, "output": output}`（§2.2.3 必填字段补齐） |
| F-CR-005 | major | `:93,97` E-NODE-RESULT-005/006 错误码补齐 |
| F-CR-006 | major | `_check_attempt_or_fail` E-NODE-RESULT-007/008 补齐 |
| F-CR-007 | major | main `except WorkflowError` E-NODE-RESULT-099 兜底 |
| F-CR-009 | major | `rebuild_run_state` dead import 删除（grep 验证） |
| F-CR-010 | major | 测试 `read_events` dead import 删除 |
| F-CR-018 | minor | `read_events`/`RunState.rebuild` 挪入 try 块 + `except Exception` 兜底 |
| F-CR-023 | minor | test_ac06 补 `assert completed["data"]["attempt"] == 1` |
| F-CR-025 | minor | 7 处 run_dir 解包改 `_`（subagent 超报范围正确） |
| F-CR-026 | minor | test_ac11 `simulated_cmd` 死变量删 |

## rev2 新引入 finding 裁决（4 候选）

### F2-CR-004（minor 保留）

- [design-consistency · minor] → critic: `not_rebutted` → **保留 minor**
- 位置：`tests/lib/test_save_node_result.py:253`
- 描述：F-CR-023 补的 `assert completed["data"]["attempt"] == 1` 锚 §2.2.3 必填字段 `attempt`，但漏 `output`（test_ac06 传入 `{"fixed": True}` 未验证写回）
- 建议：一行追加 `assert completed["data"]["output"] == {"fixed": True}`

### 降级 suggestion（3 条 critic not_proven）

- **F2-CR-001** (sec) `:92-93` E-NODE-RESULT-005 含路径泄漏 — 与 rev1 F-CR-002/019 同质先例（CLI stderr 路径暴露不构成 sec finding）
- **F2-CR-002** (sec) `:388-390` `except Exception` 异常原文暴露 — 与 F-CR-019 同质（顶层兜底允许 exc 原文）
- **F2-CR-003** (eh) `:388-390` `except Exception` 缺类型名 — 非吞没，仅 grep 便利性 nit

## Minor（不阻塞 sign-off）

### [F2-CR-004] tests/lib/test_save_node_result.py:253

```python
# 当前
assert completed["data"]["attempt"] == 1
# 应该追加
assert completed["data"]["output"] == {"fixed": True}
```

§2.2.3 列 `data` 必填字段为 `{attempt, output}`，断言只覆盖一半。一行可修，可在后续 housekeeping 一并处理。

## Follow-up Suggestions（不阻塞）

- F2-CR-001 / F2-CR-002 — security 信息泄漏 hardening（CLI stderr 路径暴露，与 rev1 同质先例已 reject）
- F2-CR-003 — `except Exception` 输出格式补 `type(exc).__name__`（grep 便利性提升）

## trend-G-meta 监控：**MARGINAL**（净收益正向，可 signoff）

- **净收益正向**：rev1 综合 62 → rev2 88 (+26)；10 项 rev1 finding 全闭合，仅 1 项新 minor + 3 项 suggestion
- **暴露盲点**：F-CR-023 表明 reviewer 链路对 spec 错误码 / 必填字段全集核对存在系统性疏漏（rev1 仅查局部 attempt 字段而非 attempt+output 全集）
- **沉淀建议**：把"spec 错误码/必填字段全集枚举核对"作为 reviewer 标准前置步骤；F2-CR-004 可在后续 housekeeping commit 一并修

## 审查元信息

- **审查范围**：嵌入模式 / F-007 / rev2 / 2 文件 +38/-25 + 21/-14 = ~63 行变更
- **并行 checker**：8 全集，总耗时 ~206s
- **Critic 统计**：rejected 0 / not_proven 3 / not_rebutted 1
- **综合模型**：code-quality-reviewer
- **Base SHA**：911c0fd (rev1 head)
- **Head SHA**：16d03b7
- **测试结果**：pytest tests/lib/test_save_node_result.py 11 passed in 0.52s + 全套 410 passed in 14.29s 0 回归

## 跨维度洞察

- **三维同步跃升**（design_consistency +35 / auxiliary_spec +15 / error_handling +20）证明 spec-aligned 修复（错误码表 + dead import 清理 + spec 必填字段补齐）是高 ROI 策略
- 建议沉淀到 reviewer fix 范式：spec-aligned 修复同时填三维空白
