# 代码审查报告 · REQ-2026-011 · 2026-05-15 10:58:15 (+08:00)

## 结论

**needs_attention** · 0 critical / 2 major / 4 minor

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项 ✅
- `blocked` — AI 机器评估发现关键问题

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 自动确认（F-014 起取消人类卡点 A）：

- **决策**：accept（推荐集 must=0 + suggest=2 命中，trivial 2，total 9）
- **确认人**：hj19961223@gmail.com（git config user.email）
- **确认时间**：2026-05-15 10:41:12
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker（3）
- **跳过的 checker（含原因，5 个，统一原因：路径未命中 must/suggest 任何规则）**：
  - security-checker
  - concurrency-checker
  - performance-checker
  - history-context-checker
  - auxiliary-spec-checker

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。

按 Judge 建议，存在 2 个 keep major required to address，建议派 rev2 修复后再 sign-off；若开发者坚持当前实现并接受 follow-up 处理，可在 needs_attention 状态直接 sign-off（不在 trivial 通道）。

```bash
/code-review:signoff REV-REQ-2026-011-code-F-006-001               # 对话式 sign-off（rev2 修复后推荐）
# 或：
python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-011-code-F-006-001 --decision approved
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-006 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示「reviewer 发现 → critic 反证 → Judge 裁决」的三方透明度。共 18 条候选（complexity 7 / error-handling 8 / design-consistency 4，去重合并），最终 keep 6 / drop 6 / follow-up 6。

- **F-CR-001** [error-handling · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：git blame fee71457（F-005 既有），F-006 commit febba74 未触碰；validate_state_for_cmd 抛的消息已自带 'E-WF-STATE-001' 前缀
- **F-CR-002** [error-handling · major] → critic: not_rebutted → **最终处置：keep (major)**
  - 裁决依据：workflow_loader.py:394-403 on_reject 仅校验字段存在不校验类型/范围（对比 L411-415 retry.max_attempts 校验 int>=0），yaml `max_attempts: 0/-2` 会绕过 loader 进入 reject:211 静默 fallback；F-006 是新代码引入新静默路径，应一开始就加 warning 而非延后
- **F-CR-003** [error-handling · major] → critic: not_proven → **最终处置：downgrade → follow-up（IB-22）**
  - 裁决依据：run_state.py:222-228 approval_pending 强保证 set pending_approval；reject:197 取 `pending_approval or "unknown_node"` 触发条件需 jsonl 损坏 + state 矩阵已通过双重前提；理论可达但实操路径几乎不可能
- **F-CR-004** [complexity · major] → critic: not_proven → **最终处置：merge into F-CR-005**
  - 裁决依据：main 102 行确大但项目惯例容忍（workflow_run.py main 162 行）；与 F-CR-005 CC=16 同根因合并处理
- **F-CR-005** [complexity · major] → critic: not_rebutted → **最终处置：keep (major)**
  - 裁决依据：reject.main L145-247 实测 5 print + 4 try/except + 3 isinstance 三元 + 2 if/else 输出分支，CC≈16 已超 ruff PLR0915 默认阈值 12；reviewer 给的 _extract_max_attempts 拆法合理，可同时消解 F-CR-002 / F-CR-007
- **F-CR-006** [complexity · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：workflow_continue._load_workflow_for_run 53 行同模式 3 路径降级（约定路径 + meta.yaml 降级 + 兜底），结构高度一致；属项目惯例不应阻断
- **F-CR-007** [complexity · minor] → critic: not_proven → **最终处置：follow-up（F-CR-005 修复后自然消失）**
  - 裁决依据：pyproject.toml ruff 仅 select=['F'] 不开 E501；F-CR-005 修复后 L211 长行会自然消失，无需独立处理
- **F-CR-008** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep (minor)**
  - 裁决依据：tests/e2e:90-159 单测 70 行 11 assert 跨 reject/repair/approve 三阶段，维护成本属实；拆 _assert_after_* helper 是低成本改进
- **F-CR-009** [complexity · minor] → critic: not_proven → **最终处置：follow-up（IB-23）**
  - 裁决依据：tests/lib/无 conftest.py，F-006 三处 _patch_git_branch 重复符合 lib/e2e 既有惯例；建议下轮整体抽 conftest
- **F-CR-010** [complexity · minor] → critic: not_rebutted → **最终处置：drop**
  - 裁决依据：Judge 反对 critic：CC=10 对 e2e 测试主流程合理；项目无强一致 e2e CC 阈值约束
- **F-CR-011** [error-handling · minor] → critic: not_rebutted → **最终处置：keep (minor)**
  - 裁决依据：L72/79 logging.warning 缺 run_id/node_id 属实；F-006 是新代码补全成本极低
- **F-CR-012** [error-handling · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：__main__ 块仅 catch WorkflowError 跨 4 模块（continue/save/cancel/approve）惯例一致
- **F-CR-013** [error-handling · minor] → critic: not_proven → **最终处置：follow-up（合 IB-22）**
  - 裁决依据：attempt > max 不可达需 jsonl 损坏 + 绕过 state 矩阵双重前提；属防御性代码可入 IB-22 与 F-CR-003 同根因合并
- **F-CR-014** [error-handling · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：tests/lib/test_workflow_approve.py base 既有版本同款 assert returncode + stderr 中文非空，惯例一致
- **F-CR-015** [design-consistency · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：detailed-design L497 注释「既有 ... 保持不变」+ L509 伪代码用省略号；run_dir 是 meta.yaml 降级必需依赖（reject:64 meta_path = run_dir/meta.yaml）；workflow_continue._load_workflow_for_run 也是 4 参同款
- **F-CR-016** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep (minor)**
  - 裁决依据：workflow_reject.py:114 写 prompt_ref='approval.on_reject.prompt'，detailed-design §2.2.2 L151 显式必填；test 仅断言 attempt/max_attempts，prompt_ref 字段无锚定 → 设计契约对测试漂移开窗
- **F-CR-017** [complexity · minor] → critic: not_proven → **最终处置：follow-up（合 IB-22 与 F-CR-013 同根因）**
  - 裁决依据：同 F-CR-013，合并入 IB-22
- **F-CR-018** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep (minor)**
  - 裁决依据：tests/e2e:139-159 末态校验缺 events[-1].type=='node_completed' ∧ node_id 显式断言（虽然 L153 types 间接覆盖类型，但 node_id 未单独锚定）

## Critical（必须修复）

无。

## Major（强烈建议修复）

### [complexity] scripts/lib/workflow_reject.py:145-247

main 函数 ~102 行 + CC≈16，承担 6 项职责（tty / reason 校验 / run_id / state rebuild / attempt+max_attempts 解析 / 事件写入+打印）。L211 单行 138 字符是同根因副作用。

**建议**：抽 `_extract_max_attempts(node_def, run_id, node_id) -> int`（4 行纯函数，CC≈4，集中处理 isinstance 三元 + 非法值 warning，可一并消解 F-CR-002）。配合 `_print_reject_summary(attempt, max, ts, run_id, node_id)` 抽出，main 可降到 ≤60 行 / CC≤8。

### [error_handling] scripts/lib/workflow_reject.py:211

`max_attempts = int(max_attempts_raw) if isinstance(max_attempts_raw, int) and max_attempts_raw >= 1 else DEFAULT_APPROVAL_MAX_ATTEMPTS` ——yaml 写 `max_attempts: 0/-2` 时静默回退 D-009=3 无任何 logging.warning。workflow_loader.py:394-403 on_reject 校验仅查字段存在不校验类型范围（对比 L411-415 retry.max_attempts 显式 int>=0 校验差异），运维错配置无线索。

**建议**：抽出函数后加 `logging.warning('reject: yaml on_reject.max_attempts=%r 非法（需 int≥1），降级 D-009 默认 %d', raw, DEFAULT_APPROVAL_MAX_ATTEMPTS, extra={'run_id': run_id, 'node_id': node_id})`。

## Minor（改进建议）

### [design_consistency / test_coverage] tests/lib/test_workflow_reject_repair.py:130-285

workflow_reject.py:114 写入 `prompt_ref='approval.on_reject.prompt'`，detailed-design §2.2.2 L151 显式标注必填字段，但测试断言只查 attempt/max_attempts，不锚定 prompt_ref → 字段被改名/拼写错不会被发现。

**建议**：在 `test_reject_attempt_1_writes_repair_started_inline` 加 `assert repair["data"]["prompt_ref"] == "approval.on_reject.prompt"`。

### [design_consistency / test_design] tests/e2e/test_approval_reject_repair.py:90-159

`test_full_reject_repair_approve_cycle` 单测 70 行 11 assert 跨 reject / repair / approve 三阶段——可读性 OK 但维护时定位失败成本高。

**建议**：拆 `_assert_after_reject(jsonl, expected_attempt, expected_max)` / `_assert_after_repair_completed` / `_assert_after_approve` 三个 helper（同时复用到 reject 单测）。

### [design_consistency / test_coverage] tests/e2e/test_approval_reject_repair.py:139-159

末态校验 state=='running' + pending_approval is None + L158-159 已查 output={'decision':'approved'}，但未显式断言 `events[-1].type == "node_completed"` ∧ `events[-1].node_id == node_id`（虽然 L153 `types[-len(expected_tail):]` 间接覆盖类型，但 node_id 未单独锚定）。

**建议**：加 `assert completed.get("node_id") == node_id`（已隐含在 types 校验，但显式更稳）。

### [error_handling / observability] scripts/lib/workflow_reject.py:72,79

`workflow_reject._load_workflow_node` 的 `logging.warning` 缺 run_id/node_id 上下文（workflow_continue.py:214 同款也缺，但 F-006 是新引入加 run_id 是低成本改进）。

**建议**：`logging.warning('reject 加载 workflow 失败 workflow=%s run_id=%s node_id=%s', workflow_name, run_id, node_id)`。

## Follow-up Notes（不阻塞，但值得提醒，建议入 IB backlog）

- **IB-22**：workflow_reject.py:197 + L211 加 `assert run_state.pending_approval is not None` + `assert current_attempt <= max_attempts` 防御性守卫（合并 F-CR-003 / F-CR-013 / F-CR-017，三条同根因）
- **IB-23**：tests/lib + tests/e2e 抽 conftest.py 共享 `_patch_git_branch` / `_seed_approval_pending`（合并 F-CR-009）
- **IB-24**：抽 `workflow_loader_helpers.load_workflow_for_run` 共用，消除 workflow_continue 与 workflow_reject 的 95% 重复（trend-G-meta 边缘消除，F-CR-006 dropped 但留观察）

## 未运行的 checker（路径未命中 must/suggest 任何规则）

- ⚠️ security-checker（routing 跳过）
- ⚠️ concurrency-checker（routing 跳过）
- ⚠️ performance-checker（routing 跳过）
- ⚠️ history-context-checker（routing 跳过）
- ⚠️ auxiliary-spec-checker（routing 跳过）

## Trend Monitor

- **trend-G-meta**（修复反引入新问题）：**未触发，但留观察**。F-006 自身 0 行为回归（commit msg 注 1409 passed），未引入新 bug；但 `_load_workflow_node` 是从 workflow_continue 复制的——属设计层「该抽未抽」延续，不算新引入问题。
- **trend-Q-orphan-scheduler**：不适用（F-006 不涉及 scheduler）。
- **trend-test-coverage-drift**：F-CR-016/F-CR-018 双锚定缺失是首次出现，属 F-006 新增；建议监控后续 feature 是否复现。

## 审查元信息

- **审查范围**：embedded · F-006 · feat/req-2026-011 vs develop（5 代码/测试文件 + 4 artifact，artifact 不审）
  - 9 文件 +874 -27 行
- **并行 checker**：3（complexity / error-handling / design-consistency）；5 个 routing 跳过
- **Critic 统计**：rejected 5 / not_proven 6 / not_rebutted 7
- **综合裁决**：code-quality-reviewer，最终 keep 6 / drop 6 / follow-up 6（含 3 条合并入 IB-22）
- **conclusion / score**：needs_attention / 82
- **维度评分**：design_consistency=91 / security=92 / concurrency=92 / complexity=74 / error_handling=78 / auxiliary_spec=90 / performance=92 / history_context=90
- **Base SHA**：4f0f593
- **Head SHA**：f0eba38
- **Verdict**：requirements/REQ-2026-011/reviews/code-F-006-001.json
