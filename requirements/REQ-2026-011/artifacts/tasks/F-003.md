---
schema_version: "1.0"
feature_id: F-003
title: "last_event_ts 语义复用 + heartbeat 隐式更新"
status: pending
complexity: trivial
depends_on: 
  - F-002
touches: 
  - scripts/lib/run_state.py
  - tests/lib/test_run_state_last_ts.py
created_at: "2026-05-14 15:21:49"
updated_at: "2026-05-14 15:21:49"
review_report: null
---

# F-003 · last_event_ts 语义复用 + heartbeat 隐式更新

> 来源：`requirements/REQ-2026-011/artifacts/features.json` · `F-003`
> 设计：`requirements/REQ-2026-011/artifacts/detailed-design.md`（按 ac_refs 检索）
> priority=P0 · ac_refs=AC-A3 · pr_group=P0-baseline · estimate_days=0.5

## 需求描述

复用 RunState.last_event_ts 字段作为 heartbeat（D-011），不引入独立 heartbeat 事件类型。确认 append_event / append_events 自动塞 ts 字段；RunState.rebuild 在每条事件上无条件更新 state.last_event_ts = ts（既有代码已支持，本 feature 主要是补 unit test 锚定不回归 + 文档说明）。落地 ADR D-011 + AC-A3 baseline。

## 触及范围

> **frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）解析。
> 本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- scripts/lib/run_state.py
- tests/lib/test_run_state_last_ts.py

### 涉及模块（features.json `modules`）

- scripts/lib/run_state.py
- tests/lib/test_run_state_last_ts.py

## 依赖

### 依赖的 feature

- F-002

> 实际派发前 `dispatch_precheck.py` 校验 `depends_on` 所有前置 feature 的 task.md `status == done`。

## 验收标准（features.json `acceptance`）

- [ ] append_event 不传 ts → 自动塞 ISO8601 UTC ts
- [ ] append_events 批量 5 条 → 每条都被塞 ts（若未传）
- [ ] RunState.rebuild 读 5 条事件 → state.last_event_ts == 最后一条事件的 ts
- [ ] node_ready 事件不被排除在 last_event_ts 更新外（与既有事件等同）

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] 接口实现符合详细设计（detailed-design.md 对应 §）
- [ ] 单元测试覆盖主流程
- [ ] 边界条件 / 反向回归 acceptance 全部命中
- [ ] 无明显安全 / 并发 / 性能问题
- [ ] `post-dev-verify` 门禁通过（`scripts/gates/run.py --trigger=post-dev --req=REQ-2026-011 --feature=F-003`）
- [ ] `/code-review` 通过 + sign-off approved
