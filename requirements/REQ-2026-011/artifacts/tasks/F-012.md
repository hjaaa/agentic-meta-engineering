---
schema_version: "1.0"
feature_id: F-012
title: "launcher fuzzy 匹配 + workflow list --json"
status: pending
complexity: light
depends_on: []
touches: 
  - scripts/lib/workflow_command_dispatcher.py
  - scripts/lib/workflow_list.py
  - tests/lib/test_launcher_fuzzy.py
  - tests/lib/test_workflow_list_json.py
created_at: "2026-05-14 15:21:49"
updated_at: "2026-05-14 15:21:49"
review_report: null
---

# F-012 · launcher fuzzy 匹配 + workflow list --json

> 来源：`requirements/REQ-2026-011/artifacts/features.json` · `F-012`
> 设计：`requirements/REQ-2026-011/artifacts/detailed-design.md`（按 ac_refs 检索）
> priority=P2 · ac_refs=AC-09 · pr_group=P2 · estimate_days=1.0

## 需求描述

scripts/lib/workflow_command_dispatcher.py 或 launcher 入口接 difflib.get_close_matches(query, registry_keys, n=1, cutoff=0.6) 接入编辑距离 ≤ 2 fuzzy 匹配；硬编码 ≥ 10 词 fuzzy 词典覆盖 standard-8phase / code-review-embedded 等常见拼写变体。scripts/lib/workflow_list.py main() 加 --json flag，输出 json.dumps(list_result, ensure_ascii=False) 单行；不破坏既有人类可读 stdout（缺省仍为表格）。落地 AC-09。

## 触及范围

> **frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）解析。
> 本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- scripts/lib/workflow_command_dispatcher.py
- scripts/lib/workflow_list.py
- tests/lib/test_launcher_fuzzy.py
- tests/lib/test_workflow_list_json.py

### 涉及模块（features.json `modules`）

- scripts/lib/workflow_command_dispatcher.py
- scripts/lib/workflow_list.py
- tests/lib/test_launcher_fuzzy.py
- tests/lib/test_workflow_list_json.py

## 依赖

### 依赖的 feature

无

> 实际派发前 `dispatch_precheck.py` 校验 `depends_on` 所有前置 feature 的 task.md `status == done`。

## 验收标准（features.json `acceptance`）

- [ ] fuzzy：'standar-8phase'（拼写错）→ hit standard-8phase（编辑距离=2）
- [ ] fuzzy：'review-emb'（短名）→ hit code-review-embedded
- [ ] fuzzy 词典 ≥ 10 词 hit 单测全过
- [ ] workflow list --json → stdout 为有效 JSON（json.loads 不抛）；含 schema_version + items[]
- [ ] workflow list（无 --json）→ 保持既有表格输出格式不变
- [ ] workflow list --json --filter=phase=development → JSON 内仅含该 phase 的 run

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] 接口实现符合详细设计（detailed-design.md 对应 §）
- [ ] 单元测试覆盖主流程
- [ ] 边界条件 / 反向回归 acceptance 全部命中
- [ ] 无明显安全 / 并发 / 性能问题
- [ ] `post-dev-verify` 门禁通过（`scripts/gates/run.py --trigger=post-dev --req=REQ-2026-011 --feature=F-012`）
- [ ] `/code-review` 通过 + sign-off approved
