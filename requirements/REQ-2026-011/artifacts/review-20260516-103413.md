# 代码审查报告 · REQ-2026-011 · 2026-05-16 10:34:13 · F-013 rev1

## 结论

**looks_clean** · 0 critical / 1 major (follow-up) / 5 minor (follow-up) · score=85

- `looks_clean` — AI 机器评估无阻塞问题 ✅ 本次结论
- `needs_attention` — AI 机器评估发现待改进项
- `blocked` — AI 机器评估发现关键问题

## 范围

- **feature_id**：F-013 · Claude 运行参数白名单 7 字段 + node_ready contract 透传
- **base_sha**：d99c195 · **head_sha**：163ab29 (rev1，含 housekeeping 5b19bb3)
- **增量**：+641 -78 / 7 文件（2 产品代码 + 2 新建测试 + 2 修改测试 + 1 receipt）
- **测试基线**：1524 passed, 8 skipped, 0 failed（pytest tests/）

## 路由说明

本次审查路由由 `code-review-prepare` 自动决策（policy V2）：

- **决策**：accept（4 推荐 checker；files_must_hit / files_suggest_hit 命中产品代码 + 测试文件）
- **跑了哪些 checker**：complexity / error-handling / design-consistency / auxiliary-spec
- **跳过的 checker**：security / concurrency / performance / history-context（路径未命中 must/suggest）

## 待 sign-off 提示

本次评审输出 `conclusion = looks_clean`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人在 tty 执行：

```bash
/code-review:signoff REV-REQ-2026-011-code-F-013-001            # 对话式 sign-off
# 或：python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-011-code-F-013-001 --decision approved
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-013 转 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## AC-04a 改写说明（rev1 重大行为变更）

F-013 dispatch prompt 原本只要求 AC-10（7 字段 contract 透传）；但 features.json AC #5/#6 明确要求 `node_ready.data.external_action_contract`，意味着 dispatcher 必须写 node_ready 而非 node_completed。subagent 因此把 `_dispatch_skill_node / _dispatch_prompt_node / _dispatch_agent_node` 改为：

- 事件类型：`node_completed` → `node_ready`
- 返回 outcome：`completed` → `awaiting_claude_action`
- data 含 `node_kind / external_action_contract / skill|prompt|agent` 字段

**附带影响**：tests/skills/ 11 条既有测试预期 `outcome=completed` 失败 → touches 扩到 tests/skills/* + rev2 commit 163ab29 统一对齐到 AC-04a 新行为（11 测试全过）。AC-04a 改写是 features.json AC #5/#6 隐含要求 + F-007 fail-closed 流程的契约前提（save_node_result.py 期望 state=awaiting_claude_action ∧ 末位 node_ready），三方一致。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"三方透明度。

**Major (1 keep follow-up)**

- **F-CR-003+007** [complexity-checker · major (merge)] → critic: not_rebutted → **最终处置：follow-up（IB sweep）**
  - 裁决依据：`scripts/lib/workflow_loader.py:323-374` `_validate_nodes_schema` CCN 21→39 (+18) / 嵌套深度 2→5 (+3)（Judge 独立 ast 实测高于 critic 报 28/5）。F-013 直接引入。归 follow-up 而非 keep：与 IB-32（`_classify_nodes` CCN≈18）/ IB-37（`dispatch` CCN=12）历史处置完全同模式归 IB sweep；rev2 1524 passed 0 failed 证明功能正确；trend-G-meta 六连正向终结不应在 F-013 单点破断。
  - 建议：抽 `_validate_node_run_params(node, nid, report, file_label)` helper（IB sweep PR 同批）

**Minor (5 keep follow-up)**

- **F-CR-005** [design-consistency-checker · minor]（critic 已降级，原 major） → critic: not_rebutted → **最终处置：follow-up（IB-37 同批）**
  - 裁决依据：`scripts/lib/workflow_dispatcher.py:221/258/304` 三处 `_dispatch_*_node` 新写 `node_ready` 事件缺 `run_id` 字段；`detailed-design.md:101 + §3.3.3:677` 明确"写入侧保持冗余"约定。rebuild 不消费 → 功能 OK，影响仅日志/审计/外部消费，降 minor follow-up 合理。`_dispatch_agent_node` 还需把 `run_state` 加入签名。
- **F-CR-004** [design-consistency / auxiliary-spec · minor]（critic 已降级，原 major） → critic: not_rebutted → **最终处置：follow-up（IB-34 doc-refresh + F-001 重审）**
  - 裁决依据：`workflow_loader.py:355-359` `idle_timeout` 拒 None；features.json AC #3 + detailed-design.md:129/859 描述 `int|null` 合法，与实现矛盾。根因 = F-001 既有 schema + design 未澄清 nullable 语义；F-013 dispatch prompt 让 subagent"沿用既有逻辑"是正确执行（不应越权改 F-001 已 looks_clean 的 schema）。F-013 测试固化错误行为是"按既有行为补回归"，但确实把 doc-code drift 钉死。归 IB-34 doc-refresh + F-001 schema 重审。
- **F-CR-010** [error-handling-checker · minor] → critic: not_rebutted → **最终处置：follow-up（IB sweep）**
  - 裁决依据：`workflow_loader.py:364-369` W100 错误消息缺 actual value（与既存:285/295 `实际 {nid!r}` 模板不一致）；归 IB sweep 同模式补齐。
- **F-CR-013** [design-consistency-checker · minor] → critic: not_rebutted → **最终处置：follow-up**
  - 裁决依据：`tests/skills/test_workflow_dispatcher_dispatch.py:55-72` TC-D1 `valid_outcomes` 列表只列 7 值缺 `awaiting_claude_action`（F-002 遗漏 + F-013 没顺手 sweep）。但 `test_run_state_new_events.py:178-182` 已专门验 AC-6 awaiting_claude_action 存在，语义未漏。
- **F-CR-015** [auxiliary-spec-checker · minor] → critic: not_rebutted → **最终处置：follow-up**
  - 裁决依据：`tests/lib/test_dispatch_skill_contract.py:73` `_node_completed_event` 函数定义但从未被调用（AC-04a 改写后所有 node_ready 路径不再期望 node_completed）。保留或删都可。
- **F-CR-016** [auxiliary-spec-checker · minor] → critic: not_rebutted → **最终处置：follow-up（IB-39 lint sweep）**
  - 裁决依据：`workflow_loader.py:360,370` 两行 121 字符越 pyproject.toml `ruff line-length=120`（F-013 引入；同文件 pre-existing 越限 4 行不归 F-013）。

**Drop (9 项)**

- **F-CR-001** [complexity] critic: rejected → drop（pre-existing：`workflow_dispatcher.py` 总行数 715，blame bcdc200d 2026-05-12；与 D-014 trend-G-meta 经验一致）
- **F-CR-002** [complexity] critic: rejected → drop（pre-existing：`workflow_loader.py` 总行数 891，blame fee71457 2026-05-11）
- **F-CR-008** [complexity] critic: rejected → drop（pre-existing：`_validate_when_and_var_refs` CCN=24 + `_validate_node_substructures` CCN=25 + 嵌套 7/6 全 blame fee71457）
- **F-CR-009** [complexity] critic: rejected → drop（reviewer 自承函数 body 41 行 < 阈值 80 行）
- **F-CR-011** [error-handling] critic: rejected → drop（`_build_external_action_contract` fail-safe：dispatch_node:177-189 已有 except Exception 兜底，调用方 node 经 schema loader 已校验）
- **F-CR-012** [error-handling] critic: rejected → drop（pre-existing：dispatch_node except 块二次失败丢错误，blame bcdc200d）
- **F-CR-014** [design-consistency] critic: rejected → drop（已知 IB-34 doc-refresh：`ALLOWED_NODE_FIELDS / _validate_node_fields` 设计 §3.4.2 提及但 loader 走 per-field if-block 模式无此集合）
- **F-CR-018** [complexity] critic: rejected → drop（reviewer 自承 PASS：`_build_external_action_contract` CCN=1 / 10 行符合预期）
- **F-CR-020** [auxiliary-spec] critic: rejected → drop（reviewer 自承 advisory 不算 finding）

**Not proven (3 项 → drop)**

- **F-CR-006** [auxiliary-spec] critic: not_proven → drop（`_build_external_action_contract` 单行 docstring：10 行 CCN=1 helper docstring 规范非硬约束，同文件 `_is_valid_thinking` 等 helper 也单行 docstring）
- **F-CR-017** [auxiliary-spec] critic: not_proven → drop（W100 消息"元素必须全为字符串"主语仍是「节点 X.field」模板，模板解读偏狭）
- **F-CR-019** [auxiliary-spec] critic: not_proven → drop（test helper `_make_run_state` 2 行无 docstring，非硬规范）

## Critical（必须修复）

_无_

## Trend Monitors

- **trend-G-meta** — 正向终结持续 6 连：F-007→F-008→F-010→F-011→F-012→**F-013** 同 looks_clean 85-90 模式；本轮 22 candidate 经 Judge 独立验证 0 keep critical/major-blocking + 6 follow-up；pre-existing 污染（F-CR-001/002/008/012）准确剔除
- **trend-CR-A-module-bloat** — 单函数维度新信号：`_validate_nodes_schema` CCN +18（21→39）单点最大恶化；与 IB-32 `_classify_nodes` (CCN≈18) / IB-37 `dispatch` (CCN=12) 同模式延续；建议 IB sweep 时 helper 抽取一并做
- **trend-D-design-doc-drift** — 单点新信号：`node_ready` 事件 `run_id` 字段三处缺漏 vs detailed-design §2.2.1 + §3.3.3 写入侧冗余约定；F-013 AC-04a 改写责任主体；下一次同类 dispatcher 改写需在 spec 卡点把 `run_id` 列入必填模板
- **trend-CR-B-fail-closed-test-gap** — 未触发：本期 dispatcher contract 透传 + run_param schema 校验均有正/负向测试覆盖（24 new + 11 旧测试对齐到 AC-04a，rev2 1524 passed 0 failed）

## 维度评分

| 维度 | 评分 | 说明 |
|---|---|---|
| complexity | 75 | F-CR-003+007 CCN/嵌套 follow-up |
| error_handling | 88 | F-CR-010 W100 缺 actual value follow-up |
| design_consistency | 82 | F-CR-005 run_id 缺漏 + F-CR-013 DispatchOutcome 7 vs 8 |
| auxiliary_spec | 80 | F-CR-004 / F-CR-015 / F-CR-016 共 3 项 follow-up |
| security | 90 | 0 issue |
| concurrency | 90 | 0 issue（checker 未跑，inferred 基线） |
| performance | 90 | 0 issue（checker 未跑，inferred 基线） |
| history_context | 92 | 0 issue（checker 未跑，inferred 基线） |
| **综合分** | **85** | conclusion=looks_clean |

## Follow-up Notes (IB sweep candidates)

- **IB-32 复用**：`_validate_nodes_schema` CCN 21→39 抽 `_validate_node_run_params` helper（IB sweep PR 同批）
- **IB-34 doc-refresh**：`detailed-design.md idle_timeout` nullable 语义澄清 + F-001 schema 重审是否接受 None
- **IB-37 同批**：dispatcher 三处 `_dispatch_*_node` 补 `'run_id': run_state.run_id`（`_dispatch_agent_node` 需把 `run_state` 加入签名）
- **IB sweep**：W100 错误消息追加 actual value（loader:364-369 对齐既存 :285/:295 f-string 模式）
- **IB-39 lint sweep**：`workflow_loader.py:360,370` 两行 121 字符拆行
- **IB sweep**：`tests/skills/test_workflow_dispatcher_dispatch.py` TC-D1 `valid_outcomes` 补 `awaiting_claude_action`；`tests/lib/test_dispatch_skill_contract.py:73` `_node_completed_event` 死代码处置

## verdict 落盘

`requirements/REQ-2026-011/reviews/code-F-013-001.json` (REV-REQ-2026-011-code-F-013-001, score=85, conclusion=looks_clean)
