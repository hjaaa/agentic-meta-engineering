# 代码审查报告 · REQ-2026-011 · 2026-05-15 21:42:36

## 结论

**needs_attention** · 0 critical / 2 major / 3 minor · score=85

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项 ✅ 本次结论
- `blocked` — AI 机器评估发现关键问题

## 路由说明

本次审查路由由 `code-review-prepare` 自动决策：

- **决策**：accept（自动模式，files_must_hit=0, files_suggest_hit=1, files_trivial=0, files_total=5）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-15 21:30:27
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker
- **跳过的 checker（含原因）**：
  - security-checker：路径未命中 must/suggest 任何规则
  - concurrency-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则
  - auxiliary-spec-checker：路径未命中 must/suggest 任何规则

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。本批 F-CR-008 fail-closed 测试缺位归 keep major，按 Judge `suggestions[2]` 建议**本 PR 补 3 个 pytest.raises(WorkflowError) 用例**后派 rev2 再评。

未 sign-off 时 feature-lifecycle-manager 不会把对应 feature 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-CR-001** [complexity-checker · major] → critic: not_rebutted → **最终处置：keep → follow_up**
  - 裁决依据：workflow_continue.py 533 行确超阈值 500（实测 wc -l）；但本批仅扩展 +234 行未引入新文件。IB-31 已立 sub_workflow_poller.py 拆分预案，归后续 PR
- **F-CR-002** [complexity-checker · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：_handle_child_failed_by_policy 实测控制流嵌套 = try(1) → if/elif/else(2) = 深度 3 < 阈值 4；reviewer 把字典字面量缩进算入控制流
- **F-CR-003** [complexity-checker · minor] → critic: not_rebutted → **最终处置：keep → follow_up**
  - 裁决依据：_poll_sub_workflows 10 分支点（if/for/if/if/for/if/if/if/elif/elif）+ on_failure or 'fail' 三元 → CC=11 超阈值 10；归 IB-31 同批拆
- **F-CR-004** [complexity-checker · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：_main_loop 实测深度 2（while → if node is None），远低于阈值 4；reviewer 自承"视觉/格式问题"
- **F-CR-005** [error-handling-checker · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：**by-design** —— features.json:346 AC-08 + detailed-design.md §4.5 mermaid 均明示 cancelled→{child_force_killed, node_failed}，不要求 workflow_failed；reviewer 误判 spec
- **F-CR-006** [error-handling-checker · major] → critic: not_proven → **最终处置：drop**
  - 裁决依据：三处 `raise WorkflowError from exc` 保留 chained traceback；main() L516-519 顶层 + __main__ L529-533 双层捕获；可追溯性已满足
- **F-CR-007** [design-consistency-checker · minor] → critic: not_rebutted → **最终处置：keep → follow_up**
  - 裁决依据：事件 data.on_subworkflow_failure=None 在 completed/cancelled 写 None、failed 写实际策略值；二者不冲突但同名混读
- **F-CR-008** [error-handling-checker · major] → critic: not_rebutted → **最终处置：keep（本 PR 建议补）**
  - 裁决依据：612 行新测试 grep `pytest.raises\|WorkflowError` 全文 0 hit；workflow_continue.py:61/106/179 docstring 三次明示 fail-closed 契约 → 回归保护完全缺位
- **F-CR-009** [error-handling-checker · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：continue/skip/fail 三分支实测同一 try 内；reviewer 把 try 边界判错
- **F-CR-010** [error-handling-checker · minor] → critic: not_rebutted → **最终处置：keep → follow_up**
  - 裁决依据：L243-246 WARN 缺 run_id；对照同模块 L328 IB-10 风格基线应加
- **F-CR-011** [design-consistency-checker · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：scope 5 文件不含 detailed-design.md（review-scope.json:13-17）；归 IB-34 doc-refresh 批次
- **F-CR-012** [design-consistency-checker · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：同 F-CR-011，不在 scope，归 IB-34

## Critical（必须修复）

_无_

## Major（强烈建议修复）

### [test_coverage · error_handling] tests/lib/test_poll_subworkflows.py:1-453 + tests/e2e/test_sub_workflow_backfill.py:1-159

612 行新增测试零 `pytest.raises(WorkflowError)` 覆盖；workflow_continue.py:61/106/179 docstring 三次明示 "OSError → 包成 WorkflowError 向上抛（fail-closed）" 契约，但任何 reviewer 删除 `raise from exc` 改为 `pass` 都不会被测试发现，主链路契约失去回归保护。

**建议**：本 PR 补 3 个 pytest.raises(WorkflowError) 用例（_handle_child_completed / _handle_child_failed_by_policy / _handle_child_cancelled 各一次），mock append_event raise WorkflowError，断言异常向上传播 + chained __cause__ 保留。

### [complexity · maintainability] scripts/lib/workflow_continue.py:1-533

模块 533 行超阈值 500（F-009/010/011 三连扩展信号）；归 follow_up 后续 PR（IB-31 已立预案）。

**建议**：拆 sub_workflow_poller.py 单独模块，下个 PR 优先级（不阻断本 feature）。

## Minor（改进建议）

### [complexity] scripts/lib/workflow_continue.py:209-276

`_poll_sub_workflows` CC=11 超阈值 10。

**建议**：抽 `_dispatch_terminal_event` 把 if/elif/elif 三分支收敛，与 F-CR-001 同批入 IB-31。

### [readability · consistency] scripts/lib/workflow_continue.py:70,115,188

事件 `data.on_subworkflow_failure=None` 在 completed/cancelled 路径与 L267-268 注释 "None→默认 fail" 同名字段双语境易误读。

**建议**：completed/cancelled 路径改写 `"n/a"` 字符串或省略字段。

### [readability · error_handling] scripts/lib/workflow_continue.py:243-246

`_poll_sub_workflows` WARN 缺 run_id 上下文；对照同模块 L328 IB-10 风格基线应加 run_id。

**建议**：WARN 字符串改为 `f'WARN: ... (run_id={run_state.run_id!r}, node_id={node_id!r}, warnings={sub_warnings})'`。

## Follow-up Notes（不阻塞，但值得提醒）

- F-CR-001（workflow_continue.py 533 行超阈值）→ IB-31 sub_workflow_poller.py 拆模块预案应升优先级
- F-CR-003（_poll_sub_workflows CC=11）→ 与 F-CR-001 同批入 IB-31
- F-CR-007（on_subworkflow_failure=None 同名双语境）→ 小可读性改进，IB sweep
- F-CR-010（WARN 缺 run_id）→ 同模块风格一致，IB sweep
- F-CR-011 + F-CR-012（detailed-design.md §4.5 mermaid jsonl_path 缺 + continue 分支缺）→ IB-34 doc-refresh 批次（development→testing 切换前同批闭环）

## Trend Signals

- **trend-CR-A-module-bloat（新增）**：F-009 / F-010 / F-011 连续三个 feature 在 workflow_continue.py 增量（_render_status 注入 / _dispatch_loop_node OSError 双捕 / sub_workflow 三 handler + _poll）；模块 533 行 + _poll_sub_workflows CC=11 双重超阈 → IB-31 拆模块预案升优先级
- **trend-CR-B-fail-closed-test-gap（新增）**：F-007 main loop / F-008 path_lock acquire / F-011 sub_workflow 三连出现 "docstring 明示 fail-closed → 实现 raise from exc 正确 → 测试零 pytest.raises 覆盖" 模式；建议建 IB 系统化补回归（含 F-007/008 历史遗漏）
- **trend-D-design-doc-drift**：F-CR-011/012 是 F-009 / F-010 同一信号延续，由 IB-34 doc-refresh 批次系统化收敛

## 未运行的 checker（如有）

- ⚠️ security-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ concurrency-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ performance-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ history-context-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ auxiliary-spec-checker（原因：路径未命中 must/suggest 任何规则）

## 审查元信息

- **审查范围**：embedded REQ-2026-011 / F-011（5 文件，+875 / -0 行）
- **并行 checker**：3（complexity / error-handling / design-consistency）
- **Critic 统计**：rejected 4 / not_proven 3 / not_rebutted 5
- **综合模型**：code-quality-reviewer
- **Base SHA**：823442b
- **Head SHA**：faa6807
- **Review ID**：REV-REQ-2026-011-code-F-011-001
- **5 维分数**：correctness 88 / readability 82 / consistency 92 / safety 75 / maintainability 80
- **8 维分数（verdict）**：design_consistency 90 / security 90 / concurrency 90 / complexity 80 / error_handling 72 / auxiliary_spec 90 / performance 90 / history_context 90
