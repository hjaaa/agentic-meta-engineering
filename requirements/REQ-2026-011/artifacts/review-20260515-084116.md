# 代码审查报告 · REQ-2026-011 · F-005 · 2026-05-15 08:41:16

## 结论

**blocked(67)** · 1 blocker / 0 major / 3 minor

- 1 blocker required_fix（F-CR-001：dispatcher 不调 substitute_vars，所有 artifact 节点真实运行时必失败）
- 1 minor required_fix（F-CR-005：测试硬编码绝对路径绕过 $VAR 展开层，对 F-CR-001 真实场景零覆盖；与 F-CR-001 配对修复）
- 2 minor follow-up（F-CR-002/F-CR-003：dispatch_node CCN=11 + 文件 580 行，沉淀到独立重构窗口）

## 路由说明

- **决策**：accept（命中 1 suggest 文件 / 0 must / 3 trivial / 8 总文件）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-15 08:30:01
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker
- **跳过的 checker（含原因）**：
  - security-checker：路径未命中 must/suggest 任何规则
  - concurrency-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则
  - auxiliary-spec-checker：路径未命中 must/suggest 任何规则

## 待 sign-off 提示

本次评审输出 `conclusion = blocked`，**这只是 AI 的机器评估**，不代表合并通过。本结论包含 required_fixes，不应直接 sign-off：

- rev2 修复完成后会重新评审，新 verdict 通过后再走 sign-off
- 若用户判断需绕过 rev2 直接 sign-off rejected：

```bash
python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-011-code-F-005-001 --decision rejected
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-005 转 done；GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-CR-001** [error-handling-checker · critical] → critic: not_rebutted → **最终处置：required_fix（blocker）**
  - 裁决依据：critic 5 条反证全成立——调用签名 line 155 缺 run_state/env、函数体 537-580 未调 substitute_vars、yaml 19+ 处 $VAR 引用、run_artifact_checks 不展开 shell 变量、同模块其他 4 类 handler 全调 substitute_vars。Judge 升级到 blocker（standard-8phase.yaml 8 个 artifact 块在生产路径上 100% 失败，AC-02 真实可跑承诺未达成）
- **F-CR-002** [complexity-checker · minor] → critic: not_rebutted → **最终处置：follow_up**
  - 裁决依据：dispatch_node CCN=11 超阈值 1，属代码味道非功能 bug，沉淀重构窗口；不阻塞 looks_clean
- **F-CR-003** [complexity-checker · minor] → critic: not_rebutted → **最终处置：follow_up**
  - 裁决依据：workflow_dispatcher.py 580 行超 500 阈值 80 行，属重构而非缺陷，与 F-CR-002 共一次重构窗口处理
- **F-CR-004** [error-handling-checker · minor] → critic: rejected → **最终处置：dropped**
  - 裁决依据：scripts/lib/run_artifact_checks.py 不在 F-005 touches 内，本次 PR 该文件 0 改动，scope-out follow-up
- **F-CR-005** [error-handling-checker · minor] → critic: not_rebutted → **最终处置：required_fix（与 F-CR-001 配对）**
  - 裁决依据：测试全文 grep $ARTIFACTS_DIR / substitute_vars 0 命中，对 F-CR-001 真实场景零覆盖；测试通过给出错误信心；rev2 修 F-CR-001 时必须配套补含 $VAR 的真实展开测试用例

## Critical / Blocker（必须修复）

### [F-CR-001 · design_consistency + error_handling] scripts/lib/workflow_dispatcher.py:155 + 537-580

`_dispatch_artifact_node(node, run_dir, root, jsonl_path)` 调用签名缺 `run_state` / `env`，函数体未对 spec 字符串调 `substitute_vars`。standard-8phase.yaml 中 8 个 artifact 块共 19+ 处 `$ARTIFACTS_DIR` 等 $VAR 引用，全部以字面值传入 `run_artifact_checks` → `(cwd / "$ARTIFACTS_DIR/requirement.md").resolve()` → 永远不存在 → 所有 artifact 节点必然 failed。同文件 skill/prompt/bash/approval handler 均调 `substitute_vars`，唯独 artifact 漏调。

**建议**：
1. `workflow_dispatcher.py:155` 把调用改为 `_dispatch_artifact_node(node, run_state, run_dir, root, env, jsonl_path)`；函数签名同步补两参
2. 函数内对 spec 做深度遍历：对 `must_exist` / `must_not_exist` 列表的每项 str、`schema_check[].script` 与 `args[]`、`must_contain_sections[].file`、`must_match_regex[].file` 全部调 `substitute_vars(s, run_state.node_outputs, env, escape_for_bash=False)` 后再喂给 `run_artifact_checks`（路径模式 escape 必须为 False，否则路径被加引号反而 break）

## Major（强烈建议修复）

无。

## Minor（改进建议）

### [F-CR-005 · error_handling · required_fix 配对] tests/lib/test_dispatch_artifact.py:192-193

TC-F05-3 (`test_real_check_meta_script_exists`) 直接用 `str(check_meta_path)` / `str(tmp_path / "nonexistent-meta.yaml")` 构造 spec，全程绕过 `$ARTIFACTS_DIR` 字面字符串路径；test_dispatch_artifact.py 全文 grep `$ARTIFACTS_DIR` / `substitute_vars` 0 命中，整套测试无任何用例覆盖变量展开路径。

**建议**：增补至少 1 条 TC：spec 写 `$ARTIFACTS_DIR/foo.txt`，run_state 注入 `ARTIFACTS_DIR=tmp_path/artifacts`，断言展开后 `must_exist` 命中真实文件——这是 F-CR-001 的最小覆盖。

## Follow-up Notes（不阻塞，但值得提醒）

- **F-CR-002**：scripts/lib/workflow_dispatcher.py:111-177 dispatch_node CCN=11 超阈值 1。建议下次重构窗口以字典分发表（`_NODE_TYPE_KEY_TO_DISPATCHER: dict[str, Callable]`）替换 elif 链，每加节点类型 +1 行而 CCN 保持常数（≈4）
- **F-CR-003**：scripts/lib/workflow_dispatcher.py 580 行（基线 528 +52）超 500 阈值 80 行。建议下次重构窗口拆 `_dispatch_*_node` 到 `scripts/lib/node_dispatchers/<type>.py`（`_dispatch_artifact_node` 可先行移到 `node_dispatchers/artifact.py` 44 行），主文件压至 100 行
- **F-CR-004（dropped, scope-out follow-up）**：scripts/lib/run_artifact_checks.py:77 `except (FileNotFoundError, subprocess.TimeoutExpired)` 与同文件 `_check_sections` 的 `except OSError` 风格不一致；本次 PR 不在 touches，留作独立 PR 处理（PermissionError / BlockingIOError 等 OSError 子类罕见但显式更稳）

## 未运行的 checker（如有）

- ⚠️ security-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ concurrency-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ performance-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ history-context-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ auxiliary-spec-checker（原因：路径未命中 must/suggest 任何规则）

## 审查元信息

- **审查范围**：F-005 · artifact 第 8 类 dispatcher + check_meta.py 修正（8 文件，+343 -4 行；其中 3 代码文件 +315 -1）
- **并行 checker**：3，总耗时 ~250s（complexity 148s / error-handling 268s / design-consistency 169s）
- **Critic 统计**：rejected 1 / not_proven 0 / not_rebutted 4
- **综合模型**：code-quality-reviewer (claude-opus-4-7)
- **Base SHA**：2471343
- **Head SHA**：5a96880
- **REV-ID**：REV-REQ-2026-011-code-F-005-001
- **verdict 路径**：requirements/REQ-2026-011/reviews/code-F-005-001.json
