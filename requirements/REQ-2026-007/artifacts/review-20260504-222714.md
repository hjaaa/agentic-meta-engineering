# 代码审查报告 · REQ-2026-007 · F-002 · 2026-05-04 22:27:14

## 结论

**needs_attention** · 0 critical / 5 major / 6 minor（drop 6 / follow-up 6）

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项 ✅ 当前
- `blocked` — AI 机器评估发现关键问题

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：accept（推荐 3 个 checker）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-04 22:15:55
- **跑了哪些 checker**：error-handling-checker / design-consistency-checker / auxiliary-spec-checker
- **跳过的 checker（含原因）**：
  - complexity-checker：路径未命中 must/suggest 任何规则（注：Judge 复盘时仍把 list_requirements 89 行作为 complexity 维度记录，下次可考虑把 .py 改 ≥ 200 行新增升入 must 触发 complexity）
  - security-checker：路径未命中 must/suggest 任何规则
  - concurrency-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人执行：

```bash
/code-review:signoff REV-REQ-2026-007-code-F-002-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-007-code-F-002-001 --trivial  # 纯文档变更快速通道（本次不适用）
```

未 sign-off 时 feature-lifecycle-manager 不会把对应 feature 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

**当前阶段建议**：先按下方 must-fix 5 条（F-2 / F-3 / F-4 / F-12 / F-541）派 subagent 修复 → 重跑 `/code-review F-002` 复审 → 复审 `looks_clean` 后再 sign-off。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-1** [design-consistency · minor] → critic: not_proven → **drop**
  - 裁决依据：phase-rules.md 切换链已含 testing → completed；`/requirement:archive` 命令尚未真正存在（F-003 任务），phase-rules 不写虚命令路径反而正确
- **F-2** [design-consistency · major] → critic: not_rebutted → **keep（must-fix）**
  - 裁决依据：features.json + tasks/F-002.md 验收 TC-F2-7 明确要 stderr 含 'mutually exclusive' 英文，实现 ValueError 文案是中文"互斥"——按字面验收 grep 会失败
- **F-3** [design-consistency · minor] → critic: not_rebutted → **keep（must-fix）**
  - 裁决依据：tasks/F-002.md TC-F2-3 措辞 jsonschema validate 与实现（check_meta.py 自定义校验）不符；建议改任务文案而非翻代码
- **F-4** [design-consistency · minor] → critic: not_rebutted → **keep（must-fix）**
  - 裁决依据：phase-rules.md:33 archived_at 时间示例 `2026-04-23 15:30:00` 不含 offset 但描述写 "ISO 8601 with offset"，与 context/team/engineering-spec/time-format.md 自相矛盾
- **F-5** [error-handling · major] → critic: not_rebutted → **follow-up（may-defer）**
  - 裁决依据：list_requirements.py YAML 解析失败 silent skip + rc=0，与 check_meta.py:223 设计断裂；推迟理由：list 是只读展示工具，rc=0 + stderr 警告语义可接受；建议下个 PR 集中统一错误模型
- **F-6** [error-handling · minor] → critic: not_rebutted → **follow-up**
  - 裁决依据：与 F-5 同源 silent skip 路径
- **F-7** [error-handling · minor] → critic: not_rebutted → **follow-up**
  - 裁决依据：与 F-5 同源
- **F-8** [error-handling · minor] → critic: not_rebutted → **follow-up**
  - 裁决依据：F-5 收尾时套 rel() 即可
- **F-9** [auxiliary-spec · major] → critic: not_proven → **drop**
  - 裁决依据：pyproject.toml ruff select=['F'] 不报 A001；本函数内未用到 builtins.all，无实质风险
- **F-10** [complexity · major] → critic: not_rebutted → **follow-up**
  - 裁决依据：拆函数和 F-5~F-8 错误模型一起做更经济
- **F-11** [auxiliary-spec · major] → critic: rejected → **drop**
  - 裁决依据：项目无 isort 强制，ruff F 类不管 import 顺序
- **F-12** [auxiliary-spec · major] → critic: not_rebutted → **keep（must-fix）**
  - 裁决依据：tests/lifecycle/test_list_filter.py:6 ruff F401 实测报错，CI 直接 fail
- **F-13** [design-consistency · minor] → critic: not_rebutted → **follow-up**
  - 裁决依据：当前 schema 字段确实没用到，加 schema 形参属于"为统一而统一"，价值低
- **F-14** [auxiliary-spec · minor] → critic: not_proven → **drop**
  - 裁决依据：本 PR 注释已多处解释 why，CLAUDE.md "注释解释为什么" 是软原则
- **F-15** [auxiliary-spec · minor] → critic: rejected → **drop**
  - 裁决依据：pyproject.toml `python_functions = ["test_*", "should_*"]` 明确双轨
- **F-16** [auxiliary-spec · minor] → critic: rejected → **drop**
  - 裁决依据：既有 tests/__init__.py 同样 0 字节零换行，符合项目惯例
- **F-541** [auxiliary-spec · major / Judge 自查新增] → 直接 keep → **keep（must-fix）**
  - 裁决依据：scripts/lib/list_requirements.py:53 `raise ValueError(f"顶层不是 mapping")` 是无占位符 f-string，ruff F541 实测报错，CI 直接 fail

## Critical（必须修复）

无。

## Major（强烈建议修复）

### [验收文案] tests/lifecycle/test_list_filter.py:125 + scripts/lib/list_requirements.py:104

features.json + tasks/F-002.md TC-F2-7 验收要求 stderr 含英文 `'mutually exclusive'`，但实现 ValueError 文案是中文 `"--all 与 --phase 互斥"`，测试匹配 `match="互斥"` 也是中文。按字面验收 grep `mutually exclusive` 会失败。

**建议**：在 ValueError 文案加双语，如 `"--all 与 --phase 互斥（mutually exclusive），不能同时使用"`，并同步测试 match 为 `"mutually exclusive"`。

### [验收文案] requirements/REQ-2026-007/artifacts/tasks/F-002.md:57

TC-F2-3 措辞 'jsonschema validate' 与实现（check_meta.py:150 手写 `_check_archived_at_state_machine` 而非 jsonschema）不符。

**建议**：改任务文案为 `python3 scripts/lib/check_meta.py <path>` 拒收非法组合，明确验证命令；不动代码。

### [文档自相矛盾] .claude/skills/managing-requirement-lifecycle/reference/phase-rules.md:33

archived_at 时间示例 `2026-04-23 15:30:00` 不含 offset 但描述写 "ISO 8601 with offset"，与 context/team/engineering-spec/time-format.md 第 7-9 行规范（`YYYY-MM-DD HH:MM:SS`，Asia/Shanghai 不带后缀）矛盾，会误导 F-003 实现者。

**建议**：改描述为 `格式：YYYY-MM-DD HH:MM:SS（Asia/Shanghai，不带 offset；详见 context/team/engineering-spec/time-format.md）`。

### [Lint] tests/lifecycle/test_list_filter.py:6

`from datetime import datetime` 未使用，ruff F401 实测报错（pyproject.toml ruff select=['F'] 真启用），CI 直接 fail。

**建议**：删除该 import。

### [Lint] scripts/lib/list_requirements.py:53

`raise ValueError(f"顶层不是 mapping")` 是无占位符 f-string，ruff F541 实测报错，CI 直接 fail。

**建议**：去掉 f 前缀或加占位符（如 `f"{rel(path)}: 顶层不是 mapping"`）。

## Minor（改进建议）

无（minor 全部进 follow-up 或 drop）。

## Follow-up Notes（不阻塞，但值得提醒）

下面 6 条建议单独开 PR `chore: list_requirements 错误模型对齐 check_meta` 一次性收尾，本 feature 不阻塞：

- **list_requirements.py 错误处理设计与 check_meta.py 断裂**：YAML 解析失败、created_at 解析失败、缺字段都 silent skip + rc=0，导致 CLI/CI 无法感知损坏文件（来自 F-5 / F-6 / F-7）
- **list_requirements.py:52-54 except 兜底 str(exc) 路径泄漏**：用 `type(exc).__name__` 替代或路径替换（来自 F-8）
- **list_requirements.py:82-170 list_requirements() 函数 89 行超 60 行规范**：建议拆 `_scan_meta_files` / `_apply_filters` / `_sort_records` 三个内部函数（来自 F-10）
- **check_meta.py:150 `_check_archived_at_state_machine` 签名缺 schema 参数**：与既有 `_check_*` 风格断裂，统一时再补（来自 F-13）

## 未运行的 checker（如有）

按 routing 决策跳过，理由见路由说明段：

- ⚠️ complexity-checker（原因：路径未命中 must/suggest）
- ⚠️ security-checker（原因：路径未命中 must/suggest）
- ⚠️ concurrency-checker（原因：路径未命中 must/suggest）
- ⚠️ performance-checker（原因：路径未命中 must/suggest）
- ⚠️ history-context-checker（原因：路径未命中 must/suggest）

## 审查元信息

- **审查范围**：F-002 增量（4 commits 1a96398..3330f71，10 文件，+419 -10 行）
- **并行 checker**：3，对抗验证 1，综合裁决 1
- **Critic 统计**：rejected 3 / not_proven 3 / not_rebutted 10
- **综合模型**：code-quality-reviewer
- **Base SHA**：033cc4d
- **Head SHA**：3330f71
- **verdict 文件**：requirements/REQ-2026-007/reviews/code-F-002-001.json
