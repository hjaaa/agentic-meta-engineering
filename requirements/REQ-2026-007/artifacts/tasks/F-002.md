---
feature_id: F-002
title: phase-rules.md #9 completed + meta.archived_at + list 默认过滤
status: done
complexity: low
depends_on: []
created_at: "2026-05-04 21:57:27"
updated_at: "2026-05-04 22:35:00"
review_report: requirements/REQ-2026-007/reviews/code-F-002-002.json
---

# F-002 · phase-rules.md #9 completed + meta.archived_at + list 默认过滤

## 需求描述

- 8 阶段表加第 9 行 `completed`；切换链补 `testing → completed`（命令路径 `/requirement:archive`）
- `archived_at` 字段语义说明（detail-design §4.4 / §4.1）
- 改 `context/team/engineering-spec/meta-schema.yaml`：`archived_at` 加字段类型约束 + 与 `phase=completed` 双字段判断（`phase != completed && archived_at != ''` 视为非法）
- 改 `.claude/commands/requirement/list.md` 参数表加 `--all` / `--phase`；默认隐藏 `phase=completed`（§3.4.3）
- `--all` 与 `--phase` 互斥同传 exit 1
- 新增 `tests/lifecycle/test_list_filter.py` 覆盖默认过滤 / `--all` / `--phase` / 互斥四种路径

**协作信息**：本 feature 与 F-001 改动无重叠模块，可并行；F-003 依赖本 feature 提供 `phase=completed` 与 `archived_at` 字段。

## 接口/数据结构

- meta-schema：`archived_at: string | null`，格式 ISO8601 with offset；`phase != completed && archived_at != ''` → invalid
- list 命令参数：`--all`（boolean） / `--phase <name>`（string，可枚举）；互斥
- 默认行为：列出时过滤掉 `phase=completed` 的需求

interfaces_frozen: true

## 依赖

### 依赖的 feature

无

### 依赖的模块

- `.claude/skills/managing-requirement-lifecycle/reference/phase-rules.md`
- `context/team/engineering-spec/meta-schema.yaml`
- `.claude/commands/requirement/list.md`
- `tests/lifecycle/test_list_filter.py`

## 触及范围

- `.claude/skills/managing-requirement-lifecycle/reference/phase-rules.md`
- `context/team/engineering-spec/meta-schema.yaml`
- `.claude/commands/requirement/list.md`
- `tests/lifecycle/test_list_filter.py`

## 验收要点（acceptance）

- TC-F2-1: phase-rules.md 8 阶段表新增第 9 行 `completed`；切换链段含 `testing → completed`
- TC-F2-2: phase-rules.md `archived_at` 字段说明段含「ISO8601 with offset；archive 命令成功后写入；空表示已 completed 但未归档」
- TC-F2-3: meta-schema.yaml 含 `archived_at` 字段类型约束 + 状态机规则；python3 scripts/lib/check_meta.py <path> 拒收非法组合
- TC-F2-4: pytest `tests/lifecycle/test_list_filter.py::test_default_hides_completed` 绿
- TC-F2-5: pytest `test_all_includes_completed` 绿
- TC-F2-6: pytest `test_phase_filter` 绿（`--phase development` 仅列 development 阶段）
- TC-F2-7: pytest `test_all_and_phase_mutex` 绿（同传 exit 1 + stderr 包含 `mutually exclusive`）

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] phase-rules.md / meta-schema.yaml / list.md 三处文档同步
- [ ] 单元测试覆盖主流程
- [ ] `--all` / `--phase` 互斥校验
- [ ] 默认过滤不破坏既有 list 输出格式
- [ ] `/code-review` 通过
