---
feature_id: F-003
title: archive 命令完整链 + 自举 V-08
status: pending
complexity: high
depends_on: [F-002]
created_at: "2026-05-04 21:57:27"
updated_at: "2026-05-04 21:57:27"
review_report: null
---

# F-003 · archive 命令完整链 + 自举 V-08

## 需求描述

- 新建 `.claude/commands/requirement/archive.md`（detail-design §3.4.2 骨架）
- 新建 `.claude/skills/managing-requirement-lifecycle/reference/archive-rules.md`（§3.5 全文骨架：4 预检 / 5 步执行 / 三问串行 / 错误矩阵）
- 改 `SKILL.md` 加 archive 子动作 + 意图映射
- Skill 实现 `archive_requirement` 入口（§3.1 签名 + `ArchiveResult` dataclass）
- 4 项预检失败映射 `R-ARCHIVE-*` 错误码（§3.5.1）
- 原子写 `meta.yaml.tmp` + `os.replace`（§3.5.2 第 1 步）
- `process.txt [archived]` 事件走 `requirement-progress-logger` Skill（§4.3）
- 三问串行按倾向 A 案——Skill 返回三个问句给主 Agent，主 Agent 串行问；同时支持 `--yes-experience` / `--yes-local-branch` / `--yes-remote-branch` 跳问 flag（§3.5.5）
- 副作用动作降级矩阵（§3.5.4）：经验失败 → 仍 exit 0 + 打 error；本地 `-D` 拒绝 → 透传 git error（D-014）；远程 already-deleted 折叠
- 新增 `tests/lifecycle/test_archive.py`：4 预检 × pass/fail（mock `gh pr view`、`git status`、meta yaml）+ 三动作 yes/no/skipped/failed × 3
- 自举 V-08：本需求 PR merge 后用 archive 归档自身

## 接口/数据结构

- `archive_requirement(req_id: str, *, yes_experience: bool = False, yes_local_branch: bool = False, yes_remote_branch: bool = False, force: bool = False) -> ArchiveResult`
- `ArchiveResult` dataclass：`experience: Literal['yes','no','skipped','failed']` / `local_branch: ...` / `remote_branch: 同上 + 'already-deleted'`
- 错误码：`R-ARCHIVE-PHASE` / `R-ARCHIVE-DIRTY` / `R-ARCHIVE-NO-PR` / `R-ARCHIVE-PR-NOT-MERGED`

interfaces_frozen: true

## 依赖

### 依赖的 feature

- F-002（提供 `phase=completed` 状态合法性 + `archived_at` 字段语义）

### 依赖的模块

- `.claude/commands/requirement/archive.md`
- `.claude/skills/managing-requirement-lifecycle/reference/archive-rules.md`
- `.claude/skills/managing-requirement-lifecycle/SKILL.md`
- `scripts/lib/archive_runner.py`
- `tests/lifecycle/test_archive.py`

## 触及范围

- `.claude/commands/requirement/archive.md`
- `.claude/skills/managing-requirement-lifecycle/**`
- `scripts/lib/archive_runner.py`
- `tests/lifecycle/test_archive.py`
- `requirements/REQ-2026-007/meta.yaml`（自举 V-08）

## 验收要点（acceptance）

- TC-F3-1: pytest `test_precheck_phase_invalid` 绿（phase=development → exit 1 + stderr 含 `R-ARCHIVE-PHASE`）
- TC-F3-2: pytest `test_precheck_dirty` 绿（git status 非空 → `R-ARCHIVE-DIRTY`）
- TC-F3-3: pytest `test_precheck_no_pr` 绿（meta.pr_number=0 → `R-ARCHIVE-NO-PR`）
- TC-F3-4: pytest `test_precheck_pr_not_merged` 绿（gh pr view state=OPEN → `R-ARCHIVE-PR-NOT-MERGED`；`--force` 跳过本预检 → exit 0）
- TC-F3-5: pytest `test_three_prompts_yes_path` 绿（三问全 y → ArchiveResult.experience=yes/local_branch=deleted/remote_branch=deleted）
- TC-F3-6: pytest `test_three_prompts_no_path` 绿（三问全 N → 三 outcome=skipped + meta + process.txt 仍写）
- TC-F3-7: pytest `test_failure_degradation` 绿（experience 调用 raise → outcome=failed + archive exit 0；git branch -d 拒绝 → outcome=failed 透传 error；远程已删 → outcome=already-deleted）
- TC-F3-8: V-01 沙盒 REQ-2099-007 跑 archive：phase=completed + archived_at 写入 + process.txt `[archived]` 事件追加 + 终端反馈 5 行格式正确（spec §5.3）
- TC-F3-9: V-08 自举——本需求 PR merge 后跑 `/requirement:archive`，验证：`yq '.archived_at' requirements/REQ-2026-007/meta.yaml` 输出非空 ISO8601；`grep -c '\[archived\]' requirements/REQ-2026-007/process.txt` 等于 1（幂等校验）

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] 4 项预检全实现并映射错误码
- [ ] 单元测试覆盖主流程 + 降级矩阵
- [ ] 三问 flag 跳问路径
- [ ] `archive_at` 原子写 + process.txt 写入幂等
- [ ] `/code-review` 通过
