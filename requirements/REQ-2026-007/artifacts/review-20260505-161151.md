# 代码审查报告 · REQ-2026-007 · F-004 · 20260505-161151

## 结论

**needs_attention** · 0 critical / 1 major / 4 minor

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项 ✅ 本次
- `blocked` — AI 机器评估发现关键问题

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：accept（routing.yaml 推荐：design-consistency + auxiliary-spec）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-05 15:56:46
- **跑了哪些 checker**：design-consistency-checker / auxiliary-spec-checker
- **跳过的 checker（含原因）**：
  - complexity-checker：路径未命中 must/suggest 任何规则
  - security-checker：路径未命中 must/suggest 任何规则
  - concurrency-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - error-handling-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则

> ⚠️ 注：F-004 含 +993 行 Python（核心模块 + 测试），但 complexity / error-handling / security 都被 routing.yaml 跳过——`scripts/lib/**.py` 未列入这些 checker 的 must/suggest 规则。这是 routing.yaml 的覆盖 backlog，本轮已由用户在卡点 A accept，由 design-consistency + auxiliary-spec 兜底。

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人执行：

```bash
/code-review:signoff REV-REQ-2026-007-code-F-004-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-007-code-F-004-001 --trivial  # 纯文档变更快速通道（本次不适用——含 major）
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-004 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

**本次 conclusion=needs_attention 含 1 项 major（F-001 process.txt 事件缺失），建议先修后 sign-off。**

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示「reviewer 发现 → critic 反证 → Judge 裁决」的三方透明度。

- **F-001** [design-consistency · major] → critic: not_rebutted → **最终处置：must-fix**
  - 裁决依据：submit_codex.py 全文 0 处写 process.txt（grep 验证）；archive_runner.py:488 是同 detail-design 对称模块；features.json TC-F4-8 acceptance 强制要求两事件；SKILL.md:60-61 白名单已登记 submit_codex 触发方。四方证据闭合
- **F-002** [design+auxiliary · minor / V-09 拼合 hack] → critic: rejected → **最终处置：drop**
  - 裁决依据：submit-rules.md:11 注释明文授权运行时拼合 + V-09 grep 实跑仅一处命中
- **F-003** [design-consistency · minor / 状态机最后一行缺 stderr 注释] → critic: not_rebutted → **最终处置：follow-up**
  - 裁决依据：submit-rules.md:158 与上方 8 行风格不一致；doc-grade 顺手补，不阻塞合并
- **F-004** [design-consistency · minor / 401-403-404 折叠到 5xx] → critic: not_proven → **最终处置：follow-up**
  - 裁决依据：超出 detailed-design §3.3.3 授权（仅授权连接失败/DNS/SSL）；fail-closed 是合理工程取舍，二选一处置（推荐 doc-grade 补登记）
- **F-005** [auxiliary-spec · minor / submit_with_codex 70 行] → critic: not_proven → **最终处置：drop**
  - 裁决依据：编排型入口已委托 8 个独立 helper；CLAUDE.md ≤60 行是建议非强制
- **F-006** [auxiliary-spec · minor / req_id dead param] → critic: not_rebutted → **最终处置：follow-up**
  - 裁决依据：与 F-001 联动修——F-001 在该函数内追加 [codex-review-triggered] req=<id> 后 req_id 会自然变 used
- **F-007** [auxiliary-spec · minor / 章节标记型注释] → critic: not_proven → **最终处置：drop**
  - 裁决依据：archive_runner 同款风格；编排型代码段章节标记是合理例外
- **F-008** [auxiliary-spec · minor / 函数内 import argparse] → critic: not_proven → **最终处置：drop**
  - 裁决依据：合理 lazy import（仅 CLI 路径用，不影响库 import 时间）
- **F-009** [auxiliary-spec · minor / GhApi5xx 缩写大小写] → critic: rejected → **最终处置：drop**
  - 裁决依据：scripts/gates/plugins/gh_auth.py:35 GhAuthGate 即仓库既有惯例
- **F-010** [auxiliary-spec · minor / MagicMock unused import] → critic: not_rebutted → **最终处置：follow-up**
  - 裁决依据：lint 级噪音，与 F-003/F-006 一并 trivial 批次清掉
- **F-011** [auxiliary-spec · minor / 测试命名] → critic: rejected → **最终处置：drop**
  - 裁决依据：features.json acceptance 字面冻结测试函数名（interfaces_frozen=true）
- **F-012** [auxiliary-spec · minor / f-string 混用] → critic: not_proven → **最终处置：drop**
  - 裁决依据：仓库 style guide 未禁；submit_codex.py 实际全用 f-string
- **F-013** [auxiliary-spec · minor / 状态机代码块无语言标记] → critic: not_proven → **最终处置：drop**
  - 裁决依据：与 detailed-design §3.6 同款裸 ``` 风格
- **F-014** [auxiliary-spec · minor / 三常量段位置] → critic: not_proven → **最终处置：drop**
  - 裁决依据：导引锚点合理化（V-09 唯一事实源置顶）
- **F-015** [auxiliary-spec · minor / --codex 说明文本过长] → critic: rejected → **最终处置：drop**
  - 裁决依据：slash command 文档详尽是 UX 需求；submit.md patch 是 detailed-design §3.4.1 精确指令

## Critical（必须修复）

无。

## Major（强烈建议修复）

### [design-consistency] scripts/lib/submit_codex.py（全文 0 处写 process.txt）

**F-001**：缺 `[codex-review-triggered]` / `[codex-review-received]` 两个 process.txt 事件写入。

四方证据闭合：

1. `detailed-design.md` §4.3 表格强制要求两事件
2. `features.json` F-004 acceptance TC-F4-8 写明「process.txt 含 [codex-review-triggered] + [codex-review-received] 各一条」
3. `requirement-progress-logger/SKILL.md:60-61` 白名单登记触发方为 `submit_codex` → 本 Skill
4. 对称参照 `archive_runner.py:488` 的 `_append_process_event` 是同设计文档下的合规实现

**建议**：参照 `archive_runner.py` 的 `_append_process_event` 模式：

- `_trigger_codex_comment` 成功后追加 `[codex-review-triggered] round=N pr=#num`
- `_persist_round` 成功后追加 `[codex-review-received] verdict=<v> round=N`

复用 `archive_runner._now_cst_str` 风格，append 瞬间取 now（progress-logger SKILL.md 硬约束）。修复时把 `req_id` 自然带入两条事件的写入路径，可顺带消除 F-006 的 dead param。

## Minor（改进建议）

> 本节仅含 `final_disposition=keep` 且 severity=minor 的 finding；本次 4 条 minor 全部 final_disposition=follow-up，已迁至下方 Follow-up Notes 段。

无。

## Follow-up Notes（不阻塞，但值得提醒）

- **F-003**（来自 design-consistency）：submit-rules.md:158 状态机最后一行 `[error-exit-1] --> exit 1` 缺 stderr 注释；改为 `[error-exit-1] --> exit 1 (stderr ❌ gh api repeated 5xx during poll; aborting)` 与异常文案表对齐。doc-grade
- **F-004**（来自 design-consistency）：submit_codex.py:211-212 把 401/403/404 折叠到 GhApi5xx，超出 detailed-design §3.3.3 授权（仅授权连接失败/DNS/SSL）。二选一：A. detailed-design §3.3.3 补登记『401/403/404 也折叠到 5xx 计数器（fail-closed 保守）』(推荐 doc-grade)；B. 401/403/404 改单次 abort 直接 exit 1 + 专属错误文案 (code-grade)
- **F-006**（来自 auxiliary-spec）：submit_codex.py:156 `_trigger_codex_comment` 的 `req_id` 形参 dead；建议与 F-001 联动修（写入 `[codex-review-triggered] req=<id>` 时 req_id 自然变 used）
- **F-010**（来自 auxiliary-spec）：tests/lifecycle/test_submit_codex.py:20 `MagicMock` unused import；删除即可，lint 级顺手清

## 未运行的 checker（如有）

> ⚠️ 以下 checker 因 routing.yaml 路径未命中规则，本轮跳过。如有疑虑，可在卡点 A 选 `a`（all）强制全跑。

- ⚠️ complexity-checker（原因：scripts/lib/**.py 未列入该 checker 的 must/suggest 规则）
- ⚠️ security-checker（原因：同上）
- ⚠️ concurrency-checker（原因：同上）
- ⚠️ performance-checker（原因：同上）
- ⚠️ error-handling-checker（原因：同上）
- ⚠️ history-context-checker（原因：同上）

## 审查元信息

- **审查范围**：F-004 · submit --codex 子模式 · 嵌入模式（7 文件，+1068 / -2 行）
- **并行 checker**：2 个（design-consistency / auxiliary-spec），6 个 skipped
- **Critic 统计**：rejected 4 / not_proven 7 / not_rebutted 4
- **综合模型**：code-quality-reviewer
- **Base SHA**：5b3d895
- **Head SHA**：7fff5fb
- **verdict 文件**：`requirements/REQ-2026-007/reviews/code-F-004-001.json`
