# 代码审查报告 · 20260521-archive-runner-auto-pr · 2026-05-22 08:46:42 · Round 2

## 结论

**needs_attention** · 0 critical / 1 major / 3 minor · **score 82**（↑ from R1 62）

- 范围：F-001 累计 `scripts/lib/archive_runner.py` +306 -50（e332f2e..47bc7e8）
- verdict 文件：`reviews/code-F-001-002.json`（supersedes `code-F-001-001`）
- Round-1 → Round-2：**blocked → needs_attention**（critical/major 全部 FIXED；剩余仅复杂度 + 命名/注释类）

## Round-1 confirmed_fixes

| Finding | R1 严重度 | R2 验证状态 |
|---|---|---|
| **F-20** archive_requirement 主流程删除 `_cleanup_worktree_before_archive` 调用 | critical | ✅ FIXED — AST 验证 L1004-1088 无调用，函数定义保留 L954 供 F-003 |
| **F-05** push 路径加 `_check_branch_safe_for_remote_op` 防御 | major | ✅ FIXED — helper L69-95，三处对称（push L655 / del-local L725 / del-remote L842） |
| **F-17** 7 处 `_run()` 包 `_run_or_abort` 兜 TimeoutExpired/FileNotFoundError/OSError | major | ✅ FIXED — helper L184-201，7/7 替换（L559/573/614/629/659/674/685） |

## 路由说明

- **决策**：`all`（diff 仍为灰色文件主导，升 8 全集兜底）
- **确认人**：`hj19961223@gmail.com`
- **确认时间**：2026-05-22 08:30:56
- **跑了哪些 checker**：8 全集
- **跳过的 checker**：无

## 人工确认提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。

`needs_attention` 默认建议先修复（特别是 F-06 major 复杂度，本期 frozen interface 不阻碍函数体内拆 helper）。如判断风险可接受，需在主对话**显式接受风险**（如"接受当前 needs_attention 风险，转 done"）后 feature-lifecycle-manager 才会把 F-001 转 done。`GATE-REVIEW-VERDICT` 在 phase-transition / submit 时对 needs_attention 仅触发 warning（strict 模式才升 fail）。

## 裁决明细

> 20 候选 finding：rejected 11 / not_proven 5 / not_rebutted 4；最终 keep 4 + follow-up 2 + drop 14。

### keep（4 条，进 needs_attention 段）

- **F-06** [complexity · major] → critic: not_rebutted → **keep**
  - `_commit_archive_metadata` CC=16（AST + radon 双源实测）；critic 反驳"interfaces_frozen 阻碍重构"——锁的是模块对外签名，函数体内可抽 helper
- **F-07** [complexity · minor] → critic: not_rebutted → **keep**
  - `_push_feat_branch` CC=11，超阈值 1 格
- **F-15** [aux-spec · minor] → critic: not_rebutted → **keep**
  - L551 / L645 两处 `# noqa: ARG001` 缺一行说明（同文件 L53/54/100/503 均遵守惯例）
- **F-17** [aux-spec · minor] → critic: not_rebutted → **keep**
  - L243 `allowed: list[str] = []` 与实际语义反向（收集的是 non-whitelisted）；调用方 L590 已用 non_whitelisted 印证

### follow-up（2 条，hardening sprint 处理）

- **F-01** [security · major] → critic: not_proven → **follow-up**
  - L766 `git switch base_branch` 前缺 `_check_branch_safe_for_remote_op` 校验；事实层成立但 PoC `-f` 不存在 git switch 短选项，攻击模型需 meta.yaml 预先污染（仓库已被攻陷场景）
- **F-02** [security · minor] → critic: not_proven → **follow-up**
  - 黑名单 vs 白名单，建议改 `re.fullmatch(r'[a-zA-Z0-9_./-]+', ...)`；与 F-01 合并到 hardening 批次

### drop（14 条）

- **F-03** [security · minor] rejected：`_PROTECTED_BRANCHES` 是行业惯例公开值，非敏感
- **F-04** [security · minor] rejected：自承不可触发（git status 已归一化）
- **F-05** [error · minor] rejected：项目 pyproject.toml 无 [tool.mypy] / pyright 配置，mypy 门禁前提不存在
- **F-08** [complexity · major] not_proven → drop：`_delete_local_branch` CC=17 主体既有债（commit 2fed367 引入 auto-switch），本期 delta ≤2 CC
- **F-09** [complexity · critical] rejected：file_lines 既有债延续（round-1 F-02 同主题已 rejected）
- **F-10** [concurrency · major] rejected：speculative F-003 隐患，F-003 尚未实现
- **F-11** [performance · minor] not_proven → drop：N≤5 影响极小（F-14 延续）
- **F-12** [performance · minor] not_proven → drop：N≤5 cost 可忽略
- **F-13** [performance · minor] rejected：调用 1 次性能可忽略
- **F-14** [aux-spec · minor] rejected：docstring 语义自洽（"进程没起来"= FileNotFoundError+OSError 2 类 + "超时"= TimeoutExpired 1 类 = 3 类，与标题"三类"一致）
- **F-16** [aux-spec · minor] rejected：L100 noqa: N802 已带说明 `— 保持大写以便 mock`
- **F-18** [design · minor] rejected：`_delete_local_branch.strict` 属 F-003 范围
- **F-19** [design · minor] rejected：`_delete_remote_branch.legacy_resurrect` 属 F-007 范围
- **F-20** [design · minor] rejected：`_render_summary.stage` 属 F-003 范围

## Major（建议修复）

### [complexity] scripts/lib/archive_runner.py:551 `_commit_archive_metadata`

**CC=16（AST + radon 双源实测）**，超阈值 10 共 6 格；函数体 92 行（exec_lines 80 踩线）。decision 节点统计：12 if/for + 4 BoolOp。

`interfaces_frozen: true` 锁的是**模块对外的 callable 签名**（参考 review-20260521-170215.md:46 既有共识），函数体内重构允许。建议三段拆分：

```python
def _idempotent_skip_if_commit_exists(req_id: str) -> bool: ...   # L558-570 → CC~3
def _collect_paths_to_add(raw_lines, req_id) -> list[str]: ...    # L587-611 → CC~5
def _do_commit(req_id, to_add) -> None: ...                       # L613-642 → CC~4

def _commit_archive_metadata(req_id, result):                     # 拆后 CC~6
    if _idempotent_skip_if_commit_exists(req_id):
        return
    raw_lines = _check_dirty_and_get_lines(req_id)
    to_add = _collect_paths_to_add(raw_lines, req_id)
    if not to_add:
        return
    _do_commit(req_id, to_add)
```

拆后预计 CC 6/3/5/4 全部合规。**建议本期顺手修**——这是 round-2 唯一 major，修完 conclusion 可升 looks_clean。

## Minor（改进建议）

### [complexity] scripts/lib/archive_runner.py:645 `_push_feat_branch`

CC=11，超阈值 1 格。round-2 引入的 `_check_branch_safe_for_remote_op` 调用（1 if + 1 BoolOp）+ L693 三元 or-chain 是主要 driver。建议拆 `_check_head_eq_remote(branch) -> bool` + `_do_push(branch, ...) -> None`。

### [aux-spec] scripts/lib/archive_runner.py:551,645 `# noqa: ARG001` 缺说明

```python
# 改前
def _commit_archive_metadata(req_id: str, result: ArchiveResult) -> None:  # noqa: ARG001
def _push_feat_branch(meta: dict[str, Any], req_id: str, result: ArchiveResult) -> None:  # noqa: ARG001

# 改后
def _commit_archive_metadata(req_id: str, result: ArchiveResult) -> None:  # noqa: ARG001 — result 保留供 F-003 finalize 写 outcome
def _push_feat_branch(meta: dict[str, Any], req_id: str, result: ArchiveResult) -> None:  # noqa: ARG001 — 同上
```

### [aux-spec/readability] scripts/lib/archive_runner.py:243 `_filter_dirty_lines_by_whitelist` 局部变量反义

```python
# 改前
allowed: list[str] = []
for line in lines:
    ...
    if _is_whitelist_path(raw_path, req_id):
        continue
    allowed.append(raw_path)  # 实际是 non_whitelisted
return allowed

# 改后
non_whitelisted: list[str] = []
for line in lines:
    ...
    if _is_whitelist_path(raw_path, req_id):
        continue
    non_whitelisted.append(raw_path)
return non_whitelisted
```

调用方 L590 已用 `non_whitelisted` 接收，函数内统一即可。

## Follow-up Notes（不阻塞，hardening sprint 处理）

- **F-01 base_branch 缺校验**：L766 `git switch base_branch` 前仅 L755 空值检查，无选项/refspec 防御；事实层成立但攻击模型需 meta.yaml 预先污染。建议下次 hardening sprint 把 `_check_branch_safe_for_remote_op` 复用到 base_branch 上
- **F-02 正向白名单优于黑名单**：建议改 `re.fullmatch(r'[a-zA-Z0-9_./-]+', branch)`，与 F-01 同批次

## Cross-Dimension Insights

1. **复杂度 + auxiliary 同源**：F-06 / F-07 拆 helper 时同步明确 `ArchiveResult` 写入路径，顺手消化 F-15 的 `# noqa: ARG001` 解释义务
2. **F-01 / F-02 攻击面分析**：meta.yaml 不属 untrusted input（工具链内部写入 + Hook 拦截），git switch 对 `-X` 类输入通常返回非零被 fail-soft 接住；当前调用方 fail-closed/fail-soft 双层兜底已足够本期 ship

## 未运行的 checker

无（8 全集均已运行）

## 审查元信息

- **审查范围**：F-001 累计 diff（`scripts/lib/archive_runner.py` +306 -50，e332f2e..47bc7e8）
- **并行 checker**：8（complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec）
- **Critic 统计**：rejected 11 / not_proven 5 / not_rebutted 4
- **综合模型**：code-quality-reviewer
- **Base SHA**：e332f2e
- **Head SHA**：47bc7e8
- **Verdict 文件**：`reviews/code-F-001-002.json`（score=82，supersedes code-F-001-001）
- **Score 拉分项**：design_consistency 55→92 / security 70→90 / error_handling 65→88
