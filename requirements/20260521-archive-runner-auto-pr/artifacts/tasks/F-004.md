---
schema_version: "1.0"
feature_id: F-004
title: "meta-schema 加 archive_pr_number + check_meta 状态机"
status: done
complexity: light
depends_on: []
touches:
  - context/team/engineering-spec/meta-schema.yaml
  - scripts/lib/check_meta.py
  - .claude/skills/managing-requirement-lifecycle/templates/meta.yaml.tmpl
created_at: "2026-05-21 16:55:00"
updated_at: "2026-05-21 17:20:00"
review_report: requirements/20260521-archive-runner-auto-pr/artifacts/review-20260521-170215.md
---

# F-004 · meta-schema + check_meta 状态机

## 需求描述

1. `context/team/engineering-spec/meta-schema.yaml` 在 `optional_fields` 段追加 `archive_pr_number`；`fields` 段加新字段定义（详细设计 §4.1 C-4 落点）
2. `scripts/lib/check_meta.py` 新增 `_check_archive_pr_number_state_machine` 函数（详细设计 §4.2）
3. `templates/meta.yaml.tmpl` 同步加字段（紧跟 `pr_number: 0` 下一行，C-4 物理位置）
4. 历史 15 个 completed REQ 不需要 backfill（状态机表格场景 3 PASS）

状态机 4 场景：

| phase | archive_pr_number | 校验结果 |
|---|---|---|
| != completed | 0 / 缺失 | PASS |
| != completed | > 0 | FAIL state-machine |
| == completed | 0 / 缺失 | PASS（兼容历史 + 半完成重跑） |
| == completed | > 0 | PASS（正常完成） |

## 接口/数据结构

`interfaces_frozen: true`：

```python
def _check_archive_pr_number_state_machine(
    meta: dict[str, Any], report: Report, file_label: str
) -> None: ...
```

调用注入点：`check_meta.py` 内 `_check_archived_at_state_machine`（来源：scripts/lib/check_meta.py:150）同位置顺序追加。

meta-schema.yaml fields 段新增：

```yaml
archive_pr_number:
  type: int
  required: false
  description: "归档 PR number（archive 阶段 1 创建后写入；finalize 阶段读取校验 MERGED）；0/缺失视同未创建。状态机：phase=completed 且字段>0 → 已完成归档；phase=completed 且字段=0 → 半完成可重跑 archive；phase!=completed 且字段>0 → 非法。"
```

## 依赖

### 依赖的 feature

无（顶层 feature，最早可开始）

### 依赖的模块

- 现有 `check_meta.py` Report / Severity 类
- 现有 `_check_archived_at_state_machine` 模式（参考实现）

## 触及范围

- `context/team/engineering-spec/meta-schema.yaml`（optional_fields + fields 段）
- `scripts/lib/check_meta.py`（新增状态机函数 + main 注入）
- `.claude/skills/managing-requirement-lifecycle/templates/meta.yaml.tmpl`（C-4 物理位置）

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] meta-schema.yaml 字段 + description
- [ ] check_meta.py 状态机 4 场景覆盖
- [ ] templates/meta.yaml.tmpl 字段位置 C-4 落点
- [ ] 历史 15 REQ 不需要 backfill 验证
- [ ] `/code-review` 通过
