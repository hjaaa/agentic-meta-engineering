# 代码审查报告 · 20260521-archive-runner-auto-pr · F-006 · 2026-05-22 14:15:00

## 结论

**needs_attention** · score=74 · 0 critical / 2 major required-fix / 2 minor required-fix · 5 follow-up · 2 drop

- 范围：F-006（archive 双阶段文档同步），2 个 markdown 文件 +230 -25
  - `.claude/commands/requirement/archive.md` (+43 -7)
  - `.claude/skills/managing-requirement-lifecycle/reference/archive-rules.md` (+187 -18)
- Reviewed commit：`628a3c7`（base `68a2b3d`）
- verdict 文件：`reviews/code-F-006-001.json`

---

## 路由说明

- **决策**：`accept`（命中 must + suggest）
- **确认人**：hj19961223@gmail.com（git config）
- **确认时间**：2026-05-22 14:10:05
- **跑了哪些 checker**：`design-consistency-checker`、`auxiliary-spec-checker`（2/8）
- **跳过的 checker（路径未命中 must/suggest 规则）**：
  - `complexity-checker`
  - `security-checker`
  - `concurrency-checker`
  - `performance-checker`
  - `error-handling-checker`
  - `history-context-checker`

---

## Required Fixes（4 项，按优先级）

### 1. F-002（major）— `archive.md:131` `--keep-branch` 描述与 F-003 实现不符

**问题**：F-003 后阶段 1 archive 已彻底移除三件套调用（`archive_runner.py:1680-1684` 注释明示「三件套调用已全部移除」），`--keep-branch` 在阶段 1 实际无任何效果；但 archive.md L131 仍写「`--keep-branch` 跳过删分支两问（历史行为保留兼容）」，会误导用户。

**修法**：将 L131 改为
```
阶段 1：`--no-experience` 跳过经验沉淀问。`--keep-branch` 已被 F-003 弃用（阶段 1 archive 不再删分支），仅保留 CLI 参数签名兼容。
```

### 2. F-003（major）— `archive-rules.md:326` 标题"10 步"vs 列表实际 12 步

**问题**：§A.3 标题写「finalize 10 步顺序」，下方代码块 L328-340 实际列编号 1~12 共 12 步。

**修法**：标题改为
```
### §A.3 finalize 12 步顺序
```
（或合并若干步骤压缩到 10 条；以改标题成本更低）

### 3. F-004（minor）— `archive-rules.md:280` "4 项预检"应为"5 项"

**问题**：同一文档内 §1 标题 + L123 + L250 均写「5 项预检」，archive.md L149 也写「5 项」；唯独 L280 末行写「4 项预检」。

**修法**：L280 改为
```
archive 命令始终 exit 0（除非 5 项预检挂）。
```

### 4. F-005（minor）— `archive.md:51` 参数表未标注 `--keep-branch` 弃用

**问题**：`archive_runner.py:2006-2007` argparse help 已明示「已被 F-003 弃用」，但 archive.md 参数表用户视角文档中无任何弃用标记。

**修法**：参数表 `--keep-branch` 行改为
```
| `--keep-branch` | false | （已被 F-003 弃用，阶段 1 不再删分支；保留兼容 CLI 参数签名）|
```

---

## Follow-up（5 项 cosmetic，建议本次顺手清掉以避免下次重复触发同类 finding）

| F-id | file | 议题 | 修法 |
|---|---|---|---|
| F-006 | archive-rules.md:121 | `## archive 前 CI gate 预检` 标题无编号 | 改为 `## 1.5 archive 前 CI gate 预检` 或重命为 `### §1.X` 子节 |
| F-007 | archive-rules.md:70 | "进入第 §2 步" 中英冗余 | 改为 `进入 §2` 或 `进入第 2 步` |
| F-008 | archive-rules.md:20,49 | 状态流转图代码块缺 lang 标签 | 加 ` ```text ` |
| F-009 | archive-rules.md:328 | §A.3 步骤伪代码块缺 lang 标签 | 加 ` ```text ` |
| F-010 | archive-rules.md L15/16/199/211/212/224 (`<id>`) vs L71/105/178/190/191/312 (`<req_id>`) | 占位符混用 | 统一为 `<req_id>` |

---

## 裁决明细

| F-id | checker | 原 severity | critic verdict | Judge 处置 | 理由 |
|---|---|---|---|---|---|
| F-001 | design-consistency | major | not_rebutted | **drop** | Judge 推翻 critic：`archive_runner.py:1340` 是 `_delete_remote_branch.legacy_resurrect` 参数定义位（flag 核心实现入口），引用合理；critic 反证「应指 :2041」不准确（:2041 不存在该 flag 行，:2033 才是 argparse 注册位） |
| F-002 | design-consistency | major | not_rebutted | **keep（required-fix）** | archive_runner.py:1680-1684 注释明示三件套调用已移除，archive.md:131 描述误导 |
| F-003 | design-consistency | major | not_rebutted | **keep（required-fix）** | 标题与正文步数直接矛盾，基础 sanity |
| F-004 | design-consistency + auxiliary-spec | minor | not_rebutted | **keep（required-fix）** | 同文档内数字 4/5 混用 |
| F-005 | design-consistency | minor | not_rebutted | **keep（required-fix）** | 用户视角文档与 argparse help 不同步 |
| F-006 | auxiliary-spec | minor | not_rebutted | **follow-up** | 标题编号 cosmetic |
| F-007 | auxiliary-spec | minor | not_rebutted | **follow-up** | 中英冗余 cosmetic |
| F-008 | auxiliary-spec | minor | not_rebutted | **follow-up** | 代码块 lang 标签 cosmetic |
| F-009 | auxiliary-spec | minor | not_rebutted | **follow-up** | 同 F-008 |
| F-010 | auxiliary-spec | minor | not_rebutted | **follow-up** | 占位符统一 cosmetic |
| F-011 | auxiliary-spec | minor | **not_proven** | **drop** | 双阶段表 archive.md 3 列 vs archive-rules.md 4 列；分层信息密度差异本身合理，无规范强制对齐 |

---

## 跨维度洞察

design-consistency 与 auxiliary-spec 两维度的 finding **80% 集中在"叙述与实现/自身不一致"**：

- F-002（叙述 vs 源码）
- F-003（标题 vs 正文）
- F-004（同一文档内数字 4 vs 5）
- F-005（用户视角文档 vs argparse help）
- F-010（占位符 `<id>` vs `<req_id>`）

建议在 reviewer 工序后期加一遍 grep 自检脚本（数字 / 占位符 / 步骤数 / flag 状态），单次可批量收口。F-006 是文档同步 feature——这类一致性是它的核心交付，下次改可考虑引入更系统的 doc-lint。

---

## 人工确认提示

本次评审输出 **conclusion = needs_attention**，这只是 AI 的机器评估，不代表合并通过。

按协议（feature-lifecycle-manager）：

- **默认动作**：主 Agent 把本报告 + 4 条 required-fix 交给**同一 sonnet subagent** 重派 rev2 修复（不是主 Agent 亲自修），状态保持 `in-progress`
- **风险接受路径**：如用户判断 4 条 required-fix 风险可接受（例如"文档发布前会人工 review，不阻塞合并"），需在主对话**显式接受风险**（如"接受 F-006 当前 needs_attention 风险，转 done"）后才允许把 status 翻 `done`，并在 `process.txt` 留一行 `[development] F-006 用户显式接受 needs_attention 风险转 done`

未获用户软确认时 feature-lifecycle-manager 不会把 F-006 转 done。
