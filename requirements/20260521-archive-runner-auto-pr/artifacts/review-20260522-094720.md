# 代码审查报告 · 20260521-archive-runner-auto-pr · 2026-05-22 09:47:20

scope: feature F-002（自动创建归档 PR）

## 结论

**needs_attention** · score=75 · 0 critical / 1 major / 7 minor

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项 ✅（本次）
- `blocked` — AI 机器评估发现关键问题

## 路由说明

本次审查路由由 `code-review-prepare` 自动确认：

- **决策**：all（diff 全是灰色文件，未命中 routing.yaml must/suggest，升 8 全集兜底）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-22 09:35:23
- **跑了哪些 checker**：8 全集（complexity / security / concurrency / performance / error_handling / design_consistency / history_context / auxiliary_spec）
- **跳过的 checker**：无

## 人工确认提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。

- 0 `required_fixes` + 1 major（complexity 重构建议）+ 7 minor（建议性技术债）
- 默认建议先修复 1 major（抽 `_handle_existing_pr_state` helper）后再转 done
- 如判断风险可接受，需在主对话**显式接受风险**（如"接受当前 needs_attention 风险，转 done"）后才允许转 done
- 无 `blocked` 项，不触发 fail-closed

未获用户软确认时 feature-lifecycle-manager 不会把 F-002 转 done；GATE-REVIEW-VERDICT 在 phase-transition / submit 时 `needs_attention` 仅触发 warning（strict 模式才升 fail）。

## 裁决明细

> 三方透明度：reviewer 发现 → critic 反证 → Judge 裁决

### Drop（critic 反证 + Judge sanity-check 成立）

- **F-13** [design_consistency · major] MERGED 缺 meta=0 前置 → critic: rejected → **drop**
  - 依据：`_precheck_phase` (archive_runner.py:261) 已强制 phase=completed 时 archive_pr_number=0；MERGED 路径不会因为有旧 archive_pr_number 进入
- **F-10** [performance · minor] 模板 read_text 每次 disk IO → critic: rejected → **drop**
  - 依据：archive 单次执行，lru_cache 无收益
- **F-14** [design_consistency · minor] --json 增加 url 字段 → critic: rejected → **drop**
  - 依据：url 用于回填 result.archive_pr_url，功能扩展无副作用
- **F-08** [concurrency · minor] tmp 路径与 _atomic_write_meta 冲突 → critic: not_proven → **drop**
  - 依据：_atomic_write_meta tmp 后缀 .yaml.tmp 与 mkstemp .md 后缀互不相干
- **F-12** [error_handling · minor] _write_archive_pr_number 写盘失败抛原生异常 → critic: not_proven → **drop**
  - 依据：与 _atomic_write_meta (line 461) 既有惯例一致，独立施加规则不合理
- **F-16** [history · minor] archive_runner.py 是热点文件 → critic: not_rebutted → **drop**
  - 依据：信息性提示，无可执行 finding
- **F-18** [auxiliary · minor] _check_existing_archive_pr 命名语义不准 → critic: not_proven → **drop**
  - 依据：函数职责清晰，无误导调用方
- **F-19** [auxiliary · minor] _create_archive_pr docstring 缺设计章节引用 → critic: not_proven → **drop**
  - 依据：docstring 已含 C-3 决策 anchor，章节引用收益小

### Downgrade

- **F-05** [security · critical→minor] branch 缺 _check_branch_name_safe 前置校验 → critic: not_proven → **downgrade**
  - 依据：上游 _push_feat_branch (line 1414) 已对 branch 跑 _check_branch_safe_for_remote_op；gh CLI 不走 git refspec；降到 minor 作深度防御
- **F-06** [concurrency · major→minor] TOCTOU 窗口 → critic: not_rebutted → **downgrade**
  - 依据：TOCTOU 理论成立但单进程顺序执行 + D-006 hook 锁实际风险窗口极窄
- **F-07** [concurrency · major→minor] _write_archive_pr_number 写整 meta dict → critic: not_rebutted → **downgrade**
  - 依据：与既有 _atomic_write_meta 写整 meta 惯例一致，非新引入反模式；但仅改单字段可精简

### Keep

- **F-001+F-002** [complexity · major] _create_archive_pr CC=13 / 嵌套 5 层 → critic: not_rebutted → **keep**
- **F-003+F-04+F-15** [auxiliary · minor] 死 import tempfile @ 627（5 checker 重票） → critic: not_rebutted → **keep**
- **F-09** [error_handling · minor] MERGED abort msg 缺 meta_pr 对照 → critic: not_rebutted → **keep**
- **F-11** [error_handling · minor] 模板缺失抛原生 FileNotFoundError → critic: not_rebutted → **keep**
- **F-17** [auxiliary · minor] docstring "Step 8/9" 风格与既有 "阶段 1 step 6/7" 不一致 → critic: not_rebutted → **keep**

## Critical（必须修复）

无。

## Major（强烈建议修复）

### [complexity] scripts/lib/archive_runner.py:735-807

`_create_archive_pr` 圈复杂度 CC=13、嵌套深度 5（branch 检查 → existing 检查 → state 分支 → MERGED/CLOSED/OPEN 三态 → 冲突校验）。

**建议**：抽出 `_handle_existing_pr_state(existing, meta, req_id, result) -> int` 把 MERGED/CLOSED/OPEN 三态处理与冲突检查内聚，主函数降到 CC<8、嵌套 ≤3。

## Minor（改进建议）

### [security] scripts/lib/archive_runner.py:752

`_create_archive_pr` 内未对 `meta.branch` 单独跑 `_check_branch_name_safe`。上游 `_push_feat_branch (line 1414)` 已覆盖该 branch 的安全校验且 gh CLI 不走 git refspec，但作为防御深度建议在此 fail-closed 前置校验一次。

**建议**：在 `branch = ...` 之后插入 `_check_branch_name_safe(branch)`。

### [concurrency] scripts/lib/archive_runner.py:760-807

`_check_existing_archive_pr` → `_do_create_archive_pr` 之间存在理论 TOCTOU 窗口（gh pr list → gh pr create）。单进程顺序执行 + D-006 hook 锁兜底下风险可控。

**建议**：在 `_do_create_archive_pr` 内对 `gh pr create` 失败 "already exists" 做二次 pr list 兜底（深度防御）。

### [concurrency] scripts/lib/archive_runner.py:830-836

`_write_archive_pr_number` 用 `yaml.safe_dump` 写整个 meta dict。与 `_atomic_write_meta (line 461)` 既有惯例一致，但本步骤仅改 `archive_pr_number` 单字段，可精简到 read-modify-write 风格。

**建议**：函数内先 `_load_meta(req_id)` 取最新磁盘状态，只覆盖 archive_pr_number 字段。

### [error_handling] scripts/lib/archive_runner.py:610

`_render_archive_pr_body` 的 `tmpl_path.read_text()` 在模板文件缺失时抛原生 `FileNotFoundError`，未走 `_abort` 路径。

**建议**：包 try/except + `_abort("R-ARCHIVE-PR-CREATE-FAILED", f"模板缺失 req={req_id}: {tmpl_path}", req_id)`。

### [error_handling] scripts/lib/archive_runner.py:768-776

MERGED abort 错误信息已含 PR number，但缺当前 `meta.archive_pr_number` 值的对照展示。

**建议**：在 message 末尾追加 `当前 meta.archive_pr_number=<value>` 方便用户排障。

### [auxiliary_spec] scripts/lib/archive_runner.py:627

`_check_existing_archive_pr` 内 `import tempfile` 但实际未使用，属死 import（5 checker 重票）。

**建议**：删除 line 627 死 import；line 673 `_do_create_archive_pr` 的 import tempfile 才真正使用。建议同步上移到文件顶部 import 区（与其余 stdlib 一致）。

### [auxiliary_spec] scripts/lib/archive_runner.py:740 / 813

docstring 首行使用 `Step 8/9` 与既有 `阶段 1 step 6/7`（line 1408 注释 + `_push_feat_branch:1007` docstring）风格不一致。

**建议**：统一为 `阶段 1 step 8/9`。

## Follow-up Notes（不阻塞，但值得提醒）

- F-06 TOCTOU + F-08 tmp 路径 + F-07 写整 meta dict 三条并发问题同根因——若未来引入 archive_runner 并发场景，需统一加 req-level lockfile（来自 F-06）
- F-13 / F-14 drop 提示 reviewer 应该亲读 `_precheck_phase` 上游兜底，避免误报

## 未运行的 checker

无（8 全集均运行）。

## 审查元信息

- **审查范围**：F-002 增量（1 文件，+249 -0 行）
- **并行 checker**：8 个
- **Critic 统计**：rejected 3 / not_proven 5 / not_rebutted 11
- **综合模型**：code-quality-reviewer (Judge)
- **Base SHA**：712bcf6
- **Head SHA**：f354f5b（receipt 9de6df6 同 tree）
- **Review JSON**：`requirements/20260521-archive-runner-auto-pr/reviews/code-F-002-001.json`
