# F-005 测试套件代码审查（rev1）

- **时间戳**：2026-05-22 15:25:00
- **REQ-ID**：20260521-archive-runner-auto-pr
- **Feature**：F-005（测试套件 37 TC）
- **Commit**：9888f80（base 24a4d7f）
- **审查文件**：tests/lifecycle/test_archive_runner.py (+545) / tests/lifecycle/test_finalize.py (+731) / tests/lib/test_check_meta_archive_pr_number.py (+128)
- **Pytest**：37/37 全绿 0.10s
- **机器结论**：`needs_attention`
- **Score**：78 / 100
- **路由**：`.review-scope.json.routing_decision.decision = all`，8 checker 全跑 + 1 critic 复审；用户在 tty 已确认（confirmed_at=2026-05-22 15:07:23）

---

## 1. 结论摘要

测试套件功能正确（37 TC 全绿），但存在 **3 处 ruff F401 unused import** 与 **1 处 TC-F3-18 OR 弱断言**。前者会让 `quality-check.yml:82` 的 `ruff check tests --select=F` 直接红，后者违反 `features.json:138` 对 9 条 fail-closed 路径必须断具体 R-* 错误码的硬约束。这 **4 项 major 在合入 PR 前必须修**，其余 9 项均降到 follow-up / information，不阻塞合入。

> 软确认提示：本审查是 AI 机器结论。是否将 4 项 major 视为"合入门禁"由用户在主对话决定（建议视为门禁——CI 会直接红）。

---

## 2. 13 项 Finding 处置表

| F-id | Checker | Severity (原) | Critic verdict | Judge 处置 | 理由（含 file:line 证据） |
|---|---|---|---|---|---|
| F-001 | design-consistency | major | not_rebutted | **keep major / required-fix** | test_finalize.py:729 `assert "not fully merged" in err or "R-FINALIZE-LOCAL-BRANCH-FAILED" in err` 是 OR 断言；features.json:138 明文 "TC-F3-18 九个 fail-closed 路径必须断言 stderr 含具体 R-* 错误码"。OR 让 transport stderr 单独成立也通过，违反 spec |
| F-002 | design-consistency | minor | not_rebutted | **follow-up** | test_finalize.py:616-618 TC-F3-15 用 `"--keep-worktree" in process_content` 裸字符串；同文件 TC-F3-12/13/14（L523/549/575）都断 `"(--keep-X)"` 含括号格式。一致性破绽但不会假绿 |
| F-003 | auxiliary-spec | major | not_rebutted | **keep major / required-fix** | test_finalize.py:24-25 `from archive_runner import (ArchiveResult, ...)`；AST 扫描确认 ArchiveResult 全文件零引用。本地 `ruff check --select=F` 直接报 F401。pyproject.toml:20 `select=["F"]` + quality-check.yml:82 `ruff check tests --select=F` → **CI 必红** |
| F-004 | auxiliary-spec | major | not_rebutted | **keep major / required-fix** | test_finalize.py:15 `from typing import Any, Optional`；Optional 全文件零引用。同 F-003，CI 必红 |
| F-005 | auxiliary-spec | major | not_rebutted | **keep major / required-fix** | test_check_meta_archive_pr_number.py:17 `import pytest`；该文件 6 测试函数全部为普通 `def test_*()`，无 `pytest.raises` / `pytest.fixture` / `pytest.MonkeyPatch` 等用法（已 AST 核验）。pytest 框架靠文件名/函数名发现测试，无需 import pytest 对象。CI 必红 |
| F-006 | error-handling | major | **not_proven** | **downgrade → follow-up** | test_finalize.py:468-471 TC-F3-10 仅断 SystemExit code==1，未断 stderr 含 R-FINALIZE-CWD-FAILED；critic 反证：features.json:138 强制清单为 TC-F1-3/F2-2/F2-3/F2-4/F3-2/F3-3/F3-4/F3-11/F3-18 **不含 TC-F3-10**。Judge 独立核验 features.json 确认 critic 反证成立。降为 follow-up |
| F-007 | complexity | minor | **not_proven** | **drop → information** | _make_run_stub (L99-110)：外层 def (1) + 内层 def _stub (2) + for (3) + if (4) + if (5)。但通用嵌套度量只计 control-flow（for/if/if = 3 级），critic 反证有道理。函数行数 12 行，可读性正常，降为 information |
| F-008 | auxiliary-spec | minor | not_rebutted | **follow-up** | test_archive_runner.py:312/354/385/415 函数内 `import json`；模块顶部已 import json（同文件 L9-14 区域）。属 PEP 8 微小偏离 |
| F-009 | auxiliary-spec | minor | not_rebutted | **follow-up** | test_finalize.py:90,458 函数内 `import worktree_manager`；模块顶部 sys.modules 操作时已隐式依赖此模块。可重构但不紧急 |
| F-010 | auxiliary-spec | minor | not_rebutted | **follow-up** | 节分隔注释 `# ---------- fixture helpers ----------` / `# ---------- TC-F3-1: finalize happy path ----------` 等为英文短语；CLAUDE.md「代码注释使用简体中文」。但 TC 名称（如 TC-F3-1）本身是标识符 |
| F-011 | auxiliary-spec | minor | not_rebutted | **follow-up** | test_finalize.py:113/117/121 _ok/_fail/_merged_pr_json 缺 docstring；同文件 _make_run_stub (L99) 已有 docstring 风格不统一 |
| F-012 | error-handling | minor | not_rebutted | **follow-up** | subprocess TimeoutExpired/FileNotFoundError 异常路径零覆盖；当前 _run stub 全部走 returncode 分支。spec 未要求，记入 backlog |
| F-013 | performance | minor | not_rebutted | **follow-up** | fake_repo function-scope fixture × 37 TC 重复 mkdir ~185 syscall；当前总耗时 0.10s 远低于任何阈值，纯优化不紧急 |

**统计**：keep major = 4 / drop = 1 / follow-up = 8（含降级 1）/ information = 0

---

## 3. Required Fixes（4 项，合入前必修）

### Fix-1：TC-F3-18 OR 断言改为强 R-* 错误码断言

**位置**：`tests/lifecycle/test_finalize.py:729`

**当前代码**：
```python
assert "not fully merged" in err or "R-FINALIZE-LOCAL-BRANCH-FAILED" in err, (
    f"stderr should contain git error or R-FINALIZE-LOCAL-BRANCH-FAILED, got: {err!r}"
)
```

**修法**：拆为两条 AND 断言，或核对实现实际输出后保留唯一 R-* 断言：
```python
# 选项 A：与 spec features.json:138 严对齐（推荐）
assert "R-FINALIZE-LOCAL-BRANCH-FAILED" in err, (
    f"stderr must contain R-FINALIZE-LOCAL-BRANCH-FAILED (fail-closed 强制), got: {err!r}"
)
# 可选附加 transport stderr 断言（不再用 or）
assert "not fully merged" in err, f"应同时透传 git 原始 stderr, got: {err!r}"
```

**理由**：features.json:138 明文要求 TC-F3-18 在 9 条 fail-closed 路径强制断言列表中。若实现确实只输出 transport stderr 而不输出 R-* 错误码，应反过来修实现（archive_runner.py 的 _delete_local_branch strict 分支），而不是用 OR 兜底测试。

### Fix-2：删除 `tests/lifecycle/test_finalize.py:15` 的 `Optional`

**位置**：`tests/lifecycle/test_finalize.py:15`

**修法**：
```python
# 改前
from typing import Any, Optional
# 改后
from typing import Any
```

**验证**：`ruff check tests/lifecycle/test_finalize.py --select=F` 应无 F401。

### Fix-3：删除 `tests/lifecycle/test_finalize.py:25` 的 `ArchiveResult` 导入

**位置**：`tests/lifecycle/test_finalize.py:23-30`

**修法**：
```python
# 改前
from archive_runner import (  # noqa: E402
    ArchiveResult,
    ArchivePrompt,
    _FINALIZE_WARN_FORCE_ONLY,
    _FINALIZE_WARN_FORCE_AND_YES,
    finalize_requirement,
)
# 改后（删 ArchiveResult 一行）
from archive_runner import (  # noqa: E402
    ArchivePrompt,
    _FINALIZE_WARN_FORCE_ONLY,
    _FINALIZE_WARN_FORCE_AND_YES,
    finalize_requirement,
)
```

**验证**：`ruff check tests/lifecycle/test_finalize.py --select=F` 应无 F401；pytest 应仍 37/37 全绿。

### Fix-4：删除 `tests/lib/test_check_meta_archive_pr_number.py:17` 的 `import pytest`

**位置**：`tests/lib/test_check_meta_archive_pr_number.py:17`

**修法**：直接删除 `import pytest` 行。该文件 6 个测试函数（TC-F4-1 ~ TC-F4-6）全是普通 `def test_*()` 形式，无 `pytest.raises` / `pytest.fixture` / `pytest.MonkeyPatch` 任何对象用法，pytest 框架靠测试发现机制自动识别。

**验证**：`ruff check tests/lib/test_check_meta_archive_pr_number.py --select=F` 应无输出；pytest 仍能发现并跑这 6 个 TC。

**统一验证命令**：
```bash
ruff check tests/lifecycle/test_finalize.py tests/lib/test_check_meta_archive_pr_number.py tests/lifecycle/test_archive_runner.py --select=F
# 期望：no output / exit 0

pytest tests/lifecycle/test_finalize.py tests/lifecycle/test_archive_runner.py tests/lib/test_check_meta_archive_pr_number.py -v
# 期望：37 passed
```

---

## 4. Follow-up（8 项，不阻塞合入）

| 编号 | 来源 | 描述 | 建议优先级 |
|---|---|---|---|
| FU-1 | F-002 | TC-F3-15 三 keep flag 断言改为 `(--keep-X)` 含括号格式，与 TC-F3-12/13/14 统一 | low |
| FU-2 | F-006 | TC-F3-10 可加 `assert "R-FINALIZE-CWD-FAILED" in capsys.readouterr().err`，与 docstring 自描述对齐（spec 不强制） | low |
| FU-3 | F-008 | test_archive_runner.py 4 处函数内 import json 上提到模块顶部 | low |
| FU-4 | F-009 | test_finalize.py 2 处函数内 import worktree_manager 与顶部 sys.modules 合并 | low |
| FU-5 | F-010 | 节分隔注释改中文（如 "# ---------- TC-F3-1: finalize 正常路径 ----------"） | low |
| FU-6 | F-011 | _ok / _fail / _merged_pr_json 补 docstring | low |
| FU-7 | F-012 | 加 subprocess TimeoutExpired / FileNotFoundError 异常路径 TC（spec 未要求，记 backlog） | low |
| FU-8 | F-013 | fake_repo 改 session-scope + copytree 优化（当前 0.10s 远低于阈值，纯优化） | trivial |

---

## 5. 跨维度洞察

1. **auxiliary_spec × CI lint 联动**：3 处 F401 unused import 不是独立小问题，而是 **CI 必红的同类型缺陷集**（typing / archive_runner / pytest 三个 import 都是同一类）。提示本次测试套件交付前漏了一道 `ruff check tests --select=F` 自检。建议在 pre-commit 配 ruff 钩子防回归。

2. **design_consistency × auxiliary_spec 双信号**：TC-F3-18 OR 弱断言（F-001）与 3 处 unused import（F-003/F-004/F-005）同时出现，提示 **测试套件未与 features.json:138 强制清单 + ruff 规则做对照交叉检验**。两类问题都属"可机器自动发现"，应进 CI 而非依赖 reviewer 兜底。

3. **error_handling × spec 边界**：TC-F3-10 缺 R-* 断言（F-006）属"spec 未强制但 docstring 自描述了"，与 TC-F3-15 keep 后缀（F-002）属同类——TC 的实际断言强度弱于 docstring 表达的预期。可视为测试套件成熟度的二阶信号，记 backlog 不阻塞。

4. **trend-G-meta baseline**：F-005 是新建测试套件首次审查（rev1），无 rev0 历史对比，**不适用 trend-G-meta 终结判定**。本次 13 finding（4 keep major + 8 follow-up + 1 drop）记为 baseline，后续 rev2 若 4 项 required-fix 修毕、follow-up 数量不增，可进入 trend-G-meta 监控视野。

---

## 6. Final Verdict（< 300 字）

测试套件 37 TC 全绿、覆盖完整、mock 边界清晰，是高质量交付。但合入前必须修 4 项 major：(1) TC-F3-18 OR 弱断言违反 features.json:138 fail-closed 强制清单（design_consistency）；(2-4) typing.Optional / archive_runner.ArchiveResult / pytest 三处 unused import 会让 CI `ruff check tests --select=F` 直接红（auxiliary_spec）。前者是 spec 一致性，后者是 CI 阻塞——任一项不修都不能合入。其余 8 项 follow-up（断言强度加固 / 节分隔注释 / helper docstring / 异常路径覆盖等）属测试套件成熟度二阶信号，可进 backlog 异步消化。F-006/F-007 在 critic 反证下分别降为 follow-up / drop，与 features.json 与嵌套度量标准严对齐。

**机器结论 needs_attention**：4 项 major 需在合入前清零；是否视为"合入门禁"由用户在主对话给出软确认。

---

## 7. 落盘产物

- 14 字段 verdict：`requirements/20260521-archive-runner-auto-pr/reviews/code-F-005-001.json`
- Markdown 报告：本文件 `requirements/20260521-archive-runner-auto-pr/artifacts/review-20260522-152500.md`

