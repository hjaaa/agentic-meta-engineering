# 代码审查报告 · 20260521-archive-runner-auto-pr · 2026-05-21 14:39:40

## 结论

**blocked** · 1 critical / 5 major / 4 minor

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项
- `blocked` — AI 机器评估发现关键问题 ← 本次

阻断原因：F-010 实测触发 `check_sourcing.py --strict` E002 错误（`Total: 1 error, 0 warning`），CI strict gate 会让 PR 失败。

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 自动确认：

- **决策**：all（4/6 trivial + 2 灰色 → 升 8 全集兜底）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-21 14:27:52
- **跑了哪些 checker**：complexity-checker, security-checker, concurrency-checker, performance-checker, error-handling-checker, design-consistency-checker, history-context-checker, auxiliary-spec-checker
- **跳过的 checker（含原因）**：无

## 人工确认提示

本次评审输出 `conclusion = blocked`，**这只是 AI 的机器评估**，不代表合并通过。

- `blocked` → fail-closed，必须先把 critical 问题（F-010）修掉再重审，无法人工确认绕过。

GATE-REVIEW-VERDICT 在 phase-transition / submit 时会 hard-block `blocked`（error 级）。建议路径：先修 F-010 → 重跑 `/code-review` → conclusion 降为 `needs_attention` 或 `looks_clean` 后再继续推进。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"checker 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-001** [concurrency · major] → critic: not_proven → **最终处置：follow-up（推 detail-design）**
  - 裁决依据：场景 4 已 fail-closed exit 1 + 保留 commit；"半状态"是推断，属 detail-design 阶段细化具体写入顺序
- **F-002** [concurrency · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：单开发者 + owned worktree 已闭合并发模型，CI 不进 owned worktree，多进程占用守护属过度设计
- **F-003** [performance · major] → critic: not_proven → **最终处置：follow-up**
  - 裁决依据：5s 预算挂在场景 1（首次调），requirement.md:79 明示"多了 push + gh pr create 两次"；场景 2/3 不在此预算内，属误读
- **F-004** [error-handling · major] → critic: not_rebutted → **最终处置：keep (major)**
  - 裁决依据：场景 2 finalize 4 个子动作（chdir / branch -D / push --delete / worktree remove）失败策略均未定义，单点失败处置（fail-fast / best-effort / 部分回滚）是异常模型决策，建议 definition 收尾前加 ARS-9
- **F-005** [error-handling · major] → critic: not_rebutted → **最终处置：keep (major) — 强调不可逆**
  - 裁决依据：archive_pr_number=0 / 缺失 / gh pr view 失败三路径未覆盖，任一路径误判 MERGED 即不可逆删分支；建议加 ARS-10 明确三路径判定逻辑
- **F-006** [design-consistency · major] → critic: not_rebutted → **最终处置：keep (major)**
  - 裁决依据：ARS-1/2/3 在 plan.md 决策记录里无对应 D-* 条目，下游 detail-design 读 plan.md 会漏 fail-closed / yes flag / --force 范围三条约束
- **F-007** [design-consistency · major] → critic: not_rebutted → **最终处置：keep (major)**
  - 裁决依据：F-004 描述仅"meta-schema 加 archive_pr_number 字段"，漏「check_meta.py 兼容历史 meta 缺字段」修改；范围与 feature 列表不对齐
- **F-008** [history-context · major] → critic: not_proven → **最终处置：follow-up**
  - 裁决依据：finalize 内部 cwd/HEAD 切换顺序、与 2fed367 自动切 base_branch 的交互，属 detail-design 时序细化
- **F-009** [auxiliary-spec · major] → critic: not_rebutted → **最终处置：keep (major)**
  - 裁决依据：plan.md 第 14/15/16/17/23/25/26/40/43/69/75 行 backtick 内容大量丢失（如 `--finalize` / `--no-codex` / `archive_pr_number` 等），客观语法损坏，detail-design 读 plan.md 会得到残缺契约
- **F-010** [auxiliary-spec · major → **critical**] → critic: rejected（**误判**） → **最终处置：keep (critical) — Judge 升级**
  - 裁决依据：critic 漏看了行内文字，主 Agent 实测 `python3 scripts/lib/check_sourcing.py --strict ...` 输出 `❌ E002: 第 112 行引用路径不存在：scripts/lib/archive_runner.py:695-696 已具备 resolve_main_repo_root 能力 / Total: 1 error`；strict 模式 CI gate 直接阻断
- **F-011** [concurrency · minor] → critic: not_proven → **最终处置：follow-up**
  - 裁决依据：PR state==CLOSED 错误文案属 detail-design 文案细化
- **F-012** [performance · minor] → critic: not_proven → **最终处置：follow-up**
  - 裁决依据：gh api vs git ls-remote 工具选型属 detail-design 决策
- **F-013** [error-handling · minor] → critic: not_proven → **最终处置：follow-up**
  - 裁决依据：--legacy-resurrect-remote 退出码/stderr 格式属 detail-design 异常码定义
- **F-014** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep (minor)**
  - 裁决依据：plan.md F-003 描述漏「写 meta.yaml.worktree.cleanup.removed_at」字段持久化动作
- **F-015** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep (minor)**
  - 裁决依据：requirement.md:120 删除线条目 1 内部仍保留 `[待用户确认]` 字面量，check_sourcing W003 enumeration 计数可能误命中
- **F-016** [history-context · minor] → critic: not_rebutted → **最终处置：follow-up**
  - 裁决依据：history-context 事实陈述（archive_runner.py 90 天 6 次提交属热点），保留作为下阶段输入提示
- **F-017** [history-context · minor] → critic: not_proven → **最终处置：follow-up**
  - 裁决依据：_delete_remote_branch 触发时机改造属 detail-design 实施细节
- **F-018** [auxiliary-spec · minor] → critic: not_rebutted → **最终处置：keep (minor)**
  - 裁决依据：requirement.md:96 引用「C-1 强约束」全文未定义，编号体系断裂
- **F-019** [auxiliary-spec · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：meta.yaml.absolute_path 多行折行是 yaml dumper 自动产物，非缺陷
- **F-020** [auxiliary-spec · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：notes.md 0 字节占位文件由 bootstrap 创建，0 字节无"末尾换行符"语义

## Critical（必须修复）

### [auxiliary-spec] requirements/20260521-archive-runner-auto-pr/artifacts/requirement.md:112

`（来源：scripts/lib/archive_runner.py:695-696 已具备 resolve_main_repo_root 能力）`——括号内行号后混入说明文字，破坏 `check_sourcing.py` 的路径锚定正则 `[\w/.-]+:\d+(-\d+)?$`，触发 E002。

**实测**：
```
$ python3 scripts/lib/check_sourcing.py --strict requirements/20260521-archive-runner-auto-pr/artifacts/requirement.md
❌ E002: 第 112 行引用路径不存在：scripts/lib/archive_runner.py:695-696 已具备 resolve_main_repo_root 能力
Total: 1 error, 0 warning
```

**建议**：拆成两段——`（来源：scripts/lib/archive_runner.py:695-696）已具备 resolve_main_repo_root 能力`。修后重跑应为 `0 error / 0 warning`。

## Major（强烈建议修复）

### [error-handling] requirements/20260521-archive-runner-auto-pr/artifacts/requirement.md 场景 2 step 4.i-iii (F-004)

场景 2 finalize 4 个子动作失败策略未定义：
- `os.chdir(主仓根)` 失败 → fail-closed 还是 fail-soft？
- `git pull --ff` 冲突 / `git branch -D` 不存在 / `git push --delete` 失败 → 三类失败的退出码与文案？
- `git worktree remove --force` 仍失败（worktree 内有未提交 stage 文件等） → 阻塞 finalize 整体退出还是 fail-soft 写日志？

**建议**：在 definition 收尾前加 **ARS-9：finalize 子步骤失败策略基线**，明确 fail-closed 范围 / fail-soft 边界 / 退出码体系。

### [error-handling] requirements/20260521-archive-runner-auto-pr/artifacts/requirement.md:52 场景 2 step 2 (F-005)

archive_pr_number 三路径异常未覆盖：
1. `archive_pr_number=0`（默认值，表示场景 1 未完成）
2. 字段缺失（老 meta.yaml 兼容）
3. `gh pr view` 失败（gh 未登录 / 网络 / PR 被手工删）

任一路径若误判为 MERGED，步骤 4 直接删本地+远程 feat 分支 → **不可逆数据丢失**（PR 实际未 merged 时丢分支）。

**建议**：加 **ARS-10：archive_pr_number 三路径判定**，明确每种路径下 finalize 的行为（abort / 提示用户 / 异常恢复路径）。

### [design-consistency] requirements/20260521-archive-runner-auto-pr/plan.md:65-83 决策记录 (F-006)

ARS-1（PR 创建失败 fail-closed） / ARS-2（finalize 默认询问 Y/N） / ARS-3（--force 仅跳 merged 检查）在 plan.md 决策记录里没有对应 D-* 条目——plan.md 只有 D-001（≈ARS-5+6） / D-002（≈ARS-4+7） / D-003（≈ARS-8）。

**风险**：detail-design 阶段开发人员只读 plan.md 可能漏掉 ARS-1/2/3 约束。

**建议**：plan.md 决策记录追加 **D-004（ARS-1 fail-closed 镜像）** / **D-005（ARS-2 yes flag 镜像）** / **D-006（ARS-3 --force 范围镜像）**。

### [design-consistency] requirements/20260521-archive-runner-auto-pr/plan.md F-004 描述 (F-007)

F-004 描述仅写「meta-schema 加 archive_pr_number 字段」，requirement.md:91 范围里有「check_meta.py 兼容历史 meta 缺该字段（不挂 schema 检）」，两处不对齐。

**风险**：CI 对历史 meta.yaml（PR #83 之前）跑 check_meta strict 会挂。

**建议**：F-004 描述补「+ check_meta.py 对历史 meta 缺该字段做 backward-compat 容错」，或拆 F-008 单独承载 check_meta.py 改动。

### [auxiliary-spec] requirements/20260521-archive-runner-auto-pr/plan.md 多行 (F-009)

plan.md 第 14/15/16/17/23/25/26/40/43/69/75 行的 backtick 行内代码内容大量丢失——例如 F-004 描述显示为 `meta-schema 加  字段`（中间空白）、F-003 显示 `跳过 codex（）+ 跳过 rebase（）`（flag 名缺失）。

**根因推测**：从原始数据写盘时 yaml 多行字符串引号嵌套问题，把 backtick 内的 `--finalize` / `--no-codex` / `--skip-rebase` / `archive_pr_number` / `--legacy-resurrect-remote` / `gh pr create` 等标识符吃掉了。

**建议**：对照 requirement.md 同义段落重新写一份 plan.md（核心需求 / 锁定设计点 / features 列表 / 决策记录四个段都受影响）。

## Minor（改进建议）

### [design-consistency] requirements/20260521-archive-runner-auto-pr/plan.md F-003 (F-014)

F-003 描述漏「写 `meta.yaml.worktree.cleanup.removed_at`」字段持久化动作；requirement.md 场景 2 step 4.3 + D-003 Consequences 都提了这个动作。

**建议**：F-003 描述末追加「+ 写 meta.yaml.worktree.cleanup.removed_at」，与 requirement.md / D-003 保持一致。

### [design-consistency] requirements/20260521-archive-runner-auto-pr/artifacts/requirement.md:120 (F-015)

待澄清清单条目 1 已用 `~~删除线~~` 标记为「已确认（2026-05-21）」，但删除线内仍保留 `[待用户确认]` 字面量。check_sourcing W003 的 enumeration 计数正则可能误命中，造成"标记数 ≠ 清单数"假阳性。

**建议**：把 `[待用户确认]` 字面也一并划掉，或改为 `[已确认]`。

### [auxiliary-spec] requirements/20260521-archive-runner-auto-pr/artifacts/requirement.md:96 (F-018)

范围 → 不包含小节写「详见 C-1 强约束」，但 requirement.md 全文无 `C-1` 定义；与 ARS / D / F 编号体系不一致。

**建议**：改为「详见后续 detail-design 的约束清单」或在 requirement.md 补「## 约束（C-*）」段定义 C-1。

## Follow-up Notes（不阻塞，但值得提醒）

- F-001：场景 1 gh pr create 失败后 meta.yaml 中间状态语义需在 detail-design 锁定（archive_pr_number 写入时机分两步 commit 还是同批 commit）
- F-003：5s 性能预算的覆盖范围在 detail-design 拆为「场景 1 预算 / 场景 2 预算 / 场景 3 预算」三行表述
- F-008：detail-design 时序图需明确 `resolve_main_repo_root → chdir → cleanup_worktree_if_owned` 三步顺序，并与 2fed367 的"自动切 base_branch"逻辑对齐 cwd/HEAD 切换顺序
- F-011：detail-design 补 PR state==CLOSED 时的错误提示文案模板
- F-012：detail-design 决定 `--legacy-resurrect-remote` 的远程探测方式（gh api vs git ls-remote）并写决策记录
- F-013：detail-design 给 `--legacy-resurrect-remote` 冲突场景定退出码（建议 exit 1）+ stderr 格式（`[LEGACY_RESURRECT_CONFLICT] ...`）
- F-016：archive_runner.py 90 天 6 次迭代属高频热点，本期叠加副作用范围需在 testing 阶段重点回归
- F-017：detail-design 明确改 `_delete_remote_branch` 的触发时机（从第一次 archive 调用挪到 `--finalize`），与 ARS-4 一致

## 未运行的 checker（如有）

无（8/8 全跑）。

## 审查元信息

- **审查范围**：embedded mode @ feat/req-20260521-archive-runner-auto-pr，base=develop (e968168) → head=e9083e3，定 definition 阶段产出（6 文件，+336 -0 行）
- **并行 checker**：8 个全跑
- **Critic 统计**：rejected 4 / not_proven 8 / not_rebutted 8（**Judge 修订**：F-010 从 rejected 升 not_rebutted critical）
- **综合模型**：code-quality-reviewer
- **Base SHA**：e968168eab13ee8bd1329508f11a56c944d48a1d
- **Head SHA**：e9083e3c74529a624ef578256a601b833339ab49
