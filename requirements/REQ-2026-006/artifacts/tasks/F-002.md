---
feature_id: F-002
title: PR-2 删除旧热路径 4 个文件 + registry/base.py 清理 + CI settings.json 引用校验
status: pending
complexity: low
depends_on: [F-001]
created_at: 2026-05-04T12:05:00+08:00
updated_at: 2026-05-04T12:05:00+08:00
review_report: null
estimate_days: 0.8
---

# F-002 · PR-2 删除旧热路径 4 个文件 + registry/base.py 清理 + CI settings.json 引用校验

## 需求描述

旧热路径退役 PR——**必须在 F-001 合入并验证 1-2 天后再合**，避免新旧并行窗口风险。

具体动作：
- 删除 4 个文件（detail-design §2.1）：
  - `.claude/hooks/protect-branch.sh`
  - `scripts/gates/triggers/pre_tool_use.sh`
  - `scripts/gates/plugins/protect_branch.py`
  - `scripts/gates/plugins/bash_write_protect.py`
- 同步删 `tests/gates/test_protect_branch.py` + `tests/gates/test_bash_write_protect.py` 与相关 fixture
- 修 `scripts/gates/registry.yaml`：删 `GATE-PROTECT-BRANCH` / `GATE-BASH-WRITE-PROTECT` 两条 gate 定义；改 `escape_hatches` 中 `skips_gates: [GATE-BASH-WRITE-PROTECT]` 为 `[]`（detail-design §2.2）
- 修 `scripts/gates/plugins/base.py`：`TRIGGERS` 元组与 `GateContext.trigger` Literal 删除 `'pre-tool-use'` 枚举值（detail-design §2.3）
- grep 全仓 `pre-tool-use` 字面量与 `GATE-PROTECT-BRANCH` / `GATE-BASH-WRITE-PROTECT` 调用点，同步更新或删除
- 新增 CI step `Verify settings.json hook references exist`（detail-design §2.4），PR-2 之后保留为常驻校验

## 接口/数据结构

无新接口。本 PR 仅删除/调整：
- `registry.yaml` 减少 2 条 gate 定义
- `base.py` `TRIGGERS` Literal 收窄（pre-commit / phase-transition / submit / ci / post-dev）
- `escape_hatches` 字段语义不变，仅清理 `skips_gates` 数组

## 依赖

### 依赖的 feature

- **F-001**（必须先合入并稳定 1-2 天，确保新 guard 已替代旧 protect-branch 行为）

### 依赖的模块

- F-001 已落地的 `.claude/hooks/pre-tool-use-guard.sh`（提供等价保护）
- `.claude/settings.json` 已切到新 guard（PR-1 改）

## 触及范围

- `.claude/hooks/protect-branch.sh`（删除）
- `scripts/gates/triggers/pre_tool_use.sh`（删除）
- `scripts/gates/plugins/protect_branch.py`（删除）
- `scripts/gates/plugins/bash_write_protect.py`（删除）
- `scripts/gates/registry.yaml`（修改）
- `scripts/gates/plugins/base.py`（修改）
- `.github/workflows/quality-check.yml`（追加 step）
- `tests/gates/`（删除两个 test 文件 + fixture 同步）

## 验收清单（acceptance / TC）

- [ ] **TC-F2-1**: 4 个旧文件 `git status` 显示 deleted
- [ ] **TC-F2-2**: `pytest tests/gates/ -v` 全绿（含 base.py Literal 调整后所有测试 fixture 同步）
- [ ] **TC-F2-3**: `python3 scripts/gates/run.py --validate-registry` exit 0
- [ ] **TC-F2-4**: sandbox REQ-2099-001 完整需求生命周期 4 trigger（phase-transition / pr-submit / save-review / manual）跑完不报错（V-02 验收）
- [ ] **TC-F2-5**: CI 'Verify settings.json hook references exist' step 通过（settings.json 不再引用已删文件）
- [ ] **TC-F2-6**: `grep -rn 'pre-tool-use' scripts/` 仅剩注释或文档；无活代码引用
- [ ] **TC-F2-7**: 在 develop 分支调 Edit/Write/MultiEdit 仍被新 guard 阻断（行为不变，回归保护）

## 开发笔记

（实施过程中随手记。注意：删 base.py 的 `'pre-tool-use'` Literal 后，所有 `tests/gates/test_*.py` 的 fixture 引用要同步——首轮评审 suggestion 第 3 条提示需 grep 一下。）

## 完成自检

- [ ] 4 个文件确实删除（`git diff --stat` 验证）
- [ ] 单元测试覆盖回归保护（develop 仍阻断）
- [ ] `python3 scripts/gates/run.py --validate-registry` 通过
- [ ] sandbox 完整 REQ 周期跑通
- [ ] `/code-review` 通过
