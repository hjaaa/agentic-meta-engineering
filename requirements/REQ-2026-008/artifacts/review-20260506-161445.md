# 代码审查报告 · REQ-2026-008 · F-004 · 2026-05-06 16:14:45

## 结论

**needs_attention** · 0 critical / 3 major / 13 minor · score 78

> AI 机器评估发现待改进项，需 round-2 修复后重审。**无核心安全/正确性阻塞。**

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：all（用户在 tty 选择全 8 跑）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-06 15:56:13
- **跑了哪些 checker**：complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec（8 全集）
- **跳过的 checker**：无

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人在 tty 终端执行：

```bash
/code-review:signoff REV-REQ-2026-008-code-F-004-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-008-code-F-004-001 --trivial  # 纯文档变更快速通道（本次 needs_attention 不适用）
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-004 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"checker 发现 → critic 反证 → Judge 裁决"的三方透明度。

### CRITICAL 候选（3 条 → 全部反证降级或 drop）

- **F-1** [complexity · critical] → critic: not_rebutted → **最终处置：keep（降级 major）**
  - 裁决依据：客观超阈（_main_inner 152 行 / CC 33 / 60 阈值）；但 hook 入口聚合天然复杂（参考 .claude/hooks/pre-tool-use-guard.sh main 95 行先例），不宜阻塞 → 降 major + should_fix 推 round-2 重构 commit
- **F-2** [complexity · critical] → critic: not_rebutted → **最终处置：keep（降级 major）**
  - 裁决依据：def→try→with→if→for→if 5 层客观存在；与 F-1 同根因，一并 round-2 解决
- **F-3** [history · critical] → critic: rejected → **最终处置：drop**
  - 裁决依据：47bcde8 是 F-004 本次 commit；guard.sh case 顺序匹配 + Agent 分支 exec 透传不影响既有 Edit/Write/Bash/MultiEdit；32 既有 bats 全绿无回归

### MAJOR 候选（11 条 → 1 keep / 5 降级 minor / 5 drop 或 follow-up）

- **F-4** [security · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：scripts/lib/check_features.py:213-223 GATE-FEATURES-SCHEMA 已强制 `^F-\d{3}$` 校验 dep_fid，路径穿越攻击面被上游 schema gate 守住
- **F-5** [complexity · major] → critic: not_rebutted → **最终处置：降级 minor**
  - 裁决依据：B-1/B-2/B-3 三段重复客观；与 F-1 一并 round-2 重构
- **F-6** [complexity · major] → critic: not_rebutted → **最终处置：降级 minor**
- **F-7** [complexity · major] → critic: not_rebutted → **最终处置：降级 minor**
- **F-8** [error · major] → critic: rejected (降级争议) → **最终处置：降级 minor**
  - 裁决依据：cleanup 顶层 raise 退出码 1 已涵盖；docstring 三层不一致是文档对齐 minor 而非 major
- **F-9** [design · major] → critic: not_rebutted → **最终处置：keep major**
  - 裁决依据：detailed-design.md:409/446/532 + dispatch_state.py:21 docstring 写 atomic rename，实现是 truncate+write，矛盾客观；plan.md ADR D-010 必补
- **F-10** [perf · major] → critic: not_proven → **最终处置：follow-up**
  - 当前规模 (8 features, <10 reqs) 影响 < Agent 派发链；features 50+ 时统一回归
- **F-11** [perf · major] → critic: not_proven → **最终处置：follow-up**
- **F-12** [perf+conc · major] → critic: not_proven → **最终处置：drop（设计必须）**
  - 裁决依据：B-2 锁内读 task.md 是设计要求（status source-of-truth），移到锁外会读陈旧 status
- **F-13** [perf · major] → critic: not_proven → **最终处置：follow-up**
  - 当前串行约束（D-008 保守档）已规避惊群；并发 50+ 时再加 jitter

### MINOR 候选（19 条 → 11 keep / 5 follow-up / 3 drop）

- **F-14** [死代码 · minor] → critic: not_rebutted → **keep（must_fix）**：4 checker 同标，0 风险删
- **F-15** [perf+complexity · minor] → critic: not_proven → **follow-up**：subprocess lazy import 反 PEP 8 但非阻塞
- **F-16** [complexity · minor] → critic: not_rebutted → **keep（should_fix round-2）**
- **F-17** [doc · minor] → critic: not_rebutted → **keep（must_fix）**：测试后门必须 plan.md 文档化 + 代码 docstring 强化
- **F-18** [security · minor] → critic: rejected → **drop**：本次未触及 BYPASS reason 处理
- **F-19** [security · minor] → critic: not_proven → **follow-up**：设计文档 §3.3 #4 已接受兼容窗口策略
- **F-20** [conc · minor] → critic: not_proven → **follow-up**：bootstrap TOCTOU 概率极低 + 内容相同业务无影响
- **F-21** [design · minor] → critic: not_rebutted → **合并到 F-9**：同根因 docstring/spec/实现矛盾
- **F-22** [testing · minor] → critic: not_rebutted → **keep（must_fix）**：TL-006 result_q 未消费 = 关键回归无效
- **F-23** [testing · minor] → critic: not_rebutted → **keep（must_fix）**：TL-009 计时假设脆弱
- **F-24** [perf · minor] → critic: not_proven → **follow-up**
- **F-25** [perf · minor] → critic: not_proven → **follow-up**
- **F-26** [testing · minor] → critic: not_proven → **drop**：TC-F4-2 验收契约要 7，bats 实测 14 已远超；TL-009 在 lib 层覆盖
- **F-27** [i18n · minor] → critic: not_rebutted → **keep（must_fix）**：CLAUDE.md §0 硬规
- **F-28** [naming · minor] → critic: not_proven → **follow-up**：audit_log 与 guard.sh:49 命名一致设计意图
- **F-29** [i18n · minor] → critic: not_rebutted → **keep（must_fix）**
- **F-30** [i18n · minor] → critic: not_rebutted → **keep（must_fix）**
- **F-31** [naming · minor] → critic: rejected → **drop**：既有 test_ts/tc 风格一致
- **F-32** [doc · minor] → critic: not_proven → **follow-up（cosmetic）**
- **F-33** [security · informational] → critic: rejected → **drop**：进程退出 OS 回收 fd

## Major（强烈建议修复）

### [complexity] .claude/hooks/dispatch_precheck.py:228（F-1）

`_main_inner()` 单方法 152 行 / CC 33，CLAUDE.md §1.2 阈值 60 行。锁前段 80 行 + 锁内段 72 行各自超阈。

**建议（should_fix · round-2 重构 commit）**：拆 `_parse_payload(stdin)` / `_resolve_feature_context(fid)` / `_check_and_acquire(fh, fid, feature, tasks_dir, req_id)` 三私有方法，_main_inner 退化为编排层 ~25 行。

### [complexity] .claude/hooks/dispatch_precheck.py:334（F-2）

`_main_inner` 锁内代码块嵌套深度 5（def→try→with→if→for→if）；阈值 4。

**建议**：与 F-1 同根因，一并 round-2 拆分后嵌套降至 4。

### [design_consistency] scripts/lib/dispatch_state.py:116-150 + detailed-design.md §3.3（F-9 + F-21）

`StateFileHandle.write` 实现用 truncate+write 原地写，与 detailed-design.md §3.3 / dispatch_state.py:21 docstring 规定的 atomic rename（os.replace）矛盾。代码注释解释了工程合理性（rename 换 inode 致 with 块内 fd 失效），但 plan.md 无对应 ADR 记录。

**建议（must_fix）**：
1. plan.md 加 ADR D-010 记录此偏差（atomic rename → truncate+write，附 LOCK_EX 持锁内安全等价性论证）
2. 同步 dispatch_state.py:21 docstring 改为"truncate+write 原地写（rename 换 inode 与 with 块 fd 不兼容）"
3. **不回改 detailed-design.md**——D-006 经验：reviewer hash 校验粒度无 trivial 豁免，措辞修订计入重审成本（detail-design 已 round-005 looks_clean signoff）

## Minor（改进建议 / must_fix 标注）

### [死代码 must_fix] .claude/hooks/dispatch_precheck.py:60 + 64-73（F-14）

`_SHANGHAI_TZ = timezone.utc` 注释说"按 +08:00 偏移构造"但赋值是 UTC + 全文从未引用；`_logger` 创建 /tmp FileHandler 但全文无任何 _logger.xxx() 调用。4 checker 同标。

**建议**：删除 _SHANGHAI_TZ 常量 + _logger 相关 import 与 FileHandler 初始化（约 10 行）。

### [测试质量 must_fix] tests/lib/test_dispatch_state.py:192-208（F-22）

TL-006 多进程并发测试 q=mp.Queue() 被 _writer_proc put 但测试体未读取 q；timeout 路径（'err', 'TimeoutError:...'）从未被验证 — 关键回归测试漏覆盖等于无效测试。

**建议**：测试体加 `results = [q.get(timeout=1) for _ in range(2)]; assert {r[0] for r in results} == {'ok'} or {r[0] for r in results} == {'ok', 'err'}`，断言至少有一个 ok 或 ok+err 组合，且 err 必须是 TimeoutError。

### [测试质量 must_fix] tests/lib/test_dispatch_state.py:282-309（F-23）

TL-009 让进程 A 持锁 LOCK_TIMEOUT_S+1=6s，B 启动延迟 >1s 时可能假通过/假失败。

**建议**：A 持锁时间改为 `2 * LOCK_TIMEOUT_S` 或用 monkeypatch 缩短 LOCK_TIMEOUT_S 至 0.5s + Event 精确同步 B 的起始时刻。

### [文档 must_fix] .claude/hooks/dispatch_precheck.py:150-155 + plan.md（F-17）

`CLAUDE_DISPATCH_TEST_REQ_DIR_OVERRIDE` 测试后门 env，detailed-design.md 全文 grep 未命中；CLAUDE_GATES_AUDIT_ROOT 同模式但已文档化。

**建议**：
1. plan.md notes 段加一行说明（仅测试 mode，类比 CLAUDE_GATES_AUDIT_ROOT）
2. dispatch_precheck.py:150 docstring 强化"仅 bats 沙盒用例使用，生产环境该变量不应出现"

### [中英文混用 must_fix] CLAUDE.md §0 硬规

- **F-27** scripts/lib/dispatch_state.py:111 错误消息 `dispatch-state must be object: {path}` → 改 `dispatch-state 顶层必须是 object：{self._path}`
- **F-29** tests/lib/test_dispatch_state.py:43 `# ---------- fixture ----------` → `# ---------- 测试夹具 ----------`
- **F-30** tests/hooks/test_dispatch_precheck.bats:27 `# ---------- helpers ----------` → `# ---------- 辅助函数 ----------`
- **dispatch_precheck.py 多处** 错误消息 / audit_log 字符串中英文混用核查（如 `FAIL_OPEN: stdin parse error` 是设计统一的事件 tag，可保留英文 tag + 中文描述）

### [文档对齐 must_fix] scripts/lib/dispatch_state_cleanup.py:62-69 + dispatch_state.py:247（F-8）

clear_state docstring 异常透传缺 ValueError 标注；cleanup CLI 仅 catch TimeoutError + OSError。

**建议**：
1. dispatch_state.py:247 clear_state docstring 异常透传补一行 "ValueError：状态文件 JSON 格式损坏"
2. dispatch_state_cleanup.py 加 `except ValueError as exc: print(f"错误：状态文件格式损坏：{exc}", file=sys.stderr); return 1` 分支

### [should_fix · round-2 重构] complexity 类（F-5 / F-6 / F-7 / F-16）

| FID | 文件位置 | 问题 |
|---|---|---|
| F-5 | dispatch_precheck.py:345 | B-1/B-2/B-3 BLOCKED 4 步重复 3 次，抽 _block_and_exit(msg, audit_tag) |
| F-6 | dispatch_precheck.py:140 | locate_req_dir_by_branch 嵌套 5 / CC 8，抽 _scan_requirements_for_branch |
| F-7 | dispatch_precheck.py:165/206 | yaml 重复 lazy import 2 次，统一顶层 try-import + _YAML_AVAILABLE |
| F-16 | dispatch_state.py:154 | flock_state_file 嵌套 5，抽 _acquire_with_timeout(f, deadline) |

## Follow-up Notes（不阻塞，但值得提醒）

- **F-10/F-11 性能 follow-up**：`locate_req_dir_by_branch` + features.json 全量 load 在每次 hook 触发都重做，当前 8 features × <10 reqs 影响 < Agent 派发链；建议 requirements 增至 50+ 时统一回归 perf
- **F-12 锁内 B-2 设计必须**：B-2 锁内读 task.md 是 status source-of-truth 设计要求，不能移锁外（会读陈旧）；当前 depends_on ≤3 锁窗口 ~3ms 可接受
- **F-13 锁轮询 jitter follow-up**：当前 D-008 保守档串行约束已规避惊群；并发 5+ 进程时再加 random.uniform(0, 0.01) 抖动
- **F-15 subprocess lazy import**：反 PEP 8 但与 yaml/hashlib lazy 的 fail-open 设计哲学一致；可保留观察
- **F-19 schema_version 不校验**：detailed-design.md:534 "read 走兼容窗口" 设计已接受
- **F-20 bootstrap TOCTOU**：write_text("{}") 内容相同 + LOCK_EX 序列化后续读写，业务无影响
- **F-24 fsync 持锁内**：本地 SSD <5ms 影响可忽略；网络 fs/HDD 部署时再优化
- **F-25 audit mkdir 重复**：与 guard.sh audit_log 同模式（设计意图行为等价）
- **F-26 bats 缺场景 8/9**：TC-F4-2 验收 "7 fail-open" bats 实测 14 已远超；TL-009 在 lib 层覆盖场景 8
- **F-28 audit_log/parse_feature_id 命名**：audit_log 与 guard.sh:49 命名对齐设计意图；parse_feature_id 可改 _parse_feature_id round-2 顺手做
- **F-32 detailed-design §1.2.2 标注 F-005 范围**：cosmetic 文档误导，plan.md notes 一行即可

## 审查元信息

- **审查范围**：F-004 派发链 PreToolUse hook（10 文件，+1397 -3 行）
- **并行 checker**：8（complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec）
- **Critic 统计**：6 rejected / 13 not_proven / 14 not_rebutted
- **综合裁决**：code-quality-reviewer
- **Base SHA**：00050a9
- **Head SHA**：6889a5e
- **Verdict 文件**：`requirements/REQ-2026-008/reviews/code-F-004-001.json`

---

## Round-2 修复包建议（must_fix 集中提交）

合并提交一个 commit 解决 8 项 must_fix（参考 F-003 round-2 模式）：

1. F-14 删除 _SHANGHAI_TZ + _logger 死代码（~10 行）
2. F-22 TL-006 加 result_q 消费断言（~5 行）
3. F-23 TL-009 改 hold_s 或 monkeypatch（~3 行）
4. F-17 plan.md notes + docstring 强化（~5 行）
5. F-27/F-29/F-30 中英文混用统一中文（~10 行）
6. F-8 dispatch_state_cleanup.py 加 ValueError catch + dispatch_state.py:247 docstring 同步（~10 行）
7. F-9 plan.md 加 ADR D-010 + dispatch_state.py:21 docstring 同步（~30 行 ADR + 5 行 docstring）

**should_fix 推后续 commit 或合并 round-2**：F-1/F-2/F-5/F-6/F-7/F-16 重构 dispatch_precheck.py + dispatch_state.py（~80-150 行 net-zero 改动）。

修完后跑 `pytest tests/lib/test_dispatch_state.py + bats tests/hooks/test_dispatch_precheck.bats + bats tests/hooks/test_pre_tool_use_guard.bats` 全绿，重审 round-2 looks_clean → tty signoff approved。
