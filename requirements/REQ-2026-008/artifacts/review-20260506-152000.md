# 代码审查报告 · REQ-2026-008 · 20260506-152000

## 结论

**needs_attention** · 0 critical / 3 major / 1 minor

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项
- `blocked` — AI 机器评估发现关键问题

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认（`.review-scope.json`）：

- **决策**：accept
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-06 15:04:36
- **跑了哪些 checker**：security-checker / concurrency-checker / error-handling-checker / design-consistency-checker / auxiliary-spec-checker（5 个）
- **跳过的 checker（含原因）**：
  - complexity-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人执行：

```bash
/code-review:signoff REV-REQ-2026-008-code-F-003-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-008-code-F-003-001 --trivial  # 纯文档变更快速通道
```

未 sign-off 时 feature-lifecycle-manager 不会把对应 feature 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

> 当前 verdict 含 2 条 required_fixes（F-5 / F-3），按 schema CR-2 约束 conclusion 已锁定 needs_attention，**不能直接 approved**——必须先修这两条再走 review-002（supersedes 001）才能 looks_clean。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-1** [security-checker · minor] → critic: not_proven → **最终处置：downgrade（参考项，不强制修）**
  - 裁决依据：`_load_schema` 错误消息含绝对 `SCHEMA_PATH` 与 F-002 `check_features.py:57-63` 同构既存；agentic engineering 骨架仓库非生产服务，路径暴露不构成实际安全风险，severity minor 偏高。
- **F-2** [error-handling-checker · major] → critic: not_rebutted → **最终处置：should_fix（推 F-005+ 一起修）**
  - 裁决依据：plugin 层 `SchemaLoadError`（schema 文件损坏 → exit 2 语义）与 `_check_*` 累积的数据违规（exit 1 语义）共用 `Decision.FAIL + R-TASK-FRONTMATTER-INVALID code`，确实混淆退出码契约；但 F-002 `features_schema.py:142-147` 同位置同样未区分，单点修 F-003 反引入分裂——建议推到 F-005+ 抽公共 ValidationReport 时统一处理，本次仅在 notes.md 留 follow-up 锚点。
- **F-3** [error-handling-checker · major] → critic: not_rebutted → **最终处置：must_fix**
  - 裁决依据：`_parse_frontmatter_safe` 4 条出错路径（OSError / 首行非 `---` / 找不到结束 `---` / yaml.YAMLError）全无 logger 调用，对照 F-002 `features_schema.py:144/153` 同等位置已有 `logger.exception`，可观测性回退；CI/排错时无栈帧会被吞。低成本修复（4 处加 1 行）。
- **F-4** [error-handling-checker · minor] → critic: not_proven → **最终处置：downgrade（参考项）**
  - 裁决依据：`main()` 中 `OSError` 走 `exit 1` 是 F-002 同模式既有契约（IO/数据错误统一 exit 1，schema 损坏 exit 2）；单点改 F-003 反引入分裂，列入统一债清单。
- **F-5** [design-consistency-checker · major] → critic: not_rebutted → **最终处置：must_fix**
  - 裁决依据：F-003.md frontmatter 自身缺 `schema_version` 字段——schema 落地者违反自定义 schema 的"自举失败"。task-frontmatter-schema.yaml 把 `schema_version` 列为 required，ADR 注释虽说"过渡期 fail 是预期行为"，但那是针对 F-001/F-002 历史 task.md；F-003 自身这条记录在 diff 里被改动（status: in-progress），GATE-TASK-FRONTMATTER 实际跑会对此文件 fail，与 receipt.json `tests_passed: true` 矛盾。一行修复（加 `schema_version: "1.0"`）。
- **F-6** [design-consistency-checker · minor] → critic: rejected → **最终处置：dropped**
  - 裁决依据：finding 自承认"改 features.json 会导致 detail-design hash drift"——内部逻辑自相矛盾。INDEX.md 触及范围登记的正确处置在 F-005/F-007 设计层，不属 F-003 范围；本次 `receipt.files_touched` 7 项已如实反映实际改动。
- **F-7** [design-consistency-checker · minor] → critic: not_proven → **最终处置：downgrade（参考项）**
  - 裁决依据：grep 验证 `_check_required_fields` 在 `check_receipt.py:168` / `check_task_frontmatter.py:205` 都使用，分布 2:1；F-003 与多数派一致，F-002 的 `_check_top_level_required` 才是少数派。命名不对称是 F-002 的问题不是 F-003 的。
- **F-8** [auxiliary-spec-checker · minor] → critic: not_proven → **最终处置：downgrade（参考项）**
  - 裁决依据：13 个 test 函数中只 3 个嵌入 TC 编号（`test_tc_f3_1_case1/2/3`），与 acceptance criteria `TC-F3-1 case1/2/3` 锚定，是有意为之的可追溯性设计；F-002 `test_complexity_giant_fails` 同样把测试场景标识嵌入函数名。
- **F-9** [auxiliary-spec-checker · minor] → critic: rejected → **最终处置：dropped**
  - 裁决依据：grep 复核 F-002 `test_check_features.py` 同位置 separator `# ===== TC-F2-1 case2: complexity=giant → fail =====` 同样是英文+TC 编号无中文；finding 描述"F-002 有中文'枚举越界'"事实错误，F-003 风格与 F-002 完全一致。
- **F-10** [auxiliary-spec-checker · suggestion] → critic: not_rebutted → **最终处置：nice_to_have**
  - 裁决依据：`_parse_frontmatter_safe -> "dict | str"` 字符串包裹 + `from __future__ import annotations` 已存在，无须引号；项目其他文件用 `Optional[int]` / 直接 `dict[str, Any]` 不带引号。低成本风格修，severity suggestion 已是最低档。

## Critical（必须修复）

无。

## Major（强烈建议修复）

### [F-5 · self-bootstrap-failure] requirements/REQ-2026-008/artifacts/tasks/F-003.md:1-11

F-003.md frontmatter 自身缺 `schema_version` 字段——schema 落地者违反自定义 schema，"自举失败"。`GATE-TASK-FRONTMATTER` 在 phase-transition / submit / pre-commit / ci 触发时会对此文件 fail，与 receipt.json `tests_passed: true` 矛盾。

**建议**：在 F-003.md frontmatter 顶部加一行 `schema_version: "1.0"`（紧跟 `---` 之后）。修完本地手跑一次：

```bash
python3 scripts/lib/check_task_frontmatter.py requirements/REQ-2026-008/artifacts/tasks/F-003.md
# 期望 exit 0
```

### [F-3 · observability-regression] scripts/gates/plugins/task_frontmatter.py:178-200

`_parse_frontmatter_safe` 4 条出错路径（OSError 读文件失败 / 首行非 `---` / 找不到结束 `---` / yaml.YAMLError）全无 logger 调用。对照 F-002 `features_schema.py:144/153` 同等位置已有 `logger.exception(..., path=rel_path)`，本 feature 引入新的分步解析路径反而砍掉日志，CI 排障时无栈帧。

**建议**：4 处分别加：

```python
# OSError L180
logger.exception("task_frontmatter: task.md 读取失败，task_md=%s", rel_path)
# yaml.YAMLError L199
logger.exception("task_frontmatter: frontmatter YAML 解析失败，task_md=%s", rel_path)
# 首行非 --- L184 / 找不到结束 --- L193
logger.warning("task_frontmatter: frontmatter 格式不合法，task_md=%s", rel_path)
```

### [F-2 · exit-code-semantic-confusion] scripts/gates/plugins/task_frontmatter.py:140-146（should_fix · 推 F-005+ 一起修）

`_validate_task_md_file` 捕获 `SchemaLoadError`（schema 文件损坏 → CLI 层 exit 2 语义）后追加到 `failures` 列表，最终以 `Decision.FAIL + R-TASK-FRONTMATTER-INVALID + fix_hint="请检查字段是否完整"` 返回，与数据违规（CLI 层 exit 1 语义）混同，会让排查方向错误。F-002 `features_schema.py:142-147` 同位置同样存在该问题。

**建议**：本次**不修**，在 `requirements/REQ-2026-008/notes.md` 留 follow-up 锚点（与已记录的"F-005+ 抽公共 ValidationReport 一次性清理"债项合并）；F-005 派发时把 F-2/F-4/F-7 一起放入抽公共抽象的 scope。

## Minor（改进建议）

### [F-10 · stringified-future-annotations] scripts/gates/plugins/task_frontmatter.py:164-166（nice_to_have）

`_parse_frontmatter_safe` 返回类型注解 `-> "dict | str"` 字符串包裹；文件已有 `from __future__ import annotations` (L15)，PEP 563 lazy 化已生效，无须再加引号。

**建议**：去掉外层引号 → `-> dict | str`。修 F-3 时顺手改。

## Follow-up Notes（不阻塞，但值得提醒）

- **F-2 累积技术债**（来自 F-2，与 process.txt 已记录的"F-005+ 抽公共 ValidationReport"合并）：plugin 层 `SchemaLoadError` 与数据违规共用 `Decision.FAIL` 是 F-002/F-003 共有缺陷，F-005 派发时一并修复
- **F-4 累积技术债**（来自 F-4）：`main()` 中 `OSError` 走 exit 1 vs schema 损坏 exit 2 的契约边界，F-002/F-003 共有，F-005+ 抽 ValidationReport 时统一
- **F-7 命名不对称**（来自 F-7）：`_check_required_fields` vs F-002 `_check_top_level_required` 是 F-002 的少数派偏离，可在 F-002 后续维护时统一（不属 F-003 修复范围）

## 未运行的 checker

- ⚠️ complexity-checker（原因：路径未命中 routing.yaml must/suggest 任何规则）
- ⚠️ performance-checker（原因：路径未命中 routing.yaml must/suggest 任何规则）
- ⚠️ history-context-checker（原因：路径未命中 routing.yaml must/suggest 任何规则）

## 审查元信息

- **审查范围**：feature F-003（task-frontmatter-schema + check_task_frontmatter + GATE-TASK-FRONTMATTER 三件套），9 文件，+1326 -1 行
- **并行 checker**：5（security / concurrency / error-handling / design-consistency / auxiliary-spec）
- **Critic 统计**：rejected 2（F-6/F-9）/ not_proven 4（F-1/F-4/F-7/F-8）/ not_rebutted 4（F-2/F-3/F-5/F-10）
- **综合模型**：code-quality-reviewer（sonnet）
- **Base SHA**：5e29f10
- **Head SHA**：22abb5f
- **Verdict 文件**：`requirements/REQ-2026-008/reviews/code-F-003-001.json`
