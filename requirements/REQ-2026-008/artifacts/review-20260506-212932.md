# 代码审查报告 · REQ-2026-008 · 2026-05-06 21:29:32

## 结论

**needs_attention** · 0 critical / 2 major / 1 minor / 4 follow-up

- 主体扎实：F-008 的 38 个沙盒 e2e 测试 + 自举回归全绿；3 轮 hotfix 收口 receipt schema 漂移与 touches_guard 白名单
- 2 major（F-2 测试 timeout 兜底 / F-6 hotfix 测试漏 touches 登记）+ 1 minor（F-11 F-007 receipt 字段漂移）属低成本下轮收口的事实债

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：all（全 8 个 checker）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-06 21:12:15
- **跑了哪些 checker**：complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec
- **跳过的 checker（含原因）**：无

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人执行：

```bash
/code-review:signoff REV-REQ-2026-008-code-F-008-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-008-code-F-008-001 --trivial  # 纯文档变更快速通道
```

未 sign-off 时 feature-lifecycle-manager 不会把对应 feature 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-1** [performance · major] → critic: not_proven → **drop**
  - 裁决依据：性能 11.5ms/spawn 缺真实 dev session 数据；hook fail-open 限制最坏影响
- **F-2** [error-handling · major] → critic: not_rebutted → **downgrade（保留为 should_fix）**
  - 裁决依据：5 文件 subprocess.run 全无 try/except TimeoutExpired；测试代码非生产，下轮统一封装 _run_with_timeout helper
- **F-3** [error-handling · major] → critic: rejected → **drop**
  - 裁决依据：hotfix 93bd1ef 已明示 F-007 receipt concerns 为归档记录非当前状态
- **F-4** [auxiliary-spec · major] → critic: not_proven → **drop**
  - 裁决依据：仓库现状大量 `test_<plugin>_<behavior>` 已是惯例；should_xxx_when_yyy 是 Java 区块规范不强加 Python
- **F-5** [design-consistency · major] → critic: rejected → **drop**
  - 裁决依据：plan.md D-012 已建立同模式 ADR 框架；docstring 已明示 hotfix 来源
- **F-6** [design-consistency · major] → critic: not_rebutted → **downgrade（保留为 should_fix）**
  - 裁决依据：features.json F-005.touches 确不含 _whitelist / _self_referential / _pre_tool_use_guard.bats；下轮补登记 + ADR D-014
- **F-7** [complexity · minor] → critic: not_rebutted → **downgrade（nice_to_have）**
  - 裁决依据：CC=12×3 / nest=5×2 / 514 行客观超阈，但 fail-open guard chain 内聚单一职责，机械拆分反伤可读性
- **F-8** [security · minor] → critic: not_proven → **drop**
  - 裁决依据：dispatch_precheck.py 注释承认是已知 trade-off；非新增风险
- **F-9** [security · minor] → critic: not_proven → **drop**
  - 裁决依据：symlink 攻击需仓库写权限，前提即 RCE-equivalent
- **F-10** [security · minor] → critic: not_proven → **drop**
  - 裁决依据：本 diff 已清当前快照；改 git 历史成本远高于回报
- **F-11** [error-handling · minor] → critic: not_rebutted → **keep（should_fix）**
  - 裁决依据：F-007 receipt 仍保留 schema 外字段 summary/commits，与 F-001~F-006/F-008 已规范化的字段顺序不一致
- **F-12** [error-handling · minor] → critic: not_proven → **drop**
  - 裁决依据：plugins.* 在测试环境是常驻 sys.path；import 异常事实上不会发生
- **F-13** [error-handling · minor] → critic: not_rebutted → **downgrade（nice_to_have）**
  - 裁决依据：_read_current_feature 非 TimeoutError 分支裸 except return None 与同函数 TimeoutError logger.warning 风格不一致
- **F-14** [performance · minor] → critic: not_proven → **drop**
  - 裁决依据：macOS /var → /private/var symlink 实际需要 resolve
- **F-15** [performance · minor] → critic: not_proven → **drop**
  - 裁决依据：audit_root 隔离 + 不同断言面，参数表面相同但语义不同
- **F-16** [concurrency · minor] → critic: rejected → **drop**
  - 裁决依据：write_text("{}") 幂等 + 锁内骨架兜底
- **F-17** [concurrency · minor] → critic: rejected → **drop**
  - 裁决依据：reviewer 自承认无威胁场景；纯风格问题
- **F-18** [auxiliary-spec · minor] → critic: not_proven → **drop**
  - 裁决依据：dispatch_precheck.py:114 同款是仓库既有惯例
- **F-19** [auxiliary-spec · minor] → critic: not_proven → **drop**
  - 裁决依据：缺实质性差异证据
- **F-20** [auxiliary-spec · minor] → critic: not_rebutted → **downgrade（nice_to_have）**
  - 裁决依据：注释"6 类"与 parametrize 5 条不符是文档误导事实，下轮 trivial 收口顺手改
- **F-21** [auxiliary-spec · minor] → critic: not_rebutted → **downgrade（nice_to_have）**
  - 裁决依据：TL-SR-001/002 注释纯英文符号违反团队中文规范；trivial 不阻塞
- **F-23** [auxiliary-spec · minor] → critic: not_proven → **drop**
  - 裁决依据：证据偏薄；纯英文路径说明在工程语境合理
- **F-24** [design-consistency · minor] → critic: not_proven → **drop**
  - 裁决依据：schema 允许 commit_sha=HEAD；F-008 dispatch in-progress 状态合理选择
- **F-25** [design-consistency · minor] → critic: not_proven → **drop**
  - 裁决依据：features.json hash 锁定改动需 R005 重审；test_summary 已正确注明"7 用例"
- **F-26** [performance · trivial] → critic: rejected → **drop**
  - 裁决依据：tg fixture function scope 是 importlib 模块副作用要求

> history-context-checker 的 8 项均为"误判保护清单"（reviewer 不应提的）而非候选 finding，未参与裁决；保留在该报告内作下轮 reviewer 参考。

## Critical（必须修复）

无。

## Major（强烈建议修复）

无（F-2 / F-6 已 downgrade 为 should_fix）。

## Minor（改进建议）

### [error-handling] requirements/REQ-2026-008/artifacts/tasks/F-007.receipt.json:1-end

F-007 receipt 仍保留 schema 外字段 `summary` 和 `commits[]`，与 F-001/F-002/F-003/F-006/F-008 已规范化的字段顺序不一致；hotfix 93bd1ef 清空 violations 时漏改这两个字段。未来 schema 加 `additionalProperties: false` 立即失败。

**建议**：移除 F-007.receipt.json 的 `summary` 字段（迁移内容到 `test_summary`）和 `commits[]` 数组（保留 `commit_sha` 单字段）。低成本数据回填，下轮 receipt 漂移收尾顺手清理。

## Follow-up Notes（不阻塞，但值得提醒）

### [error-handling] tests/integration/test_*_sandbox.py 5 个文件

`subprocess.run(timeout=...)` 全部无 `try/except subprocess.TimeoutExpired`，超时即抛 pytest 异常 + stdout/stderr 完全丢失。CI 慢机排查难。建议下轮统一封装 `_run_with_timeout(cmd, timeout) -> CompletedProcess` helper（捕获 TimeoutExpired 并 attach 已收的 stdout/stderr）。属测试代码非生产，本 feature 是测试主体所以建议下轮统一收口，不阻塞当轮（来自 F-2）。

### [design-consistency] requirements/REQ-2026-008/artifacts/features.json F-005.touches

hotfix 新增的 `tests/hooks/test_touches_guard_self_referential.py` / `tests/hooks/test_touches_guard_whitelist.py` 实测属 F-005 测试套范畴，但 features.json `F-005.touches` 仅含原 `test_touches_guard.bats` / `test_touches_guard_concurrency.py` / `test_touches_violation.py`。修改的 `tests/hooks/test_pre_tool_use_guard.bats` 也不在 F-005.touches。下轮在 features.json F-005.touches 补登记 + 在 plan.md 加 ADR D-014 记录扩展（参考 D-012 同模式）。features.json hash 改动需考虑 R005 stale 重审成本（来自 F-6）。

### [complexity] .claude/hooks/touches_guard.py

CC=12 (`_read_touches:282` / `_main_inner:445`) / CC=11 (`_locate_req_dir:194`) / nest=5 (`_flock_receipt_file:98` / `_extract_file_paths:349`) / 总行 514 客观超阈，但 fail-open guard chain 内聚单一职责，机械拆分可能反伤可读性。建议下次重构窗口期评估 `_read_current_feature` / `_read_touches` / `_match_touches` 是否能抽 helper 模块，属 hotfix 收口期技术债登记（来自 F-7）。

### [error-handling] .claude/hooks/touches_guard.py:271-279

`_read_current_feature` 非 TimeoutError 分支 `except Exception: return None` 与同函数 TimeoutError 分支 `logger.warning` 风格不一致。建议补 `logger.debug` 而非沉默；trivial 不阻塞（来自 F-13）。

### [auxiliary-spec] tests/hooks/test_touches_guard_whitelist.py:105

注释 "6 类白名单" 与 `@pytest.mark.parametrize` 仅 5 条（TL-WL-001~005）不对应，第 6 类（receipt.json 自指）由 `test_touches_guard_self_referential.py` 覆盖。下轮 trivial 收口顺手改注释为 "5 类 req-level 过程产物（receipt.json 自指见 test_touches_guard_self_referential.py）"（来自 F-20）。

### [auxiliary-spec] tests/hooks/test_touches_guard_self_referential.py:102,125

`# ---------- TL-SR-001 ----------` / `# ---------- TL-SR-002 ----------` 注释纯英文符号无中文描述，违反团队中文注释规范。下轮补 ID 中文描述（如 `# ---------- TL-SR-001：自指 receipt.json 不记 violation ----------`），trivial（来自 F-21）。

## 未运行的 checker（如有）

无（8 个 checker 全部跑完）。

## 审查元信息

- **审查范围**：F-008（V-01~V-09 沙盒 e2e + 自举回归）+ 3 轮 hotfix（122579c receipt 自指白名单 / 93bd1ef 6 类白名单 + bats 隔离 / af04fed receipt 回填）+ 1 chore（dispatch state record）（17 文件，+1969 -36 行）
- **并行 checker**：8 / 顺序：complexity → history-context → auxiliary-spec → security → performance → error-handling → concurrency → design-consistency
- **Critic 统计**：rejected 6 / not_proven 13 / not_rebutted 7（共 25 候选 finding，1 条 F-22 已合并）
- **综合模型**：code-quality-reviewer（Judge 三方对比）
- **Base SHA**：c132ca9
- **Head SHA**：fdde6de
