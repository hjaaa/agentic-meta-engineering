---
feature_id: F-003
title: extract-experience.prompt.md 模板
status: pending
complexity: small
depends_on: []
created_at: "2026-04-26 13:53:48"
updated_at: "2026-04-26 13:53:48"
review_report: null
---

# F-003 · extract-experience.prompt.md 模板

## 需求描述

新建 `.claude/hooks/extract-experience.prompt.md`，含三类抽取目标（决策/坑/事实）+ 输出格式约束 + 上下文约束（不用工具、不写文件、≤1500 字）+ `{{TRANSCRIPT_PATH}}` 占位符。

## 接口/数据结构

详见 `requirements/REQ-2026-001/artifacts/detailed-design.md` 第 5 节（M4 Prompt 内容）。worker 通过 bash 字符串替换把占位符替换为实际 transcript 临时文件路径。

## 依赖

### 依赖的 feature

无（独立文档资产）

### 依赖的模块

- `.claude/hooks/extract-experience.prompt.md`（新建）

## 触及范围

仅 `.claude/hooks/extract-experience.prompt.md` 一个文件。

## 开发笔记

- 必须包含字面量 `{{TRANSCRIPT_PATH}}`（worker 用 `${PROMPT//\{\{TRANSCRIPT_PATH\}\}/$TMP_FILE}` 替换）
- Prompt 中明确禁止使用工具（避免误调写工具）和编造信息
- 留出"无新经验时输出 `_本轮无新经验_`"的兜底指令

## 完成自检

- [ ] 包含 `{{TRANSCRIPT_PATH}}` 占位符
- [ ] 明确禁止使用工具与编造信息
- [ ] 在真实 transcript 上手工跑一次能产出 markdown 列表（`claude -p "$(cat .claude/hooks/extract-experience.prompt.md)" --add-dir <test_dir>`）
- [ ] `/code-review` 通过
