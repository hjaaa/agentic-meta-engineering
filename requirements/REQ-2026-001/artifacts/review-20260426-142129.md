# 代码审查报告 · REQ-2026-001 / F-001 · 2026-04-26 14:21:29

## 结论

**approved** · 0 critical / 0 major / 3 minor（已在 fixup commit `3aece3e` 全部修复）

合入门槛已满足。3 条 minor 在裁决后顺手以 fixup commit 闭环；2 条 follow-up hardening 不阻塞，记入 Follow-up Notes。

## 裁决明细

> 对每条候选 finding 的"reviewer → critic → Judge"三方透明度。

- **FN-001** [security · critical] → critic: rejected → **最终处置：drop**
  - 依据：parse_field 调用点为 bash 字面量 `cwd` / `transcript_path`，HOOK_INPUT 经 `json.load` 解析后才取字段；要让 `$1` 被攻击者控制必须改本脚本源码——此时已可直接 RCE。注入路径不可达。
- **FN-002** [security · critical] → critic: rejected → **最终处置：drop**
  - 依据：SessionEnd Hook 的 stdin 由 Claude Code 在用户身份下产生，与用户同信任域；能控 stdin 的攻击者已能直接执行命令，无需通过本脚本"提权"。
- **FN-003** [security · critical] → critic: rejected → **最终处置：follow-up**
  - 依据：与 FN-002 同信任域论证；建议 WORKER 路径基于 `BASH_SOURCE` 派生作为纵深防御。
- **FN-004** [security · major] → critic: rejected → **最终处置：drop**
  - 依据：同信任域；transcript 写 notes.md 是 F-002 worker 行为。
- **FN-005** [security · major] → critic: not_proven → **最终处置：drop**（out-of-scope）
  - 依据：worker 可观测性属 F-002 范围（features.json:21 验收 TC-06/TC-07）。
- **FN-006** [security · minor] → critic: not_proven → **最终处置：follow-up**
  - 依据：分支名穿越攻击需攻击者已具本地写权；`feat/req-../../...` 含 `..` 被 git ref-format 直接拒。建议加 REQ_ID 正则白名单做纵深防御。
- **FN-007** [performance · major] → critic: rejected → **最终处置：drop**
  - 依据：本机实测 5 次 70ms（中位 70ms），未破 100ms SLA。checker 的"80-150ms"为推断未实测。
- **FN-008** [performance · minor] → critic: not_rebutted → **最终处置：keep（已修复）**
  - 依据：`echo|sed` 多余 fork，bash 参数展开 `${BRANCH#feat/req-}` 零成本等价。fixup `3aece3e` 已修。
- **FN-009** [performance · minor] → critic: not_proven → **最终处置：drop**
  - 依据：依赖 FN-007（已 drop），独立看无收益。
- **FN-010** [concurrency · major] → critic: not_proven → **最终处置：drop**（out-of-scope）
  - 依据：唯一实例锁的真正实现点在 F-002 worker（`mkdir LOCK_DIR`，detailed-design.md:188-195）；F-001 主 Hook 不实现此约束。
- **FN-011** [concurrency · major] → critic: not_proven → **最终处置：drop**（out-of-scope）
  - 依据：phase TOCTOU 修复点在 F-002 worker 二次校验，本 feature 改不动。
- **FN-012** [concurrency · minor] → critic: rejected → **最终处置：drop**（out-of-scope）
  - 依据：finding 落在设计文档里的 worker cleanup trap 段（detailed-design.md:190-195），不属 F-001。
- **FN-013** [error-handling · minor] → critic: not_rebutted → **最终处置：keep（已修复）**
  - 依据：`set -u` + 函数体裸 `$1` 是合理隐患；`${1:-}` 一字符防御。fixup `3aece3e` 已修。
- **FN-014** [error-handling · minor] → critic: not_rebutted → **最终处置：keep（已修复）**
  - 依据：line 43 注释 "符合 REQ-YYYY-NNN 格式的标准分支名已被 sed 处理" 与代码（直接 exit 0，无 fallback）不一致。fixup `3aece3e` 已修。

## Critical（必须修复）

无。

## Major（强烈建议修复）

无。

## Minor（改进建议）

3 条均已在 `fixup(F-001) 3aece3e` 闭环：

### [performance/shell-style] `.claude/hooks/extract-experience.sh:40`

`echo "$BRANCH" | sed -E 's|^feat/req-||'` 多余 fork 两个进程。**已修**为 `${BRANCH#feat/req-}`（FN-008）。

### [error-handling/robustness] `.claude/hooks/extract-experience.sh:18-26`

`set -u` 下 parse_field 函数体裸用 `$1`，未来无参调用即破坏静默退出契约。**已修**为 `${1:-}`（FN-013）。

### [doc-drift/maintainability] `.claude/hooks/extract-experience.sh:43`

注释残留 "符合 REQ-YYYY-NNN 格式的标准分支名已被 sed 处理"，与下一行 `exit 0` 实际语义不符。**已修**为 `# meta.yaml 不存在，静默跳过`（FN-014）。

## Follow-up Notes（不阻塞，但值得提醒）

- WORKER 路径建议从 `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` 派生，弱化对 CWD 的信任（来自 FN-003，纵深防御）
- REQ_ID 截取后建议加 `[[ "$REQ_ID" =~ ^[0-9]{4}-[0-9]{3}$ ]] || exit 0` 白名单（来自 FN-006，纵深防御）
- 建议在后续"Hook 安全加固"小卡里统一处理上述两条；F-002 review 时复议 FN-005（worker 可观测性）/FN-010（唯一实例锁）/FN-011（phase TOCTOU）/FN-012（watchdog 锁残留）

## 未运行的 checker

无（8 个 checker 全部并行完成）。

## 审查元信息

- **审查范围**：embedded · REQ-2026-001 / F-001（`.claude/settings.json` + `.claude/hooks/extract-experience.sh`，2 文件 +74 行）
- **并行 checker**：8（design-consistency / security / concurrency / complexity / error-handling / auxiliary-spec / performance / history-context）
- **Critic 统计**：rejected 6 / not_proven 5 / not_rebutted 3
- **综合裁决**：code-quality-reviewer
- **Base SHA**：e94992bf8d7ef73b7c0eb17076804f4306badf7f
- **Head SHA**：7366dc17ad5dcc2724f90a5332ea78a5547d9a98（feature commit）→ 3aece3e（fixup commit，3 条 minor 闭环）
