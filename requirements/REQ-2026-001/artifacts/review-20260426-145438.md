# 代码审查报告 · REQ-2026-001 / F-002 · 2026-04-26 14:54:38

## 结论

**approved**（首轮 needs_revision；fixup `bf4369c` 后所有阻塞项已闭环）· 0 critical / 1 major / 6 minor

首轮裁决 needs_revision——M-1 (LOCK_DIR 累积残留) / M-3 (stderr 误判) / M-4 (锁所有权契约) 三项要求合入前修复。subagent 已按精确改点完成 fixup commit `bf4369c`（+21/-2 行），新增 stale lock 与 stderr-only 两个测试场景验证通过，post-dev-verify 五项全过。本报告以 fixup 后状态为准，结论 approved。

## 裁决明细

> 对每条候选 finding 的"reviewer → critic → Judge → fixup"四方透明度。M-1/M-3/M-4 是 needs_revision 阻塞项，已在 fixup `bf4369c` 闭环。

- **FN-001** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep（minor follow-up doc）**
  - 依据：实现 helper 函数前置是 bash 顺序解析的必须；§4.2 伪代码后置按理可执行，但若直接 copy 会 command not found。建议同步设计文档。
- **FN-002 (M-1)** [concurrency · major] → critic: not_rebutted → **最终处置：fixup `bf4369c` 闭环（保留 major 标识，已修复）**
  - 依据：watchdog kill -9 在 acquire_lock 成功后窗口触发会让 LOCK_DIR 永久残留，hook 业务功能静默失效。设计 §4.3 line 287 仅声明"trap 不跑"，未涉及累积效应。fixup 在 acquire_lock 内加 owner PID + kill -0 探测，stale lock 自动回收。
- **FN-003 (M-2)** [concurrency · minor] → critic: not_proven → **最终处置：keep（follow-up，setsid 杀进程组）**
  - 依据：watchdog 仅 kill worker 主进程，claude -p 子进程会成孤儿，资源泄漏。watchdog 触发本就异常路径，频率证据不足；判定为后续 hardening。
- **FN-004** [concurrency · minor] → critic: not_proven → **最终处置：drop（设计决策注记）**
- **FN-005** [error-handling · major] → critic: rejected → **最终处置：drop**
  - 依据：worker 唯一调用方永远传 3 个固定参数；契约'任何路径 exit 0' 在生产路径下成立（来源：.claude/hooks/extract-experience.sh:60）。
- **FN-006 (M-4)** [error-handling · major→minor] → critic: not_proven → **最终处置：fixup `bf4369c` 闭环（注释明示锁所有权契约）**
  - 依据：当前 trap EXIT 100% 兜底，属未来扩展隐患而非现 bug。降级 minor，靠注释澄清"调用后必须立即 exit"契约。
- **FN-007 (M-3)** [error-handling · minor] → critic: not_proven → **最终处置：fixup `bf4369c` 闭环（stderr 隔离）**
  - 依据：`> RESULT_FILE 2>&1` 让 stderr-only 输出可被 `-s` 检查通过并写入 notes.md。fixup 用独立 `$STDERR_FILE` 接 stderr，stdout-only 进 RESULT_FILE。
- **FN-008** [performance · minor] → critic: not_rebutted → **最终处置：keep（follow-up 优化）**
  - 依据：`$(cat "$RESULT_FILE")` 多一次子进程；当前规模影响可忽略。
- **FN-009** [performance · minor] → critic: not_rebutted → **最终处置：drop（当前规模无影响）**
- **FN-010** [performance · minor] → critic: not_rebutted → **最终处置：drop（当前规模合理）**
- **FN-011** [aux-spec · minor] → critic: rejected → **最终处置：drop**
  - 依据：团队风格未强制 `set -euo pipefail`；同仓 `extract-experience.sh:6` 同样仅 `set -u`；脚本契约'任何路径 exit 0' 与 `set -e` fail-fast 冲突。
- **FN-012** [aux-spec · minor] → critic: not_rebutted → **最终处置：keep（minor follow-up doc）**
  - 依据：cleanup 函数置于 helper 分区之外，纯结构问题。
- **FN-013** [aux-spec · minor] → critic: not_rebutted → **最终处置：keep（minor follow-up doc）**
  - 依据：helper 函数注释偏'做了什么'少'为什么'，违反 CLAUDE.md §1.3。
- **FN-014** [aux-spec · minor] → critic: not_rebutted → **最终处置：keep（minor follow-up doc）**
  - 依据：PROMPT 替换段同上。

## Critical（必须修复）

无。

## Major（强烈建议修复）

无（FN-002/M-1 已在 fixup `bf4369c` 闭环）。

## Minor（改进建议）

### [concurrency/reliability] `.claude/hooks/extract-experience-worker.sh:18-20, 104-108`

watchdog 仅 kill worker 主进程，claude -p 子进程会成孤儿（资源泄漏，非数据问题）。建议改 `setsid` 启动 + watchdog 杀进程组。**未修，列为 follow-up**（M-2）。

### [performance] `.claude/hooks/extract-experience-worker.sh:131`

`write_section "$(cat "$RESULT_FILE")"` 多一次子进程 + 内存拷贝。建议 `{ printf header; cat "$RESULT_FILE"; } >> NOTES_FILE`。**未修，列为 follow-up**（M-5）。

### [docs/maintainability] `.claude/hooks/extract-experience-worker.sh:39, 95`

helper 函数与 PROMPT 替换段注释偏'做了什么'少'为什么'，应补 mkdir vs flock 选型 / 60 行依据 / 模板解耦原因。**未修，列为 follow-up**（M-6）。

### [docs/structure] `.claude/hooks/extract-experience-worker.sh:27`

cleanup 函数置于 helper 分区之外（因要在 trap 之前定义），结构略不一致。建议在 helper 分区顶部加注释说明。**未修，列为 follow-up**。

### [design-doc] `requirements/REQ-2026-001/artifacts/detailed-design.md` §4.2

伪代码 helper 函数置于 main 之后，bash 顺序解析下需前置；建议同步文档。**未修，列为 follow-up**（M-7）。

## Follow-up Notes（不阻塞，但值得提醒）

- M-2：worker setsid 启动 + watchdog `kill -PGID` 杀进程组（彻底治孤儿子进程）
- M-5：write_section 改为 `cat ... >> NOTES_FILE` 避免命令替换 + 内存拷贝
- M-6：helper 注释补'为什么'（mkdir vs flock / 重试次数 / 模板解耦）
- M-7：detailed-design.md §4.2 伪代码 helper 顺序修正
- 其它 minor 见上"Minor"段
- F-001 review 留下的 FN-005 (worker 可观测性 / claude 子进程日志) / FN-010 (唯一实例锁) / FN-011 (phase TOCTOU) / FN-012 (watchdog 锁残留) 在本次 F-002 中**FN-012 已通过 stale lock 探测解决**；其余三项仍为 follow-up

## 修复记录

| 阻塞项 | 位置 | fixup 实施 |
|---|---|---|
| **M-1**（major · concurrency） | acquire_lock + cleanup | mkdir 成功后写 `$LOCK_DIR/owner` 持 PID；mkdir 失败时 `kill -0` 探测 owner 存活，死则 rmdir 强清并立即重试。cleanup 内 rmdir 之前补 `rm -f $LOCK_DIR/owner`（防非空 rmdir 失败）。**新增"场景 6 stale lock"自测：伪造 PID=999999 → 探测 dead → 清锁 → 成功重试**。 |
| **M-3**（minor · error-handling） | TMP/RESULT/STDERR + cleanup + claude 调用 | 新增 `$STDERR_FILE = /tmp/req-experience-$$-claude.err`；cleanup `rm -f` 一并清理；claude 调用 `2>&1` → `2> "$STDERR_FILE"`。**新增"场景 7 stderr-only"自测：mock claude exit 0/stdout 空/stderr 有 warning → notes.md 仍写 `[hook-skipped: empty-output-bug-7263]`，stderr 不被当经验**。 |
| **M-4**（minor · maintainability） | append_skip_marker 函数注释 | 补 2 行明示锁所有权契约："acquire_lock 成功后函数不主动 rmdir，由 trap cleanup 在 exit 时释放 → 调用点必须在调用 append_skip_marker 后立即 exit 0"。 |

fixup 通过自检：
- `bash -n` 语法 OK
- 5 个原场景（正常 / claude exit 1 / 空输出 / transcript 不存在 / EXPERIENCE_HOOK_CLAUDE_BIN 注入）全部 PASS（无回归）
- 2 个新场景（stale lock / stderr-only）全部 PASS
- post-dev-verify 5 项全过

## 未运行的 checker

无（8 个 checker 全部并行完成）。

## 审查元信息

- **审查范围**：embedded · REQ-2026-001 / F-002（`.claude/hooks/extract-experience-worker.sh`，1 文件 +132 行 → 修后 +153 行）
- **并行 checker**：8（design-consistency / security / concurrency / complexity / error-handling / auxiliary-spec / performance / history-context）
- **Critic 统计**：rejected 2 / not_proven 3 / not_rebutted 9
- **综合裁决**：code-quality-reviewer（首轮 needs_revision → fixup → 闭环）
- **Base SHA**：6151ad6（feat/req-2026-001 上一个 commit）
- **Head SHA**：edf93c7（feature commit）→ bf4369c（fixup commit，3 项阻塞闭环）
