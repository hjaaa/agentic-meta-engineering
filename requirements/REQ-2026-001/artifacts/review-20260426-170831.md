# 代码审查报告 · REQ-2026-001 / F-004 · 2026-04-26 17:08:31

## 结论

**approved**（首轮 needs_revision；fixup `6e95994` 后所有阻塞项已闭环）· 0 critical / 3 major / 14 minor

首轮 23 个候选 finding 经 critic 与 Judge 三方对比：1 条 drop（FN-008 无依据阈值）、5 条降级、3 条 keep major（FN-001 / FN-011 / FN-016）。subagent fixup `6e95994` 一次性解决 3 项阻塞 + 6 项顺手 minor。重跑 10/10 PASS，结论 approved。

## 裁决明细

> 23 条候选 finding 的"reviewer → critic → Judge → fixup"四方透明度。

### 阻塞项（3 major，已在 fixup `6e95994` 闭环）

- **FN-001** [design-consistency · major] → critic: not_rebutted → **fixup 闭环**
  - 依据：detailed-design.md:336 写"两条都写入"与 run-e2e.sh:443 `assert_count_ge ≥1` 不一致。fixup 改设计文档 TC-10 期望列为"≥1 条 + 文件完整性校验，允许 notes-busy 一方 skip-write"。1 行文档对齐。
- **FN-011** [error-handling · major] → critic: not_rebutted → **fixup 闭环**
  - 依据：run-e2e.sh:9 仅 `set -u`，setup_fake_repo 内多次 git 命令任一失败被吞，后续断言基于损坏环境产生假 PASS。fixup 改为 `set -eu` 并在 mock-claude.sh 同步加（顺带 FN-015）。10 个 TC 重跑 PASS，证明负路径不被破坏。
- **FN-016** [performance · major] → critic: not_rebutted → **fixup 闭环**
  - 依据：run-e2e.sh:160-169 循环 `sleep 0.3` + `elapsed += 1` 让 max_wait 实际仅 ×0.3s（TC-06/07 实等 3s，TC-09 实等 4.5s）。当前 PASS 仅因 mock 即时返回，慢 CI 必 flake。fixup 改 `sleep 1`，elapsed 名实一致；同步修参数注释（FN-021）。

### 降级 / drop（5 条）

- **FN-008** [complexity · major] → critic: rejected → **drop**
  - 依据：全局 `~/.claude/CLAUDE.md:42` 仅"单方法 ≤60 行"，无文件 500 行硬规；reviewer 自造阈值。
- **FN-002** [security · major] → critic: not_proven → **降级 minor（hardening）**
  - 依据：测试只在测试者本地 UID 跑，PID symlink 攻击需同 UID 已具备同等文件权限。建议 `mktemp -d` 防御。
- **FN-005** [concurrency · major] → critic: not_proven → **降级 minor**
  - 依据：TC-10 断言 `≥1` + line_count 校验已覆盖文件完整性，并发触发是优化非正确性。
- **FN-010** [error-handling · critical] → critic: not_proven → **降级 minor**
  - 依据：run-e2e.sh:516-528 main 末尾已有 worker 产物兜底；INT/TERM 路径残留属同 UID 影响。配合 FN-014 加全局 trap 已收敛。
- **FN-012** [error-handling · major] → critic: not_rebutted → **降级 minor**
  - 依据：assert_contains msg 已含 'TC-NN 应...' 上下文，可手动 tail 排查；定位效率而非可信度。

### 同 fixup 顺手修的 minor（6 条）

- **FN-009 / FN-019**：setup_fake_repo 加 `skip_req_dir` 第 5 参数；TC-04 改用，消除重复 git 初始化
- **FN-013**：10 个 TC 函数内 unused `local tc=` 行删除
- **FN-022**：TC-03/TC-05 改用 `assert_not_contains` 做 negative 断言
- **FN-020**：make_hook_input / run_hook_and_wait 参数英文注释改中文
- **FN-023**：TC-08 相关三处描述统一为"约 35s"
- **FN-014**：main 入口加全局 `trap 'rm -rf /tmp/req-experience-tc*-$$' EXIT INT TERM` 兜底清理

### 保留 follow-up（5 条 minor，未修）

- **FN-003**：make_hook_input python -c 字符串内插（潜在）—— 调用方常量，建议改 os.environ 传入
- **FN-004**：mock-claude.sh 加运行环境断言（防误装到 PATH）
- **FN-006**：TC-10 断言粒度细化（"撕裂 vs notes-busy"区分）
- **FN-007**：TC-10 函数级 trap 兜底 worker 孤儿（已被 FN-014 全局 trap 部分覆盖）
- **FN-017 / FN-018**：负路径 sleep 0.5 / TC-08 sleep 35 的微小优化

## Critical（必须修复）

无。

## Major（强烈建议修复）

无（FN-001 / FN-011 / FN-016 已在 fixup `6e95994` 闭环）。

## Minor（改进建议）

### [security/hardening] `tests/run-e2e.sh:75-78` 与多处

workdir 用 PID 不可预测化建议改 `mktemp -d`。fixup 已通过 main 全局 trap 部分缓解残留风险，但路径可预测面未消除。**未修，列为 follow-up**（FN-002）。

### [concurrency] `tests/run-e2e.sh:680`（TC-10 断言粒度）

≥1 无法区分 notes-busy 跳过 vs 内容撕裂。建议加"文件以换行结尾 / 内容完整"辅助断言。**未修，列为 follow-up**（FN-006）。

### [security] `tests/mock-claude.sh:1-49`

无运行环境断言（防误装 PATH）。建议头部加 `[ -n "$MOCK_CLAUDE_MODE" ] || exit 127`。**未修，列为 follow-up**（FN-004）。

### [security/hardening] `tests/run-e2e.sh:133-136`

make_hook_input python -c 字符串内插。当前调用方常量，未来扩展易踩。建议改 os.environ。**未修，列为 follow-up**（FN-003）。

### [performance/低优] `tests/run-e2e.sh:218,247,281,308 / 381`

负路径 sleep 0.5 累计 2s 可省；TC-08 sleep 35 比 watchdog 30 多 5s。**未修，列为 follow-up**（FN-017 / FN-018）。

## Follow-up Notes（不阻塞，但值得提醒）

- 5 条 minor 见上"Minor"段
- TC-10 函数级 trap（FN-007）已被 FN-014 全局 trap 部分覆盖，剩余增量可下次顺手做

## 修复记录

| 阻塞项 | 位置 | fixup 实施 |
|---|---|---|
| **FN-001**（major · design-consistency） | detailed-design.md TC-10 期望列 | 改"两条都写入"→"≥1 条 + 文件完整性校验，允许 notes-busy 一方 skip-write" |
| **FN-011**（major · error-handling） | run-e2e.sh:9 + mock-claude.sh:1 | `set -u` → `set -eu`；mock-claude.sh 同步加 |
| **FN-016**（major · performance） | run_hook_and_wait 函数体 + 注释 | sleep 0.3 → sleep 1，elapsed 名实一致；参数注释中文化（同步 FN-021） |

fixup `6e95994` 通过自检：
- `bash -n` 语法 OK（run-e2e.sh + mock-claude.sh）
- 10 个 TC 重跑全 PASS（含 TC-08 ~35s）
- post-dev-verify 5 项全过
- 无 /tmp 残留

## 未运行的 checker

无（8 个 checker 全部并行完成）。

## 审查元信息

- **审查范围**：embedded · REQ-2026-001 / F-004（`requirements/REQ-2026-001/artifacts/tests/`，5 文件 +748 行 → 修后 +732 行 / -45 行）
- **并行 checker**：8（design-consistency / security / concurrency / complexity / error-handling / auxiliary-spec / performance / history-context）
- **Critic 统计**：rejected 1 / not_proven 3 / not_rebutted 19
- **综合裁决**：code-quality-reviewer（首轮 needs_revision → fixup → 闭环）
- **Base SHA**：3bd8d7a（feat/req-2026-001 上一个 commit）
- **Head SHA**：38069cb（feature commit）→ 6e95994（fixup commit，3 阻塞 + 6 顺手 minor 闭环）
