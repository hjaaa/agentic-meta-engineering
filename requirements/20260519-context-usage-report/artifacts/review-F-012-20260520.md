# F-012 代码审查报告 · 单元测试：TC-1~TC-8 + 评分 + 各组件

| 字段 | 值 |
|---|---|
| review_id | `REV-20260519-context-usage-report-code-F-012-001` |
| reviewed_commit | `d226a24`（`--allow-empty` audit-only）|
| base_commit | `4d9cc8c` |
| 增量 | **0 文件 / 0 行**（empty diff）|
| **conclusion** | **`looks_clean`** |
| **score** | **95** |
| 测试 | 107 passed in 0.51s + ruff check passed |

## 审查范围说明

F-012 commit 为 `--allow-empty` 审计型提交：commit message 内嵌完整 audit 报告（AC1/AC2/AC3 逐项对清单），无任何代码 / 测试改动。

routing.py 决策为 `decision=all`（files_total=0 时的兜底默认值），但 8-critic 群跑空 diff 会全部返回"无 finding"，纯属浪费 budget。本评审在无可审 diff 的前提下做存量追认，未触发并行 critic + critic-验证流。

实际待审的测试覆盖在 F-004 ~ F-011 各自迭代时已通过 TDD 写入并经对应 /code-review 验收，详见：

- `reviews/code-F-004-002.json`（ContextInventory）
- `reviews/code-F-005-002.json`（IndexGraph）
- `reviews/code-F-006-001.json`（EvidenceScanner）
- `reviews/code-F-007-001.json`（AppliedSignalClassifier）
- `reviews/code-F-008-002.json`（fetch_git_timestamps）
- `reviews/code-F-009-002.json`（UsageAggregator + scoring + classifier）
- `reviews/code-F-010-002.json`（ReportRenderer）
- `reviews/code-F-011-002.json`（CLI + main + exit codes）

## AC 对清单（F-012 audit 结论）

| AC | 要求 | 当前覆盖 | 状态 |
|---|---|---|---|
| AC1 | `_classify_status` 8 TC（TC-1~TC-8） | `test_tc1_orphan` ~ `test_tc8_active_applied_but_ref_insufficient` @ test_context_usage_report.py L591-748 | ✅ 8/8 |
| AC2 | `_compute_score` 4 边界（ref_cap=30 / applied_cap=40 / recency 30d&90d / 全 0） | `test_score_indexed_zero_and_two` / `test_score_reference_count_cap` / `test_score_applied_signal_count_cap` / `test_score_recency_boundaries` / `test_score_max_all_caps` @ L750-825 | ✅ 5/4 |
| AC3 | 6 组件 × (3 核心 + 1 异常) = 30 | ContextInventory 6 / IndexGraph 8 / EvidenceScanner 13 / AppliedSignalClassifier 17 / UsageAggregator 14 / ReportRenderer 20 / CLI 12 = 90 | ✅ 远超 30 |
| AC4 | pytest 全绿 | 107 passed in 0.51s | ✅ |

## 维度评分

| 维度 | score | issues |
|---|---|---|
| design_consistency | 95 | 0 |
| security | 95 | 0 |
| concurrency | 95 | 0 |
| complexity | 95 | 0 |
| error_handling | 95 | 0 |
| auxiliary_spec | 95 | 0 |
| performance | 95 | 0 |
| history_context | 95 | 0 |

## Critical / Major / Minor issues

无。`required_fixes: []`。

## Suggestions

1. F-012 commit `d226a24` 为 `--allow-empty` audit-only 提交（0 文件改动 / 0 行 diff），不引入代码风险。AC1/AC2/AC3 三类接受标准由 F-004~F-011 各自迭代时已通过 TDD 并经对应 `/code-review` 验收。本评审在无 diff 前提下做存量审计追认，未跑 8-critic 群。
2. 测试套件 107 passed + ruff check passed，全量回归齐绿；F-009-FU-F4 / F-010-FU-F-D / F-011-FU-M-1 / F-011-FU-M-2 等回归路径已纳入对应组件统计。
3. F-009 aggregate / F-010 render_markdown / F-011 `_run` 三处复杂度债务的 follow-up（F-009-FU-1 / F-010-FU-1 / F-011-FU-1）不在 F-012 范围；统一在 F-013 之后拉一个渲染层 refactor follow-up 合并处理。

## 引用要求自检

| 决策 | 来源 |
|---|---|
| audit-only commit 不跑 8-critic 群 | F-012 commit d226a24 `git show --stat` 显示 0 文件改动；routing.py 决策 `decision=all` 但 `files_total=0`，跑会产生空 finding |
| AC 覆盖矩阵证据 | `python3 -m pytest tests/lib/test_context_usage_report*.py -q` → 107 passed in 0.51s |
| 复杂度债务挂账 | F-009-FU-1 / F-010-FU-1 / F-011-FU-1 见 notes.md（与 review-F-009/F-010/F-011-20260520.md 链路一致） |
