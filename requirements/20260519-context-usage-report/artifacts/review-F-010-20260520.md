# F-010 代码审查报告 · ReportRenderer Markdown 4 章节 + JSON 渲染

| 字段 | 值 |
|---|---|
| review_id | `REV-20260519-context-usage-report-code-F-010-001` |
| reviewed_commit | `c8c3dbb` |
| base_commit | `29cc874` |
| 增量 | `scripts/lib/context_usage_report.py` +276 行 + `tests/lib/test_context_usage_report.py` +372 行 |
| **conclusion** | **`needs_attention`** |
| **score** | **82** |
| 测试 | 53 passed in 0.32s |

## 路由 / 维度评分

| 项 | 值 |
|---|---|
| 决策 | `all`（8 全集兜底） |
| 跑过 checker | 8/8 |
| critical | 0 |
| major | 2 |
| minor | 0（22 candidate 中 20 drop） |

## Critical issues

无。

## Major issues（2 条，均挂 follow-up）

### F-B · `render_markdown` 复杂度超阈

- **位置**：`scripts/lib/context_usage_report.py:1284-1390`
- **指标**：89 可执行行（阈值 80）/ CC=12（阈值 10）/ 嵌套深度 4（触线）
- **根因**：单方法承载 4 章节渲染
- **建议**：抽 4 个私有 helper（`_render_overview_section` / `_render_high_value_section` / `_render_to_review_section` / `_render_reference_detail_section`），主方法降至 ~15 行
- **处理**：挂 `F-010-FU-1`，与 F-009-FU-1 合并为统一渲染层重构

### F-D · `render_markdown` 接收 warnings 参数但未渲染

- **位置**：`scripts/lib/context_usage_report.py:1284`
- **根因**：方法签名收 `warnings: list[str]`（设计 `detailed-design.md:416`），函数体未消费；`render_json` 同名参数被序列化到 JSON 顶层（L1472），Markdown 侧缺失对应输出 → 接口签名引入误导性死参数
- **修复**：在「总览」章节末尾追加 `### Warnings` 小段（仅当 warnings 非空），3-5 行
- **处理**：可本次小修或挂 `F-010-FU-2`

## 裁决明细（22 候选 → 2 keep + 20 drop）

| F-id | checker / 类型 | 原始 severity | critic verdict | 最终处置 | 理由 |
|---|---|---|---|---|---|
| F-A | EH/CNC/SEC | major+minor | not_proven | drop | 单进程 CLI 无并发 tmp 冲突契约 |
| F-B | complexity | major | not_rebutted | **keep → follow-up** | 与 F-009-FU-1 同型 |
| F-C | complexity 文件 1496 行 | 2× major | rejected | drop | F-001~F-010 累积；notes.md F-009-FU-1 已挂 |
| F-D | design-consistency | major | not_rebutted | **keep → follow-up** | warnings 死参数 |
| F-E | EH mkdir Raises 文档 | major | rejected | drop | mkdir 抛 OSError，docstring 已覆盖 |
| F-F | PERF summaries 多次遍历 | minor×2 | not_proven | drop | 5000 files 仍在 0.5s SLA |
| F-G | EH json TypeError 文档 | minor | not_proven | drop | 受控类型不会触发 |
| F-H | history critical import 注释缩短 | critical | rejected | drop | F-010 仅删注释文字，无逻辑变更 |
| F-I | AUX mid-file import 注释 | minor×2 | rejected | drop | 与 F-009 drop 的 F-6 同模式 |
| F-J | AUX WHAT vs WHY 注释 | minor | not_proven | drop | navigational marker 非翻译 |
| F-K | AUX JSON_SCHEMA_URL 占位 | minor | rejected | drop | 设计 L413 已标 `[待补充]` |
| F-L | AUX `[:50]` 魔数 | minor | not_proven | drop | 一次使用，命名收益低 |
| F-M | AUX `patch('os.replace')` 路径 | minor | not_proven | drop | 当前 `import os` 写法下正确 |
| F-N | EH context_line 空字符串 | minor | not_proven | drop | 上游 invariant 保证非空 |
| F-O | SEC Markdown 表格 \| 注入 | minor | not_proven | drop | context/** 受信任 |
| F-P | SEC write 路径无边界 | minor | not_proven | drop | main() 契约保证 |
| F-Q | SEC JSON config 路径泄露 | minor | rejected | drop | 设计 L574-580 schema 要求 |
| F-R | DC __init__ 设计未声明 | minor | not_proven | drop | interfaces_frozen 不约束 ctor |
| F-S | DC generated_at 类型严格 | minor | rejected | drop | `now: datetime` 非 Optional |
| F-T | PERF context_line JSON 不截断 | minor | not_proven | drop | 与 Markdown :50 截断分工 |
| F-U | HC 文件高频变更 + 测试 import 交叉 | minor×2 | rejected | drop | history-context 事实陈述 |

## 跨维度洞察

- F-B（复杂度）与 F-D（设计偏离）指向同一根因：`render_markdown` 单方法承载过多职责。F-010-FU-1 拆分后各 `_render_*_section` 独立决定 warnings 处置——一次重构同时消除两个 finding
- 22 candidate → 2 keep（9%）的转化率与 F-009 的 21→3（14%）量级一致，checker 召回阈值偏低、critic 反证机制在 render_* 类方法上有效

## 人工确认提示

本次评审输出 **conclusion = `needs_attention`**，0 required_fixes，两条 keep major 均挂 follow-up。

**默认动作**：F-D 一行小修（在 Markdown「总览」末尾追加 warnings 段），消除接口死参数；F-B 与 F-009-FU-1 合并为 F-010-FU-1。

**风险接受路径**：如用户判断 F-D 也可挂 follow-up，请在主对话**显式回复**"接受当前 needs_attention 风险，转 done"。

未获软确认时 F-010 status 保持 `in-progress`。
