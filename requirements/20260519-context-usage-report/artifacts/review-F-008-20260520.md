# Code Review · F-008 · 20260520-110530

| 字段 | 值 |
|---|---|
| review_id | REV-20260519-context-usage-report-code-F-008-001 |
| feature_id | F-008（GitTimestamp 批量获取 + fs_mtime 回退） |
| requirement_id | 20260519-context-usage-report |
| commit | ffdc407（base 246b3c6） |
| diff stats | 2 files / +458 / -11（scripts/lib/context_usage_report.py +153 + tests/lib/test_context_usage_report.py +305 / -11） |
| verdict path | requirements/20260519-context-usage-report/reviews/code-F-008-001.json |
| checker_route | complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec（8 全集） |
| routing decision | `all`（diff 全是灰色文件 → 升 8 全集兜底） |
| confirmed_by | hj19961223@gmail.com（routing.py 自动） |

## 结论

### rev2（最终）— `code-F-008-002.json`

**conclusion = `looks_clean`**（升档自 rev1 `needs_attention`），score = 88（+8）。

- critical: 0
- major: 0（F-4 闭合 ✓）
- minor: 0（required_fixes 空）
- follow-up（不阻塞）：F-1 复杂度 + F-3 静默 fs_mtime 回退
- 自引入新问题：0（无 trend-G-meta 反模式）

rev2 fix commit `5e57260`：
- subprocess.run 加 `timeout=30`
- 新增 `except subprocess.TimeoutExpired` 走 fs_mtime 回退 + warnings `"git log 超时（30s），回退 fs_mtime"`
- 原 except 的 `{exc}` 改为 `{type(exc).__name__}`（同步收紧 F-8 路径，critic 已 rejected 但合并修复）
- 测试 14 passed（新增 `test_fetch_git_timestamps_timeout`）+ ruff rc=0

### rev1 — `code-F-008-001.json`（superseded）

**conclusion = `needs_attention`**，score = 80。

- critical: 0
- major: 1（keep — F-4 subprocess 无 timeout）
- minor: 2（keep follow-up — F-3 静默 fs_mtime 回退 / F-1 fetch_git_timestamps 复杂度超线）
- drop: 14（critic rejected 7 + not_proven 7）

**核心问题**：`subprocess.run` 缺 `timeout` 参数（concurrency/F-4）。改动成本极低（+timeout=30 + `subprocess.TimeoutExpired` catch 走 fs_mtime 分支 + warning），建议本 PR 内修。其余 follow-up 不阻塞合并。

## 路由说明

- **决策**：`all`（diff 全部命中灰色文件，未命中 must/suggest，升 8 全集兜底）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-20 10:53:28
- **跑了哪些 checker**：complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec
- **跳过的 checker**：无（8 全集）

## Keep 段

### Major

**F-4 · concurrency × error_handling** — `scripts/lib/context_usage_report.py:130-136`

`subprocess.run(...)` 未设 `timeout`。大仓库 / 网络挂载磁盘 / git 仓库损坏场景下，CLI 可能无限阻塞。fix 极小：

```python
result = subprocess.run(cmd, ..., timeout=30, check=True)
# except 增加：
except subprocess.TimeoutExpired as exc:
    warnings.append(f"git log 超时（{since_days}d 范围），回退 fs_mtime")
    return _fallback_fs_mtime(files), warnings
```

### Follow-up（不阻塞）

**F-3 · design_consistency × error_handling** — `scripts/lib/context_usage_report.py:196-203`

since 窗口内无 commit 的文件静默回退 fs_mtime，无 warning。`detailed-design.md:763` 异常表仅定义 subprocess 失败路径，未覆盖"窗口内无 commit"。建议二选一：(a) 补 design 异常表显式声明该分支为静默回退；(b) 在该分支聚合追加一条 `f"N 个文件在 since={D}d 内无 git 记录，已回退 fs_mtime"`。`last_modified_at_source=fs_mtime` 在 JSON 输出中已有信号 → 故只算 follow-up。

**F-1 · complexity** — `scripts/lib/context_usage_report.py:78-215`

`fetch_git_timestamps` 共 138 行（含 docstring / 注释）/ 圈复杂度约 17。超过项目 80 行 + 10 圈复杂度硬约束。根因是 git log 输出协议适配（commit-header + file-path + exception 三分支同居一函数）。F-008 task touches 限定单文件，本 PR 不强拆。下个 feature 抽：

```python
def _parse_git_log_output(output: str) -> dict[str, tuple[datetime, datetime]]: ...
def _build_timestamp_result(files, timestamps) -> dict[str, GitTimestamp]: ...
```

## Drop 段（裁决明细）

| F-ID | source checker | 原 severity | critic verdict | 最终 | 理由 |
|---|---|---|---|---|---|
| F-2 | complexity | major | rejected | drop | 文件 980 行属前序 feature 历史债（F-006-FU-1）；F-008 task spec 未含拆分指令，超出本 PR 边界 |
| F-5 | performance | minor | not_proven | drop | context/** 实际规模 ≪ MB 级 git log 输出，"数十 MB 全量物化"前提无证据 |
| F-6 | performance + security | minor | not_proven | drop | critic 已 find 验证 context/** 下 112 文件无 pipe 字符；触发概率 0 |
| F-7 | security | minor | rejected | drop | `path.resolve().relative_to(self._repo_root).as_posix()` 结构上不可能产出含 `..` 或绝对路径 |
| F-8 | security | minor | rejected | drop | warnings 去向 stdout（同信任域），不跨界泄漏；CalledProcessError.__str__ 默认仅含 returncode + cmd |
| F-9 | design + auxiliary | minor | rejected | drop | `now` 参数与 UsageAggregator (`detailed-design.md:383`) 接口对称设计，预留测试注入，非死参数 |
| F-10 | design-consistency | minor | rejected | drop | catch `(CalledProcessError, FileNotFoundError, OSError)` 覆盖 git 缺失 / cwd 错误 / 权限错，比 design 单 CalledProcessError 更全面（合理强化） |
| F-11 | complexity | minor | not_proven | drop | 嵌套深度 5/4 越线但仅 +1 层，影响极小；与 F-1 同根，F-1 follow-up 已覆盖 |
| F-12 | auxiliary-spec | minor | rejected | drop | `(blank line)` 是 git log 输出格式示例的字面描述，不是自然语言注释 |
| F-13 | auxiliary-spec | minor | not_proven | drop | 字段注释列对齐属风格偏好，无 spec 硬约束 |
| F-14 | auxiliary-spec | minor | rejected | drop | tests 内 `import os` 是 fixture 内惯例，非缺陷 |
| F-15 | auxiliary-spec | minor | not_proven | drop | `source` 字段注释已含 git_log/fs_mtime 二选一语义，信息量足 |
| F-16 | complexity | minor | not_proven | drop | test 文件 463 行未越 500，测试文件惯例放宽 |
| F-17 | complexity | minor | not_proven | drop | fixture 72 行属测试设施，复杂度阈值不适用 |

## Critic 对抗验证统计

| verdict | 数量 | F-ID |
|---|---|---|
| rejected | 7 | F-2 / F-7 / F-8 / F-9 / F-10 / F-12 / F-14 |
| not_proven | 8 | F-1 / F-5 / F-6 / F-11 / F-13 / F-15 / F-16 / F-17 |
| not_rebutted | 2 | F-3 / F-4 |

## 跨维度洞察

1. **concurrency × error_handling 叠加**：subprocess 无 timeout + 失败路径仅追加 warning，仓库异常（git 卡死/损坏）会先阻塞再静默 fallback，用户难定位 — 加 timeout 后建议同时 stderr 输出告警
2. **design_consistency × error_handling 一致性缺口**：design L763 异常表只定义 subprocess 失败路径，但 since 窗口内无 commit 也走 fs_mtime — 补 design 或补 warning 二选一
3. **complexity follow-up**：fetch_git_timestamps 138 行 / 圈复杂度 17 超 80/10 硬约束 — 下个 feature 抽 `_parse_git_log_output` / `_build_timestamp_result` 两个 helper

## 人工确认提示

本次评审输出 **conclusion = needs_attention**（机器评估结论，不代表合并通过）。

- 默认建议：合并前补 F-4（subprocess timeout）后重审 → 期望 looks_clean_after_fix
- 风险接受路径：如认为大仓库阻塞场景不在本工具运维边界（context/** 规模小，git 不会卡死），可在主对话**显式接受风险**（如"接受当前 needs_attention 风险，转 done"），feature-lifecycle-manager 才会把 F-008 转 done
- 软门禁由本主对话承担；GATE-REVIEW-VERDICT 在 phase-transition / submit 时 needs_attention 只触发 warning（strict 模式才升 fail），blocked 才 hard-block

未获用户软确认 → feature-lifecycle-manager 不会把 F-008 转 done。
