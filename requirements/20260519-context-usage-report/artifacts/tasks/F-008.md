---
schema_version: "1.0"
feature_id: F-008
title: GitTimestamp 批量获取 + fs_mtime 回退
status: done
complexity: light
depends_on: []
touches: ["scripts/lib/context_usage_report.py"]
created_at: 2026-05-19T08:41:35Z
updated_at: 2026-05-20T03:15:00Z
review_report: artifacts/review-F-008-20260520.md
---

# F-008 · GitTimestamp 批量获取 + fs_mtime 回退

## 需求描述

实现 git log 子进程批量获取：subprocess git log --name-only --since=<N>d --pretty=%H|%cI，缓存 path → GitTimestamp(first/last/source)；失败回退 fs_mtime，warnings 标记。

## 接口/数据结构

见 detailed-design.md §接口签名 / §数据结构（interfaces_frozen=True）

## 依赖

### 依赖的 feature

无

### 依赖的模块

无（本项无独立的模块级依赖；feature 间依赖见上）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/context_usage_report.py`

## 开发笔记

（开发过程中随手记）


## 验收标准

- GitTimestamp 含 first_commit_at / last_commit_at / source 完整字段，时区统一 UTC
- git log 失败（shallow clone 模拟、subprocess returncode != 0）→ source=fs_mtime，first_commit_at=None
- fixture: 真实 git 仓库下能解析 git log 输出 path → ts 映射
- warnings 在 fallback 时追加一条全局提示

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 单元测试覆盖主流程
- [ ] 边界条件处理
- [ ] 无明显安全/并发问题
- [ ] `/code-review` 通过
