---
schema_version: "1.0"
feature_id: F-003
title: tests/lib/test_markdown_links.py 4 类用例覆盖
status: done
complexity: light
depends_on: ["F-001"]
touches: ["tests/lib/test_markdown_links.py"]
created_at: 2026-05-19T08:41:35Z
updated_at: 2026-05-19T09:25:00Z
review_report: requirements/20260519-context-usage-report/artifacts/review-F-003-20260519.md
---

# F-003 · tests/lib/test_markdown_links.py 4 类用例覆盖

## 需求描述

为 markdown_links.py 增加 4 类 pytest 用例：LINK_RE 匹配 / slug 规则 / glob ** 扩展 / 外链与 intra-anchor 跳过。覆盖 mask_code_blocks 的 fenced / inline code 边界。

## 接口/数据结构

见 detailed-design.md §接口签名 / §数据结构（interfaces_frozen=True）

## 依赖

### 依赖的 feature

F-001

### 依赖的模块

无（本项无独立的模块级依赖；feature 间依赖见上）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `tests/lib/test_markdown_links.py`

## 开发笔记

（开发过程中随手记）


## 验收标准

- test_markdown_links.py 至少 12 个 test case，覆盖 LINK_RE / extract_links / resolve_link / slugify / extract_headings / glob_match / mask_code_blocks 7 个公开 API
- code block mask 至少 3 个用例：fenced ```、inline `code`、跨行 fenced
- pytest tests/lib/test_markdown_links.py 全绿

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 单元测试覆盖主流程
- [ ] 边界条件处理
- [ ] 无明显安全/并发问题
- [ ] `/code-review` 通过
