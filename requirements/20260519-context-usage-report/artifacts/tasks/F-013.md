---
schema_version: "1.0"
feature_id: F-013
title: Fixture 仓库 + 回归测试 + CI <5s 性能守门
status: done
complexity: light
depends_on: ["F-011", "F-012"]
touches:
  - "tests/fixtures/context_usage_report/"
  - "tests/lib/test_context_usage_report.py"
  - "tests/lib/test_context_usage_inventory.py"
  - "tests/lib/test_context_usage_git_timestamps.py"
  - "tests/lib/test_context_usage_aggregator.py"
  - "tests/lib/test_context_usage_renderer.py"
  - "tests/lib/test_context_usage_e2e.py"
  - "tests/lib/conftest.py"
  - "tests/lib/_context_usage_helpers.py"
  - "tests/lib/test_markdown_links.py"
  - "pyproject.toml"
created_at: 2026-05-19T08:41:35Z
updated_at: 2026-05-20T16:25:00Z
review_report: "artifacts/review-F-013-20260520.md"
---

# F-013 · Fixture 仓库 + 回归测试 + CI <5s 性能守门

## 需求描述

构建 tests/fixtures/context_usage_report/ 小型仿真仓库（含 context + requirements + INDEX 断链 + 孤岛 + 各种引用形式样本）；写端到端回归测试；CI 加 1000 文件 fixture 性能回归（time 命令守门 < 5s）。

## 接口/数据结构

见 detailed-design.md §接口签名 / §数据结构（interfaces_frozen=True）

## 依赖

### 依赖的 feature

F-011, F-012

### 依赖的模块

无（本项无独立的模块级依赖；feature 间依赖见上）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `tests/fixtures/context_usage_report/`
- `tests/lib/test_context_usage_report.py`

## 开发笔记

（开发过程中随手记）


## 验收标准

- fixture 仓库结构：≥ 10 个 context 文件覆盖 team/project 两种 kind + ≥ 5 个 requirements 文件 + 至少 1 个 INDEX 断链 + 至少 1 个孤岛 + 4 种引用形式样本
- 端到端测试运行 context_usage_report.main()，断言输出 JSON 各 status 计数、broken_links 数、orphans 数
- 1000 文件性能 fixture 在 CI 运行 < 5s（用 time.perf_counter 守门，超时 pytest.fail）
- 在仓库当前真实数据上跑一次，确认不修改任何非报告文件（git status 仅 reports/* untracked）

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 单元测试覆盖主流程
- [ ] 边界条件处理
- [ ] 无明显安全/并发问题
- [ ] `/code-review` 通过
