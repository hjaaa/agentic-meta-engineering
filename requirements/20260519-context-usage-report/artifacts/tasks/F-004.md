---
schema_version: "1.0"
feature_id: F-004
title: ContextInventory 组件实现
status: done
complexity: light
depends_on: ["F-001"]
touches: ["scripts/lib/context_usage_report.py", "tests/lib/test_context_usage_report.py"]
created_at: 2026-05-19T08:41:35Z
updated_at: 2026-05-19T14:13:33Z
review_report: requirements/20260519-context-usage-report/artifacts/review-F-004-20260519.md
---

# F-004 · ContextInventory 组件实现

## 需求描述

实现 ContextInventory 类，scan() 方法：rglob context/**/*.md + index-config.yaml ignore 过滤 + 剔除 INDEX.md，输出 list[KnowledgeFile]。

## 接口/数据结构

见 detailed-design.md §接口签名 / §数据结构（interfaces_frozen=True）

## 依赖

### 依赖的 feature

F-001

### 依赖的模块

无（本项无独立的模块级依赖；feature 间依赖见上）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/context_usage_report.py`

## 开发笔记

（开发过程中随手记）


## 验收标准

- ContextInventory.scan() 返回 list[KnowledgeFile]，每条带 path / rel_path / kind / size_bytes / fs_mtime 完整字段
- kind 字段正确区分 team / project（rel_path 第二段判定）
- ignore_patterns 生效（fixture: 给一个 ignore=`**/draft/**`，确认 draft 目录文件被排除）
- INDEX.md 自身被排除（rglob 不返回）
- context_dir 不存在时抛 FileNotFoundError

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 单元测试覆盖主流程
- [ ] 边界条件处理
- [ ] 无明显安全/并发问题
- [ ] `/code-review` 通过
