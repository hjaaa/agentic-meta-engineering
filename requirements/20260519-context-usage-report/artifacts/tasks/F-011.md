---
schema_version: "1.0"
feature_id: F-011
title: CLI 入口 + argparse 参数 + 退出码 + .gitignore
status: pending
complexity: light
depends_on: ["F-010"]
touches: ["scripts/lib/context_usage_report.py", ".gitignore"]
created_at: 2026-05-19T08:41:35Z
updated_at: 2026-05-19T08:41:35Z
review_report: null
---

# F-011 · CLI 入口 + argparse 参数 + 退出码 + .gitignore

## 需求描述

实现 main()：argparse 11 个参数（--context-dir / --requirements-dir / --output / --json-output / --since / --project / --only-experience / --format / --fail-on-broken-index / --fail-on-orphan / --repo-root），调度 6 个组件 + Renderer；按 detailed-design §异常处理表实现 6 档退出码。同步追加 reports/ 到 .gitignore。

## 接口/数据结构

见 detailed-design.md §接口签名 / §数据结构（interfaces_frozen=True）

## 依赖

### 依赖的 feature

F-010

### 依赖的模块

无（本项无独立的模块级依赖；feature 间依赖见上）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/context_usage_report.py`
- `.gitignore`

## 开发笔记

（开发过程中随手记）


## 验收标准

- python3 scripts/lib/context_usage_report.py 默认参数能跑通仓库当前数据，写出 reports/context-usage.md + .json
- 11 个 CLI 参数 argparse 解析正确，--help 输出可读
- 退出码 0/1/2/3/4/5 各有 fixture 触发用例
- .gitignore 末尾追加 `reports/` 一行，commit 说明「需求决策表豁免」
- stdout 摘要正常打印（总数 / 各状态计数 / warnings 数）
- stderr 警告行格式 `WARN <msg>` / `ERROR <msg>`

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 单元测试覆盖主流程
- [ ] 边界条件处理
- [ ] 无明显安全/并发问题
- [ ] `/code-review` 通过
