---
schema_version: "1.0"
feature_id: F-006
title: EvidenceScanner 4 种引用形式扫描
status: done
complexity: medium
depends_on: ["F-001"]
touches: ["scripts/lib/context_usage_report.py"]
created_at: 2026-05-19T08:41:35Z
updated_at: 2026-05-20T01:45:00Z
review_report: artifacts/review-F-006-20260520.md
---

# F-006 · EvidenceScanner 4 种引用形式扫描

## 需求描述

实现 EvidenceScanner.scan()：扫 requirements/** 下 .md / .json / .txt / .yaml，按后缀分发，识别 markdown_link / raw_path / source_marker / json_value 4 种引用形式，输出 list[ReferenceEvidence]；reviews/*.json 解析失败 fail-open warning。

## 接口/数据结构

见 detailed-design.md §接口签名 / §数据结构（interfaces_frozen=True）

## 依赖

### 依赖的 feature

F-001

### 依赖的模块

无（本项无独立的模块级依赖；feature 间依赖见上）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/context_usage_report.py`

## 开发笔记

（开发过程中随手记）


## 验收标准

- ReferenceEvidence 含 target / source / line / kind / context_line 完整字段
- 4 种引用形式 fixture 各覆盖至少 1 个用例
- JSON 解析失败 fail-open：warnings 列表追加一条，不抛异常
- context 路径必须在传入的 candidate set 内才记录（避免噪音）
- code block 内的伪引用不计入（mask_code_blocks 前置）

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 单元测试覆盖主流程
- [ ] 边界条件处理
- [ ] 无明显安全/并发问题
- [ ] `/code-review` 通过
