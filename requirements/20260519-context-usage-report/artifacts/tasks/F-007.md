---
schema_version: "1.0"
feature_id: F-007
title: AppliedSignalClassifier 复合窗口 + code block mask
status: done
complexity: medium
depends_on: ["F-006"]
touches: ["scripts/lib/context_usage_report.py"]
created_at: 2026-05-19T08:41:35Z
updated_at: 2026-05-20T02:30:00Z
review_report: artifacts/review-F-007-20260520.md
---

# F-007 · AppliedSignalClassifier 复合窗口 + code block mask

## 需求描述

实现 AppliedSignalClassifier.classify()：对每条 ReferenceEvidence 判定（A）复合上下文窗口（同 ## 二级小节 ∧ 前 5 行 + 后 10 行）命中 APPLIED_KEYWORDS，（B）显式升级声明命中 UPGRADE_PHRASES；保守优先。

## 接口/数据结构

见 detailed-design.md §接口签名 / §数据结构（interfaces_frozen=True）

## 依赖

### 依赖的 feature

F-006

### 依赖的模块

无（本项无独立的模块级依赖；feature 间依赖见上）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/context_usage_report.py`

## 开发笔记

（开发过程中随手记）


## 验收标准

- AppliedEvidence 含 reference / rule / matched_keyword / section_heading 完整字段
- 复合窗口边界正确：fixture: 关键字在前 5/后 10 行外 → 不命中；在 ## 不同小节 → 不命中
- fenced code 内关键字不命中（mask_code_blocks 生效）
- 显式升级声明任一短语命中即归类为 applied
- 保守优先：fixture: 关键字在窗口边界外但同段落 → 不命中（不可宽容判定）

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 单元测试覆盖主流程
- [ ] 边界条件处理
- [ ] 无明显安全/并发问题
- [ ] `/code-review` 通过
