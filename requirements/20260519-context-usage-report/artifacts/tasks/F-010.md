---
schema_version: "1.0"
feature_id: F-010
title: ReportRenderer Markdown 4 章节 + JSON 渲染
status: done
complexity: light
depends_on: ["F-009"]
touches: ["scripts/lib/context_usage_report.py"]
created_at: 2026-05-19T08:41:35Z
updated_at: 2026-05-20T13:30:00Z
review_report: artifacts/review-F-010-20260520.md
---

# F-010 · ReportRenderer Markdown 4 章节 + JSON 渲染

## 需求描述

实现 ReportRenderer.render_markdown() 与 render_json()：Markdown 输出固定 4 章节（## 总览 / ## 高价值知识 / ## 待治理知识 / ## 引用明细）；JSON 顶层结构与 detailed-design §数据结构 / JSON 顶层结构一致。write() 原子化（临时文件 + os.replace）。

## 接口/数据结构

见 detailed-design.md §接口签名 / §数据结构（interfaces_frozen=True）

## 依赖

### 依赖的 feature

F-009

### 依赖的模块

无（本项无独立的模块级依赖；feature 间依赖见上）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/context_usage_report.py`

## 开发笔记

（开发过程中随手记）


## 验收标准

- Markdown 报告包含 4 个 H2 标题，章节顺序固定
- JSON 报告 schema 含 generated_at / tool_version / schema_version / config / summary / files / warnings 全部字段
- datetime 字段 ISO-8601 UTC 格式（2026-05-19T08:30:00Z）
- Enum 字段序列化为 .value 字符串
- write() 写入时使用临时文件 + os.replace，Ctrl-C 中断不污染目标文件
- reports/ 目录不存在时自动 mkdir -p

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 单元测试覆盖主流程
- [ ] 边界条件处理
- [ ] 无明显安全/并发问题
- [ ] `/code-review` 通过
