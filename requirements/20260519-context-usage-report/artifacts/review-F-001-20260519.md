# F-001 代码审查报告

## 综合裁决

**conclusion: looks_clean**

8 个公开 API + `MarkdownLink` NamedTuple 全部齐备，签名与 detailed-design 一致；行为与 `check_index.py:31-200` 源头等价或合理扩展；纯函数、无 IO 副作用；docstring 完整，对设计与实际行为的细微差异（如 `slugify` 规则描述）主动标注。无阻断问题，2 条非阻断建议。

## 审查范围

- commit: `440f1e9` feat(F-001): 抽取 markdown_links.py 公共模块
- files: `scripts/lib/markdown_links.py`
- LOC: 294
- 审查标准：F-001 task.md 验收标准 4 条 + detailed-design.md §接口签名 / `scripts/lib/markdown_links.py` 章节 + 与 `check_index.py:31-200` 行为等价性

## 验收标准对照

### task.md §验收标准（4 条）

| # | 标准 | 结论 | 备注 |
|---|---|---|---|
| 1 | 新模块创建，含 8 个公开函数 / NamedTuple，签名与 detailed-design §接口签名 一致 | ✓ | `LINK_RE`, `HEADING_RE`, `MarkdownLink`, `extract_links`, `resolve_link`, `is_external_or_intra_anchor`, `slugify`, `extract_headings`, `glob_match`, `mask_code_blocks` 共 2 常量 + 1 NamedTuple + 7 函数，超出"8 个"覆盖面 |
| 2 | 纯函数，无 print / 文件 IO 写副作用 | ✓ | 全文件 grep 无 `print` / `open(..., 'w')` / `write_text` / `os.makedirs`；不读文件（输入皆为 str / Path 参数） |
| 3 | `MarkdownLink` NamedTuple 字段 text / url / line 完整 | ✓ | `:26-37`，含完整 docstring |
| 4 | `mask_code_blocks` 同长度保偏移，可被任意调用方安全前置 | ✓ | `:55-91` 实现；`:89-90` 末尾换行补偿保证 `len(result) == len(md_text)` 严格成立 |

### detailed-design §接口签名（10 个 API 逐条核对）

| API | 签名/语义匹配 | 结论 | 备注 |
|---|---|---|---|
| `LINK_RE` | `(?<!!)\[([^\]]*)\]\(([^)]+)\)` | ✓ | `:15` 与 design / check_index.py:31 字节级一致 |
| `HEADING_RE` | `^(#+)\s+(.+?)\s*$` | ✓ | `:17` 与 design / check_index.py:33 字节级一致 |
| `MarkdownLink` | NamedTuple(text, url, line) | ✓ | `:26-37` 字段顺序、类型、docstring 完整 |
| `extract_links` | 返回 list[MarkdownLink] 按行升序，mask code blocks | ✓ | `:94-124` 调用 mask_code_blocks 前置；空文本 → splitlines() → 空 list |
| `resolve_link` | (url, source_file, repo_root) → Path \| None；越界 None；不抛 | ✓ | `:153-201` 越界检查用 `resolve()` 展开 symlink 防 macOS `/var → /private/var`；`try/except Exception` 兜底 |
| `is_external_or_intra_anchor` | http/https/mailto/ftp/`#`/空 → True | ✓ | `:127-150` 含 `tel:` / `tel://`；空串显式 True；是 check_index.py:113 的超集 |
| `slugify` | check_index.py:78 行为等价 | ✓ | `:204-226` 实现与 check_index.py:78-83 完全等价；docstring 主动标注"与 detailed-design 描述差异（中文/下划线保留）" |
| `extract_headings` | list[(level, text, slug)] 按出现顺序 | ✓ | `:229-250` 三元组扩展自 check_index 的 set[slug]，符合设计 |
| `glob_match` | `**` 任意层级 + fnmatch | ✓ | `:253-294` 与 check_index.py:49-75 `_fnmatch_glob` 实现等价 |
| `mask_code_blocks` | fenced + inline code 同长度替换为空白 | ✓ | `:40-91` fenced 用 lstrip 后判断（容差略宽于 GFM 严格 ≤3 空格但更保守）；未闭合 fence 到文件末尾仍 mask（保守） |

## Finding

无阻断 / 重大问题。

## 建议（非阻断 follow-up）

- **F-001-S1 [minor · maintainability]** `extract_links:113-122` 对同一位置做了双重正则匹配（masked 行 finditer + 原始行 search 同 start 位置）。由于 `mask_code_blocks` 用等长空格替换，masked 区域内不会出现 `[](`，因此 masked 的 `m.group(1)/group(2)` 就是原始文本。当前双重匹配是防御性设计，逻辑正确但稍冗余。**建议**：F-002 替换 check_index.py 调用方时可一并简化为"masked 上 finditer，直接取 group(1)/group(2)"。不影响正确性，不阻断。

- **F-001-S2 [minor · testability]** task.md "完成自检 → 单元测试覆盖主流程" 尚未勾选；F-001 本身是抽取重构（行为等价），未自带 unit test。**建议**：在 F-002（替换 check_index.py 调用方）之前补一组 `tests/test_markdown_links.py`，至少覆盖：mask_code_blocks 同长度保偏移 / fenced 嵌套 inline / extract_links 空文本 / resolve_link 越界 / slugify 中文 / glob_match `**`。这能为 F-002 的"替换前后 check-index.sh 输出 diff 为空"验收提供前置保障。不阻断 F-001 关闭。

## 跨维度洞察

- 设计文档 §slugify 的字面描述（"非字母数字替换为 -, 折叠 -, trim"）与 `check_index.py:78` 实际行为（`[^\w一-龥\s-]` 删除而非替换，无折叠步骤）不一致。实现以源代码为准并在 docstring 显式标注，是负责任的做法。**建议在 F-008 文档同步阶段更新 detailed-design.md §slugify 描述**，避免后续 F-003 实现 `AppliedSignalClassifier` 时按文档误解。

CONCLUSION: looks_clean
