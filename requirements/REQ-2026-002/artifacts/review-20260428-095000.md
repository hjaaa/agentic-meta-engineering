# 代码审查报告 · REQ-2026-002 · 20260428-095000（round-2）

## 结论

**needs_revision** · 0 critical / 4 major / 9 minor （score=78，supersedes round-1）

scope = `feature_id: F-002`，base=3a0d2a0 head=9931fb5。Round-1 全 28 finding 已 FIXED 或登记 follow-up（design-consistency / history-context 二审 0 finding 印证）。Round-2 新发现以 `reviews_consistency.py` 实现细节 + traceability.run 存量复杂度为主。

## must_fix（本 PR 必修）

### F-r2-01 [security] reviews_consistency fix_hint 文案与 registry 矛盾 — major

`scripts/gates/plugins/reviews_consistency.py:100` 的 `fix_hint` 写「紧急绕过：git commit --no-verify（CI 仍会拦截）」，但 `registry.yaml:191-192` GATE-REVIEWS-CONSISTENCY.triggers 仅 `[pre-commit]`——CI 路径（`--trigger=ci`）不运行该 gate。承诺与实现矛盾。

**修复二选一**：
- (a) registry triggers 追加 `ci` 与 `submit`（彻底加防线，须同时修 F-r2-02 否则 CI 路径数据源不可用）
- (b) 修正 fix_hint 文案，删除"CI 仍会拦截"
- 推荐 (a)

### F-r2-02 [security/concurrency/perf/error-handling 同根因] data-source 分裂 + 静默吞默 — major

`reviews_consistency.py`：
- `:53-61` precheck 用 `ctx.changed_files`（GATE_CHANGED_FILES env）
- `:74` run() 重新调 `_get_staged_files()`（git diff --cached）
- `:146-147` `_get_staged_files()` OSError → `return []` 静默
- `:162-163` `_has_reviews_diff()` returncode 未检查

三处叠加：直接 `python3 run.py --trigger=pre-commit` / git 命令失败 / CI 路径（若 F-r2-01 修法 a）→ 校验返回假 PASS。

**修复**：
1. `run()` 改 `staged = ctx.changed_files or _get_staged_files()` 复用 precheck 数据
2. `_get_staged_files / _has_reviews_diff` 失败时返回 None（或抛异常）→ run() 转 FAIL+WARNING
3. `_has_reviews_diff` 检查 returncode != 0 时保守返回 True 触发后续校验

### F-r2-03 [complexity] traceability.run 双超阈值 — major

`scripts/gates/plugins/traceability.py:51-130` run() 逻辑行≈80 / CC≈11，超阈值（≤60 / ≤10）。round-1 F-16 fix sketch 原文已点名 run() 但实际只拆了 _resolve_xxx——属漏拆债。

**修复**：拆 `_check_features_file(req_dir) -> Optional[list[dict]] | Report` + `_check_design_traceability(req_dir, done_features) -> Report`；run() 只串调，目标 CC ≤ 4 / 行 ≤ 15。

### F-r2-06 [error-handling] 与 F-r2-02 同根因（合并修） — minor → 升级共修

`reviews_consistency.py:155-168` `_has_reviews_diff` returncode 未检查。与 F-r2-02 子点 3 同根因。

## 建议同 PR 顺手（minor）

- **F-r2-04** [aux-spec] `protect_branch.py:37-55` precheck/run 缺方法级三引号 docstring（其他 5 plugin 已按 round-1 F-25 标准补齐）
- **F-r2-05** [aux-spec] `protect_branch.py:112` `_is_reviews_json` except 分支 `import sys` 写在函数体（违反顶层 import 风格）

## 登记 F-003 plan 段（不本 PR 修）

- **F-r2-07** [concurrency] `run.py:523-527` write_audit 'w' 模式 + 微秒文件名，并发 CI matrix 同微秒碰撞会覆写（当前部署不触发）
- **F-r2-08** [performance] `run.py write_audit` fsync 当前不阻塞，F-003 audit 写循环引入时会成 major
- **F-r2-10** [security] `protect_branch.py:26` WRITE_TOOLS 不含 Bash，bash echo > reviews/fake.json 可绕（F-003 bash_write_protect 范围已声明）
- **F-r2-11** [security/error-handling] `run.py:436-440` traceback CI 抑制后 audit log 也无堆栈（_build_audit 无 traceback 字段，TODO F-003）

## 跨维度模式

- **F-r2-01 + F-r2-02 + F-r2-06 同模式**：round-1 F-02 D7-2 复活只完成一半——pre-commit 路径生效，但 CI 路径（registry triggers + run() 数据源 + subprocess 错误处理）未对齐。承诺"--no-verify 后 CI 仍会拦截"在物理上不成立。
- **F-r2-03 + F-r2-04 + F-r2-05 同模式**：round-1 F-16 / F-25 / F-23 fix sketch 已写但落地不全。本 PR 顺手补完避免后续碎片化补丁。
- **整体**：round-1 28 条已闭环，本次主要新增 reviews_consistency.py（172 行新 plugin）自身引入 4 条客观可证的实现缺陷——属新 plugin 首次落地的典型质量问题。

## 二审元信息

- 审查范围：embedded · feature_id=F-002 · round=2 · 28 文件 +713/-137 = 850 行
- 并行 checker：8（design-consistency 0 / security 4 / concurrency 2 / complexity 1 / error-handling 2 / aux-spec 2 / performance 3 / history-context 0）
- Critic 统计：rejected=0 / not_proven=3 / not_rebutted=8
- review json：requirements/REQ-2026-002/reviews/code-F-002-002.json
- supersedes：REV-REQ-2026-002-code-F-002-001
- score：78 / 100
