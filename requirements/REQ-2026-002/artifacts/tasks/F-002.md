---
feature_id: F-002
title: 全量 7 个 check 改造为 plugin + pre-commit / post-dev / CI 切到 runner
status: done
complexity: medium
depends_on: [F-001]
created_at: "2026-04-27 21:45:00"
updated_at: "2026-04-28 10:05:00"
review_report: requirements/REQ-2026-002/reviews/code-F-002-003.json
estimate_days: 9
---

# F-002 · 全量 7 个 check 改造为 plugin + pre-commit / post-dev / CI 切到 runner

## 需求描述

为剩余 5 个现存 check 实现 plugin（sourcing / review_verdict / plan_freshness / workspace_clean / traceability）；review_verdict 内部实现 R001~R007 全规则（**R005 仍保留原行为，事务化在 F-003 做**）；7 个旧 .sh 入口改写为薄壳直接 `exec` runner（兼容期）；改写 pre-commit / post-dev-verify.sh / quality-check.yml 切到 run.py。snapshot 行为契约对每个 adapter 抓基线。

## 接口/数据结构

详见 `requirements/REQ-2026-002/artifacts/detailed-design.md`：
- §3.4 其他 plugin 表（sourcing / review_verdict / plan_freshness / workspace_clean / traceability / protect_branch）
- §4.4 ci 时序（替代 quality-check.yml 5 step 为单一 step）
- §5.2 capture-baseline.sh

## 依赖

### 依赖的 feature

- F-001：runner 骨架就位、Gate 基类稳定

### 依赖的模块

- `scripts/lib/check_*.py`（adapter 复用）
- `scripts/lib/common.py`
- `.claude/skills/traceability-gate-checker/SKILL.md`（traceability plugin 调用入口）

## 验收

详见 `features.json` F-002.acceptance：
1. 7 个 adapter 各自的 snapshot 行为契约 diff 为空
2. CI workflow 简化为单一 step：`python scripts/gates/run.py --trigger=ci --strict`
3. pre-commit 切到 runner 后所有既有需求 commit 行为不变
4. post-dev-verify.sh 切到 runner 后退出码与 stderr 关键前缀行与基线一致

## 改动文件清单

- 新增：
  - `scripts/gates/plugins/sourcing.py`
  - `scripts/gates/plugins/review_verdict.py`（含 R001~R007，R005 暂保原行为）
  - `scripts/gates/plugins/plan_freshness.py`
  - `scripts/gates/plugins/workspace_clean.py`
  - `scripts/gates/plugins/traceability.py`
  - `scripts/gates/plugins/protect_branch.py`
  - `scripts/gates/migration/capture-baseline.sh`
  - `tests/gates/test_*_plugin.py`（5 个）
  - `tests/gates/baselines/`（snapshot 落盘）

- 修改：
  - `scripts/check-meta.sh` / `check-index.sh` / `check-sourcing.sh` / `check-reviews.sh` / `check-plan.sh`：改为薄壳
  - `scripts/post-dev-verify.sh`：改为薄壳
  - `scripts/git-hooks/pre-commit`：切到 runner
  - `.github/workflows/quality-check.yml`：5 step → 1 step
  - `scripts/gates/registry.yaml`：补全所有 gate 条目
