---
feature_id: F-004
title: gate-checklist.md 渲染产物化 + 旧入口删除 + F-003 carry-over
status: done
complexity: medium
depends_on: [F-001, F-002, F-003]
created_at: "2026-04-27 21:45:00"
updated_at: "2026-04-29 13:23:02"
estimate_days: 3.5
---

# F-004 · gate-checklist.md 渲染产物化 + 旧入口删除 + F-003 carry-over

## 需求描述

四块互不交叉的工作（按风险从低到高执行，每块独立 commit）：

### A. 渲染产物 + CI 强校验（核心，原 F-004 范围）
实现 `migration/render-docs.py`：从 registry.yaml 渲染 `gate-checklist.md`，文件头自动加 `<!-- generated, do not edit -->` 标记。CI 加 `render --check` step（D-007 强校验，diff 非空即 PR 失败）。

### B. 旧入口删除（adapter 已稳定即可删）
删除 7 个旧 check-*.sh 薄壳 + scripts/post-dev-verify.sh + scripts/lib/check_*.py（adapter 已复用其逻辑，仅留 save_review.py）；更新 README / onboarding 引用。

### C. submit trigger 完整化（F-003 推迟项）
- 新增 `scripts/gates/plugins/gh_auth.py`（GATE-GH-AUTH，submit trigger，验证 `gh auth status`）
- 新增 `scripts/gates/plugins/base_reachable.py`（GATE-BASE-REACHABLE，submit trigger，验证 base_branch 可达）
- 落地 `--force-with-blockers` escape_hatch CLI 解析（F-003 仅占位，实际未接通）
- 详见 detailed-design.md §4.2

### D. F-003 round-3 carry-over（spec drift + minor 修复）
- **N-001 / N-002 / N-003 / N-004**（review-20260428-203751.md major×3 + minor×1）：detailed-design.md 一次性 spec sync——
  - N-001 §3.3:370 `_caller_is_save_review_sh()` 调用补 `(ctx)` 参数
  - N-002 §3.3:392-404 删 comm fallback 死代码描述
  - N-003 §3.1 增「模块拆分说明」段，声明 review_verdict_ci.py / meta_writer.py
  - N-004 §3.3 RE_WRITE_OPS 注释 8 → 12 类同步
- **N-010**（minor）：scripts/gates/run.py:363 `_print_dry_run` 补中文 docstring
- **C-001**（plan.md F-026 carry-over）：meta_writer.py vs save_review.py 半边锁——评估 meta.yaml 全量写者持 `fcntl.lockf` 统一规范；如评估为不阻塞则只在 plan.md 落决策记录，不动代码
- **F-026 / F-038**（plan.md F-004 follow-up）：lazy stash / fsync 开关——本轮做基线测量，决定是否实施；如不实施则在 plan.md 记决策推迟到下一迭代

## 接口/数据结构

详见 `requirements/REQ-2026-002/artifacts/detailed-design.md` §5.3 render-docs.py。

## 依赖

### 依赖的 feature

- F-001 / F-002 / F-003 全部合入

### 依赖的模块

- 全部 plugin 已稳定（无新接口冻结需求）

## 验收

A 块（render）：
1. `make gates-render` 重新生成 `gate-checklist.md` 与仓内产物 diff 为空
2. CI `render --check` 在手工改 `gate-checklist.md` 后立即报错
3. gate-checklist.md 文件头有 `<!-- generated, do not edit -->` 标记

B 块（删旧入口）：
4. 删除旧入口后 `git grep -E 'scripts/(check-|post-dev-verify|lib/check_)' -- ':!CHANGELOG*' ':!requirements/'` 无残留引用
5. README / onboarding 引用更新到 `scripts/gates/`

C 块（submit trigger）：
6. `gh_auth.py` / `base_reachable.py` 单元测试覆盖（pass / fail / 异常）
7. registry.yaml 注册 GATE-GH-AUTH / GATE-BASE-REACHABLE，submit trigger 跑通
8. `--force-with-blockers` 解析后写 audit log + reason 必填校验

D 块（carry-over）：
9. detailed-design.md N-001~N-004 全部修复，detail-design 重审 stale=false 仍保持
10. `_print_dry_run` docstring 补完，新增 docstring 通过 auxiliary-spec checker
11. C-001 / F-026 / F-038 ：要么实施并补单测，要么在 plan.md 记 ADR 决策推迟到下一迭代

全局：
12. 所有现有测试 + 新单测 pass；`make gates-validate` S1~S10 全过
13. F-004 round-1 /code-review 0 critical / 0 major（接受 minor 进 carry-over）

## 改动文件清单

- 新增：
  - `scripts/gates/migration/render-docs.py`
  - `scripts/gates/plugins/gh_auth.py`
  - `scripts/gates/plugins/base_reachable.py`
  - `tests/test_gh_auth.py`
  - `tests/test_base_reachable.py`
  - （可能）`scripts/gates/migration/test_render_docs.py`

- 修改：
  - `.claude/skills/managing-requirement-lifecycle/reference/gate-checklist.md`：改为渲染产物，加 generated 标记
  - `.github/workflows/quality-check.yml`：加 `render --check` step
  - `Makefile`：加 `gates-render` 目标
  - `context/team/onboarding/agentic-engineer-guide.md`：引用更新
  - `README.md`：引用更新
  - `scripts/gates/registry.yaml`：补 escape_hatches 段最终配置 + 注册 GH-AUTH/BASE-REACHABLE
  - `scripts/gates/run.py`：`_print_dry_run` 补 docstring；落地 `--force-with-blockers` 解析（F-003 占位升级）
  - `requirements/REQ-2026-002/artifacts/detailed-design.md`：N-001~N-004 spec sync
  - `requirements/REQ-2026-002/plan.md`：C-001/F-026/F-038 决策记录（如推迟）

- 删除：
  - `scripts/check-meta.sh`
  - `scripts/check-index.sh`
  - `scripts/check-sourcing.sh`
  - `scripts/check-reviews.sh`
  - `scripts/check-plan.sh`
  - `scripts/post-dev-verify.sh`
  - `scripts/lib/check_meta.py`
  - `scripts/lib/check_index.py`
  - `scripts/lib/check_sourcing.py`
  - `scripts/lib/check_reviews.py`
  - `scripts/lib/check_plan.py`

## 执行顺序（强制）

1. **D 块先做 spec sync**（N-001~N-004，纯文档无回归风险）
2. **C 块新增 plugin**（gh_auth / base_reachable + escape_hatch CLI；新增不动旧）
3. **A 块 render-docs.py**（依赖 registry 已扩，必须 C 之后）
4. **B 块删旧入口**（最后，确保 A/C 都已稳定且 CI 双跑过）

> 严禁 B 早于 A——删旧入口前必须先证明 A 渲染等价。
