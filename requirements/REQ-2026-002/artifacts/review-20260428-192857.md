# 代码审查报告 · REQ-2026-002 · F-003 round-2 · 20260428-192857

## 结论

**needs_revision** · 0 critical / 10 major / 24 minor · score = 78

- 与 round-1（rejected, score=55, 2 critical + 13 major + 19 minor）对比：2 critical 全部闭环，32 keep findings 全部 fixed。本轮新增 issues 集中在 spec 同步 / 复杂度未拆完 / 基础设施 try 边界 / docstring 倒退 / 历史索引失效，无新增安全/事务/数据丢失承诺破坏 → 不构成 critical。
- Round-3 必修 13 条（required_fixes，详见下方）；其余 21 条按 plan.md F-004 carry-over 处理。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示 “8 个 checker 发现 → critic 反证 → Judge 裁决” 的三方透明度。Critic verdict 三档：rejected（明确反证）/ not_proven（理论存在但触发条件极端）/ not_rebutted（不足以反证保留）。

| F-id | checker / 原始 severity | critic verdict | 最终处置 | 裁决依据 |
|---|---|---|---|---|
| F-001 | design-consistency / major | not_rebutted | **keep major** | audit.py 已加 bypassed 字段，detailed-design.md §2.3 未同步，spec 与实现字段集偏离事实成立 |
| F-002 | design-consistency / minor | not_rebutted | **keep minor** | audit.exit_code 与 calc_exit_code 在 strict 下不一致；audit log 自身字段是描述性不阻断流程 |
| F-003 | design-consistency / minor | not_rebutted | **keep minor** | spec §3.3:319 仅 'w'，实现 _ALTS[5] 含 (?:w\|a\|wb\|ab) 四模式；文档同步问题 |
| F-004 | design-consistency / minor | not_rebutted | **keep minor** | spec §3.3:359 _caller_is_save_review_sh(self) 无 ctx；实现是 (self, ctx)，未同步 spec |
| F-005 | security / major | not_rebutted | **keep major** | _ALTS 8 类未覆盖 sponge/rsync/install/pathlib.write_text；F-003 H5 acceptance 至少 8 类但实际有遗漏形态 |
| F-006 | security / minor | not_rebutted | **keep minor** | bash_write_protect.py:141 cmd[:200] 写 audit log 含潜在凭据无脱敏 |
| F-007 | security+error-handling / minor | not_rebutted | **keep minor** | pr_state.py:166-171 透传 gh stderr 截断 200 字到 stderr；截断不等于脱敏（SEC-003 ≡ EH-004 同根因合并） |
| F-008 | security / minor | not_rebutted | **keep minor** | BYPASS_REASON 全文写 audit；reason 用户自由填写无脱敏 |
| F-009 | concurrency / major | rejected | **judge override → keep minor** | critic 引 D-009 串行 + save-review.sh 单一入口反证；judge 自查发现 save_review.py CLI 写 meta.yaml 路径仍无锁，race window 在串行约束下窄但客观存在；本 PR 范围外（F-002 已合 develop）登记 F-004 跟进，降级保留 |
| F-010 | concurrency / minor | not_rebutted | **keep minor** | state_io.restore_state 直接 shutil.copy2 无锁，串行约束下窗口窄事实成立 |
| F-011 | concurrency / minor | not_rebutted | **keep minor** | _PR_STATE_CACHE 模块级 None 永久缓存无 TTL；submit 单进程影响小 |
| F-012 | concurrency / minor | not_rebutted | **keep minor** | save-review.sh exec 后 comm fallback 死代码，SAVE_REVIEW_PID env 是唯一通道 |
| F-013 | complexity / critical | not_proven | **downgrade critical → major** | _validate_one_entry CC=28 是事实但函数体顺序 S2-S10 校验链每段独立 raise 无嵌套；critical 应保留给安全/事务/数据丢失承诺破坏（参考 round-1 H1/H5）。可维护性归 major 更准确 |
| F-014 | complexity / major | not_rebutted | **keep major** | _validate_registry_schema CC=18 超阈值 10 |
| F-015 | complexity / major | not_rebutted | **keep major** | review_verdict.py 397 行三套独立职责（Gate 类 + ci 扫描 + commit_staged_writes 锁） |
| F-016 | complexity / major | not_rebutted | **keep major** | _fetch_pr_state 50 行接近上限，4 类异常重复三步模式 |
| F-017 | complexity / minor | not_rebutted | **keep minor** | bash_write_protect.run() 49 行接近 60 上限 |
| F-018 | complexity / minor | not_rebutted | **keep minor** | test_phase_transition_gates_are_subset_of_submit 单测 4 个 assert 多场景 |
| F-019 | error-handling / major | not_rebutted | **keep major** | run.py:354 write_audit 在 try 外，OSError 冒泡阻断 Claude；基础设施健壮性 |
| F-020 | error-handling / major | not_rebutted | **keep major** | run.py:339 _stash_state 在 try 外，PermissionError 泄露 traceback 触发 exit 2 |
| F-021 | error-handling / minor | not_rebutted | **keep minor** | traceback.print_exc 在非 CI 暴露绝对路径 |
| F-022 | auxiliary-spec / major | not_rebutted | **keep major** | run.py:109/285 parse_args/main 全无 docstring；F-002 round-3 已建立同 PR 风格一致预期，本 PR 漏掉是规范倒退 |
| F-023 | auxiliary-spec / minor | not_rebutted | **keep minor** | run.py:106 # CLI 段头英文分隔线无中文 |
| F-024 | auxiliary-spec / minor | not_rebutted | **keep minor** | state_io.py:43 'ERROR restore_state failed' 英文运行时日志 |
| F-025 | auxiliary-spec / minor | not_rebutted | **keep minor** | run.py:418 'ERROR gate-rollback-failed' 英文 log |
| F-026 | auxiliary-spec / minor | not_rebutted | **keep minor** | _ALTS 第 2/8 条注释短，与第 3-7 条详细程度不一致 |
| F-027 | auxiliary-spec / minor | not_rebutted | **keep minor** | 4 处测试函数缺 should_/given_ 前缀 |
| F-028 | auxiliary-spec / minor | not_rebutted | **keep minor** | audit.py:77 write_audit docstring 仅 WHAT |
| F-029 | performance / major | not_proven | **downgrade major → minor** | 实测 5000 个 'open(' payload 393ms（O(n^2) 级非 exponential），1k 量级 ≤17ms；真实 Bash 命令 < 1k 字符且调用方是 Claude 本身，构造 ReDoS 需恶意输入不在威胁模型 |
| F-030 | performance / minor | not_rebutted | **keep minor** | audit.py:86 fsync 同步落盘 pre-tool-use 高频路径，已登记 F-038 carry-over |
| F-031 | performance / minor | not_rebutted | **keep minor** | registry.yaml 每次 pre-tool-use 重解析 ~8ms 无跨进程缓存 |
| F-032 | performance / minor | not_rebutted | **keep minor** | _r002 ci 循环内 N+1 schema.yaml 重读 |
| F-033 | history-context / major | not_rebutted | **keep major** | plan.md:179/188 F-026/F-038 行号在 83099b2 重构后失效，run.py 仅 465 行；needs_stash 在 run.py:338，fsync 在 audit.py:86 |
| F-034 | history-context / minor | not_rebutted | **keep minor** | audit.py:25 SCHEMA_VERSION='1.0' 自 round-1 未变，新增 bypassed 字段属向后兼容扩展但未递增小版本号 |

裁决统计：critic verdict — rejected=1 / not_proven=2 / not_rebutted=31 ；judge 处置 — drop=0 / keep=33 / downgrade=2（其中 1 条覆盖 critic rejected→keep minor，2 条采纳 critic not_proven 降级）。

## Round-1 keep findings 修复复核

Round-1 共 32 条 keep findings 转 round-2 修复。本轮 round-2 对应 commit 全部闭环：

| Round-2 commit | 修复的 round-1 finding ID |
|---|---|
| 1de0fe4 fix(F-003): round-2 修复 pre-tool-use 高频路径阻断协议+冷启动 | F-001（critical, H5 exit 协议） · F-017（perf, 三次 python3 -c 解析） · F-018（perf, load_registry 全量 import） · F-032（jsondecode 兜底） |
| 1dfcc86 fix(F-003): round-2 加固 H1 事务化三层 | F-002（critical, H1 事务化失守） · F-010（commit_staged_writes 加锁） · F-033（CI 异常 audit log） |
| 88747d8 fix(F-003): round-2 加固双轨白名单+bash_write_protect 健壮性 | F-005 · F-006 · F-007 · F-008 · F-024 · F-028 · F-030 · F-031 · F-034 · F-035 · F-036 · F-037（共 12 条 security + error-handling + auxiliary-spec） |
| e568353 fix(F-003): round-2 加固 H4 pr_state 错误处理 | F-016 · F-022 · F-025 · F-029（共 4 条） |
| edec19a fix(F-003): round-2 补 H3 submit/registry/spec 一致性 | F-003 · F-004 · F-019 · F-020 · F-023（共 5 条） |
| 83099b2 refactor(F-003): round-2 run.py 拆分 + auxiliary-spec 扫清 | F-011 · F-012 · F-013 · F-027（共 4 条复杂度 + 命名） |

复核结论：**32 fixed / 0 partial / 0 regressed**（F-026/F-038 在 round-1 critic 已转 follow-up nice-to-have 不计入 keep findings）。Round-2 修复完整度高，规范化作业到位。

## Critical（必须修复）

无。Round-1 的 2 个 critical（F-001 H5 exit 协议 / F-002 H1 事务化失守）全部闭环。F-013（_validate_one_entry CC=28）经 critic 对抗验证降级为 major：项目 critical 基准是「安全/事务/数据丢失承诺破坏」，可维护性问题归 major 更精确。

## Major（强烈建议修复 / 列入 round-3 must_fix）

### [design-consistency · F-001] scripts/gates/audit.py:39-72

audit.py build_audit 已新增 bypassed 字段（含 gate_id/reason/whitelisted）和 rollback_failed 字段，但 detailed-design.md §2.3:220-242 audit JSON schema 仍只有 passed/failed/skipped/escape_used/exit_code，spec 与实现字段集偏离。

**建议**：在 detailed-design.md §2.3 audit schema 示例中补充 bypassed 与 rollback_failed 字段定义及语义说明。修复成本极低，仅改 spec 段落。

### [security · F-005] scripts/gates/plugins/bash_write_protect.py:41-58

_ALTS 当前 8 类未覆盖 sponge / rsync / install / python3 -c pathlib.write_text 四种写法。实测 `echo x | sponge requirements/REQ-2026-002/reviews/foo.json` 与 `python3 -c "from pathlib import Path; Path(...).write_text('x')"` 均绕过。

**建议**：补 alt9~12 四条正则；本 round 必须关闭，不允许 carry-over：`rf"sponge\s+...{_PATH}"` / `rf"rsync\s+\S+\s+...{_PATH}"` / `rf"install\s+...{_PATH}"` / `rf"python3?\s+-c\s+['\"].*write_(?:text|bytes)\(.*?{_PATH}"`。

### [complexity · F-013] scripts/gates/registry.py:132-192

_validate_one_entry 圈复杂度 28（阈值 10）；S2/S3/S4/S8/S9/S10 六规则串联 if 链导致单函数承担六套独立校验语义。

**建议**：拆为 6 个私有函数 _validate_s2_plugin / _validate_s3_triggers / _validate_s4_severity / _validate_s8_fixtures / _validate_s9_requires / _validate_s10_escape_hatch；CC 28 → 各 ≤ 5；每条规则可独立测试。

### [complexity · F-014] scripts/gates/registry.py:81-129

_validate_registry_schema 圈复杂度 18（阈值 10）；S1 ID 校验循环 + S5 依赖引用循环 + S6 拓扑排序 try-except + S7 write_state plugin rollback 校验循环四段。

**建议**：拆为 _validate_s1_ids / _validate_s5_deps / _validate_s6_no_cycles / _validate_s7_rollback 四个私有函数；CC 18 → 各 ≤ 4。

### [complexity · F-015] scripts/gates/plugins/review_verdict.py:1-397

review_verdict.py 397 行已逼近 500 阈值，混合三套独立职责：ReviewVerdictGate 类主体 + ci 全量扫描（_run_all_requirements / _collect_findings / _build_ci_report）+ meta.yaml 原子写入（_commit_meta_writes / _set_dot_path）。

**建议**：拆出 review_verdict_ci.py + meta_writer.py，主文件保留 ReviewVerdictGate 类与 staged_writes/rollback 路径，单文件降至约 180 行。

### [complexity · F-016] scripts/gates/plugins/pr_state.py:132-184

_fetch_pr_state 50 行接近 60 上限；4 类异常处理（TimeoutExpired / FileNotFoundError+OSError / returncode!=0 / JSONDecodeError）各自重复 print WARNING + cache=None + return None 三步模式。

**建议**：抽 _cache_miss(pr_num, reason, exc) helper（print + set cache + return None 三步），4 个异常 handler 各缩短为 2 行；函数本体 50 → 约 30 行；新增异常类型时不再有遗漏风险。

### [error-handling · F-019] scripts/gates/run.py:354

write_audit(_build_audit(ctx, reports, rollback_failed)) 在 _execute_plan 主体 try/except 块外（try 块行 345-352）；OSError（磁盘满 / audit 目录无写权限 / fsync 失败）会向上传播至 main()，pre_tool_use.sh 把非零 rc 映射为 exit 2 阻断 Claude——audit 基础设施故障演变成 gate 系统全崩。

**建议**：包裹 try/except，失败时打 ERROR 日志后继续返回 _calc_exit_code 正常退出码：

```python
try:
    write_audit(_build_audit(ctx, reports, rollback_failed))
except Exception as audit_exc:  # noqa: BLE001
    print(f"ERROR audit 落盘失败，跳过（不阻断 gate 结果）: {audit_exc}", file=sys.stderr)
return _calc_exit_code(reports, plan, strict)
```

### [error-handling · F-020] scripts/gates/run.py:339

snapshots = _stash_state(ctx) if needs_stash else {} 在 try 块（行 345 才开始）外；shutil.copy2 PermissionError 向上传播为 unhandled exception，traceback 暴露内部路径到 stderr 并触发 exit 2。

**建议**：单独包裹 try/except，stash 失败时跳过事务化保护继续执行：

```python
try:
    snapshots = _stash_state(ctx) if needs_stash else {}
except Exception as stash_exc:
    print(f"ERROR stash_state 失败，跳过事务化保护继续执行: {stash_exc}", file=sys.stderr)
    snapshots = {}
```

### [auxiliary-spec · F-022] scripts/gates/run.py:109,285

parse_args(argv) 与 main(argv) 均无中文 docstring；同文件其他公开函数（load_registry / build_context / filter_gates）已补 docstring，submit.py parse_args 在 F-015 round-2 已补，run.py 同名漏补——规范倒退。

**建议**：补完整中文 docstring，声明入参约束 / 返回值（exit code 三态语义 0 通过 / 1 存在 error / 2 自身异常）/ 全部 flag 含义；与 submit.py parse_args F-015 round-2 风格一致。

### [history-context · F-033] requirements/REQ-2026-002/plan.md:179,188

plan.md F-026 引用 `scripts/gates/run.py:404-405`、F-038 引用 `scripts/gates/run.py:556-558`，但当前 run.py 仅 465 行；needs_stash 实际已迁至 run.py:338，fsync 已拆到 audit.py:86——commit 83099b2 拆分后行号失效，F-004 接收人按 plan.md 查找会找不到目标代码。

**建议**：plan.md:179 改 `run.py:338-339`、:188 改 `audit.py:86`；同步纠正 F-004 carry-over 索引。

## Minor（改进建议 / 大部 carry-over F-004）

### 设计一致性（3 条）

- **F-002** scripts/gates/audit.py:72 — exit_code=1 if failed else 0 在 strict 模式 + warning fail 场景下与实际进程退出码不一致
- **F-003** scripts/gates/plugins/bash_write_protect.py:51 — _ALTS[5] 实现 (?:w|a|wb|ab) 超出 spec §3.3:319 的单 'w' 写法（must_fix_round_3：spec 同步）
- **F-004** scripts/gates/plugins/bash_write_protect.py:144 — _caller_is_save_review_sh(self, ctx) 签名超出 spec §3.3:359 的 (self) 无 ctx 写法（must_fix_round_3：spec 同步）

### 安全（3 条）

- **F-006** scripts/gates/plugins/bash_write_protect.py:141 — vars={"command": cmd[:200]} 写 audit JSON，命令可能含 token 无脱敏
- **F-007** scripts/gates/plugins/pr_state.py:166-171 — gh stderr 截断 200 字打印到 stderr，截断不等于脱敏
- **F-008** scripts/gates/audit.py:46-50 — BYPASS_REASON 全文写 audit log，reason 用户自由填写无脱敏

### 并发（4 条）

- **F-009** scripts/lib/save_review.py:265-293 + scripts/lib/check_reviews.py:174-182 — CLI 写 meta.yaml 路径未持 fcntl 锁；judge override critic rejected → keep minor，本 PR 范围外登记 F-004
- **F-010** scripts/gates/state_io.py:37-50 — restore_state 直接 shutil.copy2 无锁，GateFailed 路径 + 并发改 meta.yaml 才触发
- **F-011** scripts/gates/plugins/pr_state.py:40,157,164,172,181 — _PR_STATE_CACHE None 永久缓存无 TTL
- **F-012** scripts/save-review.sh:18-19 + scripts/gates/plugins/bash_write_protect.py:171 — exec 后 comm fallback 死代码（must_fix_round_3：删除死代码 + docstring 标注）

### 复杂度（2 条）

- **F-017** scripts/gates/plugins/bash_write_protect.py:86-142 — run() 主体 49 行接近 60 上限
- **F-018** tests/gates/test_submit_next_parity.py:63-96 — test_phase_transition_gates_are_subset_of_submit 单测多场景

### 错误处理（1 条）

- **F-021** scripts/gates/run.py:437-438 — traceback.print_exc 在非 CI 暴露绝对路径

### 辅助规范（6 条）

- **F-023** scripts/gates/run.py:106 — # CLI 段头英文分隔线无中文
- **F-024** scripts/gates/state_io.py:43 — 'ERROR restore_state failed' 英文运行时日志
- **F-025** scripts/gates/run.py:418 — 'ERROR gate-rollback-failed' 英文 log
- **F-026** scripts/gates/plugins/bash_write_protect.py:44,56 — _ALTS 第 2/8 条注释短，与第 3-7 条详细程度不一致
- **F-027** tests/gates/test_pr_state_plugin.py:203 + tests/gates/test_bash_write_protect_plugin.py:89,112,130 — 4 处测试函数缺 should_/given_ 前缀
- **F-028** scripts/gates/audit.py:76-77 — write_audit docstring 仅 WHAT 缺 WHY/参数/失败行为

### 性能（4 条）

- **F-029** scripts/gates/plugins/bash_write_protect.py:51 — alt5 正则嵌套 .* + .*? 多项式回溯（critic 实测降级 major→minor，真实场景 ≤17ms 不在威胁模型）
- **F-030** scripts/gates/audit.py:85-86 — fsync 同步落盘 pre-tool-use 高频路径（已登记 plan.md F-038 carry-over）
- **F-031** scripts/gates/registry.py:65-67 — load_registry 每次 yaml.safe_load 无跨进程缓存
- **F-032** scripts/lib/check_reviews.py:80 — _r002 ci 循环内 N+1 schema.yaml 重读

### 历史上下文（1 条）

- **F-034** scripts/gates/audit.py:25 — SCHEMA_VERSION='1.0' 自 round-1 未变，新增 bypassed 字段属向后兼容扩展未递增小版本号

## Follow-up Notes（不阻塞，但值得提醒）

- F-009 / F-010：F-002 已合 develop，commit_staged_writes 路径加锁完成，但 save_review.py + check_reviews.py CLI 写 meta.yaml 仍无锁；race window 在 D-009 串行约束下窄。建议 F-004 评估纳入「meta.yaml 全量写者持 fcntl.lockf」统一规范
- F-011：_PR_STATE_CACHE 模块级 None 永久缓存，submit 单进程跑完即退影响小，但 long-running pytest 场景会暴露——若未来加 governance 工具长驻进程需加 TTL
- F-026 / F-038：plan.md 已登记 F-004 carry-over 但行号在本轮重构后失效（F-033 must_fix），需 plan.md 同步纠正
- F-029 ReDoS：实测 1k 字符级真实 Bash 命令 ≤17ms 不构成性能问题，但 alt5 正则的 .*open\\(.*?.*? 嵌套结构是技术债，未来若 H5 加更多 alt 时建议改两阶段匹配（先锚定前缀再做 PATH 匹配）

## 未运行的 checker

无。8 个 checker（design-consistency / security / concurrency / complexity / error-handling / auxiliary-spec / performance / history-context）全部完成。

## 审查元信息

- **审查范围**：F-003 round-2 增量（base = round-1 reviewed_commit b5df0fe / head = f771d76）
- **增量规模**：25 文件 / +1738 / -634
- **services**：agentic-meta-engineering
- **mode**：embedded（feature-scoped）
- **并行 checker**：8（design-consistency / security / concurrency / complexity / error-handling / auxiliary-spec / performance / history-context）
- **Critic 统计**：rejected 1（F-009，judge 覆盖→keep minor）/ not_proven 2（F-013 critical→major / F-029 major→minor）/ not_rebutted 31
- **综合裁决**：code-quality-reviewer 三方对比 = checker evidence + critic counter-evidence + 自查源码
- **base_sha**：b5df0fe（round-1 reviewed_commit）
- **head_sha**：f771d76
- **base_branch**：develop（feat/req-2026-002 派生）
- **current_branch**：feat/req-2026-002
- **timestamp**：2026-04-28 19:28:57 (Asia/Shanghai)
- **verdict**：requirements/REQ-2026-002/reviews/code-F-003-002.json（supersedes code-F-003-001）
