# 代码审查报告 · REQ-2026-010 · 2026-05-11 16:40:21

## 结论

**blocked** · 1 critical / 0 major / 2 minor

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项
- `blocked` — AI 机器评估发现关键问题 ✅ 本次

主体代码 OK：`main()` schema 校验逻辑清晰、删 `_is_requirement_template` 替换为 `workflow.category` 符合 detailed-design §1.4、单测覆盖 AC-1/AC-2 两条 acceptance。但 **F-003.md frontmatter `touches` 字段未声明实际改动的两个测试文件**，receipt.json 已固化 6 条 violations，GATE-TOUCHES-VIOLATION 会硬挡 phase-transition (`development → testing`) 与 `/requirement:submit` 两个关口——`signoff approved → done` 切到 testing 时会被拦下，无法收尾。

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：accept（接受推荐 4 路）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-11 16:25:39
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker / auxiliary-spec-checker
- **跳过的 checker（含原因，4 路）**：
  - security-checker：路径未命中 must/suggest 任何规则
  - concurrency-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则

## 待 sign-off 提示

本次评审输出 `conclusion = blocked`，**这只是 AI 的机器评估**，不代表合并通过。`blocked` 路径要求开发者修复 critical / blocker 项后重派 rev2 审查（不直接走 signoff）。如确认 critical 不修也要 signoff 强行通过，命令模板：

```bash
python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-010-code-F-003-001 --decision rejected
# 或修完 F-8 后派 rev2：再次 /code-review 走 routing.py 卡点 A
```

未 sign-off 时 feature-lifecycle-manager 不会把对应 feature 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-1** [complexity · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：`scripts/lib/workflow_run.py:313-323` 由 commit `fee71457` (F-002) 引入，本次 diff 范围 `2271902..29b7b10` 不含。git blame 验证。

- **F-2** [complexity · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：`notes.md:13` G-2 follow-up 已立案（`F-005/F-008 启动前评估按 TestClass 拆分`）；F-003 本次 +108 行属 acceptance 必需（AC-1/AC-2 单测覆盖）。主体责任已归档，转 suggestions 提醒后续 feature 不可再膨胀。

- **F-3** [complexity + error-handling + design-consistency · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：`workflow_loader.py:10-27` 错误码表显示 W0xx (W000/W001) 是文件级、W1xx (W100~W153) 是 schema/语义级；`('error', 'w1', 'w0', '缺少')` 全集合命中 load_workflow 错误码空间，断言有效非掩盖。

- **F-4** [error-handling · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：`Report.render()` (common.py:96-115) 已含 file path + 错误码 + Total 摘要，业务主键齐全，无需额外 `ERROR: 模板 ... ` 前缀。

- **F-5** [error-handling · minor] → critic: not_rebutted → **最终处置：keep follow-up**
  - 裁决依据：契约存在事实成立（`workflow_loader.py:186-188` 显式守门），workflow_run.py:229 已注释标注。F-001 同款 follow-up 实践延续。

- **F-6** [design-consistency · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：result vs load_result 变量命名差异属主观风格，CLAUDE.md §1.1 无伪代码变量名一致性硬规范。

- **F-7** [design-consistency · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：设计 `workflow.category` attr 表达 vs 实现 `workflow.get("category")` dict 调用，LoadResult.workflow 是 `dict[str, Any] | None`，实现正确，仅文档措辞差异。

- **F-8** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep（升级 minor → blocker）**
  - 裁决依据：F-003.receipt.json:4-34 已固化 6 条 touches_violations 客观事实；`touches_violation.py:118-142` 见非空即 R-TOUCHES-VIOLATION-PRESENT FAIL，gate 在 phase-transition development→testing 与 /requirement:submit 路径均挂载，无 test 文件豁免规则。不修则两个关键关口硬挡，影响 == blocker。

- **F-9** [aux · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：routing.yaml 既有 3 处 >120 字符注释（行 6/29/73），项目无 yaml 行长硬规范。

- **F-10** [aux · minor] → critic: not_rebutted → **最终处置：keep minor**
  - 裁决依据：同文件既有 class docstring 用 `TC-Fx-y：<描述>` 单行；新增 `TestLoadWorkflowSchemaGate` 用多行 `F-003 acceptance：` 前缀，客观风格不一致。可顺手修。

- **F-11** [aux · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：CLAUDE.md §7 仅约束 should/given-when-then 命名模式，未对方法名相似度设阈值。

## Critical（必须修复）

### [design-consistency / process-gate] requirements/REQ-2026-010/artifacts/tasks/F-003.md:9

`F-003.md` frontmatter `touches` 字段仅声明 `scripts/lib/workflow_run.py`，但本次 diff 实际改动了 `tests/skills/test_workflow_commands.py`（+108 行）与 `tests/skills/test_workflow_bootstrap.py`（+4 -28 行）两个未授权文件。

`F-003.receipt.json:4-34` 已固化 6 条 touches_violations，`scripts/gates/plugins/touches_violation.py:123-142` 见非空 violations 即抛 `R-TOUCHES-VIOLATION-PRESENT` FAIL。该 gate 在：
- phase-transition `development → testing` 切换
- `/requirement:submit` 命令

两个关键关口硬挡，且**不带 `legacy-bypass` tag**——`meta.yaml.legacy: true` 无法豁免。

**建议**：

1. F-003.md frontmatter `touches:` 字段追加两项：
   ```yaml
   touches:
     - scripts/lib/workflow_run.py
     - tests/skills/test_workflow_commands.py
     - tests/skills/test_workflow_bootstrap.py
   ```
2. 清空 `F-003.receipt.json` 的 `touches_violations[]`（重跑触发 touches_guard 清零，或手动写空数组）
3. 验证：`python3 scripts/gates/run.py --trigger=phase-transition --req=REQ-2026-010 --from=development --to=testing` 不再出 `R-TOUCHES-VIOLATION-PRESENT`

## Minor（改进建议）

### [error-handling] scripts/lib/workflow_run.py:229

`workflow = load_result.workflow` 依赖 `load_workflow` 契约 `errors==0 ⇒ workflow != None`（`workflow_loader.py:186-188` 显式守门）。当前调用方无 assert / 防御式 if，仅靠 :229 行尾注释 `dict，由 load_workflow 保证非 None` 显式标注契约。

**建议**：keep follow-up，参考 F-001 同款实践（项目惯用注释而非 assert）；若日后 `load_workflow` 行为变更（errors=0 但 workflow=None），应同步更新 :229 注释或加 assert。

### [auxiliary-spec] tests/skills/test_workflow_commands.py:911-916

`TestLoadWorkflowSchemaGate` class docstring 用 `F-003 acceptance：` + 多行 AC-1/AC-2 风格，同文件既有 `TestGenerateRunIdConcurrencySafe` / `TestOSErrorWrappedAsWorkflowError` / `TestGenerateReqId` 等均用单行 `TC-Fx-y：<描述>` 或 `<函数名> <描述>` 风格。

**建议**：本轮顺手修（5 秒事），统一为：

```python
"""TC-F3-1：main() schema 校验门禁——非法 yaml exit 1，合法模板正常路由。"""
```

AC-1/AC-2 详细描述下沉到各测试方法 docstring（当前已有）。

## Follow-up Notes（不阻塞，但值得提醒）

- F-5 `workflow_run.py:229` workflow 非 None 契约依赖：F-001 同款，注释守门即可（保留）
- F-2 / G-2 trend：`test_workflow_commands.py` 已达 1010 行，必须在 F-005/F-008 启动前按 TestClass 拆分（建议：`test_workflow_run_id.py` / `test_workflow_schema_gate.py` / `test_workflow_run_core.py`），否则后续 feature 每次 +100 行累积成不可逆债务
- **F-8 触发链反思**：本次 +108 行测试已纳入 F-003 acceptance 必需（验证 schema 门禁），但 frontmatter touches 未同步更新——建议 detailed-design 评审阶段就把测试文件预先纳入 touches，或 GATE-TOUCHES-VIOLATION 增加测试文件白名单豁免（涉及更高层 spec 改动，需独立议题）

## 未运行的 checker

- ⚠️ security-checker（路径未命中 must/suggest）
- ⚠️ concurrency-checker（同上）
- ⚠️ performance-checker（同上）
- ⚠️ history-context-checker（同上）

> 4 路 skip 与 routing.yaml 推荐一致；F-003 trivial 改动（schema 门禁挂载 + 删一个 helper）无安全 / 并发 / 性能 / 历史漂移风险，可接受。

## 审查元信息

- **审查范围**：F-003 + routing.yaml fix（4 文件，+136 -55 行）
- **并行 checker**：4
- **Critic 统计**：rejected 4 / not_proven 4 / not_rebutted 3
- **综合模型**：code-quality-reviewer (sonnet)
- **Base SHA**：2271902
- **Head SHA**：29b7b10
- **Verdict JSON**：`requirements/REQ-2026-010/reviews/code-F-003-001.json`
- **Review ID**：`REV-REQ-2026-010-code-F-003-001`
