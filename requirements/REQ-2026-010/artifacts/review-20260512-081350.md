# 代码审查报告 · REV-REQ-2026-010-code-F-007-001

**时间**：2026-05-12 08:13:50  
**模式**：嵌入 / REQ-2026-010 / F-007  
**reviewed_commit**：`2c23820`（base：`fc4326b` F-006 last commit）  
**范围**：3 files / 537 insertions / 14 deletions

## 路由说明

- **决策**：`accept`（routing.py 自动）
- **确认人**：hj19961223@gmail.com（git config）
- **确认时间**：2026-05-12 08:02:28
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker / auxiliary-spec-checker（命中 suggest 规则 2 / must 0 / trivial 0）
- **跳过的 checker**：
  - `security-checker`：路径未命中 must/suggest 任何规则
  - `concurrency-checker`：路径未命中 must/suggest 任何规则
  - `performance-checker`：路径未命中 must/suggest 任何规则
  - `history-context-checker`：路径未命中 must/suggest 任何规则

---

## 结论

**verdict**：`needs_attention`  
**score**：75 / 100  
**严重度分布**（裁决后）：0 critical / 4 major / 4 minor  
**follow-up**：2 项

> Judge 给的是 `needs_attention`（score 75）——4 个 major keep 是显著证据债。建议派 rev2 闭合 keep（预计 ≤ 40min），不建议直接 signoff。F-007 是 rev1，本次未触发 trend-G-meta 循环，但 F-4（docstring 失实）属 F-005 rev1 F-20 / F-006 rev1 F-15 同款模式，**说明 subagent 出场就再次引入同款问题**——rev2 派发 prompt 必须显式要求 grep 全文搜 "stub" / "F-006 实现" / "F-008 实现" 这类前置 feature 失实表述。

---

## Critical / Major findings（rev2 必修，keep）

### F-1 (major) — main() 函数超阈值

**file**：`scripts/lib/workflow_continue.py:122-209`

main() span=88 行（L122-L209），有效代码行 ≈ 63，**直接命中 complexity-checker.md L18 spec 阈值 `80-200 行 = major`**。CC=13（spec ≤ 10 阈值已超）。critic 判 not_proven 是漏读 spec L18 严重度档位——本 Judge 翻回 keep major。

修复建议（来自 003）：拆出两个私有函数：
1. `_load_workflow_for_run(run_state, run_dir, root)` 承担 workflow_name 推断 + 路径查找 + yaml 降级（L179-L200，约 22 行）
2. `_setup_run(run_id, root)` 承担 run_dir 解析 + jsonl 读事件 + RunState.rebuild（L144-L153，约 10 行）

拆分后 main() 缩至 ≈ 35 行、CC ≤ 7。

### F-2 (major) — bare `except Exception: pass` 吞 yaml

**file**：`scripts/lib/workflow_continue.py:190-194`

```python
try:
    meta = yaml.safe_load(meta_path.read_text(encoding="utf-8"))
    workflow_path = root / meta.get("workflow_template_path", str(workflow_path))
except Exception:
    pass
```

YAMLError / UnicodeDecodeError / PermissionError 全静默吞没，调用方继续以原 workflow_path（不存在）调 load_workflow，只看到 W000「文件不存在」，真实根因消失。critic not_rebutted，独立证据无 spec 豁免。

修复建议：至少 `print(f"WARN: 读 meta.yaml 失败，跳过降级查找：{exc}", file=sys.stderr)` 让根因可追溯。可顺手提取 `_resolve_workflow_path` 私有函数，让异常边界清晰可单测（与 F-1 重构同步）。

### F-3 (major) — _main_loop 无 try/except 包裹

**file**：`scripts/lib/workflow_continue.py:208`

`_main_loop(...)` 在 main() L208 直接调用，未包 try/except WorkflowError。**关键证据**：测试中 `workflow_continue.main()` 在 `test_workflow_commands.py:143/312/353/368/448/728` + `test_legacy_run_compat.py:209` 共 **8 处**被直接调用，**全部不走 `__main__`**——所以 L213-217 那层 except WorkflowError 兜底完全保护不到测试场景。`_main_loop` 内 L79-85 unknown node 分支 `append_event` 抛 WorkflowError 时会产生未格式化 traceback 而非统一 `ERROR: ...` 输出，exit code 由运行时决定。

修复建议：
```python
try:
    _main_loop(run_state, workflow, run_dir, root, jsonl_path)
except WorkflowError as exc:
    print(f"ERROR: {exc}", file=sys.stderr)
    return 1
return 0
```

### F-4 (major) — docstring "main loop stub" 失实（trend-G-meta 同款）

**file**：`scripts/lib/workflow_continue.py:5`

```
反扫 jsonl 重建 RunState，做状态校验，进 main loop stub（F-006 实现完整 loop）。
```

F-007 已落地真实 _main_loop，docstring 仍写 stub + 把实现归属错误地推给 F-006。**与 F-005 rev1 F-20 / F-006 rev1 F-15 完全同款的"docstring 失实 / 自我否定"反模式**——trend-G-meta 历史教训：subagent 实现完功能后忘改顶部 docstring。

修复建议：改为「反扫 jsonl 重建 RunState，做状态校验，进 main loop（F-007 落地最小骨架：completed / failed / approval_pending；完整失败矩阵 / loop / sub_workflow 由 F-008 / F-011 接入）。」

---

## Minor findings（rev2 必修，keep）

### F-5 — _main_loop 其他 outcome 静默 break

**file**：`scripts/lib/workflow_continue.py:116-119`

`else: break` 处理 loop_continue / loop_done / sub_workflow_pending 等 outcome 时：不写任何事件、不改 state。调用方拿到 exit 0 + state="running"，无法感知这是预期外路径。

修复建议（可推迟到 F-008，但 rev2 至少加一行 WARN print 让日志可追）：
```python
else:
    print(f"WARN: outcome={result.outcome} 未实现（由 F-008/F-011 接入）", file=sys.stderr)
    break
```

### F-7 — TC-F7-1 第一个 with patch 块调试遗留

**file**：`tests/skills/test_workflow_continue_main_loop.py:172-186`

```python
with patch("workflow_dispatcher.dispatch_node") as mock_dispatch:
    mock_dispatch.return_value = DispatchResult(outcome="completed", output="hello")
    base_run_state.state = "running"
    base_run_state.current_node = "node-a"
    _main_loop(...)
    # 此时应进入 node-b，但 mock_dispatch 被调用 2 次
    # ... 我们需要改进 mock

with patch("workflow_dispatcher.dispatch_node") as mock_dispatch:
    # 改进版
    ...
```

第一个 with 块的执行结果被第二个块 reset，但**仍会调用 dispatch + 写 jsonl 事件**，产生额外噪声。注释解释的是 what/调试思路而非 why——典型"调试遗留物"。

修复建议：删除 L172-L186，仅保留改进版。

### F-9 — MagicMock dead import

**file**：`tests/skills/test_workflow_continue_main_loop.py:15`

`from unittest.mock import MagicMock, patch` —— MagicMock 全文件 grep 无引用。

修复建议：改为 `from unittest.mock import patch`。

### F-10 — append_event 函数体内 dead import

**file**：`tests/skills/test_workflow_continue_main_loop.py:355`

```python
def test_main_loop_unknown_node_writes_workflow_failed_event(...):
    workflow = {"nodes": []}
    run_state = RunState(...)
    from run_state import append_event  # 导入后从未调用
    _main_loop(...)
```

修复建议：删除 L355 这行（read_events 已在文件顶部 L26 导入）。

---

## Follow-up Notes（不动代码，写入 notes.md）

### F-11 — _next_node 的 node_map 参数 dead

**file**：`scripts/lib/workflow_continue.py:35-49`

```python
def _next_node(current_node: dict, node_map: dict[str, dict], hint: str | None) -> str | None:
    if hint is not None:
        return hint
    return current_node.get("next")
```

`node_map` 参数从未在函数体使用。critic not_rebutted 成立。但考虑到 F-008 可能引入显式跳转（如 `next: {if: ..., then: node-x}`）会用到 node_map 做合法性校验——属预留扩展点。Judge follow-up：F-008 接入显式跳转时若仍不用，必须删；若用，必须补 docstring 说明用途。

### F-12 / F-13 — detailed-design.md §1.7 伪代码与实现偏离（spec-lag）

**file**：`requirements/REQ-2026-010/artifacts/detailed-design.md` §1.7（L214-L215）

- L214 伪代码显式调 `append_event node_completed`，实现把该事件写权下放给 `_dispatch_*_node`，main loop 仅更新 RunState（workflow_continue.py:94-103）——双写防范是良性偏离
- L215 node_outputs 赋值为 `result.output` 裸值，实现存三键 dict `{output, state, data}`（workflow_continue.py:97-100），与 RunState.rebuild（run_state.py:162-166）一致

critic not_proven（spec-lag 归 spec 侧，F-005 rev3 precedent 已确立此处置）。Judge follow-up：F-008 PR 内同步 §1.7 伪代码两处：(1) 删除 main loop 内 append_event node_completed 调用 + 加注释「事件由 dispatcher 负责」；(2) node_outputs 改为三键 dict。

---

## Drop findings（裁决明细）

| F-id | checker | 原 severity | critic verdict | 最终处置 | 理由 |
|---|---|---|---|---|---|
| F-6 | complexity-checker | minor | not_proven | drop | _main_loop CC=7 在 spec 阈值内（≤ 10）；checker 自己标的"预警"非 finding |
| F-8 | complexity-checker | minor | not_proven | drop | TC span=64 行在 spec ≤ 80 阈值内；预警与 finding 同源 |

---

## 裁决明细（全候选 finding）

| F-id | checker | severity | critic verdict | 最终处置 | 备注 |
|---|---|---|---|---|---|
| F-1 | complexity | major | not_proven | **keep（major）** | critic 漏读 spec L18 严重度档；span=88 落在 80-200 major 区间 |
| F-2 | error-handling | major | not_rebutted | **keep（major）** | bare except 吞 yaml + 无 spec 豁免 |
| F-3 | error-handling | major | not_rebutted | **keep（major）** | 测试 8 处直调 main() 不走 __main__ 兜底 |
| F-4 | auxiliary-spec | major | not_rebutted | **keep（major）** | docstring 失实，trend-G-meta 同款 |
| F-5 | error-handling | minor | not_rebutted | **keep（minor）** | else break 静默退出 |
| F-6 | complexity | minor | not_proven | drop | CC=7 阈值内，仅预警 |
| F-7 | auxiliary-spec | minor | not_rebutted | **keep（minor）** | TC-F7-1 调试遗留 with 块 |
| F-8 | complexity | minor | not_proven | drop | TC span=64 阈值内 |
| F-9 | auxiliary-spec | minor | not_rebutted | **keep（minor）** | MagicMock dead import |
| F-10 | auxiliary-spec | minor | not_rebutted | **keep（minor）** | append_event 函数体内 dead import |
| F-11 | auxiliary-spec | minor | not_rebutted | follow-up | _next_node 参数 dead，F-008 时回看 |
| F-12 | design-consistency | minor | not_proven | follow-up | §1.7 伪代码 spec-lag |
| F-13 | design-consistency | minor | not_proven | follow-up | §1.7 node_outputs 格式 spec-lag |

verdicts: rejected=0 / not_proven=4 / not_rebutted=9 → **keep 8 / drop 2 / follow-up 3**

---

## 修复策略（如派 rev2，预计 ≤ 40min）

按 severity + 关联度排序：

1. **F-4（major）** — workflow_continue.py L5 docstring 同步 F-007 实现状态（5min）
2. **F-2 + F-1 联动（major）** — workflow_continue.py L186-194 bare except 改 print stderr，并把 yaml 降级抽 `_resolve_workflow_path` 私有函数；同时按 F-1 建议拆 `_load_workflow_for_run` + `_setup_run`，main() 缩至 ≈ 35 行 / CC ≤ 7（20min）
3. **F-3（major）** — main() L208 用 try/except WorkflowError 包裹 _main_loop，统一 `ERROR: ...` + return 1（5min）
4. **F-5（minor）** — _main_loop else 分支加一行 WARN print（2min）
5. **F-7（minor）** — test_workflow_continue_main_loop.py 删 L172-186 第一个 with 块（3min）
6. **F-9 + F-10（minor 双 dead import）** — 删 MagicMock import + L355 append_event import（2min）
7. **F-11 / F-12 / F-13** → notes.md follow-up

### rev2 派发硬规则（trend-G-meta 防御 · 关键）

> rev N-1 keep finding 修复后必须全文搜同模式 + 同 helper 风格不一致——不许"只改 reviewer 报告点出的具体行"。

**F-007 专属强化项**（trend-G-meta 三连击预警）：

1. **docstring 失实同模式扫描**：修 L5 后必须 `grep -n "stub\|F-005 实现\|F-006 实现\|F-008 实现\|F-011 实现" scripts/lib/workflow_continue.py` + `tests/skills/test_workflow_continue_main_loop.py`——扫剩余"前置 feature 失实归属"
2. **dead import 同模式扫描**：修 MagicMock / append_event 后必须 `grep -n "^from\|^import\|    from .* import" tests/skills/test_workflow_continue_main_loop.py` 逐个核对引用计数
3. **bare except 同模式扫描**：修 L190-194 后必须 `grep -rn "except.*:" scripts/lib/workflow_continue.py` 全文核对每个 except 是否记录原因
4. **同 helper 拆分风格扫描**：拆出 `_load_workflow_for_run` / `_setup_run` 后，对照同模块 `_build_node_map` / `_next_node` 已有命名风格保持一致（小写下划线、动词在前）

---

## 待 sign-off 提示

本次评审 conclusion = `needs_attention`（**不是 looks_clean**），4 个 major keep 是显著证据债。强烈建议派 rev2 闭合后再 signoff；如人类裁定接受当前结果（接受 4 major + 4 minor 留 follow-up），需在 sign-off rationale 中明确记录「F-007 rev1 接受 trend-G-meta 同款 docstring 失实 + bare except 吞异常 + main() 超长 + _main_loop 异常无包裹」的妥协理由。

**两个路径**：

A) **rev2 闭合 8 项 keep（推荐，40min）** → 跑第二轮 /code-review → looks_clean 0 keep → signoff

B) **直接 signoff 当前结果**（不推荐——4 个 major 都是反复出现的反模式，长期会进一步加剧 trend-G-meta）：

```bash
python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-010-code-F-007-001 --decision approved
# slash command 糖：
/code-review:signoff REV-REQ-2026-010-code-F-007-001 --decision=approved
```

⚠️ 未 sign-off 时 feature-lifecycle-manager 不会把 F-007 转 done；GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

---

## 报告文件

`requirements/REQ-2026-010/artifacts/review-20260512-081350.md`（本文件）
