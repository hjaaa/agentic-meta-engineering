# 代码审查报告 · REV-REQ-2026-010-code-F-006-002

**时间**：2026-05-11 22:39:23  
**模式**：嵌入 / REQ-2026-010 / F-006 / rev2  
**reviewed_commit**：`c59aa6f`（base `4c3c4d2` rev1 head）  
**范围**：6 files / 41+ / 37-

## 路由说明

- **决策**：`accept`（routing.py 自动）
- **跑了哪些 checker**：complexity / error-handling / design-consistency / auxiliary-spec
- **跳过**：security / concurrency / performance / history-context

---

## 结论

**verdict**：`blocked`  
**score**：65 / 100  
**严重度分布**：1 blocker + 1 major + 2 minor  
**trend-G-meta**：**第 2 次触发**（rev1→rev2 自引入 4 项不一致，同 F-005 rev2→rev3 模式）

> ⚠️ rev1 keep 9 项闭合但自引入 4 项新不一致（1 blocker §1.5↔§1.6 + 1 major code↔§2.3 + 2 minor 测试/dead）。
> 必须派 rev3 终结 trend-G-meta，参考 F-005 rev3 经验：rev3 改动 ≤ 10 行 + 0 新引入 = 终结。

---

## Critical / Blocker findings

### F-3 (blocker) — detailed-design.md §1.5 内部签名不一致

**file**：`requirements/REQ-2026-010/artifacts/detailed-design.md:159-162`

rev2 改了 §1.6 7 类 stub 函数签名（加 `run_state`），但 §1.5 `dispatch_node` 伪代码的 4 处调用点未同步：

- L159 `_dispatch_skill_node(node, env, jsonl_path)` ❌ → 应为 `(node, run_state, env, jsonl_path)`
- L160 `_dispatch_prompt_node(node, env, jsonl_path)` ❌ → 应为 `(node, run_state, env, run_dir, root, jsonl_path)`
- L161 `_dispatch_bash_node(node, env, run_dir, jsonl_path)` ❌ → 应为 `(node, run_state, env, run_dir, root, jsonl_path)`
- L162 `_dispatch_approval_node(node, env, jsonl_path)` ❌ → 应为 `(node, env, run_state, jsonl_path)`

文档内部自相矛盾。F-007 / F-010 / F-011 接管时按 §1.5 还是 §1.6 写都可能出错——硬挡。

---

## Major findings

### F-4 (major) — approval substitute_vars 未传 escape_for_bash=True

**file**：`scripts/lib/workflow_dispatcher.py:348` + `detailed-design.md:331`

- 代码 L348 `substitute_vars(raw_prompt, run_state.node_outputs, env)` 默认 `escape_for_bash=False`
- 文档 §2.3 明确"skill / prompt / approval 节点走默认 True（防注入）"
- 同文件 L204 (skill) / L248 (prompt) 已显式传 True，唯独 approval 漏

critic 反证 "approval prompt 仅入 jsonl 不入 shell" 局部成立（无注入路径），但 spec 显式规定 + 防御深度 + 一行修复零成本 → keep。

修：`substitute_vars(raw_prompt, run_state.node_outputs, env, escape_for_bash=True)`

---

## Minor findings (keep)

### F-1 — bash returncode!=0 测试断言缺失

**file**：`tests/skills/test_workflow_dispatcher_bash_skill_prompt.py:120-131`

`test_bash_node_nonzero_returncode_via_dispatch_node` 用 `bash exit 2` 触发 rev2 新兜底 `f"exit code N (no stderr/stdout)"`，但只断言 `outcome=="failed"` 和事件类型，**未断言 `result.error` 内容**——CI 不保护新兜底字符串。

修：L131 追加 `assert result.error and ("exit code" in result.error or result.error.strip())`

### F-5 — DispatchResult dead import

**file**：`tests/skills/test_workflow_dispatcher_bash_skill_prompt.py:39`

rev2 删 MagicMock 后 `DispatchResult` 暴露为 dead（grep 计数=1，仅 import 行；同文件无构造/isinstance/类型注解）。

修：从 import 块删 `DispatchResult,`

---

## Follow-up

### F-2 — stdout-only 失败路径无测试

stderr=""/stdout 非空/returncode!=0 中间分支无独立测试。业务命令通常 stderr 写错误，非紧急。rev3+ 视进度补。

---

## Drop

| F-id | checker | severity | critic verdict | 理由 |
|---|---|---|---|---|
| F-6 | aux | minor | rejected | 项目未启 isort（pyproject ruff `select=['F']`），F-005/F-006 rev1 同款已 rejected |

---

## 裁决明细（全候选）

| F-id | checker | severity | critic verdict | 最终处置 |
|---|---|---|---|---|
| F-1 | error-handling | minor | not_rebutted | keep |
| F-2 | error-handling | minor | not_proven | follow-up |
| F-3 | design-consistency | critical | not_rebutted | **keep (blocker)** |
| F-4 | design-consistency | major | not_proven | keep (Judge 翻回 major) |
| F-5 | auxiliary-spec | minor | not_rebutted | keep |
| F-6 | auxiliary-spec | minor | rejected | drop |

verdicts: rejected=1 / not_proven=2 / not_rebutted=3

---

## rev3 修复指令（必跑）

### F-3 blocker（detailed-design.md §1.5 L159-162）

```text
L159 → if "skill" in node:                            return _dispatch_skill_node(node, run_state, env, jsonl_path)
L160 → if "prompt" in node or "prompt_file" in node:  return _dispatch_prompt_node(node, run_state, env, run_dir, root, jsonl_path)
L161 → if "bash" in node:                             return _dispatch_bash_node(node, run_state, env, run_dir, root, jsonl_path)
L162 → if "approval" in node:                         return _dispatch_approval_node(node, env, run_state, jsonl_path)
```

### F-4 major（workflow_dispatcher.py:348）

`substitute_vars(raw_prompt, run_state.node_outputs, env)` → 末尾加 `, escape_for_bash=True`

### F-1 minor（test_*.py:131）

末尾追加 `assert result.error and ("exit code" in result.error or result.error.strip())`

### F-5 minor（test_*.py:39）

import 块删 `DispatchResult,`

### trend-G-meta 终结硬规则

- 改动量 ≤ 10 行（4 spec + 1 code + 1 test + 1 import + 测试运行）
- 不引入任何范围外改动
- 全量跑 pytest tests/skills/test_workflow_dispatcher_*.py 必须 100% pass
- 同模式横扫：每改一处，grep 同款（F-3 改后再 grep §1.5 dispatch_node 调用是否还有遗漏；F-4 改后看其他 substitute_vars 调用是否一致）
- commit msg：`fix(F-006): rev3 闭合 spec §1.5↔§1.6 + approval escape + 2 minor（终结 trend-G-meta）`

目标：rev3 looks_clean / score ≥ 88 / 0 自引入。

---

## 报告文件

`requirements/REQ-2026-010/artifacts/review-20260511-223923.md`（本文件）
