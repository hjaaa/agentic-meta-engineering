# 代码审查报告 · REQ-2026-010 · F-011 rev2 · 2026-05-12 12:57:47

## 结论

**looks_clean** · 0 critical / 0 major / 5 minor（均为 rev1 既有 follow-up）· 分数 88/100（rev1 74 → +14）

- **`looks_clean` — AI 机器评估无阻塞问题** ← 本次结论
- `needs_attention` — AI 机器评估发现待改进项
- `blocked` — AI 机器评估发现关键问题

Supersedes：rev1 `REV-REQ-2026-010-code-F-011-001`（needs_attention 74）

## 路由说明

embedded 模式 / 单 feature F-011 / rev2 修复增量：

- **决策**：accept（命中 suggest）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-12 12:53:18
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker
- **跳过的 checker（含原因）**：security / concurrency / performance / history-context / auxiliary-spec（路径未命中 must/suggest）

## 待 sign-off 提示

本次评审输出 `conclusion = looks_clean`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人执行：

```bash
/code-review:signoff REV-REQ-2026-010-code-F-011-002 --decision=approved
# 或底层等价命令（需 tty）：
python3 scripts/lib/save_review.py signoff \
  --rev-id REV-REQ-2026-010-code-F-011-002 \
  --decision approved
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-011 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## rev1 keep findings 闭合状态（5/5 全闭）

| Finding | rev1 severity | rev2 处置 | 闭合验证 |
|---|---|---|---|
| F-7 dispatcher repo_root=sub_run_dir | major | **闭合** | workflow_dispatcher.py:434 已传 `repo_root=root` |
| F-7b 测试反向锁错误 | major | **闭合** | test_workflow_dispatcher_f011.py:236-238 断言翻成 `captured_kwargs.get("repo_root") == root` |
| F-6 loop 事件多写 run_id | minor | **闭合** | workflow_dispatcher.py:386-396 两条 loop_iteration_* 事件已无 run_id 字段 |
| F-9 缺连续 N 次序列断言 | minor | **闭合** | 新增 test_loop_dispatch_until_done_sequence（test:260-279），max_iterations=3 连续 dispatch + 模拟 workflow_continue +1，断言完整 `["loop_continue", "loop_continue", "loop_done"]` |
| F-8 loop_counters 内存 +1 不写事件 | major（rev1 已建议 follow-up）| **登记完成** | notes.md:91-105 含问题位置 / 影响 / 修复时机（F-008 收口前）/ 两种修复方向 / 引用 review，符合 rev1 suggestion 2 字面 |

未修的 F-1 / F-4 / F-5（rev1 minor，已声明 follow-up）：rev2 范围内未触及对应代码（dispatch_node 函数体、顶层 except），与 rev1 状态一致。

## rev2 自引入新 finding

**无。** 三 checker 独立扫描结论：

- **complexity-checker**：rev2 改动函数 `_dispatch_loop_node` / `_dispatch_sub_workflow_node` CC=3 / 行数<25 / 嵌套=2；新增测试 CC=2 / 16 行；全合规
- **error-handling-checker**：F-6 已闭；同模式扫描发现的入口层 `node_started/node_failed` run_id 字段是 rev1 base 既有（L132/162），非 rev2 引入
- **design-consistency-checker**：F-7 / F-7b / F-8 / F-9 全闭；§1.6 / §3 / §4 设计签名对齐；无新偏离

## trend-G-meta 状态

**未触发。** rev2 是纯修复 PR（+53 -12 / 4 文件），三 checker 全部确认零新引入。修复未反引入新坑——dispatch prompt 加同模式扫描规则后**第二次成功终结 trend-G-meta**（F-011 rev3 之后的延续）。

## Critical（必须修复）

（无）

## Major（强烈建议修复）

（无）

## Minor（rev1 既有 follow-up，rev2 未触及）

### [F-1-carry] dispatch_node 圈复杂度逼近阈值

**位置**：`scripts/lib/workflow_dispatcher.py:110-167`

dispatch_node 圈复杂度 9-10（阈值 10），严格算含 `or` 条件为 11。7 类节点封闭枚举短期可控。

**建议**：rev1 既有 follow-up，rev2 未触及该函数，保留不修。F-008 收口前如新增节点类型应同步重构为 dispatch table。

### [F-4-carry] dispatch_node except 丢栈

**位置**：`scripts/lib/workflow_dispatcher.py:155`

`except Exception as exc` 只记 `str(exc)`，丢失 traceback。

**建议**：rev1 既有 follow-up，可在 F-008 收口前加 `logger.exception` 记完整 traceback。

### [F-5-carry] workflow_run.main 异常被顶层吞

**位置**：`scripts/lib/workflow_dispatcher.py:434`

workflow_run.main 内部少数残余异常路径会穿透到 dispatch_node L155 被压缩为统一 `node_failed.error`。

**建议**：rev1 既有 follow-up，建议 F-008 收口窗口在 _dispatch_sub_workflow_node 内单独 try-except 细分错误类别。

### [F-entry-run-id] 入口层 node_started/node_failed 仍写 run_id

**位置**：`scripts/lib/workflow_dispatcher.py:132 / :162`

dispatch_node 入口层写的 `node_started` / `node_failed` 事件仍带 `run_id` 字段；skill/prompt/bash/approval/loop 五类节点内部写的事件均不带。同模式扫描发现的不一致点（rev1 base 既有，非 rev2 引入）。

**建议**：下一轮 dispatcher 统一选择"全写"或"全不写"，二选一对齐。

## Follow-up Notes（不阻塞，但值得提醒）

- **F-8（loop_counters 事件流）**：notes.md:91-105 已登记。F-008 收口前以事件流方式落地（两种修复方向已记录）。
- **dispatcher 入口层 run_id 字段对齐**：F-008 收口窗口一并清理。
- **dispatch_node 重构 dispatch table**：新增节点类型时一并做（防 CC 超阈）。

## 未运行的 checker

- ⚠️ security-checker（路径未命中 must/suggest）
- ⚠️ concurrency-checker（路径未命中 must/suggest）
- ⚠️ performance-checker（路径未命中 must/suggest）
- ⚠️ history-context-checker（路径未命中 must/suggest）
- ⚠️ auxiliary-spec-checker（路径未命中 must/suggest）

## 审查元信息

- **审查范围**：embedded / F-011 rev2 / 4 文件（+53 -12）
- **并行 checker**：3（complexity / error-handling / design-consistency）
- **rev2 keep finding 闭合率**：5/5（100%）
- **rev2 自引入**：0
- **综合模型**：code-quality-reviewer
- **Base SHA**：6ab086a（rev1 head）
- **Head SHA**：b58bd26（rev2 head）
- **Verdict 文件**：`requirements/REQ-2026-010/reviews/code-F-011-002.json`
- **Supersedes**：`REV-REQ-2026-010-code-F-011-001`（rev1）
