---
schema_version: "1.0"
feature_id: F-009
title: workflow_status.py 父子路径收敛到 sub_runs/
status: pending
complexity: trivial
depends_on: []
touches:
  - scripts/lib/workflow_status.py
created_at: "2026-05-11 14:03:20"
updated_at: "2026-05-11 14:03:20"
review_report: null
---

# F-009 · workflow_status.py 父子路径收敛到 sub_runs/

## 需求描述

把 workflow_status.py 中 nodes/<id>/run_id 间接索引改为直接迭代 run_dir/sub_runs/<node_id>/，子 run id 取目录名。与 workflow_rollback_subrun.py 既有约定对齐。

## 接口/数据结构

详见 `artifacts/detailed-design.md`。要点：
- 子 run 物理位置 = `<run_dir>/sub_runs/<node_id>/`
- workflow_status 直接 listdir 遍历，子 run id 即目录名
- 删除旧的 nodes/<id>/run_id 间接索引代码

## 依赖

### 依赖的 feature

无

### 依赖的模块

无（与 workflow_rollback_subrun.py 已对齐）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/workflow_status.py`

## 验收标准（features.json acceptance）

- status 输出的子 run 树与 rollback 跨父子归档前后看到的子 run 集合一致
- 无历史 nodes/ 残留路径（grep 确认）

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] grep 无残留旧路径
- [ ] status 输出与 rollback 一致性测试
- [ ] `/code-review` 通过
