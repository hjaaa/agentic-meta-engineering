---
schema_version: "1.0"
feature_id: F-010
title: AC-05 mock_agent_dispatch fixture + 替换 2 条占位 e2e
status: pending
complexity: heavy
depends_on:
  - F-008
touches:
  - tests/e2e/conftest.py
  - tests/e2e/test_code_review_embedded.py
  - tests/e2e/test_sub_workflow_lifecycle.py
created_at: "2026-05-11 14:03:20"
updated_at: "2026-05-11 14:03:20"
review_report: null
---

# F-010 · AC-05 mock_agent_dispatch fixture + 替换 2 条占位 e2e

## 需求描述

新建 tests/e2e/conftest.py 的 mock_agent_dispatch fixture（monkeypatch 替换 _dispatch_agent_node 返回预设 output）；重写 test_code_review_embedded.py 与 test_sub_workflow_lifecycle.py，删除文件头"不真启 subagent / 不真派 Agent"注释，断言 jsonl node_completed + output 非空。

## 接口/数据结构

详见 `artifacts/detailed-design.md`（AC-05 段）。fixture 形态：
```python
@pytest.fixture
def mock_agent_dispatch(monkeypatch):
    # monkeypatch.setattr("workflow_dispatcher._dispatch_agent_node", fake)
    ...
```
fake 函数返回 `DispatchResult(outcome='completed', data={'output': <预设>})`。

## 依赖

### 依赖的 feature

- F-008（main loop 已可端到端跑节点）

### 依赖的模块

- pytest monkeypatch（已有）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `tests/e2e/conftest.py`
- `tests/e2e/test_code_review_embedded.py`
- `tests/e2e/test_sub_workflow_lifecycle.py`

## 验收标准（features.json acceptance）

- test_code_review_embedded.py 头部注释不再含"不真启 subagent"字样
- test_sub_workflow_lifecycle.py 头部注释不再含"不真派 Agent"字样
- 至少一处断言：assert any(e['type']=='node_completed' and len(json.dumps(e['data']['output']))>0 ...)
- mock fixture 不调用真实 Claude API

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] fixture 不调真实 Claude API（无网络/无 anthropic.Client）
- [ ] 两文件头注释清理干净
- [ ] node_completed + output 非空断言到位
- [ ] `/code-review` 通过
