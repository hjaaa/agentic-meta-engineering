---
schema_version: "1.0"
feature_id: F-005
title: workflow_dispatcher.py 框架（DispatchResult + 7 类节点 stub）
status: pending
complexity: medium
depends_on:
  - F-004
touches:
  - scripts/lib/workflow_dispatcher.py
  - scripts/lib/run_state.py
created_at: "2026-05-11 14:03:20"
updated_at: "2026-05-11 14:03:20"
review_report: null
---

# F-005 · workflow_dispatcher.py 框架（DispatchResult + 7 类节点 stub）

## 需求描述

新建 workflow_dispatcher.py：定义 DispatchOutcome / DispatchResult / dispatch_node 入口；为 7 类节点提供 stub 函数（先返回 DispatchResult(outcome='completed')），逐 feature 替换。配合 _build_env 注入变量。

## 接口/数据结构

详见 `artifacts/detailed-design.md` §5.1。DispatchOutcome 枚举 7 个值：
`completed / failed / approval_pending / loop_continue / loop_done / sub_workflow_pending / skipped`。
dispatch_node 入口签名：
```python
def dispatch_node(node: NodeSpec, run_state: RunState, ctx: DispatchContext) -> DispatchResult: ...
```

## 依赖

### 依赖的 feature

- F-004（template 必须先参数化，引擎注入的变量才能用上）

### 依赖的模块

- `scripts/lib/run_state.py`（扩展事件 schema + loop_counters）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/workflow_dispatcher.py`（新建）
- `scripts/lib/run_state.py`（扩展）

## 验收标准（features.json acceptance）

- dispatch_node 7 类节点匹配正确（agent/skill/prompt/bash/approval/loop/sub_workflow）
- 每次调用写 node_started + node_completed 事件
- 未知节点类型抛 WorkflowError 并写 node_failed
- _build_env 含 RUN_ID/RUN_DIR/META_PATH/ARTIFACTS_DIR/ARGUMENTS + 已 completed 节点 output

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] DispatchResult 7 outcome 枚举与详细设计一致
- [ ] 单元测试覆盖 7 类节点 stub + 未知类型异常路径
- [ ] _build_env 变量注入完整
- [ ] `/code-review` 通过
