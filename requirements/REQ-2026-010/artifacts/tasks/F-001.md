---
schema_version: '1.0'
feature_id: F-001
title: _generate_req_id + REQ-YYYY-NNN 编号策略
status: in-progress
complexity: light
depends_on: []
touches:
- scripts/lib/workflow_run.py
- tests/skills/test_workflow_commands.py
- requirements/REQ-2026-010/artifacts/features.json
created_at: '2026-05-11 14:03:20'
updated_at: '2026-05-11 14:30:53'
review_report: artifacts/review-20260511-142322.md
---

# F-001 · _generate_req_id + REQ-YYYY-NNN 编号策略

## 需求描述

在 workflow_run.py 中新增 _generate_req_id 函数，扫 requirements/ 下现有 REQ-YYYY-NNN 取 max+1，原子化建顶层目录，EEXIST 重试 3 次。与已有 _generate_run_id 一致的并发安全策略。

## 接口/数据结构

详见 `artifacts/detailed-design.md` _generate_req_id 段。契约要点：
- 返回 str，格式 `REQ-YYYY-NNN`（NNN 三位零填充）
- 内部 os.mkdir 原子化建 requirements/<id>/ 顶层目录
- 遇 EEXIST 最多重试 3 次再上抛

## 依赖

### 依赖的 feature

无

### 依赖的模块

无

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/workflow_run.py`

## 验收标准（features.json acceptance）

- 调用返回 'REQ-2026-XXX' 格式的字符串
- 并发 3 个进程同时调用，无 EEXIST 冲突未消化
- max+1 策略与 _generate_run_id 一致；通过现有单测模式覆盖

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 单元测试覆盖主流程
- [ ] 边界条件处理（空 requirements/ 目录、年份跨越）
- [ ] 无明显安全/并发问题
- [ ] `/code-review` 通过
