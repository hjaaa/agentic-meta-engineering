---
schema_version: "1.0"
feature_id: F-004
title: standard-8phase.yaml 7 处节点路径参数化
status: pending
complexity: light
depends_on:
  - F-002
touches:
  - .claude/workflows/requirement/standard-8phase.yaml
created_at: "2026-05-11 14:03:20"
updated_at: "2026-05-11 14:03:20"
review_report: null
---

# F-004 · standard-8phase.yaml 7 处节点路径参数化

## 需求描述

把所有 'runs/$RUN_ID/' 字面量替换为 $RUN_DIR 或 $META_PATH（视上下文）；archive-finalize 节点的分支名也用 $RUN_ID 派生。引擎注入这些变量。

## 接口/数据结构

详见 `artifacts/detailed-design.md` §5.2。变量约定：
- `$RUN_DIR` = 顶层 run 目录（requirements/<id>/ 或 runs/<id>/，D-007 双路径）
- `$META_PATH` = `$RUN_DIR/meta.yaml`
- `$ARTIFACTS_DIR` = `$RUN_DIR/artifacts`
- `$RUN_ID` 仅作派生 branch / id 等场景使用

## 依赖

### 依赖的 feature

- F-002（_bootstrap_requirement 必须先把 $RUN_DIR / $META_PATH 准备好）

### 依赖的模块

无

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `.claude/workflows/requirement/standard-8phase.yaml`

## 验收标准（features.json acceptance）

- grep 'runs/\$RUN_ID/' .claude/workflows/requirement/standard-8phase.yaml 返回 0 个匹配
- bash 节点替换后语义不变（yq 命令针对 $META_PATH 写入）
- D-007 双路径下：requirements/<id>/ 与 runs/<id>/ 两种 run 都能跑过 archive-finalize

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 替换前后 yaml 语义一致（grep + diff 双重核对）
- [ ] 双路径都能跑通 archive-finalize
- [ ] `/code-review` 通过
