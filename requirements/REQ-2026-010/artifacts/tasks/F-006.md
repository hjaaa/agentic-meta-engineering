---
schema_version: "1.0"
feature_id: F-006
title: bash/skill/prompt 节点完整实现
status: done
complexity: medium
depends_on:
  - F-005
touches:
  - scripts/lib/workflow_dispatcher.py
  - scripts/lib/run_state.py
  - tests/skills/test_workflow_dispatcher_bash_skill_prompt.py
  - tests/skills/test_workflow_dispatcher_dispatch.py
  - requirements/REQ-2026-010/artifacts/detailed-design.md
created_at: "2026-05-11 14:03:20"
updated_at: "2026-05-11 22:55:00"
review_report: artifacts/review-20260511-225100.md
---

# F-006 · bash/skill/prompt 节点完整实现

## 需求描述

实现 _dispatch_bash_node（subprocess.run + 变量预替换 escape_for_bash=False）/ _dispatch_skill_node（主 Claude 跑 skill）/ _dispatch_prompt_node（主 Claude 跑 prompt 字符串）。

## 接口/数据结构

详见 `artifacts/detailed-design.md` §5.1。关键约束：
- bash 节点 subprocess returncode!=0 → DispatchOutcome.failed，error=stderr
- 变量替换走 `substitute_vars` escape_for_bash=False（裸字面值）
- prompt/skill 节点：output 写入 node_completed.data.output

## 依赖

### 依赖的 feature

- F-005（DispatchResult / dispatch_node 框架已就位）

### 依赖的模块

- `substitute_vars`（已有）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/workflow_dispatcher.py`
- `scripts/lib/run_state.py`
- `tests/skills/test_workflow_dispatcher_bash_skill_prompt.py`

## 验收标准（features.json acceptance）

- bash 节点：subprocess returncode!=0 转 node_failed，error=stderr
- bash 节点：$RUN_DIR/$META_PATH 等变量被预替换为裸字面值（escape_for_bash=False）
- prompt/skill 节点：主 Claude 调用后 output 写入 node_completed.data.output

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] 3 类节点单测覆盖
- [ ] bash 失败路径 error 字段完整
- [ ] 变量替换 escape 模式正确
- [ ] `/code-review` 通过
