---
schema_version: "1.0"
feature_id: F-003
title: 挂载 load_workflow schema 校验
status: pending
complexity: trivial
depends_on: []
touches:
  - scripts/lib/workflow_run.py
  - tests/skills/test_workflow_commands.py
  - tests/skills/test_workflow_bootstrap.py
created_at: "2026-05-11 14:03:20"
updated_at: "2026-05-11 22:30:00"
review_report: null
---

# F-003 · 挂载 load_workflow schema 校验

## 需求描述

workflow_run.py main() 在 glob 找到模板后、生成 ID 前插入 load_workflow(template_path) 调用；若 report.errors > 0 则打印 report.render() + exit 1。

## 接口/数据结构

详见 `artifacts/detailed-design.md`。关键点：
- 调用 `workflow_loader.load_workflow(path)` 返回 `(Workflow, ValidationReport)`
- errors > 0 → stderr 输出 `report.render()` + sys.exit(1)
- 校验通过才继续后续 _generate_req_id / _bootstrap

## 依赖

### 依赖的 feature

无

### 依赖的模块

- `scripts/lib/workflow_loader.py`（已有）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。
本正文段是供人快速参考的副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/workflow_run.py`
- `tests/skills/test_workflow_commands.py`
- `tests/skills/test_workflow_bootstrap.py`

## 验收标准（features.json acceptance）

- 构造 schema 不合法的 yaml，run 命令 exit 1 且 stderr 含校验报告
- schema 合法的现有模板（standard-8phase / code-review-embedded）不被误判

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] 接口实现符合详细设计
- [ ] 单元测试覆盖主流程
- [ ] 边界条件处理
- [ ] 无明显安全/并发问题
- [ ] `/code-review` 通过
