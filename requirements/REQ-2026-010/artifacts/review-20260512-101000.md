# 代码审查报告 · REQ-2026-010 · 2026-05-12 10:10:00 · F-008 rev2

## 结论

**needs_attention** · 0 critical / 0 major / 5 minor

- rev1 13 keep + 1 follow-up 全部闭合 ✅
- rev2 自引入 5 minor（其中 F-NEW-1 与 rev1 follow-up-1 同模式 dead import — meta 信号差）
- **trend-G-meta 第 4 次循环本 rev 未终结**（与 F-007 rev2→rev3 同模式，建议派 rev3 闭合 30 min 以内）
- 综合 score：78（高于 rev1 62；低于 F-007 rev3 90）

## 路由说明

- **决策**：accept（F-014 自动路由）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-12 09:55:38（rev2 路由）
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker / auxiliary-spec-checker
- **跳过的 checker**：security / concurrency / performance / history-context（路径未命中 must/suggest）

## 待 sign-off 提示

本次 `conclusion = needs_attention`，**这是 AI 机器评估**，不代表合并通过。trend-G-meta 第 4 次循环未终结，**强烈建议派 rev3 闭合 5 minor 后再 signoff 最新 rev**。

如确需 signoff 当前 rev：

```bash
/code-review:signoff REV-REQ-2026-010-code-F-008-002
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-008 转 done。

## 裁决明细

- **F-NEW-1** [aux=major / EH=minor] → critic: not_rebutted → **最终处置：keep (minor)**
  - 裁决依据：rev1 follow-up-1 同模式当时未分级 → 本次升 major 缺一致性；单点 2 行 import 无运行时影响；但 meta 信号差进 cross_dimension_insights。
- **F-NEW-2** [aux+EH minor] → critic: not_rebutted → **keep (minor)**
  - 裁决依据：TC-F8-6 已正确用 capsys 验证 stderr，TC-F8-7 既无 capsys 注入也无 stderr 断言 — 与 docstring 承诺脱节。
- **F-NEW-3** [aux minor] → critic: not_rebutted → **keep (minor)**
  - 裁决依据：TC-F8-8 docstring 第一句指错测试对象（`_main_loop` 应为 `_handle_failure`）；第二句澄清，属 cosmetic。
- **F-NEW-4** [aux minor] → critic: not_rebutted → **keep (minor)**
  - 裁决依据：`workflow_dispatcher.py:173-184` 实为 `_dispatch_agent_node` 2 行 stub，docstring 风格反例；新函数应参照 `:185-200 _dispatch_skill_node`。
- **F-NEW-5** [DC minor] → critic: not_rebutted → **keep (minor)**
  - 裁决依据：grep detailed-design.md 仅找到 §1.2 + §1.7，无 §1.2.2 锚点。
- **F-NEW-6** [DC spec-lag] → critic: not_rebutted → **follow-up（独立 PR）**
  - 裁决依据：`_handle_failure -> bool` 行为改进正确；归 spec-lag follow-up，独立 PR 修 spec 签名。
- **F-NEW-7** [complexity minor] → critic: not_rebutted → **keep (minor)**
  - 裁决依据：wc -l 520 行（4% over），subagent 已自报；建议按 outcome 路由再拆。
- **F-SL-1** [DC spec-lag] → **follow-up**：detailed-design.md §1.7 伪代码 `failed → break` 与 _handle_failure 返回 bool 矛盾。
- **F-SL-2** [DC spec-lag] → **follow-up**：run_state.py:177 node_failed rebuild current_node=None 与 spec §2.1 不一致；F-008 通过重读 jsonl 绕开。
- **F-FU-1** [EH 既有] → **follow-up**：main() 启动时未消费 run_state.warnings。
- **F-FU-2** [EH 既有] → **follow-up**：main() 仅 catch WorkflowError，非 WorkflowError 异常穿透。

## Critical（必须修复）

无。

## Major（强烈建议修复）

无。

## Minor（改进建议）

### [aux+EH / dead_import / trend-G-meta] tests/skills/test_workflow_continue_failure_handler.py:255-256

TC-F8-7 函数体内 `import io` + `import unittest.mock as mock` dead import（函数体未使用），与 rev1 follow-up-1 同模式 dead import 治标不治本信号。

**建议**：直接删 2 行 import。

### [aux+EH / doc_drift] tests/skills/test_workflow_continue_failure_handler.py:247-269

TC-F8-7 docstring 承诺"stderr 含 WARN"，但函数签名无 capsys + 函数体无 stderr 断言。

**建议**：
```python
def test_handle_abort_unknown_on_failure_degrades_and_writes_workflow_failed(self, jsonl_path, capsys):
    ...
    captured = capsys.readouterr()
    assert "WARN" in captured.err
```

### [aux / doc_drift] tests/skills/test_workflow_continue_failure_handler.py:281

TC-F8-8 docstring 写"_main_loop 默认走 retry 路径"，但函数体实际调 `_handle_failure`。

**建议**：docstring 改 `_main_loop` → `_handle_failure`。

### [aux / doc_drift / trend-G-meta] scripts/lib/workflow_continue.py:64/123/165

`_handle_retry / _handle_skip / _handle_abort` docstring 写"参照 workflow_dispatcher.py:173-184 _dispatch_*_node 风格"。但 :173-184 实际是 `_dispatch_agent_node` 2 行 stub（F-010 占位），与新函数多段 docstring 不匹配。

**建议**：参照行改 `:185-200 _dispatch_skill_node`（或 `:257 _dispatch_bash_node`），或直接删该参照说明。

### [design-consistency / doc_drift / trend-G-meta] scripts/lib/workflow_continue.py:9

模块 docstring 引"详细设计 §1.2.2 / §1.7"。detailed-design.md 仅有 §1.2（_bootstrap_requirement）+ §1.7，无 §1.2.2 子节。

**建议**：改 `§1.2.2 / §1.7` → `§1.7`（模块仅对应 §1.7）。

### [complexity] tests/skills/test_workflow_continue_main_loop.py

520 行超 spec 500 上限 20 行（4%）。

**建议**：按 outcome 路由再拆，将 approval_pending / loop_* / sub_workflow_* 用例移至独立文件，每文件 ≤ 400 行。

## Follow-up Notes（不阻塞，但值得提醒）

- **F-NEW-6**（spec-lag · DC）：`_handle_failure -> bool` 行为改进正确，独立 PR 同步 spec §1.7:236 签名。
- **F-SL-1**（spec-lag · DC）：detailed-design.md §1.7 伪代码 `failed → break` 与实际 bool 返回矛盾。
- **F-SL-2**（spec-lag · DC）：run_state.py:177 node_failed rebuild current_node=None 与 spec §2.1 不一致；F-008 已通过重读 jsonl 绕开。
- **F-FU-1**（既有 · EH）：main() 启动时未消费 run_state.warnings。
- **F-FU-2**（既有 · EH）：main() 仅 catch WorkflowError；建议补 except Exception → workflow_failed 写盘 + exit 1。

建议把三处 spec-lag 合并开"detailed-design / run_state SD 同步 PR"统一收口。

## 未运行的 checker

- ⚠️ security-checker / concurrency-checker / performance-checker / history-context-checker（路径未命中 must/suggest 任何规则）

## 跨维度洞察（Judge 综合）

1. **trend-G-meta 第 4 次循环本 rev 未终结**：rev1 follow-up-1 刚清完 module-level dead import，rev2 在新增 TC-F8-7 时又引入函数体内 dead import — 改一处同模式应全文 grep 检查；建议 PR 自检清单纳入"dead import 自检"项。
2. **F-NEW-1 × F-NEW-2 在同一测试用例**：TC-F8-7 docstring 承诺验证 stderr 但既无 capsys 注入也未断言，叠加 dead import → 表明该用例编写时未完整执行 "docstring → 断言 → 清理 import" 闭环。
3. **spec-lag follow-up 三连**：(a) §1.7 `_handle_failure -> None` vs `-> bool`；(b) §1.7 / §2.1 workflow_failed 事件 node_id 位置；(c) run_state.py:177 node_failed rebuild current_node=None vs spec §2.1 — 合并开 SD 同步 PR。

## 审查元信息

- **审查范围**：embedded mode / F-008 rev2（6 文件，+638 -304 行）
- **并行 checker**：4
- **Critic 统计**：rejected 0 / not_proven 0 / not_rebutted 7
- **综合模型**：code-quality-reviewer (Judge)
- **Base SHA**：247aed9 (rev1)
- **Head SHA**：71ce9b0 (rev2)
- **rev-id**：REV-REQ-2026-010-code-F-008-002（supersedes 001）
- **verdict 文件**：reviews/code-F-008-002.json
