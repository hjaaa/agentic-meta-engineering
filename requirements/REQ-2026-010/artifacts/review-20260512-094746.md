# 代码审查报告 · REQ-2026-010 · 2026-05-12 09:47:46 · F-008 rev1

## 结论

**blocked** · 2 critical / 6 major / 5 minor

- AI 机器评估发现关键问题（2 critical 行为偏离 + 死代码），必须 rev2 闭合才能继续。
- trend-G-meta 第 4 次循环命中（F-005/F-006/F-007 三连终结后再现）：docstring 与代码/spec 不一致复利债。
- 综合 score：62（与 F-005 rev1 62 持平，低于 F-007 rev1 75）。

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 自动决策（F-014+ 取消 tty 卡点）：

- **决策**：accept
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-12 09:37:11
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker / auxiliary-spec-checker
- **跳过的 checker（含原因）**：
  - security-checker：路径未命中 must/suggest 任何规则
  - concurrency-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则

## 待 sign-off 提示

本次评审输出 `conclusion = blocked`，**这只是 AI 的机器评估**，不代表合并通过。

考虑到 blocked 必须先派 rev2 闭合 critical/major，**当前不建议直接 signoff**；rev2 提交并 review 完成后再 signoff 最新 rev。如确需 signoff 当前 rev 的 verdict（仅作历史留痕），开发者本人执行：

```bash
/code-review:signoff REV-REQ-2026-010-code-F-008-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-010-code-F-008-001 --trivial  # 纯文档变更快速通道（本 rev 不适用）
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-008 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示 "reviewer 发现 → critic 反证 → Judge 裁决" 的三方透明度。

- **F-1** [design-consistency · critical] → critic: not_rebutted → **最终处置：keep（critical）**
  - 裁决依据：亲自比对 workflow_continue.py:260 与 detailed-design.md:226 — 默认值字面不同 ("abort" vs "retry")，行为路径完全反转（首次失败 retry vs abort）。critic 未给反证，独立调研确认。
- **F-2** [design-consistency · critical] → critic: not_rebutted → **最终处置：keep（critical）**
  - 裁决依据：workflow_dispatcher.py:36-44 Literal 严格 7 项无 skipped；spec §1.7:213-228 路由表也列了 skipped 但 dispatcher 实际不返回 — 三方分歧已是 F-008 自己引入的死分支，必须在本 PR 闭合（spec 侧可单独 follow-up）。
- **F-3** [design-consistency · major] → critic: not_rebutted → **最终处置：keep（major）**
  - 裁决依据：spec §2.1 表与 §1.7:258 要求 node_id 在 data 内；代码 line 108 把 node_id 放顶层 — RunState.rebuild 反扫读 data.node_id 会失败。
- **F-4** [design-consistency · major] → critic: not_rebutted → **最终处置：keep（major）**
  - 裁决依据：spec §2.1 node_skipped data 字段定义 'reason: <str>'（实际错误），代码 line 126 硬编码 'on_failure=skip'；line 127 original_error 字段在 spec 未列出 — 超规范扩展。
- **F-5** [error-handling · major] → critic: not_rebutted → **最终处置：keep（major）**
  - 裁决依据：line 83 `events, _ = read_events(...)` 显式丢弃 warnings；read_events 文档（run_state.py:225）说损坏行进 warnings — 一旦 jsonl 末尾损坏，node_failed 事件丢失 → fail_count 低估 → retry 永远 < max_retries → 无限循环。
- **F-6** [complexity · major] → critic: not_rebutted → **最终处置：keep（major）**
  - 裁决依据：实测 56-164 行共 109 行（含空行/注释），逻辑代码约 80-90 行；3 个 if-elif-else 主分支 + retry 内嵌 if + max_retries 比较 + abort 退路 ≈ CC 10-11。CLAUDE.md ≤80 行 / ≤10 CC 双超。
- **F-7** [complexity · major] → critic: not_rebutted → **最终处置：keep（major）**
  - 裁决依据：wc -l 实测 640 行，spec 建议 500 行上限明确超出 28%。
- **F-8** [auxiliary-spec · major] → critic: not_rebutted → **最终处置：keep（major）**
  - 裁决依据：_advance_after_completed line 180 硬编码 state='completed'，但 line 172 docstring 声称覆盖 skipped 场景 — 死分支被 F-2 处理后自然消失，当前 docstring 仍属文档与代码矛盾。
- **F-9** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep（minor）**
  - 裁决依据：trend-G-meta 第 4 次循环命中：docstring 复利债。建议 rev2 同时改 docstring + 代码 + spec。
- **F-10** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep（minor）**
  - 裁决依据：F-008.md frontmatter touches 仅 workflow_continue.py 1 项，实际 diff 含测试 +281 行 — touches_guard 软拦截会触发 WARN。
- **F-11** [error-handling · minor] → critic: not_rebutted → **最终处置：keep（minor）**
  - 裁决依据：line 272-278 else 仅 break + state='running' — 下次 continue 会复现，死循环风险确实存在；触发概率低 → minor 合理。
- **F-12** [auxiliary-spec · — ] → critic: rejected → **最终处置：drop**
  - 裁决依据：与 F-8 同根因（_advance_after_completed state 硬编码），不重复计入。
- **F-13** [complexity · minor] → critic: not_proven → **最终处置：keep（minor）**
  - 裁决依据：critic 称 CC 12 无工具佐证。独立调研 _main_loop while + 7 个 elif + 内嵌 if 估算 CC ≈ 11-12；分支结构清晰可数 → 翻回 keep 但保持 minor（属路由表自然分支）。
- **F-14** [auxiliary-spec · — ] → critic: rejected → **最终处置：drop**
  - 裁决依据：base_sha 无此 import，是 F-008 新加；reviewer "加重既有乱序" 前提错误。
- **F-15** [auxiliary-spec · minor] → critic: not_rebutted → **最终处置：keep（minor）**
  - 裁决依据：TC-F8-1~4 注释 grep 实测确实缺 '# ===' 分隔符；TC-F8-5 有 — 单文件内风格不一致。
- **F-16** [auxiliary-spec · — ] → critic: rejected → **最终处置：drop**
  - 裁决依据：与 F-8 同根因，不重复计入。
- **F-follow-up-1** [history-context · — ] → critic: — → **最终处置：follow-up**
  - 裁决依据：F-007 rev3 遗留 dead import (test_workflow_commands_advanced.py:36)，建议 rev2 顺带闭合，不计入 F-008 keep。

## Critical（必须修复）

### [design-consistency / behavior_deviation] scripts/lib/workflow_continue.py:260

`on_failure = node.get("on_failure", "abort")` 与 `detailed-design.md §1.7:226` 字面不同。spec 默认 `"retry"` — 节点首次失败时应静默重试等待下次 continue；实现默认 `"abort"` — 立即写 `workflow_failed` 终结 run。两者行为完全相反。

**建议**：`node.get("on_failure", "abort")` → `node.get("on_failure", "retry")`。

### [design-consistency / dead_code / trend-G-meta] scripts/lib/workflow_continue.py:267-269

`elif outcome == "skipped"` 是死分支。已交叉验证：
- `workflow_dispatcher.py:36-44` `DispatchOutcome` Literal 严格 7 项（completed / failed / approval_pending / loop_continue / loop_done / sub_workflow_pending / sub_workflow_done），不含 skipped
- spec §1.7 路由表列了 skipped 但 dispatcher 实际不会返回 — 三方分歧（spec ↔ dispatcher ↔ main loop）
- F-008 沿用 spec 错误描述实现了不可达代码

**建议**：删除 line 267-269 死分支 + 同步改 line 1-9 模块 docstring（移除 "skipped" 第 8 outcome 表述）+ line 204 `_main_loop` docstring + spec §1.7 路由表 skipped 行（spec 改动可走 follow-up PR）。

## Major（强烈建议修复）

### [design-consistency / event_schema] scripts/lib/workflow_continue.py:107-113, 155-162

`workflow_failed` 事件 `node_id` 放事件顶层（line 108）而 spec §1.7:258 + §2.1 要求 `data.node_id`。RunState.rebuild 反扫读 `data.node_id` 时会读不到 → 失败节点定位失败。

**建议**：把 `node_id` 移到 `data` 内，与 `node_completed` / `node_failed` 事件结构对齐。

### [design-consistency / event_schema] scripts/lib/workflow_continue.py:120-130

`node_skipped` 事件 `data` 字段偏离 spec §2.1：`reason` 硬编码 `"on_failure=skip"` 而 spec 要求实际错误信息；新增 `original_error` 字段超规范。

**建议**：`reason = error or "on_failure=skip 但无具体错误"`；移除 `original_error` 字段。

### [error-handling / observability] scripts/lib/workflow_continue.py:83

`events, _ = read_events(jsonl_path)` 丢弃 warnings。`read_events` 文档（run_state.py:225）说损坏行进 warnings — 一旦 jsonl 末尾损坏，node_failed 事件丢失 → fail_count 低估 → retry 永远 < max_retries → **无限循环风险**。可观测性盲区。

**建议**：
```python
events, warnings = read_events(jsonl_path)
if warnings:
    print(f"WARN: jsonl 含 {len(warnings)} 行损坏，retry 计数可能偏低: {warnings}", file=sys.stderr)
```

### [complexity] scripts/lib/workflow_continue.py:56-164

`_handle_failure` 93 代码行 / CC ≈ 11 双超阈值（CLAUDE.md ≤80 行 / ≤10 CC）。

**建议**：拆 `_handle_retry` / `_handle_skip` / `_handle_abort` 三个私有助手，`_handle_failure` 退化为路由（CC 降至 4）。

### [complexity] tests/skills/test_workflow_continue_main_loop.py

测试文件 640 行超 spec 500 行建议上限（超 28%）。

**建议**：按 outcome 主题拆 3-4 文件，例如 `test_workflow_continue_failure_handler.py` 承接 TC-F8 失败矩阵单测。

### [auxiliary-spec / doc_drift] scripts/lib/workflow_continue.py:167-184

`_advance_after_completed` docstring 声称覆盖 4 场景（含 skipped），但 line 180 硬编码 `state='completed'`。因 F-2 已证 skipped 是死分支，functional 无运行时影响，但 docstring 与代码矛盾误导后续维护。

**建议**：F-2 删除 skipped 分支后，同步把本 docstring 缩为 3 场景（completed / loop_done / sub_workflow_done）。

## Minor（改进建议）

### [design-consistency / doc_drift / trend-G-meta] scripts/lib/workflow_continue.py:1-9, 204

模块 docstring + `_main_loop` docstring 把 skipped 列为第 8 outcome 失实。

**建议**：与 F-2 合并修复。

### [design-consistency / frontmatter] requirements/REQ-2026-010/artifacts/tasks/F-008.md:10-12

`touches` 未列 `tests/skills/test_workflow_continue_main_loop.py`（实际 diff 含 +281 行）。

**建议**：frontmatter `touches` 补充测试文件路径。

### [error-handling / infinite_loop] scripts/lib/workflow_continue.py:272-278

未知 outcome else 分支仅 WARN+break 但 `run_state.state` 留 `"running"` → 下次 continue 再撞同节点同 outcome → 静默死循环。

**建议**：写 `workflow_failed` 事件 + `run_state.state="failed"`。

### [complexity] scripts/lib/workflow_continue.py:187

`_main_loop` CC ≈ 11-12 超 10（critic not_proven 但实际 8 个 if-elif 分支 + 内嵌 if + while，明显超）— 降级为 minor 因属路由表式自然分支。

**建议**：outcome → handler 字典映射重构（与 F-005 rev3 G-5 `STATE_EVENT_MAP` precedent 同款解法）。

### [auxiliary-spec / code_style] tests/skills/test_workflow_continue_main_loop.py:460/483/509/534

TC-F8-1~4 注释缺 `# ===` 分隔区块，与 TC-F7-x / TC-F8-5 风格不一致。

**建议**：补齐分隔符，与既有风格统一。

## Follow-up Notes（不阻塞，但值得提醒）

- **F-007 遗留 dead import**：`tests/skills/test_workflow_commands_advanced.py:36` 顶层 `import workflow_run`，F-007 rev3 标记 follow-up 由 F-008 顺带处理；建议 rev2 顺手闭合（来自 F-follow-up-1）。

## 未运行的 checker

- ⚠️ security-checker（路径未命中 must/suggest 任何规则）
- ⚠️ concurrency-checker（路径未命中 must/suggest 任何规则）
- ⚠️ performance-checker（路径未命中 must/suggest 任何规则）
- ⚠️ history-context-checker（路径未命中 must/suggest 任何规则）

## 跨维度洞察（Judge 综合）

1. **trend-G-meta 第 4 次循环命中**：F-005/F-006/F-007 三连终结后 F-008 再现（F-2 + F-8 + F-9 同模式 — 改代码不改 docstring/spec 的复利债）。F-007 rev3 自评 "1 误标 0 keep" 后续 PR 立即再现 — 建议把 "docstring 与代码一致性" 升为 design-consistency-checker 的明确专项规则。
2. **失败处理路径可观测性盲区**：F-1（默认 abort）× F-11（未知 outcome state 留 running）× F-5（warnings 丢弃）三者组合 — 一旦节点首次失败立即 abort、jsonl 末尾损坏被吞、未知 outcome 静默死循环，事故复盘链断裂。rev2 应同时修这三点，并在 abort 路径补 "本次 abort 来自默认策略，非显式配置" 提示。
3. **spec ↔ dispatcher ↔ main loop 三方分歧（F-2）暴露 SSOT 缺失**：建议把 `DispatchOutcome` 7 项作为 SSOT，spec 引用代码而非反过来；这是后续 F-010/F-011 接入 agent/loop/sub_workflow 时必须先解决的债。

## 审查元信息

- **审查范围**：embedded mode / F-008（2 文件，+465 -52 行）
- **并行 checker**：4（按 routing 自动决策；4 个跳过）
- **Critic 统计**：rejected 3 / not_proven 1 / not_rebutted 12
- **综合模型**：code-quality-reviewer (Judge)
- **Base SHA**：a5878ab
- **Head SHA**：247aed9
- **rev-id**：REV-REQ-2026-010-code-F-008-001
- **verdict 文件**：reviews/code-F-008-001.json
