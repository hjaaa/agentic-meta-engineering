# 代码审查报告 · REV-REQ-2026-010-code-F-006-001

**时间**：2026-05-11 22:10:41  
**模式**：嵌入 / REQ-2026-010 / F-006  
**reviewed_commit**：`4c3c4d2`（base：`517c090` F-005 last commit）  
**范围**：5 files / 598 insertions / 21 deletions

## 路由说明

- **决策**：`accept`（routing.py 自动）
- **确认人**：hj19961223@gmail.com（git config）
- **确认时间**：2026-05-11 21:59:05
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker / auxiliary-spec-checker（命中 suggest 规则 3 / must 0 / trivial 1）
- **跳过的 checker**：
  - `security-checker`：路径未命中 must/suggest 任何规则
  - `concurrency-checker`：路径未命中 must/suggest 任何规则
  - `performance-checker`：路径未命中 must/suggest 任何规则
  - `history-context-checker`：路径未命中 must/suggest 任何规则

---

## 结论

**verdict**：`looks_clean`  
**score**：88 / 100  
**严重度分布**（裁决后）：0 critical / 1 major / 8 minor  
**follow-up**：2 项

> ⚠️ Judge 给的是 `looks_clean`（score 88），但同时列了 9 项 keep finding（含 1 major 注释失实），明显边界态势——本次评审输出 conclusion = looks_clean，这只是 AI 的机器评估，不代表合并通过。建议在 signoff 前先派 rev2 闭合 keep（预计 ≤ 30min），或人类裁定接受 trivial 通道。

---

## Critical / Major findings

### F-15 (major) — 4 处失实/陈旧注释

**file**：`scripts/lib/workflow_dispatcher.py:170, 180, 191, 225`

- L170 章节 banner『7 类节点 stub 函数（F-006/F-007/F-011 替换真实逻辑）』——F-006 已落地，bash/skill/prompt 不再是 stub
- L180 `_dispatch_agent_node` docstring『真实逻辑在 F-006 实现』——事实错误：F-006 实现的是 bash/skill/prompt，agent 属 F-010
- L191 `_dispatch_skill_node` docstring『主 Claude 集成层 stub 语义』——已落地 substitute_vars 真实现
- L225 `_dispatch_prompt_node` docstring 同款 stub 措辞

trend-G-meta 历史显示注释失实是 reviewer 高敏感点；rev2 必修。

---

## Minor findings（keep）

### F-1 — test /tmp 硬编码

**file**：`tests/skills/test_workflow_dispatcher_bash_skill_prompt.py:326-350`

`test_skill_node_args_node_output_substituted` 签名拿了 `jsonl_path` 形参却不用，line 328 自建 `Path("/tmp/test_skill_noderef.jsonl")` 反复 unlink。anti-pattern + 与同文件 13 个其他用 tmp_path 的测试不一致。pyproject 暂无 xdist 故并发风险低，但仍应统一。

### F-4 — bash 失败 error 字段空兜底

**file**：`scripts/lib/workflow_dispatcher.py:316-321`

returncode!=0 且 stderr 为空时，`node_failed.data.error=""`，下游无诊断信息。建议 `error = stderr or stdout or f"exit code {proc.returncode}"`。

### F-8 — spec-lag：dispatch_node prompt_file 文档未同步

**file**：`requirements/REQ-2026-010/artifacts/detailed-design.md` §1.6

L140-142 已加 `or 'prompt_file' in node`；§1.5:160 伪代码未同步。

### F-9 — spec-lag：approval 签名文档未同步

**file**：`requirements/REQ-2026-010/artifacts/detailed-design.md` §1.6

实际 `(node, env, run_state, jsonl_path)` vs 文档 `(node, env, jsonl_path)`。

### F-10 — spec-lag：approval escape_for_bash 文档措辞

**file**：`requirements/REQ-2026-010/artifacts/detailed-design.md` §2.3

L346 `substitute_vars(..., env)` 默认 `escape_for_bash=False`；文档"其它节点默认 True"措辞有歧义。

### F-11 — MagicMock dead import

**file**：`tests/skills/test_workflow_dispatcher_bash_skill_prompt.py:25`

`from unittest.mock import patch, MagicMock` — MagicMock body 内 0 引用。

### F-12 — repo_root dead fixture

**file**：`tests/skills/test_workflow_dispatcher_bash_skill_prompt.py:63-66`

repo_root fixture 定义后无任何测试用例参数引用。

### F-14 — REPO_ROOT dead import

**file**：`scripts/lib/workflow_dispatcher.py:26`

`from common import REPO_ROOT, WorkflowError` — REPO_ROOT body 内 0 引用。

---

## Follow-up Notes（不动代码，写入 notes.md）

### F-2 — TimeoutExpired 路径未读 exc.stderr/exc.output

**file**：`scripts/lib/workflow_dispatcher.py:288-295`

`subprocess.TimeoutExpired` 默认 `.stderr=None`；现实场景丢字段风险低。F-007 续跑路径若需 partial output，改 Popen+timeout 方案。

### F-6 — _dispatch_agent_node 签名缺 run_state

**file**：`scripts/lib/workflow_dispatcher.py:170-179`

F-006 范围明确不动 agent_node（属 F-010 mock 边界）；签名不对称是真实债务，F-010 接管时补齐 `run_state` 形参。

---

## Drop findings（裁决明细）

| F-id | checker | 原 severity | critic verdict | 最终处置 | 理由 |
|---|---|---|---|---|---|
| F-3 | error-handling | major | rejected | drop | run_state.py:147-210 RunState.rebuild 子事件全靠 node_id 索引；仅 workflow_started 读 run_id。事件级 run_id 非必需 |
| F-5 | error-handling | minor | rejected | drop | 本仓库 jsonl event stream 即日志机制；同模块 _dispatch_approval / _build_env 均不调 logging.*（F-005 已确认设计选择） |
| F-7 | design-consistency | major | rejected | drop | detailed-design.md:443 §6 失败矩阵明示『node_failed{error: stderr}』为 bash 路径设计内决策；§1.5 入口 except 是兜底 |
| F-13 | auxiliary-spec | minor | rejected | drop | pyproject.toml:13 ruff `select=['F']` 仅 Pyflakes；isort(I) 未启，项目未强制 import 顺序 |
| F-16 | auxiliary-spec | minor | not_proven | drop | 「」是 GB/T 15834-2011 合法中文书名号；与 F-005 rev3 全角空格/逗号清理不同类 |

---

## 裁决明细（全候选 finding，含 drop）

| F-id | checker | severity | critic verdict | 最终处置 |
|---|---|---|---|---|
| F-1 | complexity/eh (merged) | minor | not_proven | keep |
| F-2 | error-handling | major | not_proven | follow-up |
| F-3 | error-handling | major | rejected | drop |
| F-4 | error-handling | minor | not_rebutted | keep |
| F-5 | error-handling | minor | rejected | drop |
| F-6 | design-consistency | major | rejected | follow-up |
| F-7 | design-consistency | major | rejected | drop |
| F-8 | design-consistency | minor | not_rebutted | keep |
| F-9 | design-consistency | minor | not_rebutted | keep |
| F-10 | design-consistency | minor | not_rebutted | keep |
| F-11 | auxiliary-spec | minor | not_rebutted | keep |
| F-12 | auxiliary-spec | minor | not_rebutted | keep |
| F-13 | auxiliary-spec | minor | rejected | drop |
| F-14 | auxiliary-spec | minor | not_rebutted | keep |
| F-15 | auxiliary-spec | minor (Judge 升 major) | not_rebutted | keep |
| F-16 | auxiliary-spec | minor | not_proven | drop |

verdicts: rejected=5 / not_proven=3 / not_rebutted=8

---

## 修复策略（如派 rev2）

按严重度降序，预计 ≤ 30min：

1. **F-15（major）** — workflow_dispatcher.py L170/180/191/225 4 处注释同步实现状态
2. **F-4（minor）** — workflow_dispatcher.py:319 stderr or stdout or returncode 三段兜底
3. **F-11/F-12/F-14（dead 三连）** — 删 MagicMock import / repo_root fixture / REPO_ROOT import
4. **F-1（test /tmp 硬编码）** — 改用 jsonl_path fixture
5. **F-8/F-9/F-10（detailed-design.md 文档同步）** — §1.6 + §2.3 三处补全
6. **F-2 / F-6** → notes.md follow-up

### rev2 派发硬规则（trend-G-meta 防御）

> rev N-1 keep finding 修复后必须全文搜同模式 + 同 helper 风格不一致——不许"只改 reviewer 报告点出的具体行"。

具体动作：
1. **同模式扫描**：修一处 dead import → grep 全文件 `from ... import`，扫剩余 dead
2. **同 helper 风格扫描**：修一处注释 → 看同模块其余 docstring 是否还有同款 stub 措辞
3. **同语义类别区分**：F-006 范围内必修 vs F-010 / F-007 范围只 follow-up

---

## 待 sign-off 提示

本次评审 conclusion = `looks_clean`，这只是 AI 的机器评估，不代表合并通过。

**两个路径**：

A) **rev2 闭合 9 项 keep（推荐，30min）** → 跑第二轮 /code-review → looks_clean 0 keep → signoff

B) **直接 signoff 当前结果**（接受 1 major + 8 minor 留 follow-up）：

```bash
python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-010-code-F-006-001 --decision approved
# slash command 糖：
/code-review:signoff REV-REQ-2026-010-code-F-006-001 --decision=approved
```

⚠️ 未 sign-off 时 feature-lifecycle-manager 不会把 F-006 转 done；GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

---

## 报告文件

`requirements/REQ-2026-010/artifacts/review-20260511-221041.md`（本文件）
