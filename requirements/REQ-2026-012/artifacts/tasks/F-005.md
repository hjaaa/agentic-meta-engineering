---
schema_version: "1.0"
feature_id: F-005
title: "ruff F 类语义清理（4 处 F821：RoutingPlan + Any）"
status: done
complexity: light
depends_on:
  - F-004
touches:
  - tests/lib/test_code_review_routing.py
  - tests/lifecycle/test_submit_codex.py
created_at: "2026-05-16 21:35:00"
updated_at: "2026-05-17 16:07:56"
---

# F-005 · ruff F 类语义清理（4 处 F821：RoutingPlan + Any）

> 来源：`requirements/REQ-2026-012/artifacts/features.json` · `F-005`
> 设计：`requirements/REQ-2026-012/artifacts/detailed-design.md` §2.5 + §2.6
> ac_refs=AC-4（第二步）

## 需求描述

补 4 处 F821 typing import：
- `tests/lib/test_code_review_routing.py` 顶部追加 `from scripts.lib.code_review_routing import RoutingPlan`（覆盖 line 396 / line 732）。
- `tests/lifecycle/test_submit_codex.py` 顶部追加 `from typing import Any`（覆盖 line 744 / line 790）。

实施前先 `grep -n "class RoutingPlan\\|^RoutingPlan" scripts/lib/code_review_routing.py` 确认类名仍是 RoutingPlan。完成后 `ruff check scripts tests --select=F` 应退码 0。依赖 F-004 已合入（PR 顺序硬链）。

## 触及范围

- `tests/lib/test_code_review_routing.py`
- `tests/lifecycle/test_submit_codex.py`

## 实施步骤

1. `grep -n "class RoutingPlan\\|^RoutingPlan" scripts/lib/code_review_routing.py` 确认类名。
2. 编辑 test_code_review_routing.py：顶部 import 区加 RoutingPlan（若已有 `from scripts.lib.code_review_routing import ...` 行则合并）。
3. 编辑 test_submit_codex.py：顶部 import 区加 `from typing import Any`（若已有 `from typing import` 行则合并）。
4. `ruff check scripts tests --select=F`：应退码 0。
5. `pytest tests/lib/test_code_review_routing.py tests/lifecycle/test_submit_codex.py`：全过。

## 验收

- `ruff check scripts tests --select=F` 退码 0（F-004 89 + F-005 4 全清）
- 两个文件顶部 import 区分别含 RoutingPlan / `from typing import Any`
- pytest 相关测试通过
