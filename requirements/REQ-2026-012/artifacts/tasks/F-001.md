---
schema_version: "1.0"
feature_id: F-001
title: "tests/lib/test_routing_e2e.sh 入 CI（含草稿 PR 验证 Ubuntu runner）"
status: pending
complexity: light
depends_on: []
touches:
  - .github/workflows/quality-check.yml
created_at: "2026-05-16 21:35:00"
updated_at: "2026-05-16 21:35:00"
---

# F-001 · tests/lib/test_routing_e2e.sh 入 CI（含草稿 PR 验证 Ubuntu runner）

> 来源：`requirements/REQ-2026-012/artifacts/features.json` · `F-001`
> 设计：`requirements/REQ-2026-012/artifacts/detailed-design.md` §2.2 改动 C
> ac_refs=AC-1

## 需求描述

在 `.github/workflows/quality-check.yml` 末尾追加新 step `Run routing e2e shell tests` 调用 `bash tests/lib/test_routing_e2e.sh`。step 位置：放在 `Check gate-checklist.md is in sync with registry` step 之后；不动其它 step。实施前先开草稿 PR 仅推这一 step 跑一次 Ubuntu runner 验证（D-005），确认 `PASS=3 FAIL=0` 后再合入正式 step。

## 触及范围

- `.github/workflows/quality-check.yml`

## 实施步骤

1. 本地分支基于 `develop` 切 `feat/req-2026-012-f001`（在主 feat 分支上单独提交一个 commit 也可）。
2. 编辑 quality-check.yml，末尾追加：
   ```yaml
   - name: Run routing e2e shell tests
     run: bash tests/lib/test_routing_e2e.sh
   ```
3. `gh pr create --draft --base develop --title "[draft] F-001 routing e2e CI 验证"`。草稿 PR 阶段无新文件 touches，只触发 workflow 跑一次 step 拿信号；step 文本改动已在步骤 2 计入 `.github/workflows/quality-check.yml` 这一 touches。
4. 取 run-id 并等待完成：
   ```bash
   RUN_ID=$(gh run list --workflow=quality-check.yml --branch=feat/req-2026-012 --limit=1 --json databaseId -q '.[0].databaseId')
   gh run watch "$RUN_ID"
   gh run view "$RUN_ID" --log | grep -E 'Run routing e2e shell tests.*PASS=3 FAIL=0' || echo "FAIL: routing-e2e CI step"
   ```
5. 若失败，本地调脚本并重 push；不合并草稿 PR。
6. 草稿 PR 验证通过后，把改动并入正式 feat 分支 PR。

## 验收

- workflow yml 在 render-docs --check step 之后存在 `Run routing e2e shell tests` step
- 草稿 PR 的 GitHub Actions run 中此 step exit 0，日志含 `PASS=3 FAIL=0`
- 现有 9 个 step 的 name / run 字段未变（git diff 限于追加段）
