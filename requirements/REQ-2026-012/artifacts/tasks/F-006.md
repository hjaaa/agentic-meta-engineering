---
schema_version: "1.0"
feature_id: F-006
title: "CI ruff 范围扩到 scripts + tests"
status: done
complexity: trivial
depends_on:
  - F-005
touches:
  - .github/workflows/quality-check.yml
created_at: "2026-05-16 21:35:00"
updated_at: "2026-05-17 16:16:00"
---

# F-006 · CI ruff 范围扩到 scripts + tests

> 来源：`requirements/REQ-2026-012/artifacts/features.json` · `F-006`
> 设计：`requirements/REQ-2026-012/artifacts/detailed-design.md` §2.2 改动 B
> ac_refs=AC-4（收尾）

## 需求描述

改 `.github/workflows/quality-check.yml` line 82 区域：`ruff check scripts/ --select=F` → `ruff check scripts tests --select=F`。不动 pyproject.toml 的 ruff 配置（D-008，benchmarks 实测 0 条无需 extend-exclude）。依赖 F-004 + F-005 全部合入，否则 CI 当场红。

## 触及范围

- `.github/workflows/quality-check.yml`（行 82 区域）

## 实施步骤

1. 编辑 workflow yml line 82：`ruff check scripts/` → `ruff check scripts tests`。
2. 本地预跑 `ruff check scripts tests --select=F`：必须退码 0（F-004 + F-005 已合入）。
3. 推 PR；workflow ruff lint step 必须 exit 0。

## 验收

- `grep -E 'ruff check scripts tests --select=F' .github/workflows/quality-check.yml` 命中
- PR 触发的 workflow run 中 ruff lint step exit 0
- pyproject.toml 未改（ruff 配置段无 diff）
- F-006 单独 git revert 后 workflow ruff 范围回退 scripts/，F-004/F-005 清债成果保留
