# 代码审查报告 · REQ-2026-014 · F-001 · rev-002 · 2026-05-18 12:11:03

## 结论

**looks_clean** · 0 critical / 0 major / 2 minor（均为 rev-001 carry-over defer）

review_id：`REV-REQ-2026-014-code-F-001-002`（supersedes `REV-REQ-2026-014-code-F-001-001`）
score：93 / 100（rev-001 87 → rev-002 93，+6）

## 路由说明

- **决策**：`all`（8 全集兜底，但本轮采用 4-checker focused 派发：design-consistency / error-handling / auxiliary-spec / performance — rev-2 surface 仅触及 rev-1 finding 4 维度）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-18 12:02:00
- **Base SHA**：87985c38（rev-001 head）
- **Head SHA**：2c133dafa（rev-002 head）

## 待 sign-off 提示

本次评审 `conclusion = looks_clean`，**仍是 AI 机器评估**。请开发者本人执行：

```bash
python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-014-code-F-001-002 --decision approved
# 或：
/code-review:signoff REV-REQ-2026-014-code-F-001-002
```

sign-off 后 F-001 可转 `done`。

## rev-001 → rev-002 9 修复闭环情况

| rev-1 ID | 维度 | rev-2 处置 | 状态 |
|---|---|---|---|
| F-002 | security | 不修（call-site 责任，by design defer） | **carry-over defer**（保留） |
| F-004 + F-005 | performance | 抽 `_WHITESPACE_OR_UNDERSCORE_RE` / `_NON_ALNUM_RE` 模块级预编译 | **closed** |
| F-006 | error-handling | `raise SlugError(...) from e` | **closed** |
| F-007 | error-handling | docstring 列 3 条 None 路径 | **closed** |
| F-008 | error-handling | 消息含 `date={date_str!r} slug={slug!r}` | **closed** |
| F-009 | test | 5 处 pytest.raises 补 `match=` | **closed** |
| F-010 | design-doc | 不修（out-of-touches，detail-design rev-006 follow-up） | **carry-over defer**（follow-up） |
| F-011 | test naming | 改名 `..._returns_path_under_requirements` | **closed** |
| F-017 | aux-spec | docstring 三段格式（入参 / 返回 / 来源） | **closed** |
| F-018 | aux-spec | docstring 补入参 + 返回值 | **closed** |

**7 in-place closed + 2 合理 defer（F-002 call-site / F-010 out-of-touches）。**

## trend-G-meta 检查（rev-2 是否引入新 minor）

design-consistency-checker 候选 3 条；critic 全 rejected；Judge 采纳。

- **G-001** `_NON_ALNUM_RE` 注释缺"已 lower"前置 → **rejected**：OD-1 锁死叶子 + 同函数 3 行内局部不变量，预防性注释违反 YAGNI
- **G-002** derive_slug_from_title 第 3 条 None 路径无测试 → **rejected**：fuzz 10000 次 0 命中确认路径不可达；下一轮 docstring 标"防御性兜底"作为 follow-up
- **G-003** reviewer 自述非 real finding → **rejected**

auxiliary-spec-checker 还报告 2 条 pre-existing minor（rev-1 已存在，rev-2 未触动）→ critic rejected：归 rev-1 follow-up 而非 rev-2 finding。
- PRE-001 `generate_requirement_key` docstring 缺显式入参/返回标签
- PRE-002 `is_legacy_requirement_key` 单行 docstring 未展开三段式

**rev-2 实质新引入 finding：0 条。**

## 裁决明细

- **rev1-F-004/F-005-verify** [performance] → **drop（闭环）**
  - 模块顶部 6 个预编译正则 + 函数体内 0 处内联 re.sub
- **rev1-F-006-verify** [error-handling] → **drop（闭环）**
  - `except UnicodeEncodeError as e:` + `raise SlugError(...) from e`
- **rev1-F-007-verify** [error-handling] → **drop（闭环）**
  - docstring 列 (1) regex 不匹配 / (2) strip 后为空 / (3) 截断后字符集 fail
- **rev1-F-008-verify** [error-handling] → **drop（闭环）**
  - 异常消息含 date + slug 双标识
- **rev1-F-009-verify** [error-handling/test] → **drop（闭环）**
  - 5 处 pytest.raises 全补 `match=`
- **rev1-F-011-verify** [design-consistency] → **drop（闭环）**
  - test 函数名 `..._under_requirements` 对齐
- **rev1-F-017+F-018-verify** [aux-spec] → **drop（闭环）**
  - branch_for / directory_for docstring 三段格式
- **PRE-001 / PRE-002** [aux-spec, pre-existing] → critic `rejected` → **drop**
  - rev-1 既存，归 rev-1 follow-up
- **G-001** [trend-meta] → critic `rejected` → **drop**
  - OD-1 局部不变量 + YAGNI
- **G-002** [trend-meta] → critic `rejected` → **follow-up**
  - fuzz 不可达；下次触碰本函数时标"防御性兜底"
- **G-003** [reviewer 自述非 finding] → critic `rejected` → **drop**
- **rev1-F-002-defer** [security carry-over] → critic `not_rebutted` → **keep**
  - call-site 责任，F-003/F-004 评审跟进
- **rev1-F-010-defer** [design-doc carry-over] → critic `not_rebutted` → **follow-up**
  - detail-design rev-006 清理

## Critical（必须修复）

无。

## Major（强烈建议修复）

无。

## Minor（改进建议）

### [security / design-consistency] scripts/lib/requirement_naming.py:134-140 — F-002 carry-over

`directory_for_requirement_key` 仍未对 key 做合法性校验（rev-1 评审已 defer 至本 feature 之外）。

**建议**：F-003 / F-004 落地时在调用入口加 `assert is_legacy_requirement_key(key) or _NEW_REQUIREMENT_KEY_RE.match(key)`，或在本函数显式校验后抛 `SlugError`。当前所有调用方均经 `generate_requirement_key` 或 `is_legacy_requirement_key` 产出（受控源），可继续 defer。

### [history-context] scripts/lib/workflow_run.py:31 + scripts/lib/workflow_bootstrap.py:77 — F-013/F-014 carry-over

`_REQ_ID_PATTERN` 与 `_strip_req_prefix` 双轨命名仍存在（rev-1 evaluation deferred to F-003/F-004 收敛）。

**建议**：F-003 / F-004 自然收敛时一并替换为 `requirement_naming.is_legacy_requirement_key` + `branch_for_requirement_key` 入口；下一轮 code-review 跟进确认。

## Follow-up Notes

- **G-002 / F-007 docstring follow-up**：下次触碰 `derive_slug_from_title` 时把第 3 条 None 路径标"防御性兜底（不变量守护，不应有真实触发）"——既不补 test（路径不可达，会误导后人）也不删代码（删除会失去对未来正则结构变更的兜底）。
- **F-010 detail-design follow-up**：`detailed-design.md:245` §3.1 多余 `from dataclasses import dataclass` 在 detail-design rev-006 清理（可与 F-003 / F-004 touch 同章节时顺手）。
- **PRE-001 / PRE-002**：`generate_requirement_key` 与 `is_legacy_requirement_key` docstring 未达"三段式"风格统一；trivial。下次触碰任一函数时顺手补齐。

## 未运行的 checker（如有）

本轮 focused 派发，rev-2 surface 仅触及 4 维度（performance / error-handling / aux-spec / design-consistency）。
未运行 4 个 checker（complexity / security / concurrency / history-context）的判断依据：rev-2 diff 仅 9 行函数体修改 + 6 行正则 + 6 行 test 字符串调整，未引入新分支 / 新数据流 / 新历史风险面 / 新 IO；rev-1 全 8 维度审查仍有效。

> 如需 8 全集补审：`python3 scripts/lib/code_review_routing.py --mode embedded ...` 重新触发即可。

## 审查元信息

- **审查范围**：scripts/lib/requirement_naming.py (+24 -9) / tests/lib/test_requirement_naming.py (+6 -6) — 2 文件 / +30 -15
- **并行 checker**：4（design-consistency / error-handling / auxiliary-spec / performance）
- **Critic 统计**：rejected 5 / not_proven 0 / not_rebutted 2
- **综合模型**：code-quality-reviewer（Judge）
- **Base SHA**：87985c38cce7b42048a332d6da1f7c8f0871bf84
- **Head SHA**：2c133dafa5205c479f8aa745fe995009697baf6f
- **Verdict 文件**：requirements/REQ-2026-014/reviews/code-F-001-002.json
- **Supersedes**：REV-REQ-2026-014-code-F-001-001
