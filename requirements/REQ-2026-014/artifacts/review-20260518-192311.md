# Code Review Report · REQ-2026-014 / F-004 · rev-001

- **生成时间**：2026-05-18 19:23:11 (Asia/Shanghai)
- **review_id**：REV-REQ-2026-014-code-F-004-001
- **scope**：embedded / feature F-004 · workflow_bootstrap.py worktree-first 改造
- **base_sha**：d1a1d8997
- **head_sha**：7953abdd
- **diff**：5 文件 +1026 / -182
- **conclusion**：**blocked** · score **72** · 5 维度均 design_consistency=70 / complexity=72 / error_handling=78 / interface=78 / test=85
- **机器评估口径**：本次输出为 AI 综合裁决，不代表合并通过；需人类 sign-off 后才进 done

## 1. 路由说明

- **决策**：accept（命中 must=0 / suggest=3 / trivial=0 / total=5）
- **确认方**：routing.py 自动路由（取消人类卡点 A）at 2026-05-18 19:06:49 / by hj19961223@gmail.com
- **跑了哪些 checker**：complexity / error-handling / design-consistency / auxiliary-spec
- **跳过的 checker**：security / concurrency / performance / history-context（路径未命中 must/suggest 任一规则）

## 2. 结论行

**blocked**：2 条 critical 真问题 + 8 条 major + 4 条 minor，required_fixes 2 条非空 → 触发 CR-1。综合分 72 < 85 → 不满足 CR-4 looks_clean。

## 3. Critical（必修，rev2 合并修）

> 同点同因，建议同一次编辑同步消化。

### F-1 + F-2 [design_consistency × error_handling]：BootstrapError 兜底协议双违反

- **位置**：`scripts/lib/workflow_run.py:419-427`
- **症状**：
  1. `_run_requirement` 的 except BootstrapError 分支只对 `reason=='path_or_branch_exists'` 跳过，其它 reason 一律调 `_bootstrap_rollback`，**未检查 `exc.retain_worktree`**——baseline_failed_required 路径 retain_worktree=True 信号被吞，rollback step 2 `git branch -D feat/req-<key>` 销毁保留现场。违反 `detailed-design.md §4.2` + `§5.1 L1178/L1195`。
  2. `_bootstrap_rollback` 调用未传 `worktree_info`，`_bootstrap_requirement` 中 worktree_info 是局部变量未上抛。workflow_bootstrap.py:700 守卫 `worktree_info is not None and worktree_info.created and worktree_info.owner=='workflow'` 永远 false，step 1 `git worktree remove + prune` 永不执行 → 任何 step 4-7 失败都会留下孤儿 `.worktrees/feat-req-<key>/`。
- **修法**：
  - (a) 在 line 420 logging.error 前先 `if getattr(exc, "retain_worktree", False): print 保留现场提示; return 1`
  - (b) `BootstrapError` 增加 `worktree_info: Optional[WorktreeInfo] = None` 字段；`_bootstrap_requirement` 抛错时填值；`_run_requirement` 调 rollback 时 `worktree_info=exc.worktree_info` 透传

## 4. Major（rev2 顺手修，单点编辑零风险）

- **F-3** [complexity, **降级 major**] `scripts/lib/workflow_bootstrap.py:669-803` `_bootstrap_rollback` 125 行 / CC=21（阈值 80/10）；语义顺序撤销 3 步，拆 `_rollback_worktree` + `_rollback_branch` + `_rollback_artifacts` 即可
- **F-4** [design_consistency] `workflow_bootstrap.py:673-674` `_bootstrap_rollback` 签名 `artifacts_created`/`branch_created` 应 keyword-only（design §3.4 L742-750）
- **F-5** [design_consistency] `workflow_bootstrap.py:194-211` `_render_meta_yaml` owner='none' 时仍写 `wt_enabled='true'`；与同文件 line 595 run-state.jsonl 判定 `owner != 'none'` 不一致
- **F-8** [design_consistency] `workflow_bootstrap.py:455` 注释/`worktree_manager.py:361,393` 抛的 reason `'gitignore_missing'` 与 BootstrapError 枚举 `'worktree_dir_not_ignored'` 不一致（design §5.1 L1177/L1194）
- **F-10** [error_handling] `workflow_bootstrap.py:299` `_write_artifact_file` 硬编码 `branch_created=False`；F-004 worktree-first 流程下 step 5-6 写文件失败时分支已建，rollback 信号错误会漏删分支

## 5. Minor（rev2 顺手修）

- **F-11** `tests/lib/test_workflow_bootstrap_worktree.py:25` `MagicMock` unused import
- **F-12** `tests/lib/test_workflow_bootstrap_worktree.py:39` `WorktreeBootstrapError` unused import
- **F-13** `workflow_bootstrap.py:273` `_load_yaml_worktree_cfg` yaml 解析失败 `logging.debug` 应升 `warning`
- **F-17** `workflow_bootstrap.py:159` `_render_meta_yaml` `worktree_state` 死参数（函数体无引用）

## 6. Follow-up（不在 rev2，派后续 feature）

- **F-6** `workflow_bootstrap.py:469-614` `_bootstrap_requirement` 146 行——8 步骤编排语义单一，拆分易破坏可读性；建议留观察，未来若再扩则一并拆
- **F-7** `workflow_bootstrap.py` 805 行 超 500 阈值——worktree-first 改造一次性引入 498 行不可逆；建议派独立 refactor feature 拆模块（`workflow_bootstrap_rollback.py` + `bootstrap_renderer.py`），避免本期 diff 二次扩张

## 7. Drop（误报或边界外，仅入裁决明细）

F-9（函数定义顺序倒置 — Python 调用时 lookup 无 NameError）/ F-14（reason 字段缺省 'other' 不影响行为）/ F-15（template UTF-8 文件 UnicodeDecodeError 触发概率 0）/ F-16（location vs location.parent 内部 derive 等价）/ F-18（注释混排无硬规范）/ F-19（docstring WHY 删减主观判断无 diff 证据）/ F-20（测试命名 BDD 风格非硬约束）

## 8. 裁决明细

| F-id | checker | 原始严重度 | critic verdict | 最终处置 | 理由 |
|---|---|---|---|---|---|
| F-1 | design-consistency | critical | not_rebutted | **keep** | retain_worktree 守卫缺失实锤违反 §4.2 |
| F-2 | design-consistency | critical | not_rebutted | **keep** | worktree_info 未透传导致 step 1 永不触发 |
| F-3 | complexity | critical | not_rebutted | **downgrade → major** | 语义顺序撤销单一路径，拆 3 子函数即可 |
| F-4 | design-consistency | major | not_rebutted | **keep** | kw-only 偏离设计签名 |
| F-5 | design-consistency | major | not_rebutted | **keep** | wt_enabled 与 run-state.jsonl 双判定矛盾 |
| F-6 | complexity | major | not_rebutted | **follow-up** | 编排语义不易拆，留观察 |
| F-7 | complexity | major | not_rebutted | **follow-up** | 派独立 refactor feature |
| F-8 | error-handling+design-consistency | major | not_rebutted | **keep** | reason 枚举名实不一 |
| F-9 | auxiliary-spec | major | not_proven | **drop** | Python 调用时 lookup 无运行时风险 |
| F-10 | auxiliary-spec | major | not_rebutted | **keep** | F-004 新流程下硬编码加剧 rollback 漏清 |
| F-11 | auxiliary-spec | major | not_rebutted | **keep** | MagicMock 整文件 0 引用 |
| F-12 | auxiliary-spec | major | not_rebutted | **keep** | WorktreeBootstrapError 整文件 0 引用 |
| F-13 | error-handling | minor | not_rebutted | **keep** | 配置错误用户需感知，升 warning |
| F-14 | error-handling | minor | not_proven | **drop** | reason 默认 'other' 不影响行为 |
| F-15 | error-handling | minor | not_proven | **drop** | UTF-8 模板触发概率 0 |
| F-16 | design-consistency | minor | not_proven | **drop** | 内部 derive 等价 |
| F-17 | auxiliary-spec | minor | not_rebutted | **keep** | 死参数文档误导 |
| F-18 | auxiliary-spec | minor | not_proven | **drop** | 注释风格偏好 |
| F-19 | auxiliary-spec | minor | not_proven | **drop** | docstring WHY 主观判断 |
| F-20 | auxiliary-spec | minor | not_proven | **drop** | BDD 命名非硬规范 |

**统计**：keep 10（critical 2 / major 5 / minor 3）、downgrade 1、follow-up 2、drop 7 = 20 候选

## 9. 跨维度洞察

- **F-1/F-2/F-4 同根因**：BootstrapError 协议未端到端校验。建议给 BootstrapError 加 worktree_info 字段 + keyword-only 签名一次定型，3 条同步消化
- **F-5 + F-10 同根因**：owner='none' / policy=never 路径覆盖不足；rev2 修 wt_enabled 同时修 branch_created 透传
- **F-3 + F-1/F-2 同位置**：_bootstrap_rollback 调用点既错误调用又方法本身复杂；rev2 拆 3 子函数 + 加守卫 + 透传可一次编辑同步消化 3 条

## 10. 待 sign-off 提示

本次评审输出 **conclusion = blocked**（机器评估），这意味着 rev2 必修后才能 sign-off。当前不要执行 sign-off。

待 rev2 修复 + reviewer 复审 looks_clean 后，用户在 tty 执行：

```
/code-review:signoff REV-REQ-2026-014-code-F-004-002 --decision=approved
# 或纯文档变更快速通道
/code-review:signoff REV-REQ-2026-014-code-F-004-002 --trivial
```

未 sign-off 时 `feature-lifecycle-manager` 不会把 F-004 转 done；GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。
