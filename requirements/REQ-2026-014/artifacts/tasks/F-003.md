---
schema_version: "1.0"
feature_id: F-003
title: "workflow_run.py 改造（_generate_req_id 替换 + 三个 worktree 参数解析）"
status: pending
complexity: light
depends_on:
  - F-001
touches:
  - scripts/lib/workflow_run.py
  - tests/lib/test_workflow_run_worktree_args.py
created_at: "2026-05-18 10:15:21"
updated_at: "2026-05-18 10:15:21"
review_report: null
---

# F-003 · workflow_run.py 改造（_generate_req_id 替换 + 三个 worktree 参数解析）

> 来源：`requirements/REQ-2026-014/artifacts/features.json` · `F-003`
> 设计：`requirements/REQ-2026-014/artifacts/detailed-design.md` §3.3
> ADR 依据：D-012 / D-013
> interfaces_frozen: false
> （contract may evolve during implementation）
> 预估工作量：0.5 人天

## 需求描述

改 scripts/lib/workflow_run.py：(1) _generate_req_id 函数体替换为调用 requirement_naming.generate_requirement_key（保留函数名作为兼容入口）；(2) _parse_args 扩展三个新参数 --slug=<value> / --no-worktree / --worktree-policy=<auto|never|require|current>；(3) 中文标题 + 缺 --slug 时 fail-closed exit 1，stderr 提示 '中文标题需显式 --slug=<ascii-slug>' 与 D-013 链接；(4) legacy REQ-YYYY-NNN 路径在 --slug 缺失且 title 仅 ASCII 时通过 generate_requirement_key 与 _generate_req_id 共存（短期过渡）。落地 outline-design §模块划分既有改造点 + tech-feasibility §1.3 + spec §11 Phase 2。

## 接口/数据结构

详见 `requirements/REQ-2026-014/artifacts/detailed-design.md` §3.3（接口签名 / dataclass / 数据契约）+ §2（跨模块共享数据契约：meta.yaml.worktree 段 / yaml.worktree 段 / worktree.state 主状态机 / bootstrap policy 子状态机）。

## 依赖

### 依赖的 feature

F-001

### 依赖的模块

标准库 only（标准库范围内的 import 不视为外部依赖），具体见 detailed-design.md §3.3 顶部模块声明。

## 触及范围

**frontmatter `touches` 字段是机器读取入口**（`touches_guard.py` 软拦截 + `GATE-TOUCHES-VIOLATION` 硬挡）。本正文段是人工副本，两处不一致时以 frontmatter 为准。

- `scripts/lib/workflow_run.py`
- `tests/lib/test_workflow_run_worktree_args.py`

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] scripts/lib/workflow_run.py 的 _generate_req_id 函数体改为调用 requirement_naming.generate_requirement_key
- [ ] _parse_args 解析 --slug=<value> / --no-worktree / --worktree-policy=<value> 三参数，返回结构含对应字段
- [ ] 中文标题 + 无 --slug 时 main() 退 exit=1，stderr 含 '中文标题需显式 --slug='
- [ ] pytest tests/lib/test_workflow_run_worktree_args.py 全过：3 参数 happy / 中文 + 缺 slug fail-closed / ASCII 标题自动派生 / --worktree-policy 非法值 fail-closed
- [ ] tests/lib/test_workflow_run.py 现有用例（_generate_req_id legacy 兼容、并发 mkdir 重试）回归全过
- [ ] 接口实现符合 detailed-design.md §3.3
- [ ] 测试骨架已实现并全过（pytest / bats）
- [ ] check_sourcing / check_features / check_task_frontmatter 校验通过
- [ ] `/code-review` 通过且 sign-off=approved
