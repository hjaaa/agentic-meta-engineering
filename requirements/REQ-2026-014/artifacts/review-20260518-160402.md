# 代码审查报告 · REQ-2026-014 · 20260518-160402

## 结论

**needs_attention** · 0 critical / 3 major / 2 minor

- score: 82（needs_attention 区间 75-89）
- feature: F-003 · workflow_run.py 改造（_generate_req_id 替换 + 三个 worktree 参数解析）
- review_id: REV-REQ-2026-014-code-F-003-001

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：accept（must=0 / suggest=1 / trivial=0 / total=2）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-18 15:53:17
- **跑了哪些 checker**：complexity-checker / error-handling-checker / design-consistency-checker
- **跳过的 checker（含原因，全为"路径未命中 must/suggest 任何规则"）**：
  - security-checker
  - concurrency-checker
  - performance-checker
  - history-context-checker
  - auxiliary-spec-checker

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。当前有 3 条 major required_fixes 待修，**不可直接 sign-off**——建议派 rev-002 修复后再签。

如修复完成且 verdict 升级为 looks_clean，再执行：

```bash
/code-review:signoff REV-REQ-2026-014-code-F-003-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-014-code-F-003-001 --trivial  # 纯文档变更快速通道（本次不适用）
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-003 转为 done；GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 11 条候选 finding → critic 验证 → quality-reviewer 三方裁决

- **F-1** [complexity · major] → critic: not_proven → **最终处置：downgrade → suggestion**
  - 裁决依据：`_parse_args` 70+ 行 / depth=7 真实存在，但 CC=18 数值未经工具实测；拆分建议合理但不阻塞 sign-off
- **F-2** [complexity · minor] → critic: not_proven → **最终处置：downgrade → suggestion**
  - 裁决依据：`_scan_existing_requirement_keys` 重复结构属实但 CC=13 高估；提取 `_scan_dir_keys` 是可选优化
- **F-3** [complexity · suggestion] → critic: not_rebutted → **最终处置：keep（suggestion）**
  - 裁决依据：workflow_run.py 495/500 行属实；F-004 将进一步扩张，前瞻提示有效
- **F-4** [complexity · suggestion] → critic: not_rebutted → **最终处置：keep（suggestion）**
  - 裁决依据：测试 327 行 + RunArgs 样板 5+ 处重复属实；fixture 提取建议有效
- **F-5** [error-handling · major] → critic: not_rebutted → **最终处置：keep（major）**
  - 裁决依据：`SlugError(ValueError)` 非 `WorkflowError` 子类；workflow_run.py:424 抛后无任何层 catch，__main__ 仅 `except WorkflowError` → 完整 traceback 泄给用户
- **F-6** [error-handling · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：`getattr(exc, "reason", None)` 实际是正确 fail-closed 写法；None 时走 rollback + exit 1 正是预期路径
- **F-7** [error-handling · minor] → critic: not_rebutted → **最终处置：keep（minor）**
  - 裁决依据：OSError 用 `logging.debug` 静默确实会让权限错延迟到 retry 阶段才暴露；warning 级更利于排查
- **F-8** [error-handling · major] → critic: not_rebutted → **最终处置：keep（major）**
  - 裁决依据：`--worktree-policy` 无枚举白名单（合法值 detailed-design.md:862 = auto/never/require/current），任意字符串通过 → 违反 fail-closed 范式 + features.json AC5
- **F-9** [error-handling · suggestion] → critic: not_rebutted → **最终处置：keep（suggestion）**
  - 裁决依据：workflow_run.py:411 直接 print exc，exc 含 git stderr（≤ 2KB）属轻度内部信息暴露
- **F-10** [design-consistency · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：F-003 task.md `touches` 严格限定到 workflow_run.py + test；`_bootstrap_requirement` 加 worktree kwargs 必触发 GATE-TOUCHES-VIOLATION；F-004 才接管 worktree 落地
- **F-11** [design-consistency · major] → critic: not_rebutted → **最终处置：keep（major）**
  - 裁决依据：features.json F-003 AC5 + task.md self-check 第 5 条显式要求"`--worktree-policy` 非法值 fail-closed"；当前 12 TC 无该用例 + 实现层无校验

## Critical（必须修复）

_无 critical 问题_

## Major（强烈建议修复 · required_fixes）

### [error-handling · F-5] scripts/lib/workflow_run.py:424

retry 99 次上限后 `raise SlugError(...)` 完整 traceback 暴露给 stderr：`SlugError(ValueError)` 不是 `WorkflowError`，`main()` 不 catch，`__main__` 仅 `except WorkflowError` → 异常穿透所有兜底，用户看到完整堆栈。

**建议**：改为 `raise WorkflowError(...)`，或在 `_run_requirement` 末尾 try/except SlugError → `print(...; return 1)` 范式（与 __main__ 现有 WorkflowError 处理对齐）。

### [error-handling · F-8] scripts/lib/workflow_run.py:237/262

`_parse_args` 解析 `--worktree-policy` 时不做枚举值白名单校验，任意字符串（如 `bogus-value`）通过且静默存入 `RunArgs.worktree_policy`，违反 fail-closed 原则 + features.json AC5。

**建议**：解析 `--worktree-policy=<v>` 与 `--worktree-policy <v>` 两形式时统一校验 `value not in {"auto","never","require","current"}` → `raise WorkflowError("worktree-policy 非法值 <v>，合法值：auto/never/require/current")`。枚举值来源 detailed-design.md:862。

### [design-consistency · F-11] tests/lib/test_workflow_run_worktree_args.py

缺 AC5 显式要求的 `--worktree-policy=bogus` fail-closed 测试用例。当前 12 TC 仅覆盖 happy path（auto / skip）。

**建议**：补两个 TC（双形式各一），断言 `_parse_args(["tmpl","Title","--worktree-policy=bogus"])` 与 `_parse_args(["tmpl","Title","--worktree-policy","bogus"])` 均抛 `WorkflowError`，且 stderr/异常 message 含 "worktree-policy 非法值"。F-8 修复后此测试应自然过。

## Minor（改进建议）

### [error-handling · F-7] scripts/lib/workflow_run.py:119/132

`_scan_existing_requirement_keys` 对 `OSError`（权限拒绝等）用 `logging.debug` 静默；不完整 occupied 集合会让冲突推迟到 `_bootstrap_requirement` 阶段才暴露。

**建议**：改 `logging.warning(...)`，让运维可见。

## Follow-up Notes（不阻塞，但值得提醒）

- **F-1 / F-2**（来自 complexity）：`_parse_args` 与 `_scan_existing_requirement_keys` 建议下次重构时拆 helper（`_parse_option_equal_form` / `_parse_option_space_form` / `_scan_dir_keys`）。F-003 rev-002 修 F-8 时可顺手做一部分。
- **F-3**：workflow_run.py 已 495/500 行；F-004 接入 `_setup_worktree_or_branch` 时需评估抽 worktree-policy 解析到独立模块。
- **F-4**：tests/lib/test_workflow_run_worktree_args.py RunArgs 样板 5+ 处重复，建议 fixture 抽取。
- **F-9**：bootstrap 失败 stderr 文案精简，避免泄 git stderr（业务摘要 + raw 走 logging.debug）。
- **trend-H-ac-implementation-gap**（reviewer 新观察）：spec → AC → 实现链路出现 forward gap（worktree-policy 枚举 AC 写了但实现没写）。与 F-002 trend-G-meta（reverse 反引入）不同。建议下次 task.md 渲染时把 AC 条目 inline 写进 PR description 自动 checkbox，让 reviewer 强制对账。

## 未运行的 checker

- ⚠️ security-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ concurrency-checker（同上）
- ⚠️ performance-checker（同上）
- ⚠️ history-context-checker（同上）
- ⚠️ auxiliary-spec-checker（同上）

## 审查元信息

- **审查范围**：scripts/lib/workflow_run.py (+251 -109) + tests/lib/test_workflow_run_worktree_args.py (+327 -0)，2 文件，+578 -109 行
- **并行 checker**：3 个（complexity / error-handling / design-consistency）
- **Critic 统计**：rejected 2 / not_proven 2 / not_rebutted 7
- **综合模型**：code-quality-reviewer
- **Base SHA**：e73efa265dd0e6b76e5b61a1b3096373247ce601
- **Head SHA**：42c75c9768fecdfae60629bdcef90bf44d1e4f76
- **review_id**：REV-REQ-2026-014-code-F-003-001
- **五维度评分**：functional_correctness=90 / error_handling=75 / test_coverage=90 / code_quality=80 / design_consistency=78 → 加权 82
