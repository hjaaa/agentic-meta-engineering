# Code Review Report · REQ-2026-014 / F-004 · rev-002

- **生成时间**：2026-05-18 20:11:03 (Asia/Shanghai)
- **review_id**：REV-REQ-2026-014-code-F-004-002（supersedes REV-REQ-2026-014-code-F-004-001）
- **scope**：embedded / feature F-004 · workflow_bootstrap.py worktree-first 改造
- **base_sha**：d1a1d8997 / **head_sha**：bfb3bb489 / **diff**：7 文件 +1310 / -238
- **conclusion**：**looks_clean** · score **89**（+17 vs rev1 72）
- **维度评分**：design_consistency=88 / complexity=92 / error_handling=87 / auxiliary_spec=88 / security=90 / concurrency=90 / performance=90 / history_context=90
- **CR-1 满足**：required_fixes=[] · **CR-3 满足**：各维度 ≥85 · **CR-4 满足**：综合 ≥85 → 可走 sign-off

## 1. 路由说明

- **决策**：accept · 4 checker（complexity / error-handling / design-consistency / auxiliary-spec）
- **跳过**：security / concurrency / performance / history-context（路径未命中 must/suggest）

## 2. rev1 → rev2 闭环验证（10/10 closed）

| rev1 ID | 原 severity | rev2 验证位置 | 状态 |
|---|---|---|---|
| F-1 | critical | `workflow_run.py:421-432` retain_worktree 守卫 + return 1 | ✅ closed |
| F-2 | critical | `BootstrapError` 新增 `worktree_info` + caller 透传 | ✅ closed |
| F-3 | major（rev1 降级前 critical） | 拆 `_rollback_remove_worktree`/`_rollback_delete_branch`/`_rollback_clean_artifacts`；主体 27 行 / CC=2 | ✅ closed |
| F-4 | major | `_bootstrap_rollback` artifacts_created/branch_created kw-only | ✅ closed |
| F-5 | major | `_render_meta_yaml` wt_enabled 改 `owner != 'none'` 判定 | ✅ closed |
| F-8 | major | `worktree_manager.py` reason `'gitignore_missing'` → `'worktree_dir_not_ignored'` | ✅ closed |
| F-10 | major | `_write_bootstrap_artifacts` caller wrap，BootstrapError 重抛带 worktree_info | ✅ closed |
| F-11 | minor | MagicMock unused import 已删 | ✅ closed |
| F-12 | minor | WorktreeBootstrapError unused import 已删 | ✅ closed |
| F-13 | minor | yaml debug → warning | ✅ closed |
| F-17 | minor | _render_meta_yaml worktree_state 死参数已删 | ✅ closed |

## 3. rev2 新候选 finding 处置

### Suggestions（5 keep + 进 rev3 顺手 / follow-up；无 required_fixes）

- **F-103** [error-handling, minor] `workflow_bootstrap.py:286-287` yaml 顶层非 dict 静默 return `{}` 无日志；line 280 异常路径已 warning，**对称性丢失** → 建议 line 286 前补 `logging.warning("_load_yaml_worktree_cfg: top-level not dict; falling back (path=%s)", template_path)`
- **F-104** [error-handling, minor] `workflow_run.py:425` `stderr={exc}` 标签错配 → 改 `detail={exc}` 或 `error={exc}`
- **F-106** [design-consistency, minor] `workflow_run.py:422-425` retain_worktree 文案缺 `detailed-design.md §5.1 L1178` 要求的两条指引："cd worktree 查 baseline 输出" + "改 yaml.worktree.setup.baseline.required=false"
- **F-107** [design-consistency, minor] `workflow_bootstrap.py:405` `_bind_current_worktree(state: Any)` 偏离 design §3.4 L706 签名 `WorktreeState`；用 forward reference `state: 'worktree_manager.WorktreeState'` 不触发顶层 import 即可对齐
- **F-108 + F-109** [auxiliary, minor] `tests/lib/test_workflow_run_worktree_args.py:25,40` `call` + `SlugError` unused imports — **trend-G-meta 风险**（rev1 同文件修 MagicMock+WorktreeBootstrapError 后漏扫同模式）；建议同时加 ruff/pyflakes pre-commit 让工具层兜底

### Drop（critic 验证后未成立 / 已有先例）

- F-101 / F-102 — `_rollback_delete_branch` / `_rollback_remove_worktree` CC=7/6 嵌套 4 — 阈值 10/4 **边界未越线**（critic rejected）
- F-105 — 子函数命名长 — rev1 仅"建议"非约束，rev2 长名语义更清晰
- F-110 / F-111 — TC-F03-15/16 命名缺 `_then_` — rev1 F-20 同类 drop（BDD 命名非硬规范）
- F-112 / F-113 — TC docstring 用项目符号 — rev1 F-19 同类 drop（docstring 风格非硬规范）

## 4. 跨维度洞察

- **三方流程正常工作**：reviewer dispatch + critic + judge 闭环让 rev1 全部 critical/major 在 rev2 闭环；trend-G-meta 收敛（仅 F-108/F-109 unused import 漏扫，不构成新引入）
- **rev2 新引入集中在边角**：error-handling 标签精度（F-103/F-104）+ design 文案对齐（F-106/F-107）+ test hygiene（F-108/F-109）三类——无功能/契约风险，符合 looks_clean 语义
- **工程层兜底建议**：rev3 顺手修上述 5 minor 后，建议把 ruff/pyflakes 加入 pre-commit（让 unused import 类问题工具层卡）

## 5. Follow-up（不在 rev2/rev3，派后续 feature）

- **F-6** `_bootstrap_requirement` 126 行（rev2 从 146 降至 126，方向正确但仍超 80）→ 下个 sprint 拆 `_run_baseline_or_skip` + `_emit_workflow_started_event` 两子函数
- **F-7** `workflow_bootstrap.py` 874 行（rev2 增 70 行）→ 派独立 refactor feature 拆 `workflow_bootstrap_rollback.py` + `bootstrap_renderer.py`

## 6. 待 sign-off 提示

本次评审输出 **conclusion = looks_clean**（机器评估，CR-1/3/4 全满足）。可走 sign-off：

```
# tty 终端执行
/code-review:signoff REV-REQ-2026-014-code-F-004-002 --decision=approved
# 或：
python3 scripts/lib/save_review.py signoff --rev-id REV-REQ-2026-014-code-F-004-002 --decision approved
```

sign-off 后 `feature-lifecycle-manager` 将把 F-004 status 转 done 并释放 dispatch lock。

5 条 suggestion（F-103/104/106/107/108+109）可：(a) 即刻在 sign-off 前/后单独 commit 一次 nit cleanup；(b) 推迟到 rev3 顺手修；(c) follow-up 到 F-006（涉及 standard-8phase.yaml worktree config 与本次 F-106 文案同源）。
