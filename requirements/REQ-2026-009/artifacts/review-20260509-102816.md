# 代码审查报告 · REQ-2026-009 / F-005 · 2026-05-09 10:28:16

## 结论

**needs_attention** · 0 critical / 4 major / 4 minor

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项（**当前结论**）
- `blocked` — AI 机器评估发现关键问题

> AI 机器评估指出 4 条 keep+major 集中在事件类型/状态语义层（F-1/F-2/F-3 同根：F-001 白名单未扩 cancel_taskstop_failed / run_resumed / save 三个 spec 事件，F-005 实现用代理事件凑合，被 RunState.WORKFLOW_EVENT_TO_STATE 派生 state 时错误推送；F-13 是 save.md 漏 completed 的孤本错）。F-4 是 detailed-design §1.2.6 文档孤本错+自相矛盾，实现侧反而正确，应改文档不改代码。无 critical → 不阻塞，但 4 major 同向破坏状态机契约不能 looks_clean。

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：accept（推荐 checker 集）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-09 10:12:44
- **跑了哪些 checker**：design-consistency-checker, auxiliary-spec-checker
- **跳过的 checker（含原因）**：
  - complexity-checker：路径未命中 must/suggest 任何规则
  - security-checker：路径未命中 must/suggest 任何规则
  - concurrency-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - error-handling-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。

> 本次为 needs_attention，建议**先派 rev2 修复 4 keep+major** 后再 sign-off；如确认接受现状直接通过，可走 `/code-review:signoff` 但需在 sign-off 时附 reason。

```bash
/code-review:signoff REV-REQ-2026-009-code-F-005-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-009-code-F-005-001 --trivial  # 纯文档变更快速通道（不适用本次）
```

未 sign-off 时 feature-lifecycle-manager 不会把对应 feature 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-1** [design-consistency-checker · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：源码自验 workflow_cancel.py:113-118 注释自承"代理"，run_state.py:74 把 workflow_failed→state=failed 破坏 cancel 路径语义；known-bad workaround 不掩盖 spec 偏离
- **F-2** [design-consistency-checker · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：workflow_continue.py 全文 107 行零 append_event；reference/command-implementations/continue.md:33 自我宣称已写 run_resumed 是空头支票；4 处 spec 一致要求
- **F-3** [design-consistency-checker · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：workflow_save.py:26 + run_state.py:72 共谋让 save 把 state 推到 paused；矩阵 §1.3 save 在 5 态都允许暗示 save 不改 state
- **F-4** [design-consistency-checker · major] → critic: not_proven → **最终处置：follow-up（架构修文档）**
  - 裁决依据：detailed-design §1.2.6 自相矛盾（28 行 →approved 106 行 →completed）；实现 + RunState 派生表 + skill + commands 四方一致 →running，§1.2.6 是孤本错；应在 architect 修文档而非改代码
- **F-5** [auxiliary-spec-checker · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：features.json modules 是 plan-phase locator 入口；路径漂移让后续 feature 派发拉错文件
- **F-6** [auxiliary-spec-checker · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：AC pytest 路径与实际类方法名不符；自动化执行 pytest -k 不会命中
- **F-7** [auxiliary-spec-checker · major] → critic: not_proven → **最终处置：downgrade（major→minor）**
  - 裁决依据：_cmd 后缀偏离合理（避同名 F-010），但 docstring 未直接说明，新人困惑；降级 minor 提醒补一行注释即可
- **F-8** [auxiliary-spec-checker · minor] → critic: not_rebutted → **最终处置：follow-up（归并 F-17）**
  - 裁决依据：F-17 抽公共方法后命名/docstring 自动统一
- **F-9** [auxiliary-spec-checker · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：保留 minor 提醒 F-17 抽公共时记得给新函数加 docstring
- **F-10** [auxiliary-spec-checker · major] → critic: not_rebutted → **最终处置：downgrade（major→minor）**
  - 裁决依据：4 文件 datetime 函数体内 import 无循环依赖，纯 PEP8 偏好违反，无功能影响
- **F-11** [auxiliary-spec-checker · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：dispatch() importlib 是动态分发的合理写法（按 cmd 名 lazy load 子模块避免 import 8 兄弟）
- **F-12** [auxiliary-spec-checker · minor] → critic: not_proven → **最终处置：follow-up（归并 F-17）**
  - 裁决依据：subprocess 函数体内 import 与 _infer_run_id DRY 同根；F-17 抽公共后自动消除
- **F-13** [auxiliary-spec-checker · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：CMD_ALLOWED_STATES + state-matrix.md + 矩阵 §1.3 三方一致 5 态含 completed；save.md:18 4 态是孤本漏写
- **F-14** [auxiliary-spec-checker · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：workflow_run.py main 82 行超 80 上限 2 行；CLAUDE.md §1.2 写"建议"非硬性
- **F-15** [auxiliary-spec-checker · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：测试中 'Step 1-4' 是 BDD 风格惯用 token，业内通用，与 TODO/FIXME/NOTE 同性质
- **F-16** [auxiliary-spec-checker · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：源码自验 22 md 文件 last_byte 全 0a（已正确换行）；reviewer 误判
- **F-17** [auxiliary-spec-checker · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：70 行 DRY 违反横跨 7 文件，是 F-005 拆 9 命令的债

## Major（强烈建议修复 — 4 条 keep）

### [design_consistency / event_semantics] scripts/lib/workflow_cancel.py:114-118

cancel TaskStop 失败用 `type=workflow_failed + data.reason=cancel_taskstop_failed` 代理 `cancel_taskstop_failed` 事件；经 `run_state.py:74 WORKFLOW_EVENT_TO_STATE['workflow_failed']='failed'` 把 state 推到 failed，破坏 cancel 路径语义。detailed-design §1.2.9 + skill cancel.md:26 要求 type=cancel_taskstop_failed。

**建议**：先扩 F-001 `VALID_EVENT_TYPES` 加 `cancel_taskstop_failed`（不映射 WORKFLOW_EVENT_TO_STATE，保持 cancel_requested 语义），workflow_cancel.py:114 type 改为 cancel_taskstop_failed；若 F-001 扩白名单需排期，临时改用 `cancel_requested` 而非 workflow_failed 避免 state 误推。

### [design_consistency / event_semantics] scripts/lib/workflow_continue.py:1-107

continue 命令全文无 `append_event` 调用，连 import 都未引；违反 detailed-design §1.2.2 + commands/continue.md:28 + skill continue.md:17 步骤 5 + reference/continue.md:33（后者还自我宣称"F-005 已提供 jsonl 事件追加（workflow_paused → 恢复标记）✓"），spec 要求恢复时写 `run_resumed` 事件。

**建议**：先扩 F-001 白名单加 `run_resumed`（不映射 WORKFLOW_EVENT_TO_STATE 保持 running），workflow_continue.py 在 _main_loop_stub 前 append_event 写 run_resumed；reference/continue.md:33 的 ✓ 改为待 F-006 完整 loop 落地后再 ✓。

### [design_consistency / event_semantics] scripts/lib/workflow_save.py:26

`_SAVE_EVENT_TYPE='workflow_paused'` 经 `run_state.py:72 WORKFLOW_EVENT_TO_STATE` 把 state 推到 paused；detailed-design §1.3 矩阵 save 在 running/paused/approval_pending/failed/completed 五态都允许，意味 save 不应改 state，当前实现违反此契约（save 后 RunState.state=paused，从 running 跑出来的 save 后 state 漂移）。同时入口 save.md:6-8 + SKILL.md:14 承诺"追加 save 事件"未披露代理方案。

**建议**：扩 F-001 白名单加 `save` / `save_checkpoint`（不映射 WORKFLOW_EVENT_TO_STATE），_SAVE_EVENT_TYPE 改为新事件名；模块顶部注释自承"等 F-001 扩展白名单后迁移"需在 F-006 之前完成迁移，否则进入 main loop 会持续踩。

### [design_consistency / auxiliary_spec] .claude/commands/workflow/save.md:18

save.md "允许状态" 段写 `running / paused / approval_pending / failed` 四态，缺 completed；scripts/lib/workflow_state_validator.py:32 CMD_ALLOWED_STATES['save'] 含 5 态、reference/category-rules/state-matrix.md:42 也含 5 态、detailed-design §1.3 矩阵亦含 5 态，三方一致 save.md 是孤本错。

**建议**：save.md:18 加 completed；用户走 save 子命令时若 state=completed（典型场景：完成后想加备注），当前 commands md 会让用户误判被拒。

## Minor（改进建议 — 5 条 keep + downgrade）

### [auxiliary_spec] requirements/REQ-2026-009/artifacts/features.json: F-005.modules

modules 路径漂移：列出的 `state-machine.md` / `error-codes.md` 在 reference/ 下不存在；`command-implementations/<cmd>.md` 缺 `reference/category-rules/` 与 `reference/command-implementations/` 层级前缀。

**建议**：更新 features.json F-005.modules 字段对齐实际文件路径，避免 plan-phase locator 失效。

### [auxiliary_spec / test_locator] requirements/REQ-2026-009/artifacts/features.json + tasks/F-005.md

AC pytest 路径写顶层函数 `::test_each_command_happy_path`，实际 tests/skills/test_workflow_commands.py 用 TestHappyPath 类方法（test_run_creates_workflow_started_event 等），路径形式应为 `::TestHappyPath::<method>`。

**建议**：AC 改写为 `tests/skills/test_workflow_commands.py::TestHappyPath`，让自动化执行器 pytest -k 能命中。

### [history_context / code_style] scripts/lib/workflow_{approve,cancel,reject,save}.py:73/96/90/79（downgrade major→minor）

datetime stdlib import 放在 main() 函数体内（4 文件），PEP8 偏好顶部 import 且无循环依赖正当理由。

**建议**：把 `from datetime import datetime, timezone` 提到模块顶部 import 段，与 sys / pathlib 同级。

### [naming / history_context] scripts/lib/workflow_rollback_cmd.py:1（downgrade major→minor）

`_cmd` 后缀偏离 8 兄弟文件 `workflow_<cmd>.py` 命名（避免与 F-010 workflow_rollback.py 同名冲突），workflow_rollback_cmd.py:1 docstring 未直接说明此偏离原因。

**建议**：模块 docstring 加一行"_cmd 后缀避免与 F-010 workflow_rollback.py 冲突"。

### [code_style / documentation] scripts/lib/workflow_{approve,reject,cancel,rollback_cmd}.py:81/98/126/83

`_infer_run_id` 4 文件缺 docstring；continue / save / status 同名函数有 docstring 形成不一致。

**建议**：F-17 抽公共后此问题自动消除。

### [complexity / DRY] scripts/lib/workflow_{approve,cancel,continue,reject,save,rollback_cmd,status,list}.py

`_infer_run_id` 函数 7 文件复制 70 行（_infer_run_id_from_branch 命名 + 4 文件缺 docstring 同根），DRY 违反，未来分支格式变更需 7 处同步。

**建议**：抽到 scripts/lib/common.py 一处实现，签名 `infer_run_id_from_branch(repo_root: Path) -> str | None`，统一 docstring + 命名。

## Follow-up Notes（不阻塞，但值得提醒）

- **F-4**：detailed-design §1.2.6 自相矛盾（第 28 行写 →approved 第 106 行写 →completed）；实现 + RunState 派生表 + skill md + commands md 四方一致 →running，§1.2.6 是孤本错。建议在 architect / 详细设计修订时同步 §1.2.6 文字（来自 F-4）
- **F-8**：`_infer_run_id_from_branch` vs `_infer_run_id` 命名不统一，归并 F-17 一并处理（来自 F-8）
- **F-12**：subprocess 函数体内 import 与 _infer_run_id DRY 同根；F-17 抽公共后自动消除（来自 F-12）

## 未运行的 checker

- ⚠️ complexity-checker（路由跳过：路径未命中 must/suggest 规则）
- ⚠️ security-checker（路由跳过）
- ⚠️ concurrency-checker（路由跳过）
- ⚠️ performance-checker（路由跳过）
- ⚠️ error-handling-checker（路由跳过）
- ⚠️ history-context-checker（路由跳过）

## 跨维度洞察

1. **F-1/F-2/F-3 三条 major 同根**：F-001 事件类型白名单未扩 cancel_taskstop_failed / run_resumed / save 三个 spec 事件，F-005 用代理事件凑合（workflow_failed/无事件/workflow_paused），代理事件经 RunState.WORKFLOW_EVENT_TO_STATE 派生 state 时会错误推送（cancel→failed / save→paused），破坏 detailed-design §1.3 状态矩阵契约。
2. **F-8/F-9/F-17 同根**：_infer_run_id 7 文件复制是 F-005 拆 9 命令时的 DRY 债。
3. **F-4 是 detailed-design §1.2.6 文档孤本错**：实现侧反而正确，应改文档不改代码。

## 审查元信息

- **审查范围**：F-005 范围（embedded mode），34 文件，+2621 -0 行
- **并行 checker**：2（design-consistency-checker / auxiliary-spec-checker），其余 6 个被 routing.py 路径规则跳过
- **Critic 统计**：rejected 1 / not_proven 6 / not_rebutted 10
- **综合模型**：code-quality-reviewer
- **Score**：78
- **Base SHA**：b99d553
- **Head SHA**：4da0089
- **Verdict 文件**：`requirements/REQ-2026-009/reviews/code-F-005-001.json`
- **Review ID**：REV-REQ-2026-009-code-F-005-001
