# 代码审查报告 · REQ-2026-009 / F-007 · rev7 · 20260509-215748

## 结论

**needs_attention** · 0 critical / **1 keep+major** / 2 keep+minor + 1 follow-up · 2 drop

- score: **86/100**（rev1 74 → rev2 76 → rev3 82 → rev4 84 → rev5 80 → rev6 78 → **rev7 86，+8 反弹**）
- review_id: `REV-REQ-2026-009-code-F-007-007`
- supersedes: `REV-REQ-2026-009-code-F-007-006`
- reviewed_commit: `b95dd2d`
- verdict 落盘: `requirements/REQ-2026-009/reviews/code-F-007-007.json`

## 🎯 关键里程碑：3 项 anti-pattern 终结

rev7 是 rev1→rev7 七轮 review 以来质量最高的一轮：

| anti-pattern | 性质 | rev7 终结证据 |
|---|---|---|
| **design_consistency §6.7 表第 3 次漏函数** | rev3 H-9 → rev5 F-9 → rev6 又漏 — 三轮反复 | rev7 line 927 已列 `_now_iso8601 / _write_meta_json / _read_meta_json` 三函数；grep 实证落地在 `workflow_rollback_lock.py:134/139/162` ✅ |
| **auxiliary_spec E302 工具链状态背离** | rev5 ruff --fix 自报修但漏 / rev6 主模块修但测试文件引入 | rev7 测试文件 line 51-52 加空行；`pycodestyle --select=E301,E302,E303` 在主模块 + 测试文件均 0 错误 ✅ |
| **history_context dispatch 滞后** | rev3-rev6 持续高频 + 修一半夹带 | rev7 单 commit / 3 文件 / commit message **首次显式列「非目标」段**（git log 实证）✅ |

## 路由说明

- **决策**：all（用户在 tty 选 a — 全 8 个 checker）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-09 21:43:44
- **跑了哪些 checker**：8 个全跑
- **scope**：3 文件 / +77/-3 行（rev7 严格 scope 收尾）

## rev6 → rev7 进展（7 项 keep + 1 follow-up 全闭合 ✅）

| rev6 finding | rev7 闭合证据 |
|---|---|
| F-1 (rev6 自引入 major: 测试 E302) | commit b95dd2d — line 51-52 加空行 ✅ pycodestyle exit 0 |
| F-2 (rev6 自引入 major: §6.7 表第 3 次漏) | commit b95dd2d — 表格补 _now_iso8601 / _write_meta_json / _read_meta_json ✅ |
| F-3 (rev6 自引入 major: §6.3 IOError 偏离) | commit b95dd2d — 改"RollbackError（包装 OSError）" ✅ |
| F-4 (§6.6 缺 _consume_residual_new 描述) | commit b95dd2d — 表格新增 KI-step 行 ✅ |
| F-5 (_consume_residual_new os.replace 缺 try/except) | commit b95dd2d — 加 try/except + RollbackError + new_path/jsonl_path 上下文 ✅ |
| F-7 (_consume_residual_new docstring 缺 Args/Returns) | commit b95dd2d — 加 Args/Returns/Raises 4 段 ✅ |
| F-8 follow-up (KI-step-1 测试) | commit b95dd2d — 新增 test_resume_recovers_from_archive_ki_at_step1_only_new_written **但断言无红→绿区分度 → F-1 keep+major** |

## ❌ rev7 新发现 1 项 keep+major

### F-1 · KI-step-1 测试假闭合（concurrency · major）

- 路径：`tests/lib/test_workflow_rollback.py:677-732`
- critic verdict: `not_rebutted`
- **Judge 沙箱实测复现 100%**：把 `workflow_rollback.py:257-262` 的 `try/os.replace/except` 整段替换为 `pass`，新增的 `test_resume_recovers_from_archive_ki_at_step1_only_new_written` 仍 100% PASS（对照组 `test_resume_recovers_from_archive_ki_after_tail_write` 在同一 patch 下 FAIL，对比鲜明）
- 根因机理：
  1. KI-step-1 场景下 `new_path` 存在但 `tail_path` 不存在
  2. `_resume_in_progress:317-319` 续跑路径 `_consume_residual_new` 调用之后还有 `if not tail_path.exists(): _truncate_jsonl_to_tail(...)` 兜底
  3. `_truncate_jsonl_to_tail:125` 用 `with new_path.open("w")` "w" 模式直接覆写残留 .new
  4. 整条 happy path 不需要 `_consume_residual_new` 的 os.replace 也能让 .new 消失 + tail 出现 + jsonl 截断 → 4 个 assert 全部满足
- **测试断言无法证伪上游 helper 是否真工作**

### F-1 性质判断（关键）

**这不是 rev5/rev6"修一半夹带"anti-pattern 的局部复发**：
- rev5/rev6 那种是"实现修一半 + 工具链状态报告系统性脱节"双面问题
- F-1 是测试设计层面的"无效断言"：写了一个 happy path 测试，但断言条件被下游兜底逻辑天然满足

**修复 ≤ 5 行**：rev8 用 `caplog` 抓 `workflow_rollback.py:253 logger.warning` 或 mock `_consume_residual_new` 验证调用次数。**不动实现代码**。

### F-1 修复建议

```python
# rev8 应在 test_resume_recovers_from_archive_ki_at_step1_only_new_written 中加：
import logging
caplog.set_level(logging.WARNING, logger="workflow_rollback")
# ...续跑后...
assert "检测到残留 .new" in caplog.text, "F-1: _consume_residual_new 路径必须被命中"
```

## Minor 问题（2 项）

### F-2 · detailed-design.md:842 §6.3 表 archive_root vs new_path 描述不精确（error_handling · minor）

- 路径：`detailed-design.md:842`
- 表格写"含 jsonl_path / archive_root 上下文"
- 实际：`_consume_residual_new` 包装 `new_path / jsonl_path`（无 archive_root），仅 `_truncate_jsonl_to_tail` 包装 `jsonl_path / archive_root`
- 修复（≤ 3 行）：上下文列细化为两路径
  ```
  | RollbackError（包装 OSError） | jsonl 截断步骤失败 | 1（_truncate: jsonl_path / archive_root；_consume_residual_new: new_path / jsonl_path） |
  ```

## Follow-up（1 项）

| F-id | 维度 | 处置 | 理由 |
|---|---|---|---|
| F-5 | complexity | follow-up | 主模块 511 行（rev6 末 497 + F-5 try/except +5 + F-7 docstring +9 = +14）；rev6 已记账 keep+minor，rev7 trade-off 在合理范围（CC 平均 A 2.79，无新热点）；F-007 done 后随观察期下降至 ≤ 480 行 |

## Drop（2 项）

| F-id | 维度 | drop 理由 |
|---|---|---|
| F-3 | error_handling | _consume_residual_new os.replace OSError 路径无独立单测；但已通过 _truncate_jsonl_to_tail 同型测试隐含覆盖 |
| F-4 | history | detailed-design.md 90 天 26 commit 是 rev1→rev7 自然演化产物；rev7 commit message 含「非目标」段已转向，扣 history 8 分记录已足够 |

## 维度评分汇总（rev6 → rev7）

| 维度 | rev6 | rev7 | Δ | 备注 |
|---|---|---|---|---|
| design_consistency | 72 | **96** | **+24** | §6.7 表第 3 次漏 anti-pattern 终结 |
| security           | 92 | 97 | +5 | 无 finding |
| concurrency        | 91 | 83 | **-8** | F-1 假闭合扣分 |
| complexity         | 96 | 88 | -8 | 511 行 trade-off |
| error_handling     | 85 | 88 | +3 | F-2 表精确性偏离 |
| auxiliary_spec     | 70 | **95** | **+25** | E302 anti-pattern 终结（pycodestyle 全 0） |
| performance        | 94 | 97 | +3 | 无 finding |
| history_context    | 100 | 92 | -8 | 90 天 26 commit；commit 自约束机制建立 |

## 跨维度洞察

1. **3 项 anti-pattern 一次性终结**：rev3-rev6 反复出现的 §6.7 漏函数 / E302 工具链 / dispatch 滞后，rev7 一次性闭合。验证"严格 scope + commit message 显式列非目标段"的工程纪律有效。

2. **F-1 假闭合是新型 minor anti-pattern**：测试设计 anti-pattern（下游兜底覆盖上游 helper）— 不是实现 anti-pattern 复发。修复极小（≤ 5 行 caplog），不影响功能正确性。

3. **趋势曲线首次反弹**：74 → 76 → 82 → 84 → 80 → 78 → **86**，+8 升幅最大。验证 rev6 严肃警告"必须 stop the bleed"+ rev7 严格 scope 路径是有效的工程干预。

4. **rev7 工程纪律亮点**：
   - 单 commit / 3 文件 / +77/-3 行（**严格 scope** 真守）
   - commit message 首次显式列"非目标"段建立自约束（git log 实证）
   - 8 维度评分 6 升 2 降（concurrency / complexity 因 F-1 + 511 trade-off 暂降）
   - 全文件 pycodestyle / ruff exit 0（lint 真零残留）

5. **rev8 是真终结轮**：仅修 F-1 + F-2 共 ≤ 15 行，预期升 looks_clean ≥ 92。F-007 done 后该模块进入观察期 ≥ 4 周。

## rev8 优先级（**真终结轮 · 严格 scope ≤ 15 行**）

| 优先级 | finding | 修复动作 | 估时 |
|---|---|---|---|
| **P0** | F-1（major） | `tests/lib/test_workflow_rollback.py:707-732` 加 `caplog.set_level(logging.WARNING, "workflow_rollback")` + `assert "检测到残留 .new" in caplog.text` | ≤ 10 行 |
| **P1** | F-2（minor） | `detailed-design.md:842` 上下文列细化为两路径 | ≤ 3 行 |

**rev8 commit message 必须延续 rev7 自约束**：
- ❌ 严禁任何代码逻辑变更
- ❌ 严禁不在 2 项清单的 finding
- ❌ 严禁 ruff/pycodestyle 全文件批量 --fix
- ✅ 单 commit / 仅 2 文件 / 总变更 ≤ 15 行

预期 rev8 升 looks_clean ≥ 92 → trivial sign-off 通道开启 → F-007 真终结。

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`。这只是 AI 的机器评估，不代表合并通过。

**严禁**：
- ❌ `approved`（standard）通道：F-1 keep+major（测试覆盖正确性问题）
- ❌ `approved-trivial` 通道：F-1 不属于 lint/纯文档变更

**必须**：
✅ 派 rev8 严格 scope（≤ 15 行 / 单 commit / 含「非目标」段）→ rev8 升 looks_clean → trivial sign-off

```bash
# ❌ 严禁直接 sign-off：
# /code-review:signoff REV-REQ-2026-009-code-F-007-007              # 不允许 standard
# /code-review:signoff REV-REQ-2026-009-code-F-007-007 --trivial    # 不允许 trivial
```

未 sign-off 时 `feature-lifecycle-manager` 不会把 F-007 转 done；GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。
