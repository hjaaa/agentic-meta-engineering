# 代码审查报告 · REQ-2026-009 / F-007 · rev6 · 20260509-212655

## 结论

**needs_attention** · 0 critical / **3 keep+major** / 4 keep+minor · 4 drop

- score: **78/100**（rev1 74 → rev2 76 → rev3 82 → rev4 84 → rev5 80 → **rev6 78**，**第二次下探 -2，趋势确立**）
- review_id: `REV-REQ-2026-009-code-F-007-006`
- supersedes: `REV-REQ-2026-009-code-F-007-005`
- reviewed_commit: `c9b1611`
- verdict 落盘: `requirements/REQ-2026-009/reviews/code-F-007-006.json`

## ⚠️ 严肃 anti-pattern 警告

**rev6 是 6 轮以来第二次"修一半"**：闭合 12 项 keep + 自引入 3 项 major。**模块迭代质量正在劣化，必须 stop the bleed**。本轮严禁 trivial / standard sign-off，必须派 rev7 严格限定 scope（纯文档 + 纯 lint + 1 测试用例，禁止任何重构）收尾。

## 路由说明

- **决策**：all（用户在 tty 选 a — 全 8 个 checker）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-09 21:12:08
- **跑了哪些 checker**：8 个全跑

## rev5 → rev6 进展（11 项 keep 闭合 ✅ 但带新债）

| rev5 finding | rev6 闭合证据 |
|---|---|
| F-1 (rev5 自引入 major: SubRunArchive 前 E302) | commit 164945d — line 47 加空行；pycodestyle 主模块 0 错误 ✅ |
| F-2 (rev5 自引入 major: 续跑漏洞 .new 兜底缺失) | commit 164945d + 0b3adc9 — 抽 _consume_residual_new helper 在 _resume_in_progress:303 调用；红→绿独立验证 ✅ |
| F-5 (F-13 to_node 缺单测) | commit 9fc1a80 — 5 参数化用例（空串/换行/空格/特殊字符/路径穿越） ✅ |
| F-6 (_execute_with_in_progress docstring 陈旧) | commit 0b3adc9 — docstring 反映"成功路径调用"实际语义 ✅ |
| F-8 (_truncate_jsonl_to_tail 三步无 logger) | commit 0b3adc9 — 三步 logger.debug + try/except 包 RollbackError ✅ |
| F-9 (§6.7 lock 表过时) | commit 9fc1a80 — 表更新 5 个 helper（**但漏 rev6 新下沉的 3 个 → F-2 critical 新引入**） |
| F-10 (§6.4 伪码缺 KI 注释) | commit 9fc1a80 — 加 rev5 F-23 修正注释 ✅ |
| F-11 (_resolve_and_validate import 缺空行) | commit 0b3adc9 — line 132 已有空行 ✅ |
| F-12 (_get_exceptions 无 docstring) | commit 0b3adc9 — 加单行简体中文 docstring ✅ |
| F-13 (_discover_by_target_id docstring 仅一行) | commit 0b3adc9 — 扩四段（描述+Args+Returns+Raises） ✅ |
| F-15 (主模块 511 行越阈) | commit 0b3adc9 — 下沉 3 函数到 _lock；主模块 511→**497 < 500** ✅ |
| F-16 follow-up (CC=10 边界) | commit 0b3adc9 — 抽 _consume_residual_new helper 维持 _resume CC=10 ✅ |

**全部 11 项 rev5 keep + 1 follow-up 真闭合**。

## ❌ rev6 自引入新 bug（3 项 major）

### F-1 · tests/lib/test_workflow_rollback.py:52 _setup_run_dir 前 E302（aux_spec · major · rev6 新引入）

- pycodestyle 实测：`52:1: E302 expected 2 blank lines, found 1`
- 根因：rev6 commit 164945d 新增 `_setup_run_dir` helper 函数，注释块 `# ===` 后仅 1 空行直接接 `def`
- commit 0b3adc9 message 自报"pycodestyle 0 错误"实测对测试文件 exit 1 — **第 3 次工具链状态报告与实测背离**（rev5 cc2d22c "ruff --fix" / rev6 164945d "真修 E302" / rev6 又测试文件 E302）
- 与 rev5 F-1（同 E302 类，主模块）相同 anti-pattern
- 修复（1 行）：line 52 前补 1 空行

### F-2 · detailed-design.md:926 §6.7 表漏 rev6 新下沉的 3 个函数（design · major · rev6 新引入第 3 次）

- 实测：§6.7 表当前列 5 函数（`_acquire_flock / _find_in_progress_archive / _validate_resume_meta / _unlink_in_progress / _release_with_unlink`）
- 实际 lock 模块 9 个函数，rev6 commit 0b3adc9 新下沉的 `_now_iso8601`（line 134）/ `_write_meta_json`（line 139）/ `_read_meta_json`（line 162）**未补入**
- **§6.7 表 anti-pattern 第 3 次发生**（rev3 → rev4 F-9 → rev5 → rev6 又漏）— 设计文档同步不可靠
- critic 原标 critical → Judge 降级 major（不影响运行时正确性，仅文档表格不一致）
- 修复（3 行）：表格补 3 函数

### F-3 · detailed-design.md:842 §6.3 IOError 行 vs RollbackError 包装行为偏离（design · major · rev6 新引入）

- 实测：§6.3 表 IOError 行写"重抛标准异常"，但 rev6 F-8 修复（commit 0b3adc9）将 OSError 包成 RollbackError(`含 jsonl_path/archive_root 上下文`)
- F-8 实现进展未同步到 §6.3 异常契约表
- 修复（1 行）：表格 IOError 行改"OSError（jsonl 截断步骤）→ 包装为 RollbackError 抛出，包含 jsonl_path / archive_root 上下文"

## Minor 问题（4 项）

### F-4 · detailed-design.md:914-919 §6.6 缺 _consume_residual_new 路径描述（design · minor）

- §6.6 中断保护测试矩阵未列 KI-step-1 / KI-step-2 续跑场景
- 修复：加 1-2 行说明 `_consume_residual_new` 在续跑入口检测 .new 残留 + os.replace 收尾后再判 tail

### F-5 · workflow_rollback.py:248 _consume_residual_new os.replace 缺异常上下文（error_handling · minor）

- 当前直接调 `os.replace(...)`，失败抛原始 OSError 无 logger 上下文，且不包 RollbackError
- 与同 rev F-8（_truncate_jsonl_to_tail 包 RollbackError）风格不一致
- 修复（5 行）：加 try/except 包 RollbackError(f"residual .new 收尾失败：{exc}") from exc

### F-7 · workflow_rollback.py:233-241 _consume_residual_new docstring 缺 Args/Returns（aux_spec · minor）

- 当前 docstring 只有意图说明，无 Args/Returns 段
- 与同 rev `_discover_by_target_id`（已修四段结构）不对称
- 修复（4-6 行）：补 Args/Returns 段

### F-8 · KI-step-1 缺专项测试（concurrency · minor）

- 当前测试 `test_resume_recovers_from_archive_ki_after_tail_write` 覆盖 KI-step-2（tail 已写、os.replace 未执行）
- 未覆盖 KI-step-1（仅 .new 写完、tail 未写）独立场景；当前依赖 `_truncate_jsonl_to_tail` 幂等性兜底，应补专项测试
- 修复（~10 行）：加 monkeypatch 第 1 步 OSError 测试

## Drop（4 项）

| F-id | 维度 | drop 理由 |
|---|---|---|
| F-6 | error_handling | _execute_rollback docstring 不强制同步异常传播；critic 反证 not_proven |
| F-9 | concurrency | _release_with_unlink LOCK_UN/close 无 try/finally pre-existing；rev4/5 已 drop |
| F-10 | concurrency | rollback_run finally LOCK_UN/close 无内层 try/finally pre-existing；rev4/5 已 drop |
| F-11 | concurrency | _find_in_progress_archive → _acquire_flock TOCTOU pre-existing；rev3/4/5 已 drop |

## 维度评分

| 维度 | rev4 | rev5 | rev6 | Δ rev5→rev6 | 关键问题 |
|---|---|---|---|---|---|
| design_consistency | 88 | 90 | **72** | **-18** | F-2 critical / F-3 major / F-4 minor — design 系统性退步 |
| security           | 95 | 82 | **92** | +10 | F-5 真闭合（5 参数化测试） |
| concurrency        | 82 | 83 | **91** | +8 | F-2 (rev5 自引入 major) 真闭合 |
| complexity         | 78 | 91 | **96** | +5 | 主模块 497 < 500；CC 全降 |
| performance        | 92 | 92 | **94** | +2 | 0 finding |
| error_handling     | 84 | 91 | **85** | -6 | F-5/F-7 风格分裂回退 |
| auxiliary_spec     | 84 | 94 | **70** | **-24** | F-1 major + F-8 minor — 大幅退步 |
| history_context    | 96 | 98 | **100** | +2 | 0 finding；6 轮闭合追溯链完整 |

## 跨维度洞察（"修一半"沉痛分析必读）

1. **rev6 是 6 轮以来第二次"修一半"anti-pattern**：
   - rev5：修 F-24 → 反引入 F-2（archive 端做了、resume 端没对齐）+ F-1（ruff --fix 自报修但 ruff 默认不查 E302）
   - rev6：修 F-1 主模块 → 反引入测试文件 E302（同 anti-pattern 在不同文件复发）；修 F-8 archive 实现 → 忽略 §6.3 表；修 F-15 模块下沉 → 忽略 §6.7 表
   - 两轮同根：**实现层进展与文档/工具链状态报告系统性脱节**

2. **F-1 + F-2 + F-3 同根**：rev6 重构（F-15 _meta 下沉、F-8 OSError 包装）只动了实现没动文档；commit message 自报"pycodestyle 0 错误"实测背离 — **第 3 次工具链状态报告与实测脱节**。建议 rev7 起 commit pre-check 必须跑全文件 pycodestyle 而非自信表述

3. **design_consistency + auxiliary_spec 同时退步是结构性信号**：rev6 工程量集中在功能实现（concurrency / complexity / error_handling 实现），但与代码同步的"非代码工件"（设计文档表 + lint 全绿）持续被忽视。这与 rev3/rev4/rev5 反复出现的"代码先行、文档滞后"是同一 anti-pattern 的第 4 次复发

4. **趋势曲线第二次下探**：74 → 76 → 82 → 84 → 80 → **78**。rev5 已警示"首次下探 -4"，rev6 仍下探 -2 — **不是单点抖动，是趋势确立**。Judge 必须停止这个循环：rev7 必须严格限定 scope（纯文档 + 纯 lint + 1 测试），禁止任何重构/优化/新功能

5. **F-5 + F-7 暴露 rev6 重构 helper 时未照 rev3-rev5 已建立的风格规范**：archive 端 `_truncate_jsonl_to_tail` 包 RollbackError、有完整 docstring；新增 `_consume_residual_new` 不包 RollbackError、docstring 不规范。**新代码退化到第 1 轮的风格水准** — 体系学习的不持久

## rev7 优先级（**严格限定 scope**）

| 优先级 | finding | 估时 | 边界 |
|---|---|---|---|
| **P0 lint** | F-1 tests/test_workflow_rollback.py:52 加 1 行空行 | 1 行 | **仅此 1 行**，禁止顺手优化其他 lint |
| **P0 文档 1** | F-2 detailed-design.md:926 §6.7 表补 _now_iso8601/_write_meta_json/_read_meta_json | 3 行 | **仅表格** |
| **P0 文档 2** | F-3 detailed-design.md:842 §6.3 IOError → RollbackError | 1 行 | **仅表格** |
| P1 文档 3 | F-4 detailed-design.md §6.6 表加 KI 续跑场景 | 1-2 行 | **仅表格** |
| P1 风格 | F-5 _consume_residual_new os.replace 加 try/except → RollbackError | 5 行 | **仅这一函数** |
| P2 docstring | F-7 _consume_residual_new docstring 加 Args/Returns 段 | 4-6 行 | **仅 docstring** |
| P2 测试 | F-8 加 KI-step-1 monkeypatch 测试 | ~10 行 | **仅 1 个测试用例** |

**rev7 commit message 必须显式列出"非目标"**：
- ❌ 任何代码逻辑重构 / 函数抽取 / 性能优化
- ❌ 任何不在上述 7 项清单的 finding
- ❌ 任何 ruff / pycodestyle 全文件批量 --fix

**rev7 预检（必跑）**：
```bash
pycodestyle scripts/lib/workflow_rollback*.py tests/lib/test_workflow_rollback.py
# E30 类 0 个；exit 0
```

预期 rev7 升 looks_clean ≥ 92，可走 `approved-trivial` 通道。**完成 rev7 真零残留再 sign-off。此后建议模块进入 ≥ 4 周观察期**。

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`。这只是 AI 的机器评估，不代表合并通过。

**严禁**：
- ❌ `approved`（standard）通道：F-1 lint 阻断 + F-2/F-3 契约偏离，不属"零瑕疵"
- ❌ `approved-trivial`：本轮**新增了债务**而非纯闭合，trivial 通道不应承载"自引入 bug"

**必须**：
✅ 派 rev7 严格 scope 收尾 → pycodestyle 全文件 0 错误 + 设计文档表与代码 grep 双重校验 → 走 trivial 通道

```bash
# ❌ 严禁直接 sign-off：
# /code-review:signoff REV-REQ-2026-009-code-F-007-006              # 不允许 standard
# /code-review:signoff REV-REQ-2026-009-code-F-007-006 --trivial    # 不允许 trivial
```

未 sign-off 时 `feature-lifecycle-manager` 不会把 F-007 转 done；GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。
