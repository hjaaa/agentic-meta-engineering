# 代码审查报告 · REQ-2026-009 · 20260510-095335

## 结论

**needs_attention** · 0 critical / 4 major / 9 minor · score 72

> 趋势：rev1=84 → rev2=68 → rev3=72（趋势好转，但 features.json 三方漂移连续 2 个 rev 复发）

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项 ✅ 当前
- `blocked` — AI 机器评估发现关键问题

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：all（用户选 8 个 checker 全跑）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-10 09:36:20
- **跑了哪些 checker**：complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec
- **跳过的 checker（含原因）**：无（all 全跑）

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人执行：

```bash
/code-review:signoff REV-REQ-2026-009-code-F-008-003            # 对话式 sign-off（rejected → 派 rev4）
/code-review:signoff REV-REQ-2026-009-code-F-008-003 --trivial  # 纯文档变更快速通道（不适用：本 verdict 含 1 blocker required_fix + concurrency 设计层 finding）
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-008 转 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

**本 verdict 不建议 trivial 通道**：required_fixes 含 1 blocker（三方文档同步）+ concurrency 维度 F-5 是设计层 anti-pattern（controlled_sleep 60s 一刀切 → TC-F8-5 测试覆盖空洞），需要 rev4 实质修复，不是文档微调。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-1** [design-consistency-checker · critical] → critic: not_rebutted → **最终处置：keep（schema severity=major + required_fixes blocker）**
  - 裁决依据：grep 实证 features.json:306-307 + tasks/F-008.md:68-69 + detailed-design.md:1031,1046,1368 三方均写 lifecycle.py 路径；物理函数 grep 'def test_taskstop_force_kill' 命中 cancel_advanced.py:53。pytest 按 acceptance 字面跑 collected 0 items 是硬阻塞，但实际代码可绿（pytest cancel_advanced.py 8 passed），处置为 critical-but-recoverable，required_fixes 标 blocker 由 rev4 字面修复闭环
- **F-2** [history-context-checker · critical] → critic: rejected → **最终处置：drop**
  - 裁决依据：critic 反证经独立验证：grep time.sleep 实证 cancel_advanced.py:128-136 已用 threading.Event；同 commit 拆文件 + 补修是必要操作（拆 G-3 必须连带迁 G-5 否则迁过来仍 anti-pattern），不是真 anti-pattern
- **F-3** [design-consistency-checker · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：features.json:295-299 touches 数组实测仅 4 项，不含 cancel_advanced.py；与 modules:282-288（含）和 tasks/F-008.md frontmatter（含）三处不一致——与 F-1 同根因，但 touches 是独立字段需单修
- **F-4** [design-consistency-checker · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：detailed-design.md:1046 §7.5 行内仅列 test_sub_workflow_lifecycle.py 一文件 + '2 主 + 4 边界共 6 用例'描述未拆，与 F-1/F-3 同根因；spec 漂移类——保留 major
- **F-5** [concurrency-checker + performance-checker · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：代码侧实证：sub_workflow_mock.py:137-138 子 _sleep 接 (poll_interval_ms/1000)=0.01s；cancel_advanced.py:83-85 controlled_sleep(secs)=wait(timeout=max(secs,60)) 一刀切 60s 兜底；line 88 monkeypatch.setattr(agent, '_sleep', controlled_sleep) 把所有 sleep 调用替换。子线程进入 _poll_parent_cancel 后 line 137 _sleep(0.01) 实际执行 unblock_event.wait(60s)——graceful_timeout=0.2s 必然先到，TaskStop 触发——但触发时子尚未真进入业务节点，'子写 parent_cancelled 后业务节点阻塞' 边界从未真覆盖
- **F-6** [error-handling-checker · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：e2e_helpers.py:58-62 + 93-97 实证已 raise ValueError(...) from exc，不再 append 损坏行——critic 反证站得住。F-6 应是 rev1 finding 的过期残留
- **F-7** [concurrency-checker · major] → critic: not_rebutted → **最终处置：downgrade minor**
  - 裁决依据：线程时序问题真实存在（line 186 set 早于 line 187 run），但父 coordinator 5s 兜底循环（poll_interval_secs=0.02s）实际兜得住所有竞态——理论窗口存在但实测不会 flake；降为 minor 进 follow-up，避免阻塞
- **F-8** [history-context-checker · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：区块已物理删除（rev3 G-3 拆迁），违规事实不在当前 HEAD 上
- **F-9** [complexity + error-handling + auxiliary-spec · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：cancel_advanced.py:103 实测 'from sub_workflow_mock import _utc_now_iso  # noqa: F401' + line 106 actual 调用 _utc_now_iso()——noqa: F401（标记 unused import）误标
- **F-10** [complexity-checker · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：cancel_advanced.py:172 docstring vs line 196 实测仅 monkeypatch.setattr(agent, '_sleep', lambda secs: None)，无 time.monotonic 替换——文档-实现轻度漂移，保留 minor
- **F-11** [complexity-checker · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：cancel_advanced.py:43-46 实测 from test_sub_workflow_lifecycle import (_ParentCancelCoordinator, _write_event_to_jsonl)——下划线前缀视觉上属私有符号，跨模块 import 违反 PEP 8 / Python 惯例
- **F-12** [performance-checker · minor] → critic: not_proven → **最终处置：keep**
  - 裁决依据：lifecycle.py:99-100 _ParentCancelCoordinator.request_cancel_and_wait 循环每轮 get_event_types(self.child_jsonl) 实测全文重读；测试规模 jsonl < 1ms 不影响，但生产侧（F-009/F-010 真协调器）需改增量读，留 minor 提示
- **F-13** [performance-checker · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：sub_workflow_mock.py poll 全文重读不在 rev3 diff scope（rev2 存量），F-008 明确 modules 仅 +5 行 run_state.py 枚举扩展不改主体逻辑
- **F-14** [performance-checker · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：cancel_advanced.py:12 模块 docstring '总耗时 ≤ 10 秒' vs line 85 兜底 timeout=60s——文档-实现矛盾，单测正常路径靠 line 142 unblock_event.set() 兜回；保留 minor
- **F-15** [error-handling-checker · minor] → critic: not_rebutted → **最终处置：follow-up**
  - 裁决依据：lifecycle.py:109 实测是测试用 _ParentCancelCoordinator 类的协调器代码（不是生产协调器），裸 time.sleep 在 5s graceful_timeout 内可接受；纳入 follow-up 提示生产侧 F-009/F-010 真协调器需用 Event/condition variable
- **F-16** [error-handling-checker · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：lifecycle.py:349 logger.exception 用于 TC-F8-3 主动 raise 的预期路径——技术上 logger.warning 更准（exception 暗示 unexpected），但调试时 stack trace 有助定位；保留 minor 不阻塞
- **F-17** [design-consistency-checker · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：features.json:289 + detailed-design.md:1050 实证已声明 run_state.py 三事件归属（VALID_EVENT_TYPES + 'child_graceful_exited / child_force_killed / child_failed 三父侧子结局事件'）
- **F-18 / F-19 / F-20** [concurrency-checker + history-context-checker · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：notes.md:8-17 follow-up 表已含 F-4/F-7/F-16/F-20/F-29/F-32，bookkeeping 已做，不重复升级
- **F-21 / F-22** [auxiliary-spec-checker · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：controlled_sleep / slow_poll_that_blocks_after_cancel docstring 实测均含 why 描述，critic 反证成立
- **F-23** [auxiliary-spec-checker · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：cancel_advanced.py:203 def tracking_poll(parent_j: Path) -> bool: 紧跟实现无 docstring（同文件其他闭包函数都有），保留 minor
- **trend-G-meta** [history-context · minor，跨维度叠加] → **最终处置：keep**
  - 裁决依据：rev2 修 features.json:285 modules 路径，rev3 在 features.json:306-307 acceptance 路径再次漂移——同一 R005 校验对象连续两个 rev 出现同性质漂移，建议 design-consistency-checker 加 6 处全扫描

## Critical（必须修复）

> 本 verdict 无 schema-critical 级 issue。F-1 在裁决中标识为 critical-but-recoverable，落地为 schema severity=major + required_fixes severity=blocker（强制 rev4 修复闭环）。

## Required-fixes Blocker（rev4 必修闭环）

### [design-consistency] requirements/REQ-2026-009/artifacts/features.json:306-307

修 features.json:306-307 + tasks/F-008.md:68-69 + detailed-design.md:1031,1046,1368 三方文档：把 TC-F8-5/TC-F8-6 acceptance pytest 路径从 'tests/e2e/test_sub_workflow_lifecycle.py::xxx' 改为 'tests/e2e/test_sub_workflow_cancel_advanced.py::xxx'，并在 detailed-design.md §7.5 影响域追加 cancel_advanced.py。三处必须同步——R005 hash 校验 features.json，单点修不足以闭环。

## Major（强烈建议修复）

### [design-consistency] requirements/REQ-2026-009/artifacts/features.json:306-307（critical-but-recoverable）

TC-F8-5/F8-6 acceptance pytest 路径仍写 `tests/e2e/test_sub_workflow_lifecycle.py::test_taskstop_force_kill / ::test_poll_interval_boundary`，rev3 已物理迁移至 tests/e2e/test_sub_workflow_cancel_advanced.py:53,167。按 acceptance 字面跑 pytest 会 collected 0 items，AC-11/AC-12 实测路径错位。

**建议**：一并修 features.json:306-307 + tasks/F-008.md:68-69 + detailed-design.md:1031,1046,1368 三方文档（R005 hash 校验对象 = features.json，单点修不闭环）。

### [design-consistency] requirements/REQ-2026-009/artifacts/features.json:295-299

touches 数组缺 `tests/e2e/test_sub_workflow_cancel_advanced.py`（modules:284 已含、tasks frontmatter 已含），三处归属不一致。

**建议**：把 cancel_advanced.py 加入 touches 数组（与 modules 对齐）。

### [design-consistency] requirements/REQ-2026-009/artifacts/detailed-design.md:1046

§7.5 影响域仅列 test_sub_workflow_lifecycle.py（2 主 + 4 边界共 6 用例），未拆 cancel_advanced.py（与 F-1/F-3 同根因）。

**建议**：§7.5 追加 `tests/e2e/test_sub_workflow_cancel_advanced.py（TC-F8-5/F8-6 / G-3 拆出）` 并把 line 1046 的"共 6 用例"拆成两文件计数。

### [concurrency + performance] tests/e2e/test_sub_workflow_cancel_advanced.py:83-85 + tests/e2e/fixtures/sub_workflow_mock.py:137-138

`controlled_sleep(secs)=unblock_event.wait(timeout=max(secs,60))` 不区分 sleep 语义——子线程 `_sleep(poll_interval_ms/1000)=0.01s` 被强制撑成 60s wait。TC-F8-5 实际覆盖的是"子在 poll 间隔被 trap"路径，非设计意图的"子写 parent_cancelled 后业务节点阻塞"，边界测试空洞。

**建议**：拆两个 mock：`mock_poll_sleep`（立即返回，让子进入业务节点）+ `mock_business_block`（unblock_event 阻塞）；或在 controlled_sleep 内按 secs 阈值分支（< 1s 立即返回 + > 1s 走 unblock_event）。

## Minor（改进建议）

### [concurrency] tests/e2e/test_sub_workflow_lifecycle.py:185-186 + tests/e2e/test_sub_workflow_cancel_advanced.py:130-131

`agent_started.set()` 在 `agent.run()` 之前，set 仅证明线程进入 `run_agent`，未证明 `_poll_parent_cancel` 已开始；父 coordinator 5s/0.02s 循环兜底掩盖了竞态窗口。

**建议**：set() 移至 agent.run 入口或 _poll 第一次调用前；或加独立的 `poll_started_event`。

### [auxiliary-spec + complexity] tests/e2e/test_sub_workflow_cancel_advanced.py:103

`from sub_workflow_mock import _utc_now_iso  # noqa: F401` 误标——line 106 实际调用了 `_utc_now_iso()`，noqa 应删。

### [complexity] tests/e2e/test_sub_workflow_cancel_advanced.py:172

`test_poll_interval_boundary` docstring 声明"monkeypatch time.monotonic 在父侧推进虚拟时钟"，line 196 实际仅 mock `_sleep`，未 mock `time.monotonic`。

**建议**：docstring 删除 monkeypatch time.monotonic 描述（保留 _sleep no-op + cancel_written_at 真实时间戳即可）。

### [complexity] tests/e2e/test_sub_workflow_cancel_advanced.py:43-46

跨文件 import `_ParentCancelCoordinator` / `_write_event_to_jsonl`（下划线前缀私有符号），违反 Python 命名惯例。

**建议**：提取至 `tests/e2e/fixtures/e2e_helpers.py` 并去下划线（公开 helper）；或在原文件加 `__all__` 显式声明。

### [performance] tests/e2e/test_sub_workflow_lifecycle.py:99-100

`_ParentCancelCoordinator` 循环每轮全文重读 child_jsonl；测试规模影响微秒级，但生产侧协调器（F-009/F-010）若照搬此模式需改增量 tail 读。

### [performance + auxiliary-spec] tests/e2e/test_sub_workflow_cancel_advanced.py:12 + 85

模块 docstring "总耗时 ≤ 10 秒" vs line 85 controlled_sleep 兜底 timeout=60s——若 unblock_event 在线程 join 前未 set，单测理论可阻塞 60s。

**建议**：controlled_sleep 兜底改为 5s 或 docstring 改"正常路径 ≤ 10s（异常时最长 60s）"。

### [error-handling] tests/e2e/test_sub_workflow_lifecycle.py:349

TC-F8-3 `logger.exception` 用于业务预期失败路径（主动 raise RuntimeError 测 child_failed），技术上 `logger.warning` 更准（exception 暗示 unexpected）。

### [auxiliary-spec] tests/e2e/test_sub_workflow_cancel_advanced.py:203

`tracking_poll` 函数缺 docstring（同文件 controlled_sleep / slow_poll_that_blocks_after_cancel 都有）。

### [history-context · trend-G-meta] requirements/REQ-2026-009/artifacts/features.json（R005 校验对象）

rev2 修 features.json:285 modules 路径，rev3 在 features.json:306-307 acceptance 路径再次漂移——同一 R005 校验对象连续 2 个 rev 出现同性质三方文档漂移。

**建议**：design-consistency-checker 加强制 6 处全扫描清单（modules + touches + acceptance + tasks frontmatter + tasks 完成自检 + detailed-design §X.5）。

## Follow-up Notes（不阻塞，但值得提醒）

- F-15 来自 error-handling：lifecycle.py:109 `_ParentCancelCoordinator` 协调器轮询裸 time.sleep——是测试代码（不是生产）；纳入 follow-up 提示生产侧 F-009/F-010 真协调器需用 Event/condition variable
- F-7 carry-over：agent_started.set() 时序问题在父 5s 兜底下不会 flake，但若 F-009 生产侧降低 graceful_timeout 到 poll_interval 量级会真 flake
- F-12 carry-over：父 coordinator 全文重读 jsonl 在 e2e 测试不影响，但生产侧 F-009/F-010 协调器若照搬需改增量 tail 读

## 跨维度叠加洞察

1. **design_consistency × history_context**：rev2 修 features.json:285 modules 路径 → rev3 在 features.json:306-307 acceptance 路径再次漂移。同一 R005 校验对象连续两个 rev 同性质漂移，暴露 reviewer agent 缺 "features.json 全字段三方对照" 前置 checklist。建议升级 design-consistency-checker。
2. **concurrency × performance**：F-5 controlled_sleep 一刀切 60s 兜底覆盖所有 sleep 调用，导致子线程 poll 间隔 sleep(0.01) 与业务节点 sleep(60) 走同路径——测试通过靠错误路径覆盖。建议拆两个 mock。
3. **error_handling × concurrency**：F-7 set() 时序在测试 fixture 兜底下不 flake，但生产侧若降低 graceful_timeout 到 poll_interval 量级会真 flake——纳入 F-009 carry-over。

## 未运行的 checker（如有）

无 — 8 checker 全跑（routing decision=all）。

## 审查元信息

- **审查范围**：embedded mode / REQ-2026-009 F-008 rev3 / 11 文件 / +547 -208 行
- **并行 checker**：8（complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec）
- **Critic 统计**：rejected 7 / not_proven 4 / not_rebutted 12（共 23 候选 finding）
- **综合模型**：code-quality-reviewer
- **Base SHA**：2461b97（rev2 reviewed_commit）
- **Head SHA**：24b5a59
- **rev2 → rev3 趋势**：score 68 → 72（+4），rev2 4 keep（G-1 critical / G-2/G-3/G-5 major）+ G-9 minor 全闭合；rev3 自引入 1 critical（F-1 三方漂移再现）+ 3 major（F-3/F-4/F-5）+ 9 minor

## 最终评注

rev3 严格 scope 修了 rev2 的 4 个 keep finding（G-1/G-2 features.json modules 同步 + G-3 单文件 667 行拆分 + G-5 time.sleep 不对称改 threading.Event），bookkeeping（notes.md follow-up 表 + F-008.receipt.json）也补齐——闭环动作清晰。但发现 1 critical-but-recoverable + 3 major：F-1（三方文档 acceptance pytest 路径再次漂移）、F-3（features.json touches 缺）、F-4（detailed-design §7.5 未拆）、F-5（controlled_sleep 60s 一刀切兜底致 TC-F8-5 覆盖错误路径）。

**rev4 必做**：(1) features.json:306-307 + tasks/F-008.md:68-69 + detailed-design.md:1046 三方文档同步 acceptance pytest 路径；(2) features.json touches 补 cancel_advanced.py；(3) controlled_sleep 拆两个 mock（poll_sleep 立即返回 + business_block unblock_event）；(4) 顺手清 F-9/F-10/F-11/F-14/F-16/F-23 等 minor。

**判别**：needs_attention（72 分），不允许 trivial sign-off（含 1 blocker required_fix + concurrency 设计层 finding），rev4 严格 scope 后预期升 looks_clean ≥ 88。
