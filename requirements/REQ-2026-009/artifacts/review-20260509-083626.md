# 代码审查报告 · REQ-2026-009 · 2026-05-09 08:36:26

## 结论

**blocked** · 2 critical / 5 major / 9 minor

- `blocked` — AI 机器评估发现关键问题：2 条实现 bug（F-12 substitute_vars 通配语法 break 核心数据流；F-17 schema 与示例矛盾使引擎拒收 checker 输出）

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：`all`（用户选 8 全集）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-09 08:23:02
- **跑了哪些 checker**：8 个（complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec）
- **跳过的 checker（含原因）**：无

## 待 sign-off 提示

本次评审输出 `conclusion = blocked`，**这只是 AI 的机器评估**，不代表合并通过。当前 conclusion=blocked 必须先修 2 条 must_fix（F-12 + F-17）才能 sign-off：

```bash
/code-review:signoff REV-REQ-2026-009-code-F-004-001            # 对话式 sign-off（修完后用）
/code-review:signoff REV-REQ-2026-009-code-F-004-001 --trivial  # 纯文档变更快速通道（不适用本次）
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-004 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 三方透明度：reviewer 发现 → critic 反证 → Judge 裁决

- **F-1** [design-consistency · major] → critic: not_rebutted → **最终处置：keep（与 F-13 同源合并）**
  - 裁决依据：cr-prepare.md:6 frontmatter `allowed_tools=[Read,Grep]` 与正文 git+Write 调用确凿矛盾，spec §6.4 严格白名单语义会让该节点跑不起来。TC-F4-4 仅黑名单未排他验证使违规被测试固化。
- **F-2** [security · minor] → critic: not_proven → **最终处置：downgrade(minor)**
  - 裁决依据：critic 验证 shellQuote 转义防护到位，攻击链不完整；保留 minor 跟踪。
- **F-3** [performance · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：cr-critic（line 304）/ cr-judge（line 347）effort=high+model=opus 缺 idle_timeout，默认 300s 大需求场景偏紧。
- **F-4** [performance · major] → critic: not_proven → **最终处置：downgrade(minor)**
  - 裁决依据：8 checker 各自 fresh context 受 LLM token 限制，溢出阈值未量化。
- **F-5** [performance · minor] → critic: not_rebutted → **最终处置：keep**
- **F-6** [performance · minor] → critic: not_rebutted → **最终处置：keep**（dead code）
- **F-7** [complexity · major] → critic: not_rebutted → **最终处置：downgrade(minor)**
  - 裁决依据：实测 83 行 / CC≈12 / 嵌套 5；测试代码可放宽（业内惯例 100-150 行），降为 minor。
- **F-8** [complexity · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：CC≈9 在阈值 10 边界，reviewer 报 13 略夸大。
- **F-9** [complexity · info] → critic: not_rebutted → **最终处置：follow-up**（DRY 不影响功能）
- **F-10** [concurrency · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：`.review-scope.json` 工作目录根硬编码，spec §2.4 $ARTIFACTS_DIR 父子隔离已有但未应用。
- **F-11** [concurrency · major] → critic: not_proven → **最终处置：downgrade**
  - 裁决依据：与 F-15 同根，归并到 F-15 处置。
- **F-12** [concurrency · major / 关键] → critic: not_rebutted → **最终处置：keep + 升级 critical**
  - 裁决依据：亲读 scripts/lib/substitute_vars.py:30-32 VAR_REF_RE 字符类不含 `*`，`$cr-checker-*.output.findings` 字面量传给 cr-critic/cr-judge。F-004 核心数据流 break，TC-F4-2/F4-3 因 mock 流没暴露此 bug。
- **F-13** [concurrency · minor] → critic: not_rebutted → **最终处置：合并 F-1**
- **F-14** [concurrency · info] → critic: rejected → **最终处置：drop**（docstring 已自承结构性，非误导）
- **F-15** [error-handling · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：yaml 全文无 on_node_failure / retry / idle_timeout / on_subworkflow_failure 任一字段。
- **F-16** [error-handling · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：cr-prepare 异常 fallback 仅"记录警告"，下游 8 checker 静默继续跑空 diff。
- **F-17** [error-handling · major / 关键] → critic: not_rebutted → **最终处置：keep + 升级 critical**
  - 裁决依据：cr-checker-design-consistency.md:53 `{"findings": {...}}` 与 frontmatter `findings.type=array` 矛盾，引擎 schema 校验会拒收输出。
- **F-18** [error-handling · major] → critic: rejected → **最终处置：drop**
  - 裁决依据：pytest fixture 用 assert 是标准做法。
- **F-19** [error-handling · minor] → critic: not_rebutted → **最终处置：keep**
- **F-20** [error-handling · minor] → critic: not_rebutted → **最终处置：keep**
- **F-21** [design-consistency · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：denied_tools 字段表标"可选"，缺它不算违规。
- **F-22** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep**
- **F-23** [design-consistency · info] → critic: not_proven → **最终处置：drop**
  - 裁决依据：措辞歧义但测试实现正确。
- **F-24** [auxiliary-spec · major] → critic: not_rebutted → **最终处置：downgrade(minor)**（typo "并发并发"）
- **F-25** [auxiliary-spec · major] → critic: rejected → **最终处置：drop**（DUP 同 F-17）
- **F-26** [auxiliary-spec · minor] → critic: not_rebutted → **最终处置：follow-up**
- **F-27** [auxiliary-spec · minor] → critic: not_proven → **最终处置：drop**
- **F-28** [auxiliary-spec · minor] → critic: not_rebutted → **最终处置：keep**（自引用断言永真）
- **F-29** [history-context · minor] → critic: not_rebutted → **最终处置：follow-up**
- **F-30** [history-context · minor] → critic: not_rebutted → **最终处置：follow-up**
- **F-31** [history-context · info] → critic: not_rebutted → **最终处置：follow-up**

## Critical（必须修复）

### [error-handling / implementation-correctness] `.claude/workflows/prompts/code-review-embedded/cr-critic.md:42` + `cr-judge.md:46`

cr-critic / cr-judge 用 `$cr-checker-*.output.findings` 通配语法引用 8 路 findings；亲读 `scripts/lib/substitute_vars.py:30-32` VAR_REF_RE = `\$(?P<node>[a-z][a-z0-9-]*)\.output(?:\.field)?`，字符类不含 `*`，整段不会被替换，cr-critic/cr-judge 实际拿到字面量字符串而非真实 findings 数据。F-004 核心数据流（checker → critic → judge）break；TC-F4-2/F4-3 因 mock 流（mock 8 路 findings）没暴露此 bug。

**建议**：把通配引用展开为 8 个显式节点引用：

```yaml
# cr-critic prompt 输入段：
收集 8 个 checker 的所有 findings：
- $cr-checker-security.output.findings
- $cr-checker-performance.output.findings
- $cr-checker-complexity.output.findings
- $cr-checker-concurrency.output.findings
- $cr-checker-error-handling.output.findings
- $cr-checker-design-consistency.output.findings
- $cr-checker-auxiliary-spec.output.findings
- $cr-checker-history-context.output.findings
```

cr-judge.md:46 同样展开。**或**在 substitute_vars 增加 `$ALL_<prefix>.output.<field>` 聚合语法（spec §6.5 扩展）+ 加 e2e 测试真实跑一次派发流断言 critic 收到非空 findings 数组。

### [design-consistency / implementation-correctness] `.claude/workflows/prompts/code-review-embedded/cr-checker-design-consistency.md:53`

输出 JSON 示例 `{"findings": {...}, "stats": {...}}` 用对象大括号占位，但 frontmatter line 11 与 yaml `output_format.findings.type=array` 都声明数组。LLM 按示例输出会被引擎 schema 校验拒收。

**建议**：改为 `{"findings": [...], "stats": {...}}` 与其他 7 个 cr-checker-*.md 一致；批量 grep 8 个 cr-checker-*.md 文末示例段；增 prompt-lint 扫 ```json 示例与 frontmatter output_format 做 schema 对齐校验。

## Major（强烈建议修复）

### [design-consistency] `.claude/workflows/prompts/code-review-embedded/cr-prepare.md:6` (F-1 + F-13 合并)

frontmatter `allowed_tools=[Read,Grep]` 与正文要求 `git diff --name-only` (Bash) + 写 `.review-scope.json` (Write) 矛盾。spec §6.4 严格执行白名单——按 frontmatter 派发，cr-prepare 跑不起 git/写不成文件。TC-F4-4（test_prompt_structure.py:398-410）只做黑名单交集校验未做白名单排他验证（关联 F-22），把违规被测试固化通过。

**建议**：cr-prepare 是 shared context（主 Claude 跑），实际需要 Bash + Write 工具；frontmatter 改为 `allowed_tools: [Read, Grep, Bash, Write]`；TC-F4-4 加排他验证 `assert set(allowed_tools) <= {"Read","Grep"}` 仅对 fresh checker/critic/judge 生效，shared context 文件豁免。

### [concurrency] `.claude/workflows/prompts/code-review-embedded/cr-prepare.md:72` (F-10)

`.review-scope.json` 路径硬编码到工作目录根，无 run_id / pid 隔离；并发 review（如父 standard-8phase 嵌套子 code-review-embedded、用户独立 `/code-review` 同时跑）会互相覆盖。spec §2.4 已定义 $ARTIFACTS_DIR 父子隔离但未应用。

**建议**：改为 `$ARTIFACTS_DIR/review-scope.json`；或加 `<run_id>` 后缀；同步审 cr-prepare.md 所有"文件落盘"路径。

### [error-handling] `.claude/workflows/review/code-review-embedded.yaml:1` (F-15)

整文件 377 行无 on_subworkflow_failure / on_node_failure / retry / idle_timeout 任一字段。某 cr-checker hard-fail（LLM 超时 / 工具拒绝）时 cr-critic 配的 `trigger_rule: all_done` 在 spec §6.4 中容错的是 skip 不是 failed；fail 节点 output 是空字符串，cr-critic prompt 没指引"如何识别 missing checker"——critic 拿到空 verdicts 数组无报警。

**建议**：yaml 顶层加 `default_idle_timeout: 600000` + `on_node_failure: continue`；cr-critic prompt 加 "若某 checker output 为空字符串/findings=[]，在 summary 中明确标记 `{checker_name}: skipped/failed`，并在最终 verdicts 追加一条 severity=major 的 meta-finding"。

### [error-handling] `.claude/workflows/review/code-review-embedded.yaml:52` (F-16)

cr-prepare output_format 仅 required=[diff_range, scope_file, mode]，无 enum/min-length 约束；prompt:67 fallback 把空 diff 当 `HEAD~1..HEAD` 处理无 abort。下游 8 checker 静默跑空 diff 集合，cr-judge 给空报告掩盖问题。

**建议**：cr-prepare 节点加 `on_failure: abort` + prompt 在 diff_range 空时抛错而非降级；或在 8 checker 节点级 condition 检查 `cr-prepare.output.diff_range != ""`。

### [concurrency / performance] `.claude/workflows/review/code-review-embedded.yaml:304, 347` (F-3)

cr-critic（effort=high, model=opus）/ cr-judge（同）未显式 idle_timeout，默认 300s。8 路 findings 聚合 + 对抗 / 综合裁决在大 diff 下可能不够。

**建议**：cr-critic / cr-judge 加 `idle_timeout: 900000`（15 min），cr-prepare / cr-checker-* 保持 default。

## Minor（改进建议）

### [code-quality] `tests/workflows/test_prompt_structure.py:341` (F-7 降级)

`test_review_prompts_frontmatter` 函数 83 行 / CC≈12 / 嵌套深度 5。CLAUDE.md 80 行规范主针对生产代码，测试代码可放宽。建议拆 5 个子函数：`_check_existence` / `_check_required_fields` / `_check_context` / `_check_allowed_tools` / `_check_node_id_in_yaml`。

### [code-quality / test-coverage] `tests/e2e/test_code_review_embedded.py:118` (F-24 降级)

测试断言消息 "并发并发起点" 词重复 typo。改为 "并发起点"。

### [code-quality] `tests/e2e/test_code_review_embedded.py:311-312` (F-28)

`assert "cr-judge" in {n["id"] for n in [judge_node]}` 自引用断言永真无价值。替换为 `assert nodes_by_id["cr-judge"]["depends_on"] == ["cr-critic"]` 或删除。

### [code-quality] `tests/workflows/test_yaml_schema.py:42` (F-5)

`alternates = list(WORKFLOWS_DIR.rglob(...))` 在 `target.exists()` 前无短路，target 已找到时仍触发全目录扫描。改为：`alternates = [] if target.exists() else list(WORKFLOWS_DIR.rglob(...))`。

### [code-quality] `tests/workflows/test_prompt_structure.py:336` (F-6)

`_parse_review_frontmatter` 包装函数定义后未被调用。删除或 inline。

### [test-coverage] `tests/workflows/test_prompt_structure.py:398-410` (F-22)

TC-F4-4 仅做黑名单 Bash/Edit/Write 交集校验，acceptance "仅 Read/Grep" 排他性约束未覆盖。fresh context 文件加 `assert set(allowed_tools) <= {"Read", "Grep"}`。

### [auxiliary-spec] `.claude/workflows/review/code-review-embedded.yaml:347` (F-19)

cr-judge `merged_issues.items` / `adjudication.items` 仅 `type: object` 无 properties 约束，schema 校验过宽。补 properties 表（finding_id / severity / tags / file / description / suggestion）+ required。

### [error-handling] `.claude/workflows/prompts/code-review-embedded/cr-prepare.md:67` (F-20)

diff_range 空 fallback 仅"记录警告"，但 output_format 没有 warning 字段，下游无法识别。output_format 加 `warning: { type: string }` 可选字段。

### [security] `.claude/workflows/prompts/code-review-embedded/cr-prepare.md:43` (F-2 降级)

diff_range 命令注入面，shellQuote 已转义，攻击链不完整。保留 follow-up：cr-prepare 跑 git 命令时显式 `--` 隔离 + range 字段白名单 regex `^[A-Za-z0-9._/-]+(\.\.[A-Za-z0-9._/-]+)?$`。

## Follow-up Notes（不阻塞，但值得提醒）

- **F-9** yaml:82-296 8 cr-checker output_format 重复 ~200 行；建议 spec §6.4 增 yaml `anchors:` / `&checker_findings_schema` + `<<: *checker_findings_schema` 复用后批量重构。
- **F-26** test_yaml_schema.py:74 `test_code_review_embedded_loadable` 命名风格与同文件 `_yaml_is_loadable_if_present` 不一致（前者更短）。建议统一为 `_yaml_loadable_with_11_nodes` 类风格。
- **F-29** test_prompt_structure.py:287 F-003 fix 905e680 改过的 `_parse_frontmatter` 区域被 F-004 间接复用，仅热点提醒，无回归证据。
- **F-30** test_yaml_schema.py:39 F-001 引入测试期望 yaml 在 WORKFLOWS_DIR 根；F-004 落盘到 `review/` 子目录走 rglob fallback，无回归。
- **F-31** process.txt 近期被 fix(req-2026-009) 52f9ca7 改过 line 35；F-004 仅追加 line 65 无回归。

## 跨维度洞察

- **F-12 + F-17 共指 prompt-lint 治理空白**：prompt 文本作为可执行契约，但缺乏 prompt loader 阶段的一致性校验。建议补三类 lint：(a) 扫 prompt 正文中所有 `$<token>` 引用按 spec §6.5 正则验证；(b) 扫 ```json 示例段与 frontmatter output_format 做 schema diff；(c) 扫 frontmatter allowed_tools 与正文实际使用工具做一致性比对（覆盖 F-1/F-13）。一次性补上能拦下 F-1/F-12/F-13/F-17/F-22/F-25 共 6 条 finding。
- **F-3 / F-15 / F-16 共指错误处理治理空白**：本 yaml 是 sub_workflow 模板，被 standard-8phase 调用时若失败父 workflow 状态未定义。建议 spec §6.4 显式补"workflow 顶层 default_idle_timeout / on_node_failure"两字段。
- **F-10 暴露并发隔离设计漏洞**：scope_file 工作目录根写法把"独立模式"语义直接搬到"嵌入模式"。spec §2.4 已定义 $ARTIFACTS_DIR 父子隔离但未应用——建议批量审 F-004 所有"文件落盘"路径是否走 $ARTIFACTS_DIR。
- **测试覆盖与实现脱钩**：TC-F4-2 / TC-F4-3 都是 mock 流（mock 8 路 findings + mock critic verdicts），没有真实跑过 cr-checker → cr-critic → cr-judge 数据流。F-12 这个核心 bug 在 mock 流下永远暴露不出来。建议补 1 条 e2e：真实加载 yaml + 用 RunState 重放，断言 cr-critic 收到的 candidate_findings 是 8 路展开的真实数据而非通配字面量字符串。

## 未运行的 checker

无（8 个全跑）

## 审查元信息

- **审查范围**：embedded · REQ-2026-009 / F-004 · 20 文件 (+1702 / -6) · base=e58dc85 / head=c5cd329
- **并行 checker**：8（全集），耗时约 5 min
- **Critic 统计**：rejected 3 / not_proven 7 / not_rebutted 21
- **综合模型**：code-quality-reviewer (judge)
- **Base SHA**：e58dc85
- **Head SHA**：c5cd329
