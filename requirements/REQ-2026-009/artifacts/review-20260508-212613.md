# 代码审查报告 · REQ-2026-009 · F-001 · 20260508-212613

## 结论

**needs_attention** · 0 critical / 3 major / 2 minor

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项
- `blocked` — AI 机器评估发现关键问题

评分：**78 / 100**

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：accept（推荐路由）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-08 21:11:31
- **跑了哪些 checker**：`design-consistency-checker`、`auxiliary-spec-checker`
- **跳过的 checker（含原因）**：
  - `complexity-checker`：路径未命中 must/suggest 任何规则
  - `security-checker`：路径未命中 must/suggest 任何规则
  - `concurrency-checker`：路径未命中 must/suggest 任何规则
  - `performance-checker`：路径未命中 must/suggest 任何规则
  - `error-handling-checker`：路径未命中 must/suggest 任何规则
  - `history-context-checker`：路径未命中 must/suggest 任何规则

> ⚠️ 未审范围占 75%（6/8 checker 未触发）；F-001 是 13 features 基石且 +2472 行新代码，建议 F-002 收尾或集成阶段补一次全维度抽审。

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人执行：

```bash
/code-review:signoff REV-REQ-2026-009-code-F-001-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-009-code-F-001-001 --trivial  # 纯文档变更快速通道
```

未 sign-off 时 feature-lifecycle-manager 不会把对应 feature 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"reviewer 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-1** [design-consistency · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：spec L317/L334 + run_state.py:57 = `approval_approved`；detailed-design.md:106 + features.json:108 + tasks/F-003.md:67 = `approval_granted`——三处实证漂移，F-003 TC-F3-4 按字面量断言必失败
- **F-2** [design-consistency · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：workflow_loader.py:777-812 `_apply_defaults` 仅 setdefault 7 字段无 context；spec §6.13 L759 明列 prompt/loop 节点 context 默认 shared
- **F-3** [auxiliary-spec · major] → critic: not_proven → **最终处置：downgrade（major → minor）**
  - 裁决依据：实读 docstring 未自称"公开 API"；critic 反证"仅同包+tests 使用"成立；语义冲突存在但不致硬故障
- **F-4** [auxiliary-spec · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：run_state.py:41 与 workflow_loader.py:74 各定义独立 `WorkflowError`；F-001 是 13 feature 基石，下游跨模块 except 时类对象不等价——保留 major 是为锁住基石接口
- **F-5** [design-consistency · minor] → critic: not_rebutted → **最终处置：follow-up**
  - 裁决依据：Plan 1 是历史规划文档，不进入 F-001 实现验收；F-003 在 task 设计阶段会重新对照 spec
- **F-6** [design-consistency · minor] → critic: not_rebutted → **最终处置：follow-up**
  - 裁决依据：TC-F2-5 已显式归 F-002，F-001 任务清单未承诺测覆盖这两事件；更宜在 F-002 task.md 写明复用而非追加 F-001 测试
- **F-7** [auxiliary-spec · minor · `# CLI` 英文] → critic: not_proven → **最终处置：drop**
  - 裁决依据：CLI 是技术术语，单 section 标题英文化属合理例外
- **F-8** [auxiliary-spec · minor · `# RunState dataclass`] → critic: not_proven → **最终处置：drop**
  - 裁决依据：dataclass 是 Python 装饰器名
- **F-9** [auxiliary-spec · minor · DiscoveredWorkflow.source 注释] → critic: not_proven → **最终处置：drop**
  - 裁决依据：枚举值字面量本身英文
- **F-10** [auxiliary-spec · minor · _is_valid_when_segment docstring 英文] → critic: not_proven → **最终处置：drop**
  - 裁决依据：BNF 元符号在解析器场景保留英文是文献惯例
- **F-11** [auxiliary-spec · minor · substitute_vars.py 正则注释] → critic: not_proven → **最终处置：drop**
  - 裁决依据：正则命名捕获组本身英文
- **F-12** [auxiliary-spec · minor · argparse 延迟 import] → critic: not_proven → **最终处置：drop**
  - 裁决依据：PEP 8 允许函数内 import；workflow_loader 是库+CLI 双重身份
- **F-13** [auxiliary-spec · minor · 测试命名风格混用] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：实测同文件 4 种命名风格混用，违反 CLAUDE.md §7
- **F-14** [auxiliary-spec · minor · `tests/workflows/__init__.py` 空] → critic: rejected → **最终处置：drop**
  - 裁决依据：仓库 11 个 __init__.py 中 10 个 0 字节，0 字节是默认惯例

## Critical（必须修复）

无。

## Major（强烈建议修复）

### [design-consistency · spec-deviation] requirements/REQ-2026-009/artifacts/detailed-design.md:106 + features.json:108 + tasks/F-003.md:67

三处写 `approval_granted`，spec §5.4（specs/2026-05-08-workflow-unified-redesign.md:317/334）+ run_state.py:57 + dispatch-decision.md:46 已统一为 `approval_approved`；字面量漂移导致 F-003 TC-F3-4 测试断言会按错误事件名失败。

**建议**：把 detailed-design.md / features.json / tasks/F-003.md 三处 `approval_granted` → `approval_approved`；同时 F-002~F-013 全文 grep 一遍兜底。

### [design-consistency · default-missing] scripts/lib/workflow_loader.py:777

`_apply_defaults` 仅 setdefault `trigger_rule / retry / provider / model / effort / thinking / idle_timeout` 七字段，未对 prompt/prompt_file/loop 节点 `setdefault('context', 'shared')`，与 spec §6.13（line 759）默认值表偏离；F-002 派发逻辑若靠 `is None` 判分支会读到 None 而非 shared。

**建议**：在 for node 循环中加 `if ntype in ('prompt', 'prompt_file', 'loop'): node.setdefault('context', 'shared')`；同时 F-002 派发处加防御断言。

### [auxiliary-spec · exception-design] scripts/lib/run_state.py:41 + scripts/lib/workflow_loader.py:74

`WorkflowError` 在两模块独立定义且 common.py 不含此类；F-001 是 13 feature 基石，下游跨模块 except 会捕获到不同类，存在静默误捕风险。

**建议**：方案 A — 提取到 `scripts/lib/common.py` 让两模块 import 同一类；方案 B — 重命名为 `RunStateError` / `WorkflowLoadError` 让语义不重叠。F-002 启动前必须落定。

## Minor（改进建议）

### [auxiliary-spec · naming] scripts/lib/run_state.py:291

`_resolve_run_dir` 函数名带 `_` 私有前缀但 docstring/SKILL.md 称之为 D-007 公开入口；调研确认实际仅同包 + tests 调用，可视为 module-private 但需消除文档措辞冲突。

**建议**：二选一——(a) 去掉 `_` 前缀升级为公开 API；(b) 保留 `_`，把 docstring/SKILL.md 措辞从"公开入口"改为"内部双路径解析"。倾向 (b)。

### [auxiliary-spec · test-naming] tests/lib/test_workflow_loader.py

同文件混用 4 种测试命名风格：`test_should_xxx_when_yyy` / `test_field_priority_xxx` / `test_w000_xxx` / `test_invalid_yaml_14`；critic 实测后未能反驳。

**建议**：F-001 收尾或 F-002 开始前统一为 `should_xxx_when_yyy`（CLAUDE.md §7）；错误码用例可保留 `test_w000_xxx` 但归为 W 错误码专属命名空间。

## Follow-up Notes（不阻塞，但值得提醒）

- Plan 1 文档（plans/2026-05-08-workflow-engine-plan-1-schema-loader.md:186）的 approval `required: ["message"]` 与实现"二选一"不符；建议 plan.md / detailed-design 补一行说明 message / gate_message 二选一（来自 F-5）
- `parent_cancelled` / `parent_rolled_back` 已在 F-001 加入 VALID_EVENT_TYPES 但无显式测试覆盖，建议在 F-002 task.md 显式说明复用 F-001 枚举（来自 F-6）

## 跨维度洞察

- **F-001 是 13 feature 基石**：本次 needs_attention 的 3 条 major 必须在 F-002 启动前修完，尤其 `WorkflowError` 异类一旦延后，F-002~F-013 的 except 写法将全部需要 fixup
- **审范围 25%**：本次 routing 跳过 6 个 checker（complexity / security / concurrency / performance / error-handling / history-context），未审范围 75%；建议 F-002 收尾或集成阶段补一次全维度抽审
- **事件名漂移连带**：approval 事件类型字面量在 detailed-design / features.json / tasks 三处漂移，建议在 F-001 修订同时给 F-002~F-013 的 task.md 全文 grep 一遍统一

## 未运行的 checker（如有）

- ⚠️ complexity-checker（路径未命中 must/suggest 任何规则）
- ⚠️ security-checker（路径未命中 must/suggest 任何规则）
- ⚠️ concurrency-checker（路径未命中 must/suggest 任何规则）
- ⚠️ performance-checker（路径未命中 must/suggest 任何规则）
- ⚠️ error-handling-checker（路径未命中 must/suggest 任何规则）
- ⚠️ history-context-checker（路径未命中 must/suggest 任何规则）

## 审查元信息

- **审查范围**：embedded mode · F-001 · feat/req-2026-009 vs develop（33 文件，+2472 / -0）
- **并行 checker**：2（design-consistency-checker、auxiliary-spec-checker）
- **Critic 统计**：rejected 1 / not_proven 7 / not_rebutted 6
- **综合模型**：code-quality-reviewer
- **Base SHA**：1638f9c
- **Head SHA**：0b26c31
- **Reviewed Commit（含 metadata）**：386f2a4
- **verdict 落盘**：requirements/REQ-2026-009/reviews/code-F-001-001.json
