# 代码审查报告 · REQ-2026-009 · 2026-05-10 20:41:07 (rev2)

## 结论

**needs_attention** · 0 critical / 3 major / 9 minor · score **78** / 100 · supersedes rev1 (71/100)

- 3 major (keep): M-1（features.json + plan.md 同步债 — N-7/N-8/N-23 合并）/ M-2（trivial 通道 base 硬编码 — N-5）/ M-3（run_signoff CC=20 拆 helper — N-3）
- 9 minor (keep): M-4 ~ M-10 + 2 处独立 minor
- 5 既有问题已 drop（critic rejected：N-4/N-11/N-18/N-19/N-21）
- 2 not_proven 降级 drop（N-13 _EMAIL_RE 不可利用 / N-20 lazy import 假设场景未发生）
- 2 提醒级 critical → follow-up（N-1/N-2 history-context 提示，非真 bug）

## 路由说明

- **决策**：all
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-10 20:20:23
- **跑了哪些 checker**：8 全集
- **跳过的 checker**：无

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**不允许 trivial sign-off 直签**（含 3 major：M-1 同步债 / M-2 base 硬编码 / M-3 run_signoff 拆 helper）。

下一步：派 rev3 subagent 修 3 batch + 5 trivial minor，然后再签字。

```bash
# rev3 完成后
/code-review:signoff REV-REQ-2026-009-code-F-012-003
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-012 转 done。

## 裁决明细

### Drop（5 既有 + 2 not_proven，共 7 条）

- **N-4** [complexity · major] → critic: rejected → **drop**：_run_save 主体 16d12dc8/baaa092/d3edbce5 4-5 月前 commit，rev2 未触动
- **N-11** [complexity · minor] → critic: rejected → **drop**：_check_cr_rules CC=35 rev1 F-1 已识别既有
- **N-18** [concurrency · minor] → critic: rejected → **drop**：process.txt 无 flock 既有惯例
- **N-19** [concurrency · minor] → critic: rejected → **drop**：旧 CLI 直写既有，rev2 反而降低碰撞窗口
- **N-21** [perf · minor] → critic: rejected → **drop**：_load_schema N×3.66ms 既有
- **N-13** [security · minor] → critic: not_proven → **drop**：_EMAIL_RE \\Z 锚点缺失但实际不可利用（args 非 stdin）
- **N-20** [perf · minor] → critic: not_proven → **drop**：lazy import 假设场景未发生

### Follow-up（2 条 history-context 提醒级）

- **N-1**：scripts/lib/save_review.py 30 天内 2 条 fix commit + rev2 跨 fix 区域 refactor。blame 已确认 fix 完整保留，**非真 bug 仅提醒**
- **N-2**：scripts/gates/plugins/review_verdict_ci.py rev2 自身 fix（4417ded）+ 注释改动，**非真 bug 仅提醒**

### Keep（3 major + 9 minor）

详见下方分级段落。

## Critical（必须修复）

无。

## Major（强烈建议修复 — 3 项）

### M-1 [design-consistency / documentation_debt] features.json + plan.md 同步债

**file**: requirements/REQ-2026-009/artifacts/features.json:443-454 + requirements/REQ-2026-009/plan.md（缺 D-017）

合并 N-7 + N-8 + N-23：
- features.json F-012.modules[] 仍含 `scripts/lib/code_review_signoff.py` + `scripts/lib/phase_enum.py`（已删），未含 `scripts/lib/signoff.py` + `scripts/lib/canonical_phases.py`（rev2 新建）
- features.json F-012.touches[] 同上
- features.json F-012.description 未反映 rev2 Batch 3 实际是迁入 signoff.py 而非简单删除
- plan.md grep "D-017" 命中 0；rev2 拆 signoff.py 子模块 + save_review.py 缩到 423 行的架构决策仅 F-012.md Phase 3 笔记有记，非 ADR

**建议**：rev3 把 features.json modules/touches 与 tasks/F-012.md frontmatter 对齐；description 加"signoff 功能迁入 scripts/lib/signoff.py 子模块"；plan.md 补 D-017 ADR 留痕。

### M-2 [security / configuration] scripts/lib/signoff.py:75 trivial 通道 base 硬编码

合并 N-5：

`_get_trivial_diff_paths(base="main")` 硬编码 base='main'。本仓库 base_branch=develop（见 .review-scope.json），main 不存在 → git diff 走 except 注入 `['<git-diff-failed>']` 兜底为非文档 → rc=3 fail-closed 不放险。但 trivial 通道在 develop-only 仓库**永久不可用**。

**建议**：rev3 改 base 动态推导，优先 `git symbolic-ref refs/remotes/origin/HEAD`，fallback main → master → develop；或接受 base 参数从 .review-scope.json 注入。

### M-3 [complexity] scripts/lib/signoff.py:164 run_signoff 函数 CC=20

合并 N-3：

run_signoff 单函数 ~110 行 + 9 分支（tty/互斥/trivial/email/path/exists/json/sigexists/CR）CC≈15-20。rev1 F-3 完成文件级拆分但函数级未拆——是 'mv 而非 refactor' 的副产物。

**建议**：rev3 拆 3 个 helper：_check_args_mutex(args) / _resolve_signoff_decision(args) / _validate_signed_by(args) 各 15-30 行 CC≤5。

## Minor（改进建议 — 9 项）

### M-4 [concurrency] save_review.py:235 ↔ signoff.py:268 双向 import

N-9：save_review.py 顶层 `import signoff as _signoff_mod` + signoff.py 函数内 `import save_review as _sr` 形成双向依赖；延迟 import 规避 ImportError 但是反模式。**建议**：把双方共用 helper 抽 save_review_validation.py，让 signoff.py 只 import validation 模块。

### M-5 [security / diagnostics] scripts/lib/signoff.py:122-160 路径校验诊断信息

合并 N-14 + N-15 + N-17：
- (a) rc=4 stderr 仅 rev_id 不含路径、rc=6 paint 含 verdict_path——诊断信息不一致
- (b) verdict_path is None 与 not exists 同文案 'verdict not found'
- (c) resolve()+startswith 校验存在 TOCTOU 窗口（攻击面小）

**建议**：rev3 统一三处文案：
- rc=4 path None → "invalid REV-ID format"
- rc=4 not exists → "verdict file missing: {path}"
- rc=6 → "verdict file corrupted: {path}: {exc}"

### M-6 [design-consistency] _PHASE_REVIEW_DEPS 三处注释表述不齐

合并 N-22 + N-26：

scripts/gates/plugins/review_verdict.py:60 + review_verdict_ci.py:38 + scripts/lib/check_reviews.py:68。三处表述不齐：review_verdict_ci.py:37 多一行"phase-rules.md F-012 后已删"，其他两处没有。**建议**：三处头注统一格式 + 加"CI 由 test_phase_review_deps_consistency.py 兜底"。

### M-7 [error-handling] review_verdict_ci.py:38 PEP 8 import 顺序

N-10：rev1 F-12 仅修 save_review.py PEP 8，review_verdict_ci.py 同步债未跟进。bare import check_reviews 应排在 from common 之前。**建议**：trivial fix。

### M-8 [auxiliary / test_naming] tests/lib/test_check_reviews_r003_r007.py 5 函数名

N-28：rev1 F-33 r001 测试命名已改 should_xxx_when_yyy，r003_r007 未同步。**建议**：rev3 trivial 重命名。

### M-9 [auxiliary / api_contract] signoff.py 公开 API 未规范化

合并 N-24 + N-25：

scripts/lib/signoff.py 缺 `__all__` 声明 + 函数体内不必要 `from common import Report` 延迟（非循环依赖）。下游 save_review.py:237-239 通过私有名 _signoff_mod._resolve_verdict_path 访问。**建议**：加 __all__ + 合并 import + 改下游访问公开名。

### M-10 [history-context] commit message 范围超出实际

N-30：commit `21f5b98 fix(F-013) rev2 修 11 项 minor` 含 F-012 拆 signoff.py 内容但 prefix 未交叉引用 F-012；commit `d06e74b style(F-012)` 范围超出实际（含说"PEP 8 / 测试命名"但实际 commit 不含）。**建议**：未来同类多 feature 混合 commit 用 `fix(F-012,F-013)` 复合 prefix；本条仅记录不修。

### 单独 minor（已合并到上述 M-N）

- N-6 (rc=6 测试缺失) — degrade drop（rev1 F-25 遗留，超 rev2 范围 / 移到 follow-up）
- N-29 (test_phase_review_deps_consistency.py 双身份 import) — degrade drop（同 N-28 主体）

## Follow-up Notes

- **N-1 / N-2**：history-context-checker 提醒级 critical（非真 bug） — 不阻断 sign-off
- **N-6** (rc=6 测试缺失) — rev1 F-25 遗留，超 rev2 范围；rev3 可顺手补
- **N-29** (test 双身份 import) — 测试虽通过但意图弱化；rev3 可顺手改为裸 import

## 跨维度洞见（Judge 横切）

1. **横切风险一**：rev2 拆 signoff.py 引入 N-5 base 硬编码 + N-9 双向 import + N-3 run_signoff CC≈20——三项同根于"文件级拆分完成但函数级 / 依赖级拆分未跟进"。建议 rev3 一次性补
2. **横切风险二**：N-7+N-8+N-23 features.json 同步债是文档治理盲区——rev2 修代码但未连带回填 features.json modules/touches/description + plan.md D-017 ADR
3. **trend-G-meta 信号未终结**：rev1 issue 18 项 → rev2 处置 13 项闭合 + 5 项遗留 + 8+ 项新引入——拆模块未做完整代码迁移检查清单是根因；rev3 前主 Agent 强制做"rev2 引入文件 vs 同步债清单"对照

## 未运行的 checker

无。

## 审查元信息

- **审查范围**：rev1 头 (52b5a6c) → rev2 头 (53c437f) — 20 文件 / +892 / -295
- **并行 checker**：8 全集
- **Critic 统计**：rejected 5 / not_proven 2 / not_rebutted 23（共 30 候选 finding）
- **综合模型**：Claude（code-quality-reviewer agent）
- **Base SHA**：52b5a6c
- **Head SHA**：53c437f
- **Score 趋势**：rev1=71 → rev2=78 (+7)
- **trend-G-meta 状态**：未终结（rev2 自引入 9+ 项衍生 finding）
- **Judge recommendation**：rev3_strict（不允许 trivial_signoff；sonnet 即可）
- **Verdict 文件**：requirements/REQ-2026-009/reviews/code-F-012-002.json
