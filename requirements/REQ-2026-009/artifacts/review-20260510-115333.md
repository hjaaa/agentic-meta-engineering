# F-010 代码审查报告 · rev1

- **REV-ID**: REV-REQ-2026-009-code-F-010-001
- **scope**: F-010 · 9 个 /requirement:* 别名兼容期保留实现（D-009）+ /requirement:next 例外
- **base..head**: 70c7dc0..5a02e98
- **diff**: 11 文件 / +277 / -0
- **checker_route**: design-consistency-checker + auxiliary-spec-checker
- **conclusion**: `needs_attention`
- **score**: 85/100
- **生成时间**: 2026-05-10 11:53:33

## 严重度分布

| critical | major | minor | info |
|---|---|---|---|
| 0 | 2 | 2 | 0（after critic drop） |

## Keep（4 条，需在 rev2 修复）

### F-1 (major / lint) — tests/skills/test_alias_passthrough.py:7

`import re` 全文未使用。`ruff F401 [*] re imported but unused` 实测复现，`ruff check --fix` 一键修复。

**修复**：删除第 7 行 `import re`。

### F-2 (major / lint) — tests/skills/test_alias_passthrough.py:70

f-string 无插值占位符：`f"next.md 应含 [DEPRECATION-NEXT] 而非标准 [DEPRECATION]"`。`ruff F541 [*]` 实测复现，`ruff --fix` 一键修复。

**修复**：去掉 `f` 前缀改为普通字符串。

### F-7 (minor / comment) — tests/skills/test_alias_passthrough.py:46

`_read_cmd` docstring「读取命令文件内容（frontmatter 后全文）」与实现 `path.read_text()`（含完整 frontmatter）不符。

**修复**：docstring 改为「读取命令文件完整内容（含 frontmatter）」。

### F-8 (minor / comment) — tests/skills/test_alias_passthrough.py:107

`test_next_exception_kept_legacy` docstring 提「stderr 首段含 [DEPRECATION-NEXT] 例外文案」，但函数仅 `_read_cmd("next")` 读静态 .md 文件，不产生 stderr。

**修复**：docstring 改为「.md 文件正文含 [DEPRECATION-NEXT] 例外文案」。

## Drop（5 条，critic 反证成立）

| F-id | severity | 文件 | drop 理由 |
|---|---|---|---|
| F-3 | minor | archive.md:6 + submit.md:7 | B 类例外「请改用：节点」是双轨并存的必要适配；detailed-design.md:1310 明文「3 月兼容期内旧实现并存」支持 |
| F-4 | minor | test_alias_passthrough.py:62 | features.json TC-F10-1 字面只要求 [DEPRECATION] + 「截至 2026-08-08」；4 行全断言无 spec 依据 |
| F-5 | info | next.md:9 | TC-F10-5 grep 9/9 必需，§10.2.2 是最小模板不禁止扩展，finding 自身已承认「良性扩展」 |
| F-6 | minor | test_alias_passthrough.py:7-9 | pyproject.toml ruff `select=["F"]` 未启用 I 类，ruff 实测无 I001 |
| F-9 | info | test_alias_passthrough.py 多处 | E501 当前未启用，装饰分隔线不触发规则 |

## 裁决明细

| F-id | source | original | critic | final |
|---|---|---|---|---|
| F-1 | aux | major | not_rebutted | keep_major |
| F-2 | aux | major | not_rebutted | keep_major |
| F-3 | design | minor | rejected | drop |
| F-4 | design | minor | rejected | drop |
| F-5 | design | info | rejected | drop |
| F-6 | aux | minor | rejected | drop |
| F-7 | aux | minor | not_rebutted | keep_minor |
| F-8 | aux | minor | not_rebutted | keep_minor |
| F-9 | aux | info | rejected | drop |

critic verdict 分布：6 rejected / 0 not_proven / 3 not_rebutted（F-1/F-2 critic 标 not_rebutted 但 judge 直接 keep major）。

## 跨维度洞察

- **F-1/F-2 同文件同可修性**：两条 ruff major 均在 `test_alias_passthrough.py`，`ruff check --fix` 一次调用同时修；建议与 F-7/F-8 docstring 修正合并 rev2 一次提交，4 项合计 < 5 行
- **F-7/F-8 docstring 质量叠加**：两处函数注释描述与实现不符，未来维护者会误以为「测 stderr」实际「读 .md 文件」，维护成本低改动意义高
- **anti-pattern 警示**：参考 F-007 rev5/rev6 ruff 反复出现的 anti-pattern 教训，ruff major 不允许「走 trivial 跳修」——必须 rev2 显式修

## 建议

派 rev2 subagent 严格 scope 修 4 项（≤5 行改动）：
1. `ruff check --fix tests/skills/test_alias_passthrough.py`（修 F-1/F-2）
2. `:46` docstring 改「读取命令文件完整内容（含 frontmatter）」
3. `:107` docstring 改「.md 文件正文含 [DEPRECATION-NEXT] 例外文案」

预期 rev2 → score ≥ 92 → looks_clean → trivial sign-off。
