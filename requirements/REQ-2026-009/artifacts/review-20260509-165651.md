# 代码审查报告 · REQ-2026-009 / F-007 · rev2 · 20260509-165651

## 结论

**needs_attention** · 0 critical / 6 keep+major / 12 keep+minor · 6 drop · 6 follow-up

- score: **76/100**（rev1 74 → rev2 76 略升）
- review_id: `REV-REQ-2026-009-code-F-007-002`
- supersedes: `REV-REQ-2026-009-code-F-007-001`
- reviewed_commit: `05aa11e`
- verdict 落盘: `requirements/REQ-2026-009/reviews/code-F-007-002.json`

## 路由说明

- **决策**：all（用户在 tty 选 a — 全 8 个 checker）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-09 16:36:05
- **跑了哪些 checker**：8 个全跑
- **跳过的 checker**：无

## rev1 → rev2 进展

**rev1 14 项 keep finding 全部经 8 checker 一致核实闭合**：

| rev1 finding | rev2 闭合证据 |
|---|---|
| M-1/F-1 锁顺序违 §6.4 | rollback_run:655-673 顺序已为 flock → mkdir → O_EXCL ✅ |
| M-2/F-11 unlink/flock 释放分离 | finally 嵌套结构正确 ✅ |
| M-3/F-19 续跑 to_node 无元信息 | .meta.json 持久化 + RollbackResumeMismatchError ✅（但 atomic 缺口见 G-1）|
| M-4/F-20 sub_run 双路径 | _discover_sub_runs 单一发现策略 ✅ |
| M-5/F-25 929 行单文件 | 拆 4 子模块 ✅（但主模块仍 841 行 — 见 G-10）|
| M-6/F-13 path-traversal | re.fullmatch + is_relative_to ✅ |
| M-7/F-21 features.json touches | 已同步 ✅ |
| M-8/F-9 append_event 容忍 | 改 raise ✅（但顺序仍倒置 — 见 G-4）|
| M-9/F-23 monkeypatch 测试 | TC-F7-5b 落地 ✅ |
| M-10/F-26 主函数 12 步 | _resolve_and_validate + _setup_archive 提取 ✅（但仍 71 行）|
| M-11/F-29 全目录扫 | 三级策略部分缓解 ✅ |
| M-12/F-32 jsonl 双读 | events 参数复用 ✅ |
| M-13/F-3 import 函数内 | 主模块顶部 ✅（archive 模块新引入 G-20）|
| M-14/F-46 死参数 | nodes 参数已删 ✅ |

## 维度评分

| 维度 | score | 关键问题 |
|---|---|---|
| design_consistency | 70 | §6.3 异常表 7→8 / §6.7 影响域漏 4 子模块 / 主文件 841 行 |
| concurrency        | 72 | G-1 atomic / G-4 append→mv 顺序 |
| complexity         | 70 | 主文件 841 行 + rollback_run 71 行 |
| error_handling     | 78 | G-3 与 G-1 同根因 |
| auxiliary_spec     | 75 | 死 import + 死代码 + 风格 8 项残留 |
| security           | 90 | 仅 1 项防御性建议 |
| performance        | 88 | 仅 follow-up |
| history_context    | 92 | 触行属本特性自修 |

## Major 问题（6 项 · keep · rev3 必修）

### G-1 · M-3 续跑保护 atomic 链路缺口 [concurrency + error_handling]

合并 G-1 + G-2 + G-3。

- 路径：`scripts/lib/workflow_rollback.py:358-370`（_write_meta_json）/ `:407-433`（_resume_in_progress meta 读早于 flock）/ `:673-700`（_write_meta_json 失败路径）
- critic verdict: `not_rebutted`（3 子项均成立）
- 描述：M-3 修复方向正确但实现有 3 处缺口叠加：
  1. `_write_meta_json` 直接 `open("w")` + `json.dump`，非原子；崩溃中点留下截断 .meta.json
  2. `_read_meta_json` 在 `_acquire_flock` 之前调用（约 :408 vs :428），叠加 (1) 放大读到中间状态窗口
  3. `os.close(in_prog_fd)` 后 `_write_meta_json` 失败 → 跳过内层 finally → `.in_progress` 残留 + `.meta.json` 缺失 → 续跑 fallback warning（:419-424）→ M-3 to_node 一致性保护失效
- 修复建议：
  1. write-then-rename：`tmp = path.with_suffix(".tmp"); json.dump(...) ; os.replace(tmp, path)`
  2. 把 `_read_meta_json` 移到 `_acquire_flock` 之后
  3. `_write_meta_json` 调用纳入内层 try（紧贴 `os.close(in_prog_fd)`）+ 失败时把 .in_progress 一并清理

### G-4 · _archive_sub_run append_event 仍先于 shutil.move [concurrency]

- 路径：`scripts/lib/workflow_rollback_subrun.py:117-133`
- critic verdict: `not_rebutted`
- 描述：M-8 把 except 改为 raise，但**append→mv 顺序未改**。append 成功后 mv 前崩溃 → 续跑再次 append → 子 jsonl 出现两条 `parent_rolled_back`。下游消费方用计数语义判断 graceful 退出会错乱。M-8 修复方向偏（修了 warning 容忍但没修根因）。
- 修复建议：先 `shutil.move(child_run_dir, dest)` → 后对 `dest / "run-state.jsonl"` `append_event`。mv 失败时不应已写过事件。

### G-7 · 主模块 from run_state import append_event 死 import [auxiliary_spec]

- 路径：`scripts/lib/workflow_rollback.py:40`
- critic verdict: `not_rebutted`
- 描述：拆模块后 `append_event` 调用迁到 `workflow_rollback_subrun.py:110` 自行 lazy import。主模块顶部 `append_event` 已无引用。pyflakes F401 命中。这是公开 API 文件的模块级未使用导入，会误导阅读者以为主模块直接写 jsonl。
- 修复建议：删除 `append_event,` 这一行。

### G-10 · 主文件 841 行（任务框架 ≤280 软目标） [complexity + design_consistency]

- 路径：`scripts/lib/workflow_rollback.py`（整文件）
- critic verdict: `not_rebutted`
- 描述：rev2 拆出 4 子模块（archive 134 / lock 106 / subrun 151 / cmd 94），但主模块保留了 8 个委托壳函数（_acquire_flock / _archive_sub_run / _discover_sub_runs 等只是 `from X import _impl as ...; return _impl(...)` 转调，约 77 行噪声）。yaml 加载 + 拓扑工具 + 续跑编排仍在主模块。
- 修复建议：
  1. 删 8 个委托壳（约 -77 行）— 调用方直接 import 子模块
  2. yaml 加载 + 拓扑工具下沉到 `workflow_rollback_topology.py` 或合到 `_archive.py`
  3. `_resume_in_progress` + `_archive_pending_sub_runs` 移到 lock 模块或独立 `_resume.py`
  目标：≤ 400 行（rollback 域复杂可放宽，原 280 是软目标）

### G-15 · §6.3 异常契约表 7→8 类未回写 [design_consistency]

- 路径：`requirements/REQ-2026-009/artifacts/detailed-design.md:825-835`
- critic verdict: `not_rebutted`
- 描述：§6.3 表只列 7 类异常；rev2 实现已含第 8 类 `RollbackResumeMismatchError`（`workflow_rollback.py:103-104`）。OQ-DD-A9 闭合说明也写"7 类"，与实现不符。CLAUDE.md "文档即记忆"原则要求设计文档与实施同步。
- 修复建议：§6.3 表追加一行
  ```
  | RollbackResumeMismatchError | .meta.json 中 to_node 与调用方传入不一致（续跑验证失败）| 1 |
  ```
  并更新 OQ-DD-A9 闭合说明为"8 类"。

### G-16 · §6.7 影响域缺 4 个新子模块 [design_consistency]

- 路径：`requirements/REQ-2026-009/artifacts/detailed-design.md:912-918`
- critic verdict: `not_rebutted`
- 描述：§6.7 "新增文件" 段仅列 `workflow_rollback.py` + 测试 + fixtures，未包含 rev2 新增的 4 个子模块。features.json M-7 已同步，但 detailed-design 作为"唯一蓝图"未回写。
- 修复建议：§6.7 追加 4 行：
  - `scripts/lib/workflow_rollback_lock.py`（双层锁 _acquire/_release/_find_in_progress）
  - `scripts/lib/workflow_rollback_archive.py`（_collect/_move/_truncate_jsonl）
  - `scripts/lib/workflow_rollback_subrun.py`（_discover_sub_runs + _archive_sub_run）
  - 注：主 `workflow_rollback.py` 保留公开 API + dataclass + 异常 + CLI

## Minor 问题（12 项 · keep · 建议修）

### G-5 · _resume_in_progress 内联 flock fd 泄漏 [concurrency + error_handling]

- 路径：`workflow_rollback.py:427-434`
- 描述：`open(...)` + `fcntl.flock(...)` 同 try 块；OSError → ConcurrentRollbackError 但 lock_fd 未 close
- 修复：改用 `_acquire_flock` 工具函数（统一锁实现，与 G-6 同步）

### G-12 · rollback_run 122 行 + 嵌套 5 层 [complexity]

- 路径：`workflow_rollback.py:589-710`
- 描述：含 docstring 122 行；try → try → try → finally → try → if 嵌套 5 层（M-10 ≤4 目标超标）
- 修复：把 `_execute_rollback` 调用与 `in_progress_path.unlink()` 合并提取 `_execute_with_in_progress()` 一层

### G-17 · §6.2 new_current_node 注释与 M-14 不符 [design_consistency]

- 路径：`detailed-design.md:814`
- 描述：仍写"= to_node 的最近上游"，与 M-14 选择 B 不符
- 修复：改为"= to_node（rollback 后从此节点重新执行）"

### G-18 · §6.1 签名缺 repo_root 参数 [design_consistency]

- 路径：`detailed-design.md:787`
- 描述：rev1 F-17 drop 后 repo_root 参数保留，但 §6.1 未补
- 修复：§6.1 签名追加 `repo_root: Optional[Path] = None`（标 testability hatch）

### G-19 · tasks/F-007.md body 触及范围与 frontmatter 不同步 [design_consistency]

- 路径：`requirements/REQ-2026-009/artifacts/tasks/F-007.md:55-59`
- 描述：body 段仍是 rev1 旧 3 项，frontmatter 已 7 项
- 修复：body 触及范围段同步 frontmatter

### G-20 · workflow_rollback_archive.py:59 import shutil 函数内 [auxiliary_spec]

- 修复：移到模块顶部

### G-21 · test 文件 :412/:428 import json as _json 函数内 [auxiliary_spec]

- 修复：删除函数内重复 import，用顶部 json

### G-23 · test 文件 :389 crashed 死赋值 [auxiliary_spec]

- 修复：删除 crashed 变量；崩溃断言依赖后续 .in_progress 检查即可

### G-24 · test 文件 :488 first_result 死变量 [auxiliary_spec]

- 修复：删除该行

### G-25 · rev1 follow-up 风格残留 8 项 [auxiliary_spec]

- 包括：F-36（field 未用）/ F-39（# CLI 中文化）/ F-40（无插值 f-prefix）/ F-41（冒号空格）/ F-42（RollbackInProgressError 测试导入）/ F-43（6 处 monkeypatch 死参）/ F-44（list[dict] 缺泛型）/ F-45（行长 140）
- 修复：一次性 lint 通刷

### G-26 · _setup_archive docstring 写旧错误顺序 [concurrency + error_handling]

- 路径：`workflow_rollback.py:572-582`
- 描述：docstring 写"实际顺序：generate_ts → mkdir archive_root → acquire flock → O_EXCL"是旧错误顺序；rev2 实际为 flock → mkdir → O_EXCL。会严重误导维护者，可能复现 rev1 F-1 bug。
- 修复：注释改为"调用方已持 flock 后调用本函数；实际顺序：flock（调用方）→ generate_ts → mkdir → O_EXCL（调用方）"

## Drop（6 项 · 误报或不在范围）

| G-id | 来源 | 原 severity | critic verdict | drop 理由 |
|---|---|---|---|---|
| G-11 | complexity | major | not_proven | _discover_sub_runs CC ≈ 8（实测），未达 ≥10 阈值；checker CC=14 高估 |
| G-14 | complexity | minor | not_proven | TC-F7-5b 实测 80 行（非 114）；测试 601 行含 8 TC + helper 无重复 |
| G-22 | auxiliary_spec | minor | not_proven | subrun 三处 lazy import 是为规避循环依赖（lock.py 同模式），合理 |
| G-27 | security | minor | not_rebutted | 策略 2 sub_runs_dir 来自 run_dir 已经 M-6 path-traversal 防护，子目录扫无外部输入介入 |
| G-29 | security | minor | not_rebutted | to_node 仅做集合存在性校验（_resolve_and_validate:553-559），不参与路径/shell/SQL，不需要 fullmatch |
| G-H-1 | history_context | info | not_rebutted | 触行属本特性内自修 commit（4e33854/87a4d53），非外部 fix；checker 已自动忽略 |

## Follow-up（6 项 · 不阻塞 rev2 关闭）

| G-id | 处理策略 |
|---|---|
| G-6 | _resume 内联 flock 与 G-5 同根因，G-5 修复时一并替换 |
| G-9 | _discover_sub_runs 死参数 nodes_after/node_map 删除属 API breaking，合 G-10 主模块瘦身一并改 |
| G-13 | _archive_pending_sub_runs 与 _execute_sub_run_archive 重复，rev3 重构一并提取 |
| G-28 | _read_meta_json to_node 类型断言（与 G-1 atomic 修复一并加） |
| G-30 | _get_ordered_node_ids 4 次重复 — rollback 低频，单次延时 < 10ms 无影响 |
| G-31 | 策略 3 全目录扫 — 仓库 run < 100 时无影响 |

## 跨维度洞察

1. **并发 + 错误处理叠加（G-1 + G-4 同行/同流程）**：_write_meta_json 非原子 + _read_meta_json 早读 + append_event 先于 mv —— 三处叠加放大 M-3/M-8 的失效窗口。崩溃后 .meta.json 损坏 → fallback to_node + warning + 子 jsonl 已写 parent_rolled_back 但子目录未 mv → 续跑读到旧 to_node + 子状态污染。**rev3 把 G-1/G-4 视为一次性修复（同 commit）**。

2. **复杂度 + 设计一致性叠加（G-10 + G-15 + G-16 + G-7 + G-8）**：拆模块只走了一半 —— 主模块保留大量委托壳未真正减重；同时设计文档 §6.7 也因此未回写新增子模块、§6.3 漏 RollbackResumeMismatchError、还残留死 import 和死代码。**本质是"拆模块只走了一半 + 设计文档同步遗忘"**。rev3 应同步刷 §6.3/§6.7 + 删委托壳。

3. **error_handling + auxiliary_spec 叠加（G-26 docstring 误导）**：_setup_archive docstring 描述旧错误顺序，未来维护者按 docstring 重新启用 _acquire_dual_lock 会破坏 §6.4 锁顺序（M-1）。**docstring 与死代码必须同步清理**。

## rev3 优先级

| 优先级 | finding | 估时 |
|---|---|---|
| P0 critical | （无）| — |
| **P1 必修 6 项** | G-1（含 G-2/G-3）atomic + read 顺序 / G-4 append-mv 顺序 / G-7 删死 import / G-10 删委托壳 / G-15 §6.3 异常表 / G-16 §6.7 影响域 | 2-3 commits |
| P2 顺手 12 项 | G-5/G-12/G-17~G-21/G-23/G-24/G-25/G-26 | 1 commit |
| P3 nice-to-have | G-6/G-9/G-13/G-28/G-30/G-31（follow-up） | rev4 或后续 PR |

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估，不代表合并通过**。当前不建议 sign-off，建议派 rev3 修复轮处理 6 处 keep+major + 12 处 keep+minor，修完后再走一轮 critic + judge，预期升 looks_clean ≥ 88。

如确实要绕过修复直接 sign-off：

```bash
/code-review:signoff REV-REQ-2026-009-code-F-007-002              # 标准通道（不推荐，含 6 处 keep+major）
/code-review:signoff REV-REQ-2026-009-code-F-007-002 --trivial    # trivial 通道（不适用，含实质问题）
```

未 sign-off 时 `feature-lifecycle-manager` 不会把 F-007 转 done；GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。
