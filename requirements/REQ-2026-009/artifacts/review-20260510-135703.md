# 代码审查报告 · REQ-2026-009 · 20260510-135703

## 结论

**looks_clean** · 0 critical / 0 major / 3 minor

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项
- `blocked` — AI 机器评估发现关键问题

本次 F-011 rev2 评审：rev1 6 项 keep finding（3 major + 3 minor）全闭合，rev2 自引入 3 项 minor + 1 项 follow-up（既有问题加剧），趋势 F-010 rev2 93 → F-011 rev1 87 → F-011 rev2 92（+5 V 字回升，未恢复 F-010 水平差 1 分）。

**Supersedes**：REV-REQ-2026-009-code-F-011-001（rev1 needs_attention）。

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认（rev2 同 rev1 升 8 全集）：

- **决策**：all（用户在 12 文件 / must=0 / suggest=0 灰色场景下选 `a` 升全集 8 路）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-10 13:33:30
- **跑了哪些 checker**：complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec（8 全集）
- **跳过的 checker（含原因）**：无

## 待 sign-off 提示

本次评审输出 `conclusion = looks_clean`，**这只是 AI 的机器评估**，不代表合并通过。

**符合 trivial sign-off 通道**（0 critical / 0 major / 全 minor / 已修 critical+major 无新引入重大风险 / score 92 ≥ 90）。

请开发者本人执行：

```bash
/code-review:signoff REV-REQ-2026-009-code-F-011-002 --trivial  # 推荐快速通道
/code-review:signoff REV-REQ-2026-009-code-F-011-002            # 对话式 sign-off
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-011 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## rev1 6 项 keep finding 闭合验证

| rev1 ID | 闭合状态 | 验证证据 |
|---|---|---|
| **F-4** minor | **closed** | `tests/migration/test_phase_requirements_equivalence.py:46-51` try/except yaml.YAMLError → raise ValueError 含 path |
| **F-5** minor | **closed** | `tests/e2e/test_legacy_run_compat.py:126-131` 同模式包 try/except + path |
| **F-6** minor | **closed** | `tests/migration/test_phase_requirements_equivalence.py:402-407` registry yaml 同模式 |
| **F-7** major | **closed** | `tests/migration/fixtures/R004/boundary.yaml:27-28` strict:true / expected_conclusion:fail；test 文件 :312 fixture.get('strict') + :316/:321 _classify_report(report, strict=strict) 真消费；:183 report.exit_code(strict=strict) |
| **F-8** major | **closed** | `requirements/REQ-2026-009/artifacts/detailed-design.md:1099` 文案重写为 "meta.yaml 出现在 reviewed_artifacts 时的 hash drift（黑名单兜底已由 save_review.py:_check_artifact_blacklist 写入侧覆盖）"；`notes.md:43-45` D-014 决策段对齐 |
| **F-10** major | **closed** | `tests/e2e/test_legacy_run_compat.py:187-201` 真调 wf_continue_main([run_id], repo_root=tmp_path) + rc==0 断言；走 _resolve_run_dir / validate_state_for_cmd / append_event / _main_loop_stub 全路径 |

**6/6 全闭合** — design-consistency + error-handling 两个 checker 双重确认与 Judge 独立验证一致。

## 裁决明细

> rev2 9 候选 finding 的最终处置（reviewer 发现 → critic 反证 → Judge 裁决）。

- **F-1** [complexity+aux · minor] → critic: not_rebutted → **最终处置：keep+minor**
  - 裁决依据：_classify_report docstring 4 行场景与 fallthrough 实现对应不直观，但**不是死代码**（line 187 是 strict+warnings 的 fallthrough 出口），属注释清晰度问题
- **F-2** [complexity · minor] → critic: not_rebutted → **最终处置：drop**
  - 裁决依据：_run_old_chain/_run_new_chain 接 strict 透明穿越参数——rev2 已在 _run_new_chain docstring 说明"由调用方在 _classify_report 阶段消费"，参数保留是为 fixture 自描述完整性
- **F-3** [complexity+design · minor] → critic: not_rebutted（建议升 major）→ **最终处置：keep+minor（不升 major）**
  - 裁决依据：见 F-3 升级判断段。critic 误读 §8.4 门槛——expected_conclusion 是个体 fixture 预期断言（parametrize 已消费），不是汇总等价性门槛判定字段；形式缺口确认存在但不到 major
- **F-4** [complexity · info] → critic: not_proven → **最终处置：follow-up**
  - 裁决依据：561 行超 500 既有问题（rev2 加剧未引入），既有复发不升级
- **F-5** [error-handling · minor] → critic: not_rebutted → **最终处置：keep+minor**
  - 裁决依据：tests/e2e/test_legacy_run_compat.py:172-173 列表推导 `[json.loads(l.strip()) for l in f if l.strip()]` 无 try/except，与同文件 :48-53 双标客观成立；rev2 自引入需修
- **F-6** [aux · minor] → critic: not_rebutted → **最终处置：drop**
  - 裁决依据：_run_new_chain docstring 已写"由调用方消费"，_run_old_chain 没写不影响理解；过度规范化要求两 docstring 镜像
- **F-7** [aux · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：line 154 既有延迟 import / line 187 rev2 自引入；conftest path 注入是惯用模式，注释要求过度
- **F-8** [aux · minor] → critic: not_rebutted → **最终处置：keep+minor**
  - 裁决依据：4 处 ValueError 消息前缀风格三分（"Fixture YAML" / "Fixture 格式" / "legacy_run fixture meta.yaml YAML" / "registry.yaml YAML"）客观成立；建议统一模板
- **F-9** [aux · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：决策段标题英文 + ADR 结构缺失证据偏弱（其他标题也含 REQ-2026-005 / AC-SELF 等英文标识）

## Critical（必须修复）

无。

## Major（强烈建议修复）

无。

## Minor（改进建议，可挂 follow-up 不阻塞 sign-off）

### [complexity+aux] tests/migration/test_phase_requirements_equivalence.py:174-187

**F-1 · _classify_report docstring 4 场景与 fallthrough 实现对应不直观**

外层 `if report.exit_code(strict=strict) != 0` 进入后，内层 `if report.errors > 0: return "fail"` 与 fallthrough `return "fail"` 返回值相同。逻辑正确（不是死代码）但 docstring 4 场景与代码 2 分支对齐不直观。

**建议**：简化为 `if report.exit_code(strict=strict) != 0: return "fail"`，或在内层加注释说明"fallthrough 出口是 strict+warnings 场景"。

---

### [error-handling] tests/e2e/test_legacy_run_compat.py:172-173

**F-5 · 列表推导内 json.loads 无 try/except（rev2 自引入风格双标）**

`resumed_events_raw = [json.loads(l.strip()) for l in f if l.strip()]` 直接调 json.loads 无包裹。同文件 `_load_events_from_fixture:48-53` 对 json.loads 包了 try/except + ValueError + path。

**建议**：把列表推导改为显式 for 循环 + try/except json.JSONDecodeError as exc → raise ValueError(f"...path={jsonl_path}...") from exc，与 _load_events_from_fixture 风格对齐。

---

### [aux] 4 处 ValueError 消息前缀风格三分

**F-8 · ValueError 消息前缀模板不统一**

- `test_legacy_run_compat.py:130` → "legacy_run fixture meta.yaml YAML 解析失败（path=...）"（语义+文件名前缀，中文）
- `test_phase_requirements_equivalence.py:50` → "Fixture YAML 解析失败（path=...）"（首字母大写英文 Fixture）
- `test_phase_requirements_equivalence.py:406` → "registry.yaml YAML 解析失败（path=...）"（文件名前缀）

**建议**：统一为 `<artifact-type> 解析失败（path={...}）：{exc}` 模板，例如 "fixture YAML 解析失败" / "meta.yaml 解析失败" / "registry.yaml 解析失败"（小写、与 path= 语义对齐）。

## Follow-up Notes（不阻塞 sign-off）

- **F-4（既有复发）**：`tests/migration/test_phase_requirements_equivalence.py` 561 行超软阈值 500（rev1 534 → rev2 561 +27）。结构按 R001~R007 分区清晰，仓库内已有 7 个 >500 行测试同构惯例；建议下次扩 fixture 时把 _build_review_json + _setup_req_dir 等 helper 抽到 `tests/migration/helpers.py`。
- **F-3 形式缺口（独立判断不升 major 但建议长线修复）**：test_zero_mismatch_across_all_fixtures 汇总测试在循环内未读 fixture strict 字段，R004 boundary 在汇总测试两链路都 warn 而 fixture expected_conclusion=fail，属"测试矩阵覆盖度形式不一致"。功能正确（双跑等价性已通过 + parametrize 主测试已严格验证 R004 strict 路径），但 fixture metadata 自洽性应一并修。可挂下个迭代或 rev3。
- **trend-G-meta 监控**：F-009 → F-010 → F-011 三连续 feature 出现"rev1 修复反引入新问题"模式。F-011 rev2 自引入 3 minor (F-1 docstring + F-3 同步漏 + F-5 风格双标 + F-8 消息模板) 比 F-010 rev2 严重一档。建议派发 rev2 修复任务时 dispatch prompt 加一句"rev1 keep finding 修复后必须全文搜同模式 + 同 helper 风格不一致"，可预防同类自引入。

## 未运行的 checker

无（all 决策跑了 8 全集）。

## 审查元信息

- **审查范围**：12 files / +359 -48（其中代码改动 ~3 文件 ~75 行 + bookkeeping）
- **并行 checker**：8（complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec）
- **Critic 统计**：rejected 0 / not_proven 3 / not_rebutted 6（合计 9 候选 finding，合并自 11）
- **综合模型**：code-quality-reviewer (default opus)
- **Base SHA**：b6806c63868c6119c5985611f3a53cc5dbb8ad59
- **Head SHA**：2b22d172228fb7ffc1ed69997571de49f0ccd7dd
- **Verdict 落盘**：`requirements/REQ-2026-009/reviews/code-F-011-002.json`（supersedes 001）
- **Review ID**：REV-REQ-2026-009-code-F-011-002
- **测试基线**：`pytest tests/migration/ tests/e2e/test_legacy_run_compat.py`：29 passed；全套 1179 passed / 9 skipped；`ruff check`：All checks passed
