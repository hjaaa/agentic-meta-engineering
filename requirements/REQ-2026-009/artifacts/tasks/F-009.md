---
schema_version: "1.0"
feature_id: F-009
title: D-006 hook 拦截 + workflow_approve/reject.py + ai-collaboration patch
status: in-progress
complexity: light
depends_on:
  - F-005
touches:
  - .claude/hooks/pre-tool-use-guard.sh
  - scripts/lib/workflow_approve.py
  - scripts/lib/workflow_reject.py
  - context/team/ai-collaboration.md
  - tests/hooks/test_pre_tool_use_guard.py
  - tests/hooks/fixtures/workflow_approval_inputs.yaml
  - tests/lib/test_workflow_approve.py
created_at: '2026-05-08 20:17:35'
updated_at: '2026-05-10 10:58:00'
review_report: null
---

# F-009 · D-006 hook 拦截 + workflow_approve/reject.py + ai-collaboration patch

## 需求描述

落地 D-006 双层拦截：

- **B 层** `.claude/hooks/pre-tool-use-guard.sh` 新增 `APPROVAL_SLASH_PATTERN` / `APPROVAL_PYTHON_PATTERN` 常量 + `check_workflow_approval_human_only` 函数（同 case Bash 分支挂钩，~15 行 shell + 1 行调用）
- **C 层** `ai-collaboration.md` 规则三扩展为 sign-off / approval / reject 都是人类专属（+10 行）
- **CLI 层** `workflow_approve.py` / `workflow_reject.py` 顶端 `sys.stdin.isatty()` fail-closed 兜底（与原 `code_review_signoff.py:61` 同构搬到新位置）

已知绕过通道（变量间接引用 + eval）由 CLI isatty + BYPASS reason ≥ 8 字符 + PR review 三层兜底接受（与 carryover-A 一致）。

来源：detailed-design §5 全章。

## 接口/数据结构

详见 `requirements/REQ-2026-009/artifacts/detailed-design.md` §5.2~§5.7（hook patch 完整 shell 片段 + 双层兜底 + 24 条单测矩阵 + ai-collaboration 规则三扩展）。

## 依赖

### 依赖的 feature

- `F-005`（`/workflow:approve` / `/workflow:reject` 命令存在；hook 拦截依赖命令字面量）

### 依赖的模块

- 既有 `.claude/hooks/pre-tool-use-guard.sh`（已落地，本 feature 扩展）
- 既有 `scripts/lib/code_review_signoff.py:61`（isatty 同构参考）

## 触及范围

- `.claude/hooks/pre-tool-use-guard.sh`
- `scripts/lib/workflow_approve.py`
- `scripts/lib/workflow_reject.py`
- `context/team/ai-collaboration.md`
- `tests/hooks/test_pre_tool_use_guard.py`
- `tests/hooks/fixtures/workflow_approval_inputs.yaml`

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] TC-F9-1: `pytest tests/hooks/test_pre_tool_use_guard.py::test_hook_blocks_16_combinations` 绿——4 入口（slash approve / slash reject / python approve / python reject）× 4 包装（直接 / `bash -c` / 前后无关 token / 绝对路径 python）= 16 happy block + exit 2 + stderr 含 BLOCKED 消息（§5.6）
- [ ] TC-F9-2: `pytest tests/hooks/test_pre_tool_use_guard.py::test_hook_passthrough_5_boundary` 绿——5 边界放行场景：grep 字面量 / 注释中 / 字符串字面量 / `--help` / dry-run flag → exit 0（§5.6）
- [ ] TC-F9-3: `pytest tests/hooks/test_pre_tool_use_guard.py::test_known_bypass_with_isatty_block` 绿——变量间接引用 `P=/workflow:approve; eval $P` → hook 放行 + `workflow_approve.py` isatty 拒绝 + exit 2 双层验证（§5.4-§5.5）
- [ ] TC-F9-4: `pytest tests/hooks/test_pre_tool_use_guard.py::test_audit_log_no_bypass_marker` 绿——本类拦截命中时 audit log 不写 `BYPASS used`（与既有 audit_log 约定一致 / §5.6）
- [ ] TC-F9-5: `pytest tests/lib/test_workflow_approve.py::test_isatty_fail_closed` 绿——`sys.stdin.isatty()` 返回 False（pipe / CI / subagent）→ `workflow_approve.py` exit 2 + stderr 含「必须在 tty 终端执行」（§5.5）
- [ ] TC-F9-6: `grep -n 'Approval 唯一入口' context/team/ai-collaboration.md` 命中 1 行——规则三 patch 后含新增段（§5.7）
- [ ] `/code-review` 通过
