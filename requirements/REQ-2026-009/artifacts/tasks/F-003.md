---
schema_version: "1.0"
feature_id: F-003
title: standard-8phase.yaml 38 节点完整化 + 8 阶段 prompt 抽离 + 末端 3 节点
status: done
complexity: medium
depends_on:
  - F-001
  - F-002
touches:
  - .claude/workflows/requirement/standard-8phase.yaml
  - .claude/workflows/prompts/standard-8phase/**
  - tests/e2e/__init__.py
  - tests/e2e/test_standard_8phase_terminal_nodes.py
  - tests/workflows/test_prompt_structure.py
  - tests/workflows/test_yaml_schema.py  # task self-check 明确「追加 TC-F3-1」，扩 touches 显式声明
created_at: '2026-05-08 20:17:35'
updated_at: '2026-05-08 22:40:35'
review_report: artifacts/review-20260508-233156.md
---

# F-003 · standard-8phase.yaml 38 节点完整化 + 8 阶段 prompt 抽离 + 末端 3 节点

## 需求描述

落地 `standard-8phase.yaml` 38 节点完整化 + 8 阶段 prompt 抽到 `.claude/workflows/prompts/standard-8phase/*.md` + 末端 `pr-submit` / `pr-merged-gate` / `archive-finalize` 三 bash/approval 节点（detailed-design §1.5 通用性裁定，submit / archive 不入引擎层命令集，下沉为 yaml 节点）。

- `pr-submit` 节点 bash 调 `gh pr create` + 写 `meta.pr_url`
- `pr-merged-gate` approval 等用户 PR 合并后人工 approve
- `archive-finalize` bash 写 `meta.archived_at` + `outcome=shipped`
- 每节点 prompt frontmatter 与 yaml 节点字段一致（loader 校验 §2.2.1）

来源：outline §1.2 改动一览表（~600 行）；detailed-design §2.1（prompt 抽离结构）/ §1.5（末端节点决策）。

## 接口/数据结构

详见 `requirements/REQ-2026-009/artifacts/detailed-design.md` §1.5（末端节点）/ §2.1（prompt 文件布局）/ §2.2.1（节点字段一致性校验）。

## 依赖

### 依赖的 feature

- `F-001`（loader 解析 yaml + prompt frontmatter 校验）
- `F-002`（8 节点类型支持 bash / approval）

### 依赖的模块

- `.claude/skills/workflow-engine/`（F-001 / F-002 落地）
- 既有 `gh` CLI（团队工具链，已有）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**；本正文段供人快速参考。

- `.claude/workflows/requirement/standard-8phase.yaml`
- `.claude/workflows/prompts/standard-8phase/**`（8 阶段 prompt + 末端节点）
- `tests/e2e/test_standard_8phase_terminal_nodes.py`
- `tests/workflows/test_prompt_structure.py`

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] TC-F3-1: `pytest tests/workflows/test_yaml_schema.py::test_standard_8phase_loadable` 绿——38 节点全部解析 + 拓扑序合法 + 每节点 `prompt_file` 指向真实 .md（覆盖 AC-01 / AC-02）
- [ ] TC-F3-2: `pytest tests/workflows/test_prompt_structure.py::test_8phase_prompt_frontmatter` 绿——8 阶段 prompt frontmatter 必填字段（name/node_id/version）齐 + node_id 与 yaml 节点 id 一致
- [ ] TC-F3-3: `pytest tests/e2e/test_standard_8phase_terminal_nodes.py::test_pr_submit_node` 绿——mock `gh pr create` + `yq` → `pr-submit` 节点 stdout 含 pr_url + `meta.yaml.pr_url` 写入（覆盖 AC-03 末端节点）
- [ ] TC-F3-4: `pytest tests/e2e/test_standard_8phase_terminal_nodes.py::test_pr_merged_gate_approval` 绿——approval 节点 `gate_message` 字段非空 + capture_response 写 jsonl `approval_approved`（D-006 hook 拦截 AI 由 F-009 单独验证，本测试用 tty mock 直接放行）
- [ ] TC-F3-5: `pytest tests/e2e/test_standard_8phase_terminal_nodes.py::test_archive_finalize_node` 绿——`archive-finalize` bash 写 `meta.archived_at` + `outcome=shipped` + stdout 含「归档完成」提示行
- [ ] 接口实现符合详细设计 §1.5 / §2.1
- [ ] `/code-review` 通过
