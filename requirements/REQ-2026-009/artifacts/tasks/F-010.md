---
schema_version: "1.0"
feature_id: F-010
title: 9 个 /requirement:* 别名兼容期保留实现（D-009）+ /requirement:next 例外
status: pending
complexity: light
depends_on:
  - F-005
touches:
  - .claude/commands/requirement/**
  - .claude/skills/managing-requirement-lifecycle/**
  - tests/skills/test_alias_passthrough.py
created_at: '2026-05-08 20:17:35'
updated_at: '2026-05-08 20:17:35'
review_report: null
---

# F-010 · 9 个 /requirement:* 别名兼容期保留实现（D-009）+ /requirement:next 例外

## 需求描述

落地 9 个 `/requirement:*` 命令兼容期处理：3 月兼容期内（截至 **2026-08-08**）所有别名保留实际实现 + 输出 deprecation warning + 转 `/workflow:*` ARGUMENTS 透传。

例外：

1. `/requirement:next` 走独立旧实现（覆盖 spec §4.3 / §15 立即删决策，延后到 Plan 6 自举验证通过后才删）+ `[DEPRECATION-NEXT]` 例外文案
2. `/requirement:submit` / `/requirement:archive` 也走独立旧实现，新引擎语义由 standard-8phase yaml 末端 3 节点（F-003）承载

9 个 `.claude/commands/requirement/<cmd>.md` 入口处加 DEPRECATION warning 文案 + ARGUMENTS 透传到 `/workflow:<target>`。

来源：detailed-design §10.2 全章 / D-009 / outline §1.2。

## 接口/数据结构

详见 `requirements/REQ-2026-009/artifacts/detailed-design.md` §10.2.1（别名映射 + 例外）/ §10.2.2（DEPRECATION 文案模板）/ §10.2.3（兼容期到期日字段化）。

## 依赖

### 依赖的 feature

- `F-005`（`/workflow:*` 命令存在；别名转发的目标）

### 依赖的模块

- 既有 9 个 `.claude/commands/requirement/<cmd>.md`（本 feature 改造为 deprecation 模式）

## 触及范围

- `.claude/commands/requirement/**`（9 个命令）
- `.claude/skills/managing-requirement-lifecycle/SKILL.md`（next / submit / archive 例外保留逻辑）
- `tests/skills/test_alias_passthrough.py`

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] TC-F10-1: `pytest tests/skills/test_alias_passthrough.py::test_9_aliases_emit_deprecation` 绿——9 命令各调用一次 → stderr 首段含 `[DEPRECATION]` 模板（§10.2.2 + 「截至 2026-08-08」字面量）（覆盖 AC-09）
- [ ] TC-F10-2: `pytest tests/skills/test_alias_passthrough.py::test_arguments_forwarding` 绿——`/requirement:new 'X'` / `/requirement:continue REQ-2026-Y` / `/requirement:save 'note'` 等 6 个非例外别名 → 调用 `/workflow:<target>` 时 ARGUMENTS 字面量与原文一致（§10.2.1 透传规则）
- [ ] TC-F10-3: `pytest tests/skills/test_alias_passthrough.py::test_next_exception_kept_legacy` 绿——`/requirement:next` 不转发到 `/workflow:*` + 调旧 `managing-requirement-lifecycle` 的 phase-transition 子动作 + stderr 首段含 `[DEPRECATION-NEXT]` 例外文案（§10.2.1 + §10.2.2）
- [ ] TC-F10-4: `pytest tests/skills/test_alias_passthrough.py::test_submit_archive_kept_legacy` 绿——`/requirement:submit` / `/requirement:archive` 走独立旧实现 + 不转发；新引擎语义由 standard-8phase yaml 节点承载（§1.5 / §10.2.1）
- [ ] TC-F10-5: `grep -rn '截至 2026-08-08' .claude/commands/requirement/` 命中 9 个 .md 文件 + 至少 1 行（兼容期到期日字面量字段化 / §10.2.3）
- [ ] `/code-review` 通过
