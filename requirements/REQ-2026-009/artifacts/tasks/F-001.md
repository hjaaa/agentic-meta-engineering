---
schema_version: "1.0"
feature_id: F-001
title: workflow-engine 核心（loader + run-state + dispatcher）
status: done
complexity: heavy
depends_on: []
touches:
  - .claude/skills/workflow-engine/**
  - scripts/lib/workflow_loader.py
  - scripts/lib/run_state.py
  - scripts/lib/topological_sort.py
  - scripts/lib/substitute_vars.py
  - scripts/lib/run_artifact_checks.py
  - tests/lib/test_workflow_loader.py
  - tests/lib/test_run_state.py
  - tests/lib/fixtures/workflows/**
  - tests/workflows/__init__.py
  - tests/workflows/test_yaml_schema.py
created_at: '2026-05-08 20:17:35'
updated_at: '2026-05-08 21:50:35'
review_report: artifacts/review-20260508-214811.md
---

# F-001 · workflow-engine 核心（loader + run-state + dispatcher）

## 需求描述

搭 workflow-engine Skill 主体——loader 双路径 `_resolve_run_dir`（D-007）+ jsonl 读写 + RunState 反扫重建 + 拓扑排序 / 变量替换 / artifact lego 派发链路；含 14 类 yaml 错误识别 + spec §13 jsonl 损坏行兜底。

Plan 1 已合并部分（commit `6d55eaf`）含 `workflow_loader.py` 雏形 + `topological_sort.py` / `substitute_vars.py`；本 feature 在其上完成：

- `run_state.py`（事件枚举 + RunState 重建 + 反扫支持）
- `run_artifact_checks.py`（artifact_checks 节点执行器）
- `workflow-engine SKILL.md` 决策表
- `reference/dispatch-decision.md` 详尽实现说明
- AC-06 节点级 model 字段优先级 yaml > frontmatter > 顶层（详细设计 §2.2.1）由 loader 校验逻辑落实

来源：detailed-design §1.2.2 / §2.6；outline §1.2 改动一览表；spec §6 / §13。

## 接口/数据结构

详见 `requirements/REQ-2026-009/artifacts/detailed-design.md` §1.2.2（loader API）/ §2.6（dispatcher 决策）；jsonl 事件枚举依赖 §6 / §13；RunState dataclass 在 `scripts/lib/run_state.py` 中定义。

## 依赖

### 依赖的 feature

无（本 feature 是引擎层基石，所有其他 F-002 ~ F-013 都依赖它直接或间接）。

### 依赖的模块

- `scripts/lib/workflow_loader.py`（Plan 1 雏形已合并）
- `scripts/lib/topological_sort.py`（Plan 1 已合并）
- `scripts/lib/substitute_vars.py`（Plan 1 已合并）

## 触及范围

**frontmatter `touches` 字段是机器读取入口**，由 `touches_guard.py`（软拦截）和 `GATE-TOUCHES-VIOLATION`（phase-transition / submit 硬挡）两层解析。本正文段供人快速参考，两处不一致时以 frontmatter 为准。

- `.claude/skills/workflow-engine/**`
- `scripts/lib/workflow_loader.py`
- `scripts/lib/run_state.py`
- `scripts/lib/topological_sort.py`
- `scripts/lib/substitute_vars.py`
- `scripts/lib/run_artifact_checks.py`
- `tests/lib/test_workflow_loader.py`
- `tests/lib/test_run_state.py`
- `tests/lib/fixtures/workflows/**`（含 invalid-*.yaml + valid-*.yaml + sub-fixtures/**.yaml）
- `tests/workflows/__init__.py`
- `tests/workflows/test_yaml_schema.py`

## 开发笔记

（开发过程中随手记）

## 完成自检

- [ ] TC-F1-1: `pytest tests/lib/test_workflow_loader.py` 绿——拓扑排序合法 yaml 通过 + 字段优先级 yaml > frontmatter > 顶层（覆盖 AC-01 / AC-06）
- [ ] TC-F1-2: `pytest tests/lib/test_workflow_loader.py::test_invalid_yaml_14` 全绿——14 套 invalid-*.yaml fixture 全部触发对应错误码 + 行号断言（覆盖 AC-08）
- [ ] TC-F1-3: `pytest tests/lib/test_run_state.py` 绿——反扫 jsonl 重建 RunState 一致；最后一行损坏跳过 + warn；node_started 无对应 node_completed 视为残缺对（spec §13 / 风险 6）
- [ ] TC-F1-4: `pytest tests/lib/test_run_state.py::test_resolve_run_dir` 绿——`requirements/REQ-X/` 命中走兼容路径；`runs/REQ-X/` 命中走新路径；都不存在抛 `WorkflowError`（D-007）
- [ ] TC-F1-5: `pytest tests/workflows/test_yaml_schema.py` 绿——loader 加载 `standard-8phase.yaml` + `code-review-embedded.yaml` 两套 fixture 通过（与 F-003 / F-004 联动 smoke）
- [ ] 接口实现符合详细设计 §1.2.2 / §2.6 / §2.2.1
- [ ] 单元测试覆盖主流程 + 14 类 yaml 错误 + jsonl 损坏行兜底
- [ ] 边界条件处理（空 yaml / 单节点 / 循环依赖 / 重复 id 等）
- [ ] 无明显安全/并发问题（jsonl 写入 fcntl 锁）
- [ ] `/code-review` 通过
