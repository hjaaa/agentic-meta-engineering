# ============ 流程组（bootstrap 时填） ============
id: REQ-2026-003
title: 代码审查人类必经卡点（路由确认 + 结论 sign-off）
phase: technical-research
created_at: "2026-04-29 17:05:53"     # 格式 YYYY-MM-DD HH:MM:SS（Asia/Shanghai），见 context/team/engineering-spec/time-format.md
branch: feat/req-2026-003
base_branch: develop                   # feat/* 派生的基点分支：develop / main / master
project: agentic-meta-engineering      # 关联 context/project/<project>/
services: [agentic-meta-engineering]   # 涉及的服务仓库
gates_passed:
  - phase: definition
    at: "2026-04-30 10:04:37"
    gates: [GATE-META-SCHEMA, GATE-INDEX-INTEGRITY, GATE-SOURCING,
          GATE-WORKSPACE-CLEAN, GATE-REVIEW-VERDICT, GATE-TRACEABILITY,
          GATE-PLAN-FRESHNESS]
    warnings:
      - "GATE-PLAN-FRESHNESS W001: plan.md 保留 2 处模板占位符（目标 + 风险 1）（待 definition 阶段补）"
      - "GATE-PLAN-FRESHNESS W003: plan.md 范围段 '不包含' 无子条目（待 definition 阶段补）"
  - phase: technical-research
    at: "2026-04-30 10:52:42"
    gates: [GATE-META-SCHEMA, GATE-INDEX-INTEGRITY, GATE-SOURCING,
          GATE-WORKSPACE-CLEAN, GATE-REVIEW-VERDICT, GATE-TRACEABILITY,
          GATE-PLAN-FRESHNESS]
    warnings: []
pr_url: ""                             # /requirement:submit 成功后回写
pr_number: 0                           # /requirement:submit 成功后回写
log_layout: split                      # split（默认，工具日志→process.tool.log / 语义事件→process.txt） | legacy（老格式，全写 process.txt）

# ============ 语义组（definition 阶段补齐，供跨需求检索） ============
feature_area: review                     # 主导域：代码审查管道（路由器属于 checker fan-out 上游）
change_type: feature                     # 新增"按需路由"能力（带 perf 收益，但形态是 feature）
affected_modules: [review, commands, skills, scripts]   # /code-review + code-review-prepare SKILL + routing.py
tags: [code-review, routing, tty-checkpoint, token-saving]

# ============ 结果组（completed 时才填） ============
outcome: ""                    # shipped / abandoned / rolled-back
completed_at: ""               # ISO8601，completed 时填
lessons_extracted: false       # /knowledge:extract-experience 是否已跑
