# 代码审查报告 · REQ-2026-003 · 2026-05-01 10:18:04 CST · F-002 review-002

## 结论

**needs_attention** · 0 critical / 2 keep major / 6 minor · score 78 · supersedes review-001

> review-001 的 4 critical + 3 major + 5 minor 必修**全部已确认修复**；本轮新发现 4 条 major（critic 验证后 1 rejected / 3 keep），并已对其中 I-1 做 followup 修复（commit a536487）。剩 I-2 / I-3 复杂度阈值未达但功能正确——按 Judge 建议**不再派 subagent 第二刀**，留 F-003 收口或人类 sign-off 时显式承担。

## 路由说明

- **决策**：custom（review-001 已覆盖维度本轮跳过）
- **跑了哪些 checker**：complexity / error-handling / design-consistency / auxiliary-spec（4/8）
- **跳过的 checker**：security / concurrency / performance / history-context

## 待 sign-off 提示

本次 `conclusion = needs_attention`（0 critical 但有 keep major），需要你判断是否接受 I-2/I-3 复杂度债 + 已修 I-1。请在 tty 终端：

```bash
/code-review:signoff REV-REQ-2026-003-code-F-002-002 --decision=approved
```

或 `--decision=rejected` 退回再修。

> 注：verdict 文件 `reviews/code-F-002-002.json` 反映的代码状态是 commit `6995bb5`，I-1 已在 commit `a536487` 修复（_assert_plan_invariants 4 条 assert → raise RoutingSchemaError，与 H-11 同模式闭环），sign-off 时实际看到的代码已含此修复。

## review-001 必修项修复确认（4 critical + 3 major + 5 minor）

| H-id | 应修内容 | 修复结果 |
|---|---|---|
| **H-5** critical | skipped_checkers 字段名 `checker` → `name` | ✅ design-consistency 实测确认 |
| **H-6** critical | reason 三选一字面对齐 §3.3 | ✅ 三条字串字面对齐 |
| **H-7** critical | 退码 5 嵌套消息 → main 改 `print(str(e))` | ✅ trace 验证无嵌套 |
| **H-8** critical | files_must_hit/suggest_hit 改 int 命中文件唯一数 | ✅ 类型对齐 §3.1 |
| **H-9** major | I1 双向蕴含 → 去左侧自循环 | ✅ assert 表达式真生效 |
| **H-10** major | email 完整正则 fullmatch | ✅ EMAIL_RE.fullmatch 防 `a@b` |
| **H-11** major | _write_scope I1-I8 转 raise | ✅ 抽 _assert_scope_invariants + raise RoutingSchemaError |
| **H-15** minor | _git_diff_stats stderr warning | ✅ |
| **H-17** minor | datetime 字母序 | ✅ |
| **H-18** minor | _now_shanghai_display docstring 加"为什么" | ✅ |
| **H-20** minor | _now_shanghai/_display 加 __all__ | ✅ |
| **H-21** minor | _setup_test_repo docstring 返回值 | ✅ |
| **H-22** minor | T1-T5 改 should_xxx_when_yyy + 新增 pyproject.toml | ✅ pytest 29 passed |

## review-002 新发现 + 处置

| I-id | tag | severity | critic verdict | 最终处置 | 说明 |
|---|---|---|---|---|---|
| I-1 | error-handling | major | not_rebutted | **followup-fixed** | F-001 引入的 `_assert_plan_invariants` 4 处 assert 仍未转 raise；F-002 main 调用链下 AssertionError 穿透 except RoutingError。已在 commit `a536487` 4 条 assert → raise RoutingSchemaError，与 H-11 同模式闭环 |
| I-2 | complexity | major | not_rebutted | **keep** | _prompt_user 主体仍 104 行 cc≈13；UI 渲染 + 4 档分支内联未拆。功能正确，建议 F-003 收口拆 _render_plan_summary |
| I-3 | complexity | major | not_rebutted | **keep** | _write_scope 主体仍 96 行；skipped_checkers 构造 + scope dict 构造 + 原子写三段未拆。建议 F-003 收口拆 _build_skipped_checkers / _build_scope_dict |
| I-4 | complexity | major | rejected | drop | critic 实测 main 仅 50 行 cc≈4，原 finding 数据失实 |

## 已知 Major（接受 needs_attention 时承担）

### [I-2] scripts/lib/code_review_routing.py:667-770（_prompt_user）

主体 104 行 cc≈13，超 60/10。UI 渲染（L693-708）+ 4 档分支（L720-755）内联。功能正确无 bug。

**Follow-up 路径**：F-003 收口时抽 `_render_plan_summary(plan, recommended)` 承接 UI 渲染（约 16 行）→ _prompt_user 压到 60 行内。

### [I-3] scripts/lib/code_review_routing.py:862-957（_write_scope）

主体 96 行 cc≈12（已抽 _assert_scope_invariants 但仍超阈值）。skipped_checkers 构造 / scope dict 构造 / 原子写未拆。

**Follow-up 路径**：F-003 收口时抽 `_build_skipped_checkers(plan, decision) -> list[dict]`（28 行）+ `_build_scope_dict(plan, decision, args, confirmed_by) -> dict`（27 行）→ _write_scope 压到 ~40 行。

## Follow-up Notes

- **[H-1 from review-001]** 文件 1073 → 1164 行，超 500 阈值。下一 feature 拆 `code_review_routing_io.py` 子模块。
- **[H-12 from review-001]** `_git_diff_stats` 单次 `--numstat` 自算 shortstat。
- **[I-2/I-3 from review-002]** F-003 收口时第二刀拆分。

## 跨维度洞察

- **H-11 + I-1 闭环**：F-002 拆出 `_assert_scope_invariants` 时只扫了 IO 层 assert，漏了 F-001 已落盘的 `_assert_plan_invariants`。followup commit `a536487` 让两个不变量校验函数模式统一。下次同类问题（"修一刀但没扫齐"）建议派工时一次 grep 全文件 `assert ` 关键字。
- **拆分两刀策略**：复杂度修复一次拆到底成本高+风险大，分两刀（review-001 H-2/3/4 → review-002 I-2/3 → F-003 收口）是合理迭代。

## 总结评语

review-001 的 4 critical + 3 major + 5 minor 必修**全部已修复确认**——schema 契约 4 条 critical 全按 §3.1/§3.3 字面对齐，异常体系（H-7/H-11）+ 校验逻辑（H-9/H-10）+ minor 清理逐项核验通过。本轮新发现的 I-1（异常体系遗漏，与 H-11 同根）已立即 followup 修复（commit a536487，4 行 assert → raise），异常体系闭环。剩 I-2/I-3 是复杂度阈值未达但功能正确——拆第二刀回归风险中等收益边际，建议 F-003 收口或人类 sign-off 时显式承担。结论 needs_attention 是符合"4 critical 全清空 + 仍有非阻塞 major"的合理评级。

## 审查元信息

- **审查范围**：embedded / REQ-2026-003 / F-002（4 文件，+198 -103 行 vs base 8fa9e92）
- **并行 checker**：4 个（custom 路由）
- **Critic 统计**：rejected 1 (I-4) / not_proven 0 / not_rebutted 3 (I-1/I-2/I-3)
- **综合模型**：code-quality-reviewer
- **Base SHA**：8fa9e92
- **Head SHA**：6995bb5（verdict 落盘点）→ I-1 followup commit `a536487`
- **Verdict 文件**：requirements/REQ-2026-003/reviews/code-F-002-002.json
- **Review ID**：REV-REQ-2026-003-code-F-002-002（supersedes 001）
