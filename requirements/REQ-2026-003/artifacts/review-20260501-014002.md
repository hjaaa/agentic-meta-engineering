# 代码审查报告 · REQ-2026-003 · 2026-05-01 01:40:02 CST

## 结论

**needs_attention** · 0 critical / 3 major / 5 minor · score 78

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — **AI 机器评估发现待改进项（本次结论）**
- `blocked` — AI 机器评估发现关键问题

## 路由说明

本次审查路由（卡点 A 暂未启用——`routing.py main()` 待 F-002 实现，本轮走旧 8 路全集 fan-out）：

- **决策**：all（fallback，非用户 tty 确认）
- **确认人**：hj19961223@gmail.com（git config user.email）
- **确认时间**：2026-05-01T01:25:00+08:00
- **跑了哪些 checker**：complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec（8/8）
- **跳过的 checker**：无（mode_hint=all）

> 注：`tty_verified=false`——本轮 review 因 routing.py 尚未接入 commands/code-review.md，scope.json 系手工生成，未走真实 tty 卡点 A。F-002 / F-003 落地后此处会变为 `tty_verified=true`。

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人在 tty 终端执行：

```bash
/code-review:signoff REV-REQ-2026-003-code-F-001-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-003-code-F-001-001 --trivial  # 纯文档变更快速通道
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-001 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——展示 "checker 发现 → critic 反证 → Judge 裁决" 三方透明度。

- **F-1** [complexity · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：实测 `_build_plan` 第 319-419 行共 93 行（含 docstring/INV assert），超 §1.2 ≤60 行硬规范；critic 未给反证。
- **F-2** [complexity · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：文件 456 行未超阈值 500，"未来会超"是 F-002 任务范畴，不应在 F-001 落账。
- **F-3** [security · minor] → critic: not_proven → **最终处置：follow-up**
  - 裁决依据：sha 入参格式校验属 F-002 `_parse_args` 边界（设计 §2.4:170 已声明），F-001 不实现 main()，仅作跨 feature 提醒。
- **F-4** [performance · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：仅启动期一次，pattern 数为个位数；reviewer 自承"启动期 + 个位数"，性能影响可忽略。
- **F-5** [error-handling · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：`_validate_schema` 第 222 行 V4 编号在 must / suggest 段类型校验复用，未抽出独立 `_validate_rule_entry` 函数；违反 V1-V7 唯一编号设计。
- **F-6** [error-handling · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：实现已用 `(exc.stderr or "").strip()` 防御 None，测试断言只校 `exit_code`，对契约无影响。
- **F-7** [design-consistency · major] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：`_ERROR_MESSAGES` 5 条全部缺 `[routing]` 前缀 + `{detail}` / `{file}:{line}` 占位 + `fix-hint：` 段，严重偏离详设 §6 表格；F-002 main() 直接 import 后用户 stderr 体验差。
- **F-8** [design-consistency · major] → critic: not_rebutted → **最终处置：downgrade-to-minor**
  - 裁决依据：异常类 `exit_code` 走 `__init__` 注入而非详设 §2.1 写的 class-body 类属性；字面差异成立，但语义等价（exit_code 都正确赋值）。
- **F-9** [design-consistency · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：tests U8 用 14 文件而非 §10.1 写的 12（代码注释主动说明"12 是示例"）；字面偏离无功能影响。
- **F-10** [design-consistency · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：`pathspec.PathSpec.from_lines("gitignore", ...)` 是 pathspec 官方支持的字符串别名，等价于 `GitWildMatchPattern`。
- **F-11** [history-context · critical] → critic: rejected → **最终处置：drop**
  - 裁决依据：F-001 边界本就不实现 tty/main（routing.py 文件头 docstring 第 9-10 行明确委托 F-002）；3d46fcb 的"无 FAKE_TTY 后门"约束在 F-002 重新实现时仍需遵守，与本次 F-001 无关。
- **F-12** [aux-spec · major] → critic: not_rebutted → **最终处置：downgrade-to-minor**
  - 裁决依据：`__all__` 含 6 个 `_` 前缀符号是 F-001/F-002 拆分边界设计的副作用；违反 Python 惯例但功能正确，降级 minor。
- **F-13** [aux-spec · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：`_load_yaml` docstring 含英文 `"file:line detail"`，违反 CLAUDE.md §0.1 "代码注释使用简体中文"。
- **F-14** [aux-spec · minor] → critic: not_rebutted → **最终处置：keep**
  - 裁决依据：测试类名 `TestValidateSchemaMusTooLong` 拼写漏字母（应为 `Must`），影响可读性。
- **F-15** [aux-spec · minor] → critic: not_proven → **最终处置：follow-up**
  - 裁决依据：未独立证实 `_make_config` docstring 中英混杂；建议作者 self-check。

## Critical（必须修复）

无。

## Major（强烈建议修复）

### [complexity] scripts/lib/code_review_routing.py:319-419

`_build_plan` 函数体共 93 行（含 4 段 INV assert + docstring），超 CLAUDE.md §1.2 单方法 ≤ 60 行硬规范；圈复杂度 14、嵌套深度 4 均偏高。

**建议**：抽出 3 个私有：
1. `_compile_specs(config)` — must/suggest/trivial 的 PathSpec 预编译，返回三元组（~15 行）
2. `_match_file(path, must_specs, suggest_specs, trivial_spec)` — 单文件三段匹配，返回 `(hit_must, hit_suggest, hit_trivial)` 布尔三元组（~20 行）
3. `_assert_plan_invariants(plan)` — 4 段 assert 集中（~15 行）

`_build_plan` 主体压到约 25 行：编译 specs → 遍历文件调 `_match_file` → 聚合 → 调 assert → 构造 RoutingPlan。同时也缓解 F-5 的复用问题。

### [error-handling] scripts/lib/code_review_routing.py:196-260（_validate_schema）

V4 编号在 must 项结构校验和 suggest 段类型校验处复用，违反详设 §7 V1-V7 唯一编号约定。

**建议**：抽出 `_validate_rule_entry(rule: dict, section: str, idx: int) -> _RuleEntry` 处理"条目类型 + 必填字段 + checkers 元素"三件事，由 must / suggest 各自调用；保留 V4 唯一指代"条目结构错"，把"段必须是 list" 拆为独立 V3a / V4a 编号。

### [design-consistency] scripts/lib/code_review_routing.py:42-46（_ERROR_MESSAGES）

5 条错误模板全是简短占位串（如 `"stdin 不是 tty，拒绝交互确认（防 AI 绕过卡点 A）"`），缺详设 §6 表格要求的 `[routing]` 前缀 + `{detail}` / `{file}:{line}` 插值 + `fix-hint：` 段。F-002 `main()` 直接 import 后用户 stderr 体验差。

**建议**：按详设 §6 第 564-568 行表格补完整模板，例：
```python
1: "[routing] 入参非法：{detail}\n  fix-hint：检查 --mode / --requirement-id / --base-sha / --head-sha / --base-branch / --current-branch 必填项；sha 格式应为 7 位短 hash；requirement-id 格式应为 REQ-YYYY-NNN。"
```
其他 4 条同理。

## Minor（改进建议）

### [design-consistency] scripts/lib/code_review_routing.py（异常类 65-102）

`exit_code` 改用 `__init__` 注入而非详设 §2.1 写的 class-body 类属性；语义等价但签名差异。建议两选一：(a) 在类体增加 `exit_code: int = EXIT_BAD_ARGS` 类属性使签名对齐；(b) 在 detailed-design.md notes 补"实现选择 __init__ 注入"的偏差说明。

### [design-consistency] tests/lib/test_code_review_routing.py:225-246（U8）

设计 §10.1 写"12 文件 mixed → must=2 / suggest=3 / trivial=4 / 灰色=3"加起来正好 12，实测用 14 个文件。建议调整到 12，或在 features.json sourcing 注释里说明"已用 14 文件覆盖更多分类"的偏差合理性。

### [aux-spec] scripts/lib/code_review_routing.py:439（__all__）

`__all__` 含 `_ERROR_MESSAGES` / `_load_yaml` / `_validate_schema` / `_enumerate_diff_files` / `_build_plan` / `_RuleEntry` 6 个下划线开头的私有符号。违反 Python 惯例（`__all__` 只导出公开 API）。建议二选一：(a) 这些符号 rename 去掉 `_` 前缀作为半公开 API 导出（F-002 期望 import）；(b) 直接从 `__all__` 移除，F-002 用 `from module import _x` 即可（仍可访问）。

### [aux-spec] scripts/lib/code_review_routing.py:168-172

`_load_yaml` docstring 第 4 行 `"语法错时 detail 含 \"file:line detail\" 格式定位信息"` 含英文片段；违反 CLAUDE.md §0.1 "代码注释使用简体中文"。改为 `"...含 '文件名:行号 错误描述' 格式..."`。

### [aux-spec] tests/lib/test_code_review_routing.py:134

`class TestValidateSchemaMusTooLong:` 缺字母，应为 `TestValidateSchemaMustTooLong`。

## Follow-up Notes（不阻塞，但值得提醒）

- 在 F-002 实现 `_parse_args` 时，对 `--base-sha` / `--head-sha` 加格式白名单 `re.fullmatch(r'^[0-9a-f]{7,40}$', sha)`，并在 `_check_tty` 实现里**忠实复刻 commit 3d46fcb 的"无 FAKE_TTY env 后门"约束**——纯 `sys.stdin.isatty()`，不接受任何 env var 旁路（来自 F-3 / F-11）。
- 作者 self-check `tests/lib/test_code_review_routing.py:44 _make_config` docstring 是否中英混杂（来自 F-15，critic not_proven）。

## 未运行的 checker（如有）

无。

## 审查元信息

- **审查范围**：embedded / REQ-2026-003 / F-001（4 文件，+773 -311 行）
- **并行 checker**：8 个（complexity / security / concurrency / performance / error-handling / design-consistency / history-context / auxiliary-spec）
- **Critic 统计**：rejected 5 / not_proven 2 / not_rebutted 8（合计 15）
- **综合模型**：code-quality-reviewer（Judge）
- **Base SHA**：5cf7a23
- **Head SHA**：82b6217
- **Verdict 文件**：requirements/REQ-2026-003/reviews/code-F-001-001.json
- **Review ID**：REV-REQ-2026-003-code-F-001-001
