---
feature_id: F-004
title: CI 依赖 + e2e + benchmark + 防御性 contract test
status: in-progress
complexity: medium
depends_on: [F-001, F-002, F-003]
created_at: "2026-04-30 21:55:30"
updated_at: "2026-05-01 10:31:27"
review_report: null
estimate_days: 1.5
---

# F-004 · CI 依赖 + e2e + benchmark + 防御性 contract test

## 需求描述

收尾 feature：CI 加 pathspec 依赖；端到端测试（真 routing.py + 真 yaml + 假 git diff）；性能 benchmark 验收 < 200ms 目标；防御性 contract test 保 critic / judge / report 不被 skipped scope 喂崩。**不修改** critic / judge / report 三个组件的核心逻辑——只在主 Agent commands/code-review.md 上加 3 行/组件 防御性 if 短路（已在 F-003 完成）。

## 接口/数据结构

详见 `requirements/REQ-2026-003/artifacts/detailed-design.md`：

- §9.4 CI 依赖修改（quality-check.yml L25 加 `pathspec>=0.12,<1.0`）
- §10.3 端到端测试 E1-E4 四用例
- §10.4 benchmark B1-B3 三档（pytest-benchmark P95 验收）
- §11.1 critic / judge / report 防御性 contract test（T-CRITIC-1 / T-JUDGE-1 / T-REPORT-1）

## 依赖

### 依赖的 feature

- **F-001 + F-002 + F-003**：均必须 done。本 feature 是端到端验证层。

### 依赖的模块

- `pathspec>=0.12,<1.0`（在本 feature 加入 CI；本地环境应已安装）
- `pytest-benchmark`（CI 不强制 / 标 `pytest.mark.benchmark`，本地装即可）
- 既有 `.github/workflows/quality-check.yml:25` 的 `pip install pyyaml ruamel.yaml` 行
- 既有 `tests/lib/` 目录（F-001/F-002 的单测已在此）

## 触及范围

- 新增：
  - `tests/lib/test_routing_e2e.sh`（E1-E4，bash + git init 临时仓库）
  - `tests/benchmarks/test_routing_perf.py`（B1-B3，~60 行）
  - `tests/agents/test_critic_skipped_contract.py`（T-CRITIC-1）
  - `tests/agents/test_judge_skipped_contract.py`（T-JUDGE-1）
  - `tests/skills/test_report_skipped_contract.py`（T-REPORT-1）
- 修改：
  - `.github/workflows/quality-check.yml`（1 行：pip install 加 pathspec）

## 验收

详见 `features.json` F-004.acceptance：

1. **E1-E4 端到端用例全过**：
   - E1: 改 `scripts/lib/save_review.py`（must 1 命中）→ accept 后 checker_route 含 security + design-consistency
   - E2: 改 5 个 .md 文件（trivial 100%）→ 退码 0 + skipped=true + audit 写 trivial-skipped
   - E3: routing.yaml must 写 6 条 → 退码 3 + stderr 含 "V3"
   - E4: routing.yaml 写不合法 yaml → 退码 4 + stderr 含行号
2. **B1-B3 benchmark 本地至少跑过一次**：
   - B1: trivial-only 50 文件 → P95 < 100ms（含 yaml 加载冷启动）
   - B2: mixed 200 文件 → P95 < 200ms
   - B3: large 2000 文件 mixed → P95 < 200ms（边界）
3. **quality-check.yml** pip install 行包含 `pathspec>=0.12,<1.0` 字串
4. **T-CRITIC-1 / T-JUDGE-1 / T-REPORT-1 三条 contract test 全过**：mock skipped=true scope 喂入下游不导致 traceback
5. 在 develop 分支 mock feature DONE 触发 `/code-review` 嵌入模式，`routing.py --feature-id` 透传链完整

## 开发笔记

**关键 pitfall**：

- benchmark 的 B3 用例（2000 文件）应使用真实仓库 diff 模拟，而非合成；`requirement.md:73` 上限 2000 是设计目标。
- 防御性 contract test 不要求修改 critic / judge / report 内部逻辑——它们在 F-003 后已通过 `commands/code-review.md` Step 2 的 `if scope.skipped == true: return` 自动短路；本 feature 的 contract test 只是把 skipped scope 直接喂给三个 Agent / SKILL，确保它们即便被误调用也不崩溃（防御性，对未来重构兜底）。
- pytest-benchmark 在 CI 不强制：用 `@pytest.mark.benchmark` 标记，CI 默认 `-m "not benchmark"` 跳过。

## 完成自检

- [ ] E1-E4 端到端测试全过
- [ ] B1-B3 benchmark 至少跑一次（贴 P95 数字到本笔记）
- [ ] quality-check.yml 加 pathspec 后 CI green
- [ ] 3 条 contract test 全过
- [ ] mock feature DONE 触发链验证 routing.py 透明接入
- [ ] `/code-review` 通过（嵌入模式 scope 限定到本 feature）
