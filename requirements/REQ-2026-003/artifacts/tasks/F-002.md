---
feature_id: F-002
title: 卡点 A——code-review-prepare 路由建议 + tty 确认 + --all 兜底 + 删除零 finding 旁路
status: pending
complexity: medium
depends_on: []
created_at: "2026-04-30 01:35:00"
updated_at: "2026-04-30 01:35:00"
review_report: ""
estimate_days: 4
---

# F-002 · 卡点 A——code-review-prepare 路由建议 + tty 确认 + --all 兜底 + 删除零 finding 旁路

## 需求描述

实现卡点 A——`code-review-prepare` 在跑 8 个 checker 前，先输出 checker 路由建议 + 跳过原因，等开发者 tty 确认（accept / all / abort / 自定义子集）。同时删除 `code-review.md:40` 现存的「8 checker 全空 → 直接 approved」旁路（D-007 闭合），及 `code-review-report/SKILL.md` 三处「零 finding 快速路径」残留（L14 / L15 / L40）。

## 接口 / 数据结构

详见 `requirements/REQ-2026-003/artifacts/detailed-design.md`：
- §F-002.2 .review-scope.json 增量 4 字段（checker_route / skipped_checkers / mode_hint / routing_confirmed_by + 字段不变量 5 条，含 confirmed_by email 正则）
- §F-002.3 prepare 流程时序（含 --all / --trivial 分支语义；--trivial 在 prepare 不豁免卡点 A）
- §F-002.4 删除点 diff 摘要（4 处删除：commands/code-review.md:40 + code-review-report/SKILL.md:14/15/40）

## 依赖

### 依赖的 feature
无（与 F-001 并行）

### 依赖的模块
- 既有 `.claude/skills/code-review-prepare/SKILL.md`（在两阶段流程上扩展）
- 既有 `.claude/skills/code-review-prepare/reference/scope-schema.md`（在原 schema 上 append 4 字段）
- 既有 `.claude/commands/code-review.md`（删除 L40）
- 既有 `.claude/skills/code-review-report/SKILL.md`（删除 L14 / L15 / L40 残留）

## 验收

详见 `features.json` F-002.acceptance（6 条）。

## 改动文件清单

- 修改：
  - `.claude/skills/code-review-prepare/SKILL.md`（流程改两阶段 + tty 校验段落）
  - `.claude/skills/code-review-prepare/reference/scope-schema.md`（schema append 4 字段）
  - `.claude/commands/code-review.md`（删 L40 旁路）
  - `.claude/skills/code-review-report/SKILL.md`（删 L14 / L15 / L40 三处残留）
- 新增：
  - `tests/skills/test_code_review_prepare_routing.py`（TC-A1 ~ A7 全部）

## 实施注意

- ⚠️ `--trivial` **不豁免卡点 A**——只是写 `mode_hint=trivial` 透传给下游；prepare 仍要求 tty 确认。开发者会在卡点 B 看到豁免效果（自动 approved-trivial）
- ⚠️ `--all` **同样要求 tty**——防 AI 自动加 --all 绕过路由确认；非 tty 即便有 --all 也退出码 2
- ⚠️ `routing_confirmed_by.confirmed_by` 必须命中 `^[^@\s]+@[^@\s]+\.[^@\s]+$` 正则；从 `git config user.email` 读取，空值 / 主机名拒收
- ⚠️ 删除 `code-review-report/SKILL.md` L14 / L15 / L40 时，按 §F-002.4 给的替换文本而非整段删除（保留报告骨架的 critic 输出 / 综合裁决 / 禁止丢弃裁决明细 三段，仅去掉「零 finding 快速路径时该项为空」豁免说明）
- ⚠️ 验收第 6 条要求 `git grep '零 finding 快速路径'` 命中范围限制在历史 / spec 文档——实施完成前必须本地 grep 一次确认
