---
feature_id: F-003
title: routing.yaml 落地 + 三处文档同步（commands + SKILL + scope-schema）
status: pending
complexity: low
depends_on: [F-001]
created_at: "2026-04-30 21:55:30"
updated_at: "2026-04-30 21:55:30"
review_report: null
estimate_days: 1.0
---

# F-003 · routing.yaml 落地 + 三处文档同步

## 需求描述

本 feature 是 routing.py 实施的**配套文档/配置改动**：新增路径规则库 `.claude/code-review-routing.yaml`，并把 3 处文档（slash command body / prepare SKILL.md / scope-schema.md）与新 schema 对齐。**纯文档/配置改动，无生产代码逻辑**——可与 F-002 并行（依赖只在 F-001 的 schema 定稿）。

## 接口/数据结构

详见 `requirements/REQ-2026-003/artifacts/detailed-design.md`：

- §4 routing.yaml 完整内容（must 5 条 + suggest 5 条 + trivial_whitelist 7 条）
- §9.1 `.claude/commands/code-review.md` 修订面（L21 / L23-25 / 新增 Step 2-pre / L28-29 / L29-38 / L44 / L50）
- §9.2 `code-review-prepare/SKILL.md` 修订面（L14-15 / L17-21 / L17）
- §9.3 `code-review-prepare/reference/scope-schema.md` 修订面（L42-49 / L53-94 / L65 / L70-71 删除 mode_hint / L94-101 替换不变量）

## 依赖

### 依赖的 feature

- **F-001**：必须 done。yaml schema 校验逻辑（V1-V7）由 F-001 的 `_validate_schema` 实现，本 feature 写的 yaml 必须能通过该校验。

### 依赖的模块

- 既有 `.claude/commands/code-review.md`（修改 Step 2 行为）
- 既有 `.claude/skills/code-review-prepare/SKILL.md` + `reference/scope-schema.md`

## 触及范围

- 新增：
  - `.claude/code-review-routing.yaml`（v1，~45 行）
- 修改：
  - `.claude/commands/code-review.md`（~15 行净增/改）
  - `.claude/skills/code-review-prepare/SKILL.md`（~5 行净改）
  - `.claude/skills/code-review-prepare/reference/scope-schema.md`（~30 行净改）

## 验收

详见 `features.json` F-003.acceptance：

1. **routing.yaml 通过 F-001 的 `_validate_schema`**（V1-V7 全过）
2. **must 5 条命中关系符合 §4 末尾表**：scripts/lib/save_review.py → must 1；scripts/gates/run.py → must 2；commands/code-review.md → must 3；skills/code-review-prepare/SKILL.md → must 4；hooks/protect-branch.sh → must 5
3. **commands/code-review.md** 的 Step 2 不再硬编码 8 个 checker 名（`grep -c "design-consistency-checker$" .claude/commands/code-review.md` 应为 0），含 `scope.skipped` 短路判断（grep 命中）
4. **code-review-prepare SKILL.md** 中无 "trivial 不豁免" 字串残留（`grep -c "不豁免" == 0`）
5. **scope-schema.md** 中无 "concurrency 关键字" 字串残留；新增顶层 `skipped` 字段 + `routing_decision` 子段；删除 `mode_hint` 字段
6. 4 处文档与 routing.py 字段名一一对应（无 typo / 引用错位）

## 开发笔记

**关键 pitfall**：scope-schema.md 当前的 F-002 段（L53-94）已预留 `routing_confirmed_by` / `mode_hint` 字段定义。本 feature **必须**：
- 改名 `routing_confirmed_by` → `routing_decision`（语义更准确，与 routing.py 内部命名一致）
- **删除** `mode_hint` 字段（被 `routing_decision.decision` 涵盖，避免冗余）
- 新增顶层 `skipped` boolean
- 不变量段从旧 4 条替换为 §3.2 I1-I8 八条

**3 处文档改动必须同一 PR 提交**（tech-feasibility R-2 + R-3）。

## 完成自检

- [ ] routing.yaml 通过 _validate_schema 校验（手跑 F-001 测试）
- [ ] commands/code-review.md 的 fan-out 改读 checker_route
- [ ] SKILL.md trivial 描述与 routing.py 行为一致
- [ ] scope-schema.md 无关键词遗留 / 字段名与 routing.py 一致
- [ ] 4 文件 git diff 字段名对照无错位
- [ ] `/code-review` 通过（嵌入模式 scope 限定到本 feature）
