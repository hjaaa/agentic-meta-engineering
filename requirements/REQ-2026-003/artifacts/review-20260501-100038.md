# 代码审查报告 · REQ-2026-003 · 2026-05-01 10:00:38 CST · F-002 review-001

## 结论

**blocked** · 4 critical / 7 major / 9 minor · score 58

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项
- `blocked` — **AI 机器评估发现契约级 bug（本次结论）**

## 路由说明

- **决策**：all（卡点 A 暂未启用，scope 手工写）
- **确认人**：hj19961223@gmail.com
- **跑了哪些 checker**：8 全集
- **跳过的 checker**：无

## 待 sign-off 提示

本次 `conclusion = blocked`，**禁止 sign-off**。必须先修 4 critical → 重审 → 通过后才能 sign-off。

## 必修项清单（4 critical + 3 major，全部 blocker）

### [H-5 critical / design-consistency] scripts/lib/code_review_routing.py:792,806（skipped_checkers 字段名错位）

`skipped_checkers` 每项写 `{"checker": c, "reason": ...}`，但详设 §3.1 schema + scope-schema.md L88 都用 `"name"`。下游若按 `name` 字段读取会取 null。

**修复**：两处 `{"checker": c, ...}` → `{"name": c, ...}`。

### [H-6 critical / design-consistency] scripts/lib/code_review_routing.py:801,803,805（reason 字串与 §3.3 三选一不符）

实现写"suggest 命中但用户未选" / "routing 规则未命中（灰色），未纳入审查路由" / "用户手动去除"，全不在详设 §3.3 模板内。§3.3 末句明示"任何其他表述视为旧 dirty 数据"。

**修复**：替换为详设 §3.3 三选一字面字符串：
- `"路径未命中 must/suggest 任何规则"`
- `"用户在 custom 子集中未选择"`
- `"diff 全在 trivial 白名单内（skipped=true 全 8 个）"`

### [H-7 critical / error-handling+design-consistency] scripts/lib/code_review_routing.py:1005（退码 5 嵌套消息）

`_prompt_user` 抛 RoutingAbort 时 detail 已是 `_ERROR_MESSAGES[5].format(cause=...)` 完整字符串；main 第 1005 行又 `_ERROR_MESSAGES[5].format(cause=str(e))`，输出嵌套消息：

```
[routing] 本次审查已取消（[routing] 本次审查已取消（用户主动取消 (q)）。）。
```

**修复**：main 第 1005 行的 EXIT_USER_ABORT 分支改为 `print(str(e))`，因为 RoutingAbort.__init__ 已塞完整消息。

### [H-8 critical / design-consistency] scripts/lib/code_review_routing.py:865-866（routing_decision 类型错位）

`routing_decision.files_must_hit` / `files_suggest_hit` 写 `plan.files_must_hit`（`dict[str, list[str]]`），详设 §3.1 schema 是 int（`"files_must_hit": 3`）。下游消费方按 int 解析必然 TypeError。

**修复**：写命中文件唯一数：
```python
"files_must_hit": len({p for paths in plan.files_must_hit.values() for p in paths}),
"files_suggest_hit": len({p for paths in plan.files_suggest_hit.values() for p in paths}),
```

### [H-9 major / design-consistency] scripts/lib/code_review_routing.py:813-816（I1 assert 恒真）

```python
assert is_trivial_skipped == (is_trivial_skipped and final_route == [] and len(skipped_checkers) == 8)
```

A=False 时右侧短路恒 False，等式恒成立；非 trivial 路径双向蕴含失效。

**修复**：去左侧自循环：
```python
assert is_trivial_skipped == (decision.final_route == [] and len(skipped_checkers) == 8)
```

### [H-10 major / design-consistency+security] scripts/lib/code_review_routing.py:592（I6 email 完整正则未实施）

`_resolve_confirmed_by` 仅 `"@" in email`，`a@b` / `@@` 都通过；详设 I6 要求完整正则。

**修复**：
```python
import re
if not re.fullmatch(r"^[^@\s]+@[^@\s]+\.[^@\s]+$", email):
    raise RoutingError(f"git config user.email 格式不合法：{email}", exit_code=EXIT_BAD_ARGS)
```

### [H-11 major / error-handling] scripts/lib/code_review_routing.py:813-836 + main 1002（AssertionError 穿透堆栈）

`_write_scope` I1-I8 用 `assert`，触发抛 `AssertionError`；main 只 `except RoutingError`，AssertionError 穿透到 stderr 暴露完整堆栈，违反"对外接口禁暴露堆栈"规范。

**修复**：把 I1-I8 的 assert 全部改为 `raise RoutingSchemaError(f"I{n}: {detail}", ...)`，或在 main 顶层加 `except AssertionError as ae: ...` 兜底（推荐前者，与异常体系一致）。

## 强烈建议（3 条 major complexity，与 H-11 同根）

### [H-2/H-3/H-4 major / complexity] _prompt_user / _write_scope / main 三函数超阈值

| 函数 | 行数 | cc | 嵌套 |
|---|---|---|---|
| `_prompt_user` | 85 | 21 | — |
| `_write_scope` | 86 | 20 | 5 |
| `main` | 77 | — | 5 |

**修复**：与 H-11 一并重构——抽 `_assert_scope_invariants(plan, decision, skipped_checkers)` 把 I1-I8 转 raise 集中到独立函数（同时修 H-11）；`_prompt_user` 抽 `_classify_input(line)`；main 抽 `_handle_routing_result(plan, decision, args)`。一次重构同步降复杂度 + 修异常体系，覆盖 4 条 finding。

## 裁决明细

- **H-1** [complexity · critical] → critic: not_rebutted → **downgrade-to-major + follow-up**
  - 1073 行客观超 500 阈值 2.1x，但拆 IO 子模块是 100+ 行工程；本轮强求会引入更大风险。下一 feature 拆 io.py / prompt.py / validators.py
- **H-2 / H-3 / H-4** [complexity · major] → critic: not_rebutted → **keep**（建议与 H-11 同根重构）
- **H-5** [design-consistency · critical] → keep
- **H-6** [design-consistency · critical] → keep
- **H-7** [error-handling · critical] → keep
- **H-8** [design-consistency · critical] → keep
- **H-9** [design-consistency · major] → keep
- **H-10** [design-consistency+security · major] → keep
- **H-11** [error-handling · major] → keep
- **H-12** [performance · major] → critic: not_proven → **downgrade-to-minor + follow-up**（subprocess 单次 ≤ 50ms 实测，未触 200ms 整体预算）
- **H-13** [history-context · major] → **downgrade-to-minor**（与 review-001 F-12 同根，保持 minor 一致性）
- **H-14** [error-handling · minor] → critic: not_proven → **drop**（§8.4 表无明示 EOFError 必落 audit）
- **H-15** [error-handling · minor] → keep
- **H-16** [aux-spec · minor] → critic: not_proven → **drop**
- **H-17** [aux-spec · minor] → keep
- **H-18** [aux-spec · minor] → keep
- **H-19** [aux-spec · minor] → **drop**（小作用域局部变量过严）
- **H-20** [aux-spec · minor] → keep
- **H-21** [aux-spec · minor] → keep
- **H-22** [aux-spec · minor] → keep
- **H-23** [aux-spec · minor] → critic: not_proven → **drop**（"重复 import" 措辞错）

## Minor（改进建议，可与必修同批）

- **H-15** `_git_diff_stats` `except Exception: pass` 静默吞没 → 改 stderr warning + 上层可见的退化标记
- **H-17** `from datetime import datetime, timezone, timedelta` → `datetime, timedelta, timezone`（PEP 8 字母序）
- **H-18** `_now_shanghai_display` docstring 补"为什么需要两个时间格式（scope.json 用人类可读 / audit 日志用 ISO8601）"
- **H-20** `_now_shanghai` / `_now_shanghai_display` 加入 `__all__`（与同模块其他私有保持一致）
- **H-21** `tests/lib/test_code_review_routing.py:704 _setup_test_repo` docstring 返回值描述改 `(repo_path, base_sha, head_sha)`
- **H-22** T1-T5 测试方法名 `test_tN_xxx_yyy` → `should_xxx_when_yyy`

## Follow-up Notes（下一 feature 处理）

- **[H-1]** 文件 1073 行远超 500 阈值，拆 `code_review_routing_io.py` 子模块（含 `_write_scope` / `_audit_log` / `_git_diff_stats` / `_resolve_confirmed_by`）
- **[H-12]** `_git_diff_stats` 单次 `git diff --numstat` 自算 shortstat，省一次 subprocess

## 跨维度洞察

1. **复杂度 × 错误处理同根**：H-2/H-3/H-4（三函数超阈值）+ H-11（assert 异常穿透）共因 IO 层 + 不变量校验内联在 `_write_scope`。拆 `_assert_scope_invariants` 独立函数 + assert 全转 `raise RoutingSchemaError`，一次重构同步降复杂度 + 修异常体系，覆盖 4 条 finding。
2. **契约漂移成簇**：H-5（字段名）+ H-6（reason 模板）+ H-8（int vs dict）三条全在 `routing_decision` / `skipped_checkers` 输出 schema 上漂移；同一段 line 786-870 集中改可避免漏。
3. **assert 是双刃剑**：H-9（恒真 bug）+ H-11（异常穿透）+ assert 集中重构能一并解决。

## 总结评语

F-002 实现规模可观（IO 层 +539 行 / 测试 +380 行 / 12 新单测 + 6 pty 集成测全过），但出现严重质量回退：4 条 critical 都是契约级实现 bug——schema 字段名错位 / reason 模板偏离 / 退码 5 双重 format 嵌套消息 / routing_decision 类型对不上。强制 `conclusion=blocked`，禁止 sign-off。建议派同一 subagent 集中修 7 条必修（4 critical + 3 major），辅修 6 条 minor，预计 1.5-2h 完成 → 二审应可解 blocked。

## 审查元信息

- **审查范围**：embedded / REQ-2026-003 / F-002（2 文件，+919 -0 行 vs base 330e033）
- **并行 checker**：8（全集）
- **Critic 统计**：rejected 0 / not_proven 4 / not_rebutted 19
- **综合模型**：code-quality-reviewer
- **Base SHA**：330e033 / **Head SHA**：922db63
- **Verdict 文件**：requirements/REQ-2026-003/reviews/code-F-002-001.json
- **Review ID**：REV-REQ-2026-003-code-F-002-001
