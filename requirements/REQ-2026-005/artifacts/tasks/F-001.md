---
feature_id: F-001
title: FG-001 strict 模式 + reviews_consistency CI 兜底
status: done
complexity: medium
depends_on: []
created_at: 2026-05-01T11:38:11Z
updated_at: 2026-05-01T13:51:19Z
review_report: artifacts/review-20260501-214941.md
---

# F-001 · FG-001 strict 模式 + reviews_consistency CI 兜底

## 需求描述

修 3~4 个 plugin 的 `_legacy_to_report`：warning-only 时返 `Decision.FAIL`（让 audit `calc_exit_code` 的 `has_warning_fail` 触发），新增错误码 `R-WARNING-ONLY`。同时把 `GATE-REVIEWS-CONSISTENCY` 加入 CI trigger，CI 路径走全量扫描（不依赖 staged 列表）。release notes 必须说明 warning-only 渲染从 PASS 列改入 FAIL 列。

## 触及范围（modules）

- `scripts/gates/plugins/meta_schema.py`
- `scripts/gates/plugins/sourcing.py`
- `scripts/gates/plugins/plan_freshness.py`
- `scripts/gates/plugins/index_integrity.py`
- `scripts/gates/plugins/reviews_consistency.py`
- `scripts/gates/registry.yaml`
- `tests/gates/`
- `release-notes/`（新增 warning-only 列变化说明）

## 依赖

### 依赖的 feature
无

### 依赖的模块
见上方触及范围；同时依赖 `scripts/gates/audit.py` 的 `calc_exit_code` 现有 `has_warning_fail` 逻辑（不改 audit）

## 验收（acceptance from features.json）

- TC-FG1-1: `pytest test_legacy_to_report_warning_fail` 全过（4 plugin × {error_only, warning_only, mixed, empty} 矩阵）
- TC-FG1-2: `python3 scripts/gates/run.py --trigger=ci --req=REQ-2026-005 --strict` 在 plan.md 含 W003 时 exit=1
- 非 strict 同样本 exit=0（向后兼容）
- `GATE-REVIEWS-CONSISTENCY` 出现在 quality-check.yml CI trigger 的 plan 中
- release notes 文件含 "audit log warning-only 列变化" 说明

## 开发笔记

（subagent 实施时记录）

## 完成自检

- [ ] 接口实现符合详细设计（detailed-design.md FG-001 段）
- [ ] 单元测试覆盖主流程（4 plugin × 4 矩阵）
- [ ] 边界条件处理（empty / mixed warning+error）
- [ ] 向后兼容：非 strict 不变
- [ ] `/code-review` 通过
