---
feature_id: F-004
title: FG-004 escape hatch tag 限定 + submit 链路三件套 + argparse alias
status: done
complexity: high
depends_on: []
created_at: 2026-05-01T11:38:11Z
updated_at: 2026-05-03T11:12:30+08:00
review_report: artifacts/review-20260503-111038.md
---

# F-004 · FG-004 escape hatch tag 限定 + submit 链路三件套 + argparse alias

## 需求描述

`registry.yaml` 新增 `gates[*].tags` 可选字段 + `escape_hatches.force-with-blockers` 加 `skips_gates_with_tag: [review-verdict]`；`GATE-REVIEW-VERDICT` 标 `tags: [review-verdict]`。`run.py:516` `_handle_escape_hatch` 升级：失败 gate 必须命中 `skips_tags` 才放行。argparse 双 add_argument 同 dest（`--bypass-review-blockers` 推荐 / `--force-with-blockers` DEPRECATED）；argv 扫描打 stderr deprecation。新增 3 plugin（branch_match / phase_in_set / ahead_of_origin），各错误码 `R-BRANCH-MISMATCH` / `R-PHASE-NOT-SUBMITTABLE` / `R-NOTHING-TO-PUSH`。`base_reachable._resolve_base_branch` 优先读 `cli_flags.target`，submit.py 同步透传 `--target`；run.py 新增 `--target` argparse 参数。

**前置**：清查 `.claude/commands/` 和 CI workflow 中 `--force-with-blockers` 现有调用场景。

## 触及范围

- `scripts/gates/registry.yaml`
- `scripts/gates/run.py`
- `scripts/gates/triggers/submit.py`
- `scripts/gates/plugins/base_reachable.py`
- `scripts/gates/plugins/branch_match.py`（新建）
- `scripts/gates/plugins/phase_in_set.py`（新建）
- `scripts/gates/plugins/ahead_of_origin.py`（新建）
- `scripts/gates/registry.py`
- `tests/gates/`

## 依赖

### 依赖的 feature
无（与 F-001/F-003 修改文件不重叠）

### 依赖的模块
现有 escape hatch 机制 / argparse / git 相关 helpers

## 验收

- TC-FG4-1: `pytest test_branch_match / test_phase_in_set / test_ahead_of_origin` 全过
- TC-FG4-2: phase-transition `--bypass-review-blockers='测试'` 在 workspace dirty 时 GATE-WORKSPACE-CLEAN 仍 fail（escape hatch 不命中 review-verdict tag）
- TC-FG4-3: `--force-with-blockers='旧'` 命中 stderr 含 `[DEPRECATED] use --bypass-review-blockers instead, removed at 2026-11-01`
- TC-FG4-4: `submit.py --dry-run --target=main` 透传到 base_reachable，base_reachable 用 main 而非 meta.base_branch
- TC-FG4-5: registry.py S1-S10 schema 校验放行 tags 字段（pytest test_registry_schema_compat.py）
- argparse 同 dest 测试在 Python 3.11 / 3.12 各跑一次
- force-with-blockers 现有调用清查 PR 描述明确列出

## 开发笔记

## 完成自检

- [x] escape hatch tag 机制（commit 1a9d676；_handle_escape_hatch 加 tag 交集校验 + safe default registry_data=None）
- [x] 3 个新 plugin（commit 0f57857；branch_match / phase_in_set / ahead_of_origin 共 333 行 + 29 单测）
- [x] argparse alias + deprecation 提示（commit 1a9d676；TC-FG4-3 实测 stderr 命中）
- [x] submit --target 链路三处协同（commit eb9efaa；submit.py + run.py + base_reachable + ahead_of_origin）
- [x] schema 兼容（detailed-design.md:183 显式豁免，registry.py 不动；全量 375 passed）
- [x] `/code-review` 通过（looks_clean 87 / 0 critical 0 major / 2 minor follow-up / REV-REQ-2026-005-code-F-004-001 signed approved by hj19961223@gmail.com）

## Follow-up（不阻断验收，建议独立小 PR 跟进）

- F-3：`submit.py` 应自扫 raw_argv 打旧名 deprecation（6 行），避免 2026-11-01 切换日 submit 链路用户突然炸
- F-1（降级）：`base_reachable._resolve_base_branch` / `ahead_of_origin._resolve_base` 加分支名 `^[a-zA-Z0-9][a-zA-Z0-9/_.\-]*$` 白名单（8 行 + 测试），防御深度
