---
feature_id: F-003
title: FG-003 registry applies_when 字段消费 + phase 相邻表
status: done
complexity: high
depends_on: ["F-001"]
created_at: 2026-05-01T11:38:11Z
updated_at: 2026-05-03T10:06:00+08:00
review_report: artifacts/review-20260503-100421.md
---

# F-003 · FG-003 registry applies_when 字段消费 + phase 相邻表

## 需求描述

在 `run.py:281` `filter_gates` 一次性消费 5 个 `applies_when` 字段（`changed_files` 用 pathspec / `target_phase` / `current_phase_in` / `transition` / `requires`），同时一刀切删除 4 个 plugin 内的 `changed_files` precheck。新增 `phase_enum.load_adjacent_phases()` 从 `enums.phase` 有序列表推导相邻关系；`run.py:333` `_validate_phase_args` 校验 from→to 前进方向必须相邻。runner 层 grandfather：`meta.legacy=true` 时跳过 tags 含 `legacy-bypass` 的 gate。

**前置**：抓 baseline 5 次取中位数；FG-003 前后对比，超 +20% 触发降级方案。
**串行约束**：F-003 与 F-001 修改同样 4 个 plugin（meta_schema/sourcing/index_integrity/plan_freshness），depends_on=F-001 避免合并冲突。

## 触及范围

- `scripts/gates/run.py`
- `scripts/gates/plugins/meta_schema.py`
- `scripts/gates/plugins/sourcing.py`
- `scripts/gates/plugins/index_integrity.py`
- `scripts/gates/plugins/plan_freshness.py`
- `scripts/lib/phase_enum.py`
- `tests/gates/`

## 依赖

### 依赖的 feature
- F-001（同一组 plugin 修改，串行避免冲突）

### 依赖的模块
`scripts/gates/registry.py`（S1-S10 schema 校验）

## 验收

- TC-FG3-1: `pytest test_filter_gates_applies_when.py` 全过（5 字段 × 命中/不命中/空列表 不限制 矩阵）
- TC-FG3-2: 非法 phase 跳跃（bootstrap→testing） dry-run exit=2，错误码 `R-INVALID-PHASE-TRANSITION`
- TC-FG3-3: baseline +20% 内（5 次中位数对比，超标走降级方案）
- 21 条现存 gate 在 FG-003 前后过滤结果完全一致（filter_gates 升级前后回归测试）
- 4 个 plugin 内 `changed_files` precheck 代码删除（grep `changed_files` 无残留）

## 开发笔记

## 完成自检

- [x] applies_when 5 字段消费实现（commit 4ca2d37；test_filter_gates_applies_when 30/30）
- [x] phase 相邻表 + 校验（commit 92a83c8；test_phase_adjacent 13/13）
- [x] runner 层 grandfather（commit b4b64cf；GATE-TRACEABILITY tags=[legacy-bypass]）
- [x] baseline 性能对比 < +20%（11 次中位数：0.2107→0.2210s = +4.88%；详见 notes.md）
- [x] 4 plugin 双轨清理（commit effd2f2；precheck 函数体仅 `return None`）
- [x] 21 gate 过滤一致性回归（baseline vs after audit log 对比，详见 notes.md）
- [x] `/code-review` 通过（looks_clean 92 / 1 minor critic rejected drop / REV-REQ-2026-005-code-F-003-001 signed approved by hj19961223@gmail.com）
