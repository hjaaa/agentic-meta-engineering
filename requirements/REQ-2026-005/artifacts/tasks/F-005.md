---
feature_id: F-005
title: FG-005 traceability 收紧 + pr_state 降级 + CI build/test/lint + meta.legacy 误用防护
status: in-progress
complexity: medium
depends_on: ["F-001"]
created_at: 2026-05-01T11:38:11Z
updated_at: 2026-05-01T13:53:30Z
review_report: null
---

# F-005 · FG-005 traceability 收紧 + pr_state 降级 + CI build/test/lint + meta.legacy 误用防护

## 需求描述

`traceability.py:39` precheck 加 submit trigger 路径；`:160` `_feature_mentioned` 升级单词边界正则。`pr_state.py:82` gh 失败加 `git ls-remote` fallback；`:124` CLOSED → `Decision.PASS + vars[severity_hint=info] + pr_state_closed=True` 不参与 strict 升级。`pyproject.toml` 新增 `[tool.ruff]` (`select=[E,W,F]`, `line-length=120`, 不 ignore E501)；`quality-check.yml` 加 `pytest tests/gates/ -v + ruff check scripts/ --select=E,W,F` 两 step。

**reviewer 强收口**：
1. `meta_schema` plugin 新增 `_check_legacy_misuse`，`meta.legacy=true` 仅 `phase∈{completed,archived}` 时合法（错误码 `R-LEGACY-MISUSE`）
2. ruff 历史问题预扫阈值表（≤50 一次修 / 51-200 独立 PR / >200 降级 select=F 渐进）

**前置**：本地 `ruff check scripts/ --select=E,W,F --statistics` 预扫，按阈值表决定 PR 拆分策略。

## 触及范围

- `scripts/gates/plugins/traceability.py`
- `scripts/gates/plugins/pr_state.py`
- `scripts/gates/plugins/meta_schema.py`
- `pyproject.toml`
- `.github/workflows/quality-check.yml`
- `tests/gates/`
- `scripts/`（ruff 修复扫描结果）

## 依赖

### 依赖的 feature
- F-001（同改 meta_schema.py，串行避免冲突）

### 依赖的模块
现有 `traceability.py` / `pr_state.py` / GitHub Actions runner

## 验收

- TC-FG5-1: `pytest test_traceability_word_boundary`（FG-001 不会误匹配 FG-0015）+ `test_pr_state_fallback`（gh fail → ls-remote 探测 → CLOSED INFO 化）全过
- TC-FG5-2: `pytest test_legacy_misuse`（legacy=true + phase=development → R-LEGACY-MISUSE / phase=completed → 通过）全过
- TC-FG5-3: `quality-check.yml` CI 跑 `pytest tests/gates/ -v` 全绿 + `ruff check scripts/` 0 error
- ruff 历史问题预扫执行：本地输出 statistics 后按阈值表选择策略并在 PR 描述记录
- meta.legacy 字段在 meta_schema plugin 校验生效；新需求若误设 legacy=true 必报错

## 开发笔记

## 完成自检

- [ ] traceability 单词边界 + submit 路径
- [ ] pr_state ls-remote fallback + CLOSED INFO 化
- [ ] ruff + pytest CI step
- [ ] meta.legacy 误用校验
- [ ] ruff 历史问题预扫策略
- [ ] `/code-review` 通过
