# 代码审查报告 · REQ-2026-005 / F-005 Round-2 · 20260501-224831

## 结论

**looks_clean** · 0 critical / 0 major / 2 minor（均 keep；required_fixes=`[]`，不阻塞）

> Round-2 评审，supersedes `REV-REQ-2026-005-code-F-005-001`（一审 needs_attention 78）

## 路由说明

- **决策**：accept（采纳推荐 2 个 checker，聚焦 round-2 改动）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-01 22:42:43
- **跑了哪些 checker**：security-checker / concurrency-checker
- **跳过的 checker**：complexity / performance / error-handling / design-consistency / history-context / auxiliary-spec（路径未命中 must/suggest）

## 一审 major 闭合情况

| 一审 finding | 闭合状态 | round-2 实施 |
|---|---|---|
| **F-9** ruff `--select=F` ↔ 详设字面 `--select=E,W,F` 不一致 | ✅ closed | plan.md L108-116 D-008 ADR 完整记录 Context（statistics E501=289/E402=13/F401=2/F841=2 触发阈值表 >200 降级）+ Decision + Consequences（三步走渐进收紧）+ 时间戳 |
| **F-10** pr_state.py ls-remote fallback 详设承诺但实现跳过 | ✅ closed | `_ls_remote_fallback(pr_num)` 完整实现 + `_fetch_pr_state` 4 异常路径全部嵌入 fallback + run() 上层识别 `_gh_unavailable` dict 走 PASS+vars 降级 + 3 新测试 case |

## 待 sign-off 提示

`conclusion = looks_clean`，required_fixes=`[]`。在终端 tty 跑：

```bash
/code-review:signoff REV-REQ-2026-005-code-F-005-002 --decision=approved
```

## 裁决明细

| F-id | source | 原 severity | critic verdict | Judge final | 一句话 |
|---|---|---|---|---|---|
| F-1 | security | minor | rejected | **keep (minor)** | pr_state.py:216 ls-remote 异常 print exc 可能泄露 git 路径；critic 驳回（同 F-001 一审 F-2 info-leak 已 drop / 既有 `_cache_miss` 同型 print），Judge 仍标 minor 留作 hygiene |
| F-2 | concurrency | minor | rejected | **keep (minor)** | test_pr_state_fallback.py:187-190 一用例两阶段验证 + 手动 cache.clear()；critic 驳回（清缓存是 run() 走 fallback 的前置正确性需要），Judge 标 minor 留作风格收敛 |

> Critic 与 Judge 不一致：critic 把两条都 reject，Judge 仍保留 minor。原因是 Judge 把 hygiene 类问题归 minor 而非 drop——不影响"required_fixes 空 → 不阻塞"的最终判断。

## Critical / Major

无。

## Minor（改进建议，不阻塞）

### [security] `scripts/gates/plugins/pr_state.py:216` — F-1
ls-remote fallback 异常 `print(f"WARNING ... ls-remote 失败 pr={pr_num}: {exc}")`，FileNotFoundError 的 str() 含 git 二进制绝对路径。**注意**：项目内 `_cache_miss` helper（pr_state.py:184-188）在 4 个 gh 异常路径已用相同模式 print exc，本次 ls-remote handler 仅延续既有模式。如未来统一脱敏，应一并改 `_cache_miss` 而非单独挑 ls-remote。

### [test-style] `tests/gates/test_pr_state_fallback.py:187-190` — F-2
`test_fetch_pr_state_gh_fail_ls_remote_ok_returns_dict_with_unavailable_flag` 一用例两阶段验证 + 手动 `_PR_STATE_CACHE.clear()`。注释已写明意图（"清缓存，再通过 run() 验证端到端"），但与其他 7 个 case 仅依赖 autouse fixture 不一致。建议拆两个独立用例（unit / e2e）。

## Follow-up Notes

- F-1 / F-2 都属 hygiene，建议：(a) 下次有真实 gh 鉴权失效环境时跑一次 submit 端到端验证 ls-remote 在生产可达 origin；(b) 测试用例风格收敛随下个 PR
- 一审遗留 minor（F-1 / F-2 / F-3 / F-4 / F-6 / F-8 / F-12 一审编号）未在本轮处理——按一审报告 "建议下个 REQ 处理"；测试 + lint 收紧、schema 增强、文档对齐建议合并到 follow-up REQ

## 未运行的 checker

- 6 个 routing 跳过：路径未命中 must/suggest（详见路由说明）

## 审查元信息

- **审查范围**：F-005 round-2 修复（10 文件，+443 行）
- **并行 checker**：2（security / concurrency），其余 6 routing 跳过
- **Critic 统计**：rejected 2 / not_proven 0 / not_rebutted 0
- **Judge 统计**：rejected 0 / not_proven 0 / not_rebutted 2（Judge 比 critic 严，两条都标 minor）
- **综合模型**：code-quality-reviewer · score 90 / looks_clean
- **Base SHA**：0ddb3ff（一审 head）
- **Head SHA**：062186b（含 round-2 2 commits）
- **Verdict 落盘**：requirements/REQ-2026-005/reviews/code-F-005-002.json
- **supersedes**：REV-REQ-2026-005-code-F-005-001
- **meta.yaml 联动**：reviews.code.by_feature.F-005.latest = REV-REQ-2026-005-code-F-005-002（history 保留 001 + 追加 002）
