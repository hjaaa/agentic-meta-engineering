# 代码审查报告 · REQ-2026-005 / F-005 · 20260501-222558

## 结论

**needs_attention** · 0 critical / 2 major / 9 minor（required_fixes 为空，**不阻塞**但建议 round-2 集中修）

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` ← **本次结论**
- `blocked` — AI 机器评估发现关键问题

## 路由说明

- **决策**：accept（采纳推荐 4 个 checker）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-01 22:14:43
- **跑了哪些 checker**：security-checker / concurrency-checker / error-handling-checker / design-consistency-checker
- **跳过的 checker（含原因）**：
  - complexity-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则
  - auxiliary-spec-checker：路径未命中 must/suggest 任何规则

## 待 sign-off 提示

`conclusion = needs_attention`，**这只是 AI 的机器评估**。required_fixes 空 → 技术上可签 approved；但 Judge 建议优先 round-2 修 F-9 / F-10 后再签。

```bash
/code-review:signoff REV-REQ-2026-005-code-F-005-001 --decision=approved   # 接受不阻塞，开 follow-up REQ 收尾
/code-review:signoff REV-REQ-2026-005-code-F-005-001 --decision=rejected   # 不接受，要求 round-2 修后重审
```

## 裁决明细

| F-id | source | 原 severity | critic verdict | Judge final | 一句话说明 |
|---|---|---|---|---|---|
| F-1 | security | minor | not_proven | **keep (minor)** | quality-check.yml:25 ruff/pytest 未锁版本，与同行 pathspec 锁定不一致 |
| F-2 | security | minor | rejected | **keep (minor)** | meta_schema.py:196 `is True` 严格，YAML 字符串 "true" 漏报；与 check_reviews.py:369 同款，net 效果非 misuse 但留漏洞 |
| F-3 | err-handling | major | rejected | **downgrade → minor** | meta_schema.py:85 裸 except 静默二次 _load_yaml 失败；竞态罕见但留痕仍应有 |
| F-4 | err-handling | major | rejected | **downgrade → minor** | pr_state.py:126 `severity_hint` vars 字段未被 runner / audit 消费——悬挂字段；CLOSED message 用户可见，仅扩展点瑕疵 |
| F-5 | err-handling | major | rejected | **drop** | traceability.py:51 req_id 格式 → run.py 入口 `_validate_requirement_id` 已校验；plugin 重复属过度防御 |
| F-6 | err-handling | minor | not_proven | **keep (minor)** | R-LEGACY-MISUSE message 内嵌 "修复：..." 破坏 fix_hint 分层 |
| F-7 | err-handling | minor | rejected | **drop** | _feature_mentioned 字符集 `[A-Za-z0-9-]` 不含下划线（critic 有错读），现有正则正确 |
| F-8 | err-handling | minor | not_rebutted | **keep (minor)** | test_legacy_misuse.py:117 phase=None / legacy="true" 边界未测 |
| F-9 | design-cons | major | not_rebutted | **keep (major)** | quality-check.yml:40 `--select=F` 与详设 §5.3 / features.json `--select=E,W,F` 字面不一致；降级合理但无 ADR |
| F-10 | design-cons | major | not_rebutted | **keep (major)** | pr_state.py ls-remote fallback 详设/tech-feasibility/requirement/TC-FG5-1 四处明文承诺，实现+测试均跳过且无决策记录 |
| F-11 | design-cons | minor | rejected | **drop** | pyproject.toml 拆 `[tool.ruff.lint]` 是 ruff 0.2+ 正确格式；详设字面老格式过时 |
| F-12 | design-cons | minor | not_rebutted | **keep (minor)** | TC-FG5-1 验收文案含 "ls-remote 探测"，实际 test_pr_state_fallback.py 未测；同 F-10 根因 |

## Critical（必须修复）

无。

## Major（强烈建议修复，但 required_fixes 为空 → 不阻塞合入）

### [design-consistency] `.github/workflows/quality-check.yml:40` — F-9

CI step `ruff check scripts/ --select=F` 与详设 §5.3 L350 / features.json:99 / tasks/F-005.md 字面 `--select=E,W,F` 不一致。降级到 F 起步是 ruff statistics（E501=289 / E402=13）触发的合理决策（详设阈值表 >200 允许），但 5 commits 与 notes.md 都未写理由，后人难以判断是否漏改。

**建议**（二选一，取一即可解）：
1. 在 detailed-design.md §5.3 / features.json 同步加 ADR 段落："起步 select=F；后续独立 PR 收 E,W"，并在 commit message 链接 ADR
2. 直接把 quality-check.yml 改回 `--select=E,W,F`，靠 pyproject.toml 的 [tool.ruff.lint].select=["F"] 兜底（依赖 ruff 优先级）

### [design-consistency] `scripts/gates/plugins/pr_state.py:82-93` — F-10

ls-remote fallback 在 detailed-design.md §5.2 L304-L314（伪代码 + vars 字段 ls_remote_ok / ls_remote_failed）+ tech-feasibility.md:338（"gh 失败时增加 git ls-remote origin HEAD 作为网络连通性探测"）+ requirement.md:116（F10 修复点）+ TC-FG5-1 验收（"gh fail → ls-remote 探测 → CLOSED INFO 化"）四处明文承诺。实际实现完全跳过——`_fetch_pr_state` 异常 → `_cache_miss` → `return None`，无 ls-remote 调用；test_pr_state_fallback.py 也未测此分支。

**建议**（二选一，取一即可解）：
1. 实施 fallback：在 `_cache_miss` 之前加一层 `subprocess.run(["git", "ls-remote", "origin", "HEAD"])` 探测，失败再 `_cache_miss`；test_pr_state_fallback.py 补 ls-remote mock case
2. 显式回收承诺：detailed-design.md §5.2 + tech-feasibility.md:338 + requirement.md:116 + TC-FG5-1 同步删除 ls-remote 段，commit message 写 ADR 说明（如"评估后发现 git ls-remote 在无网络仍能命中 cache，效果不如直接降级 PASS"）

## Minor（改进建议）

### [security] `.github/workflows/quality-check.yml:25` — F-1
ruff/pytest 未锁版本（同行 pathspec 已锁 `>=0.12,<1.0`）。建议 `pip install ruff==X.Y.* pytest==A.B.*` 或迁 pyproject `[project.optional-dependencies]`。

### [security] `scripts/gates/plugins/meta_schema.py:196` — F-2
`meta.get("legacy") is True` 严格 identity；YAML 字符串 "true" 漏报。schema 增强：meta-schema.yaml 加 `type: boolean`；或代码兜底 `in (True, "true", "True")`。

### [error-handling] `scripts/gates/plugins/meta_schema.py:85-89` — F-3
裸 `except Exception: meta_data = {}` 静默二次 _load_yaml 失败。改 `except (OSError, yaml.YAMLError) as exc: print(f'WARNING ...', file=sys.stderr); meta_data = {}` 至少留痕。

### [error-handling] `scripts/gates/plugins/pr_state.py:125-139` — F-4
`vars["severity_hint"]="info"` 字段未被 runner/audit 消费（grep 仅本文件 + 自身测试）。两选一：runner 渲染消费（推荐与 FG-001 vars 表对齐）；或删除 `severity_hint` 仅留 `pr_state_closed=True`。

### [error-handling] `scripts/gates/plugins/meta_schema.py:204-207` — F-6
R-LEGACY-MISUSE message 内嵌 "修复：..." 破坏 LegacyReport 分层。挪到 `_legacy_to_report` 的 `fix_hint` 字段，或扩 LegacyReport.add 接口。

### [test] `tests/gates/test_legacy_misuse.py:117` — F-8
未测 `legacy=true + phase=None` / `legacy="true" 字符串` / `legacy=1`。补 3 case，断言"true"漏报现状（暴露 F-2）。

### [design-consistency] `requirements/REQ-2026-005/artifacts/features.json:113` — F-12
TC-FG5-1 验收 "gh fail → ls-remote 探测 → CLOSED INFO 化" 与实际 test_pr_state_fallback.py 不符（仅 CLOSED 部分）。随 F-10 一并处理。

## Follow-up Notes（不阻塞，下个 REQ 处理）

- **F-1 / F-2 schema 收紧**：meta-schema.yaml legacy 加 type: boolean + CI 依赖锁版本作 follow-up REQ
- **F-3 / F-4 静默化反模式**：建议团队约定 "vars[k] 必须有消费方 / `except: pass` 必须 logging.warning"
- **F-7 critic 误判存档**：`_feature_mentioned` 字符集 `[A-Za-z0-9-]` 实际不含下划线，critic 误读，已 drop——记录避免下次重复

## 未运行的 checker（如有）

- 路由跳过（非未运行）：4 个，见上方"路由说明"

## 审查元信息

- **审查范围**：F-005 / FG-005（11 文件，+445 -15 行）
- **并行 checker**：4（security / concurrency / error-handling / design-consistency），其余 4 routing 跳过
- **Critic 统计**：rejected 6 / not_proven 2 / not_rebutted 4
- **Judge 统计**：rejected 2 / not_proven 2 / not_rebutted 8（与 critic 不完全一致——Judge 把 critic 拒的反模式条目重新升回 minor）
- **综合模型**：code-quality-reviewer · score 78 / needs_attention
- **Base SHA**：d491004
- **Head SHA**：0ddb3ff（5 commits：d6f1694 / 3c4886e / c226a3f / 92c8cb8 / 0ddb3ff）
- **Verdict 落盘**：requirements/REQ-2026-005/reviews/code-F-005-001.json
- **meta.yaml 联动**：reviews.code.by_feature.F-005.latest = REV-REQ-2026-005-code-F-005-001
