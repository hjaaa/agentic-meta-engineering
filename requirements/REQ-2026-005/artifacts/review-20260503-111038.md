# 代码审查报告 · REQ-2026-005 · 20260503-111038

## 结论

**looks_clean** · 0 critical / 0 major / 2 minor (全部 follow-up，不阻断验收)

- `looks_clean` — AI 机器评估无阻塞问题

## 路由说明

本次审查路由由 `code-review-prepare` 卡点 A 确认：

- **决策**：accept（命中 must 规则 7 文件 / suggest 0 / trivial 2 / total 16）
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-03 10:56:58
- **跑了哪些 checker**：security-checker, concurrency-checker
- **跳过的 checker（含原因）**：
  - complexity-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - error-handling-checker：路径未命中 must/suggest 任何规则
  - design-consistency-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则
  - auxiliary-spec-checker：路径未命中 must/suggest 任何规则

## 待 sign-off 提示

本次评审输出 `conclusion = looks_clean`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人执行：

```bash
/code-review:signoff REV-REQ-2026-005-code-F-004-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-005-code-F-004-001 --trivial  # 纯文档变更快速通道
```

未 sign-off 时 feature-lifecycle-manager 不会把对应 feature 转为 done，GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示"checker 发现 → critic 反证 → Judge 裁决"的三方透明度。

- **F-1** [security-checker · major] → critic: not_rebutted → **最终处置：downgrade → minor (follow-up)**
  - 裁决依据：critic 的"SSH origin 真实 + spec 未声明 --target 校验责任"反证成立，但 Judge 实证调研推翻 RCE 攻击链——subprocess.run 用 list 形式无 shell；列表第 4 位是 refspec 位置（`["git", "fetch", "origin", base_branch, "--dry-run"]`），git 把 `--upload-pack=evil` 字符串作为远端 wantref refspec 发送，**不会**作为本地 fetch 选项被解析（该选项仅在紧邻 `git fetch` 后才识别）。残留风险仅是输入合法性 → 异常分支名让 git stderr 输出怪异 → 降级 minor follow-up。
- **F-2** [security-checker · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：detailed-design.md:183 显式豁免 — "S1-S10 仅做正向必填校验，无白名单严格模式，新增 tags 字段天然兼容，本需求不需要改 registry.py 的 schema 校验代码"。registry.py:_validate_s10_escape_hatch 对 cli_flag 同样无白名单——项目一贯风格。registry.yaml 是仓库内文件、改它必须走 PR review，攻击面与外部 CLI 输入有量级差异。
- **F-3** [security-checker · minor] → critic: not_rebutted → **最终处置：keep (follow-up)**
  - 裁决依据：submit.py:99-103 无条件 `runner_argv.append(f"--bypass-review-blockers={value}")`，runner.argv 永远只剩新名；run.py:622 raw_argv 扫描对走 submit.py 链路的用户必然落空。F-004 设计目标"2026-11-01 删旧名"对 submit 链路用户在整个迁移窗口完全沉默。修复成本 6 行（submit.py main 起始扫 raw_argv 自打 deprecation）。
- **F-4** [security + concurrency · minor/info] → critic: not_proven → **最终处置：drop**
  - 裁决依据：argparse last-wins 是 Python 内建语义，与 git/docker 一致（`git commit -m A -m B` 同款）；checker 自承"不构成安全/并发漏洞"，类别错配（security/concurrency 维度对 UX 问题），属于 nice-to-have 加固而非必修。

## Critical（必须修复）

无。

## Major（强烈建议修复）

无。

## Minor（改进建议）

无 keep 项进入 minor 段（F-1 / F-3 均落入 Follow-up Notes）。

## Follow-up Notes（不阻塞，但值得提醒）

- **F-3 deprecation 链路对 submit.py 失效**（来自 security-checker / not_rebutted / keep follow-up）
  - 文件：`scripts/gates/triggers/submit.py:99-103`
  - 问题：submit.py 把旧名 `--force-with-blockers` 转新名后透传 runner，run.py:622 raw_argv 扫描永远落空 → 走 submit.py 链路的用户在 2026-11-01 切换日**之前**完全收不到 deprecation 提示，切换日突然炸
  - 建议修复：submit.py main() 起始加 6 行 raw_argv 扫描自打 stderr deprecation

- **F-1 base 分支名输入合法性**（来自 security-checker / not_rebutted / downgrade follow-up）
  - 文件：`scripts/gates/plugins/base_reachable.py:67-70` + `scripts/gates/plugins/ahead_of_origin.py:60-63`
  - 问题：解析的 base 分支名（来自 ctx.cli_flags["target"]）无格式校验。subprocess list + git refspec 位置隔离已挡住 RCE 攻击链，但异常分支名仍可让 git stderr 输出怪异错误污染日志
  - 建议修复：`_resolve_base_branch` / `_resolve_base` 加 `^[a-zA-Z0-9][a-zA-Z0-9/_.\-]*$` 白名单 + 测试（防御深度，非阻断）

## 未运行的 checker（按 routing 决策跳过，非异常）

- complexity-checker（routing must/suggest 未命中）
- performance-checker（routing must/suggest 未命中）
- error-handling-checker（routing must/suggest 未命中）
- design-consistency-checker（routing must/suggest 未命中）
- history-context-checker（routing must/suggest 未命中）
- auxiliary-spec-checker（routing must/suggest 未命中）

## 跨维度洞察

- **F-3 与 F-004 设计目标的耦合风险**：审计层面看 audit log 与 stderr 行为不一致；2026-11-01 切换日走 submit.py 链路用户从未收到任何提示就遇到 argparse error，UX 突变；建议在 follow-up 阶段补 6 行修复
- **F-1 暴露的设计盲区**：F-004 引入了用户控制的 git CLI 输入（--target → fetch refspec），但 detailed-design.md 全文未声明校验责任——subprocess list + git refspec 位置已挡住 RCE，但下次类似用户输入透传到 git CLI 的特性应在设计阶段明确写校验责任归属

## 审查元信息

- **审查范围**：embedded / REQ-2026-005 / feature_id=F-004（16 文件，+1099 -62 行）
- **并行 checker**：2（security + concurrency）
- **Critic 统计**：rejected 0 / not_proven 2 / not_rebutted 2
- **综合裁决（Judge）**：code-quality-reviewer
- **Base SHA**：6158c17（F-003 评审产物 + 转 done）
- **Head SHA**：58fa308（F-004 实现完成 [save] 事件）
- **Verdict 文件**：`requirements/REQ-2026-005/reviews/code-F-004-001.json`
- **Review ID**：REV-REQ-2026-005-code-F-004-001
- **Score**：87
- **验收测试**：375 passed + 1 skipped（Python 3.11/3.12 版本路径）
