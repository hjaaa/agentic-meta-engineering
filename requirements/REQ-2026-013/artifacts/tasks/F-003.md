---
schema_version: "1.0"
feature_id: F-003
title: "archive-rules.md 新增 ci gate 预检节 + archive_runner reminder（B3 纯文档兜底）"
status: pending
complexity: light
depends_on: []
touches:
  - .claude/skills/managing-requirement-lifecycle/reference/archive-rules.md
  - scripts/lib/archive_runner.py
  - tests/lifecycle/test_archive_rules_ci_gate_precheck.py
created_at: "2026-05-17 19:00:00"
updated_at: "2026-05-17 19:00:00"
---

# F-003 · archive-rules.md 新增 ci gate 预检节 + archive_runner reminder（B3 纯文档兜底）

> 来源：`requirements/REQ-2026-013/artifacts/features.json` · `F-003`
> 设计：`requirements/REQ-2026-013/artifacts/detailed-design.md` §3.3 + §4.3
> ADR 依据：D-002（B3 纯文档兜底）/ D-008（refresh-only-current-req 子句）

## 需求描述

archive 前主 Agent 必须先跑 ci gate 全集 R-rule，避免归档后 chore PR 推到 develop 才在 CI 暴露 R005 假阳性（REQ-2026-012 经历）。纯文档兜底——不改 archive_runner 5 项硬门禁，只在 Skill 文档加 § 流程提示 + archive_runner 终端 reminder 末段加 1 行。

## 触及范围

- .claude/skills/managing-requirement-lifecycle/reference/archive-rules.md
- scripts/lib/archive_runner.py
- tests/lifecycle/test_archive_rules_ci_gate_precheck.py（新增）

## 实施步骤

1. 在 archive-rules.md 现有 § 5 项预检 之后插入新节 `## archive 前 CI gate 预检`（约 280 字，详见 detailed-design §3.3.1）：必跑命令 + refresh-only-current-req 4 步处置流程 + B1/B2 升级路径说明。
2. 改造 scripts/lib/archive_runner.py `_render_summary` 函数：在最后一行 `return` 之前 `lines.append("")` + `lines.append("🟢 archive 前请确认 ci gate exit 0：python3 scripts/gates/run.py --trigger=ci --strict")`。
3. 新建 tests/lifecycle/test_archive_rules_ci_gate_precheck.py，2 个用例：文档节存在 + reminder 行存在（详见 detailed-design §4.3）。
4. 跑 `pytest tests/lifecycle/test_archive_rules_ci_gate_precheck.py -v` 全过。
5. 用历史 completed 需求做一次 dry-run 看 `_render_summary` 输出末段是否含 🟢 reminder。

## 验收

- archive-rules.md 含 `## archive 前 CI gate 预检` 节
- 节内含 `python3 scripts/gates/run.py --trigger=ci --strict` + `refresh-only-current-req` 子句 + B1/B2 升级路径说明
- archive_runner.py `_render_summary` 输出末段含 `🟢 archive 前请确认 ci gate exit 0` reminder 行
- archive_runner 5 项硬门禁逻辑未改（git diff 限 `_render_summary` 末段追加 + archive-rules.md 新增节）
- pytest tests/lifecycle/test_archive_rules_ci_gate_precheck.py 全过
