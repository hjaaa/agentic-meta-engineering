# 代码审查报告 · REQ-2026-013 · 2026-05-17 19:42:13

## 结论

**needs_attention** · score 82 · 0 critical / 1 major / 1 minor

- `looks_clean` — AI 机器评估无阻塞问题
- `needs_attention` — AI 机器评估发现待改进项 ← **本次**
- `blocked` — AI 机器评估发现关键问题

## 路由说明

本次审查路由由 `code-review-prepare` 自动决策（F-014 后取消人类卡点 A）：

- **决策**：accept
- **确认人**：hj19961223@gmail.com
- **确认时间**：2026-05-17 19:32:31
- **跑了哪些 checker**：error-handling-checker / design-consistency-checker
- **跳过的 checker（含原因）**：
  - complexity-checker：路径未命中 must/suggest 任何规则
  - security-checker：路径未命中 must/suggest 任何规则
  - concurrency-checker：路径未命中 must/suggest 任何规则
  - performance-checker：路径未命中 must/suggest 任何规则
  - history-context-checker：路径未命中 must/suggest 任何规则
  - auxiliary-spec-checker：路径未命中 must/suggest 任何规则

## 待 sign-off 提示

本次评审输出 `conclusion = needs_attention`，**这只是 AI 的机器评估**，不代表合并通过。请开发者本人执行：

```bash
/code-review:signoff REV-REQ-2026-013-code-F-001-001            # 对话式 sign-off
/code-review:signoff REV-REQ-2026-013-code-F-001-001 --trivial  # 纯文档变更快速通道
```

未 sign-off 时 feature-lifecycle-manager 不会把 F-001 转为 done；GATE-REVIEW-VERDICT 在 phase-transition / submit 时会阻断。

## 裁决明细

> 对每条候选 finding 的最终处置和理由——完整展示「reviewer 发现 → critic 反证 → Judge 裁决」的三方透明度。

- **F-1** [error-handling-checker · major] → critic: not_proven → **最终处置：downgrade（major → minor）**
  - 裁决依据：独立扫描 `requirements/**/tasks/F-*.md` 全部 83 个文件 UTF-8 解码成功，0 非 UTF-8；GATE-TASK-FRONTMATTER 在 R005 之前已 yaml.safe_load（必然 UTF-8）。critic 反证成立；但 errors='replace' 静默替换语义本身就是反模式（违反 fail-fast），降级 minor 保留为防御性护栏。

- **F-2** [error-handling-checker · major] → critic: not_rebutted → **最终处置：keep（major）**
  - 裁决依据：本地 `cd tests/lib && pytest test_check_reviews_normalize_schema_sync.py` 实测 rc=1，FileNotFoundError 在 line 30 触发；同 schema `scripts/lib/check_task_frontmatter.py:35-36` 已用 `_REPO_ROOT` 是项目确立约定。本测试是孤例，让 schema 同步监控护栏脆弱化，违反 fail-loud。

- **F-3** [error-handling-checker · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：save_review.py:305-306 录入侧同样无外层 try/except，是项目既有现状（review_verdict_ci.py:119 亦同）。F-001 未扩大暴露面，`file_path.exists()` 前置检查已是 R005 调用约定。不构成新风险。

- **F-4** [error-handling-checker · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：`_strip_frontmatter_fields` 仅在该函数内用 re，函数局部 import 与 CLAUDE.md 外科手术式修改原则一致；task-context-builder 指引「函数局部 import 即可」明确覆盖此点。

- **F-5** [design-consistency-checker · minor] → critic: rejected → **最终处置：drop**
  - 裁决依据：`check_reviews.py:31` `_NORMALIZE_TASK_FIELDS` 与 `:34` `sys.path.insert` 同属 import 区收尾配置；上方 `from common import ...` 后跟该常量符合「import 块就近配置」惯例。

- **F-6** [design-consistency-checker · minor] → critic: not_proven → **最终处置：drop**
  - 裁决依据：实跑 8 测试全 PASS。`@pytest.mark.parametrize('evolving_status', ...)` 参数在函数内确实未使用，属代码 smell；但断言 `len(h_set)==1` 语义正确、覆盖 3 个 status 取值无遗漏。module-level vs class-method 是 pytest 合法形态二选一，不影响正确性。

- **F-7** [design-consistency-checker · major-documented] → critic: rejected → **最终处置：drop**
  - 裁决依据：`test_check_reviews_normalize.py:65-72` docstring 明确「阈值历史」三段；`plan.md` D-012 ADR 完整背书；`detailed-design.md:435` 系统级 NFR 由 testing 阶段 V-02 兜底。校准链清晰、双层文档化。

## Critical（必须修复）

_无 critical 问题。_

## Major（强烈建议修复）

### [error-handling] tests/lib/test_check_reviews_normalize_schema_sync.py:29-31

`schema_path` 使用相对路径 `"context/team/engineering-spec/task-frontmatter-schema.yaml"`，依赖 pytest 从仓库根启动。从 `tests/lib` cwd 启动 pytest 立刻触发 `FileNotFoundError`，schema 同步监控护栏完全失效。同 schema 的 `scripts/lib/check_task_frontmatter.py:35-36` 已用 `_REPO_ROOT = Path(__file__).resolve().parents[2]` 这是项目确立的约定，本测试是孤例。

**建议**：改为 `_REPO_ROOT = Path(__file__).resolve().parents[2]; schema_path = _REPO_ROOT / "context" / "team" / "engineering-spec" / "task-frontmatter-schema.yaml"`，与 `check_task_frontmatter.py` 对齐。

## Minor（改进建议）

### [error-handling] scripts/lib/check_reviews.py:307

`read_text(encoding="utf-8", errors="replace")` 在 task.md 含非 UTF-8 字节时会将其替换为 U+FFFD，hash 与 `save_review.py:305-306` 录入侧 binary hash 永久不一致，R005 静默误报。当前仓库 83/83 task.md 全 UTF-8、模板侧固定 UTF-8 写入，**实际触发概率低**；但属未来风险护栏。

**建议**：改为 `errors="strict"`，让 `UnicodeDecodeError` 显式抛出，R005 报告异常而非静默失败；与上游 GATE-TASK-FRONTMATTER schema 校验形成上下游双重防御。

## Follow-up Notes（不阻塞，但值得提醒）

- schema-sync 测试族孤例化是工程治理信号：未来新增任何依赖项目根资源的测试，应抽 `conftest.py` 注入 `_REPO_ROOT` fixture，避免相对路径反复踩坑（来自 F-2）

## 未运行的 checker（如有）

- ⚠️ complexity-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ security-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ concurrency-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ performance-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ history-context-checker（原因：路径未命中 must/suggest 任何规则）
- ⚠️ auxiliary-spec-checker（原因：路径未命中 must/suggest 任何规则）

## 审查元信息

- **审查范围**：feat/req-2026-013 vs 37e5e4e（embedded mode / F-001）（9 文件，+230 -7 行）
- **并行 checker**：2（error-handling + design-consistency）
- **Critic 统计**：rejected 3 / not_proven 3 / not_rebutted 1
- **综合模型**：code-quality-reviewer
- **Base SHA**：37e5e4e
- **Head SHA**：21d6d58
- **Verdict 落盘**：requirements/REQ-2026-013/reviews/code-F-001-001.json
